<!-- doc/src/sgml/config.sgml -->

<sect1 id="runtime-online-recovery">
 <title>在线恢复</title>
 <indexterm>
  <primary>在线恢复</primary>
 </indexterm>

 <para>
  <productname>Pgpool-II</productname> 可以在不停止服务的情况下同步数据库节点并附加节点。
  此特性称为 <acronym>"在线恢复"</acronym>。
  可以使用 <xref linkend="pcp-recovery-node"> 命令执行在线恢复。
 </para>
 <para>
  对于在线恢复，恢复目标节点必须处于分离状态。
  这意味着该节点必须通过 <xref linkend="pcp-detach-node"> 手动分离，
  或者因故障转移由 <productname>Pgpool-II</productname> 自动分离。
 </para>
 <para>
  如果您希望动态添加 <productname>PostgreSQL</productname> 服务器节点，
  请在添加 <xref linkend="guc-backend-hostname"> 及其相关参数后重新加载
  <filename>pgpool.conf</filename>。这将把新服务器注册到
  <productname>Pgpool-II</productname> 中作为一个已分离的后端节点，
  之后执行 <xref linkend="pcp-recovery-node"> 命令即可添加该服务器。
 </para>
 <!--
 <caution>
 <para>
 Make sure that <command>autovacuum</command> is stopped on the
 main node (the first node which is up and running) before starting the
 online recovery. Autovacuum can change the contents of the database which
 can cause the inconsistency after the online recovery.
    </para>
 <para>
 This applies only if you're recovering with a simple copy mechanism,
 such as the <command>rsync</command> and doesn't apply when using
 the PostgreSQL's PITR mechanism.
    </para>
  </caution>
 -->
 <note>
  <para>
   执行在线恢复时，恢复目标 <productname>PostgreSQL</> 服务器必须处于未运行状态。
   如果目标 <productname>PostgreSQL</> 服务器已经启动，必须在开始在线恢复之前将其关闭。
  </para>
 </note>

 <para>
  在线恢复分两个阶段执行。第一阶段称为"第一阶段"，第二阶段称为"第二阶段"。
  只有 <xref linkend="guc-replication-mode"> 和 <xref linkend="guc-snapshot-isolation-mode">
  需要第二阶段。对于其他模式（包括流复制模式），不执行第二阶段，
  因此不需要在 <xref linkend="guc-recovery-2nd-stage-command"> 中为该阶段提供脚本。
  即您可以安全地将其保留为空字符串。
 </para>

 <para>
  在第一阶段，通过使用 <productname>PostgreSQL</productname> 的
  <command>pg_basebackup</command>（例如从主节点的备份）创建备用（副本）节点。
  执行第一阶段期间的更新数据将被记录到 <productname>PostgreSQL</productname> 的事务日志中。
 </para>

 <para>
  在第二阶段，启动目标恢复节点。事务日志将被重放，副本节点将与主节点完全同步。
 </para>

 <para>
  您需要为每个阶段提供脚本。完整的示例脚本可在
  <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=blob_plain;f=src/sample/scripts/recovery_1st_stage.sample;hb=refs/heads/master">/etc/pgpool-II/recovery_1st_stage.sample</ulink>
  和
  <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=blob_plain;f=src/sample/scripts/recovery_2nd_stage.sample;hb=refs/heads/master">/etc/pgpool-II/recovery_2nd_stage.sample</ulink>
  获取。使用这些脚本的安装示例可在 <xref linkend="example-cluster-pgpool-config-online-recovery"> 中找到。
 </para>

 <para>
  仅在第二阶段不允许客户端连接，而在第一阶段期间可以更新或检索数据。
 </para>
 <para>
  <productname>Pgpool-II</productname> 在在线恢复中执行以下步骤：
 </para>
 <itemizedlist>

  <listitem>
   <para>
    CHECKPOINT。
   </para>
  </listitem>
  <listitem>
   <para>
    执行在线恢复的第一阶段。
   </para>
  </listitem>
  <listitem>
   <para>
    等待所有客户端连接断开（仅适用于 <xref linkend="guc-replication-mode"> 和
    <xref linkend="guc-snapshot-isolation-mode">）。
   </para>
  </listitem>
  <listitem>
   <para>
    CHECKPOINT（仅适用于 <xref linkend="guc-replication-mode"> 和
    <xref linkend="guc-snapshot-isolation-mode">）。
   </para>
  </listitem>
  <listitem>
   <para>
    执行在线恢复的第二阶段（仅适用于 <xref linkend="guc-replication-mode"> 和
    <xref linkend="guc-snapshot-isolation-mode">）。
   </para>
  </listitem>
  <listitem>
   <para>
    启动 postmaster（执行 <literal>pgpool_remote_start</literal>）
   </para>
   <para>
    <literal>pgpool_remote_start</literal> 是用于启动恢复目标
    <productname>PostgreSQL</productname> 节点的脚本。
    <literal>pgpool_remote_start</literal> 接收以下 2 个参数：
    <itemizedlist>
     <listitem>
      <para>
       要恢复的后端节点的主机名。
      </para>
     </listitem>
     <listitem>
      <para>
       主（主）节点的数据库集群路径。
      </para>
     </listitem>
    </itemizedlist>
    脚本示例可在 <xref linkend="example-cluster-pgpool-config-online-recovery"> 中找到。
     <note>
      <para>
       脚本路径和文件名是硬编码的，<command>$PGDATA/pgpool_remote_start</command> 在主（主）节点上执行。
      </para>
     </note>
   </para>
  </listitem>
  <listitem>
   <para>
    节点附加
   </para>
  </listitem>

 </itemizedlist>
 <note>
  <para>
   在 <xref linkend="guc-replication-mode"> 和 <xref linkend="guc-snapshot-isolation-mode">
   中的在线恢复存在一个限制。如果 <productname>Pgpool-II</productname> 本身安装在多个主机上
   但未启用 watchdog，则在线恢复无法正常工作，
   因为 <productname>Pgpool-II</productname> 必须在在线恢复的第二阶段停止所有客户端。
   如果有多个 <productname>Pgpool-II</productname> 主机，则只有其中一个会收到在线恢复命令，
   并将阻止来自客户端的连接。
  </para>
 </note>
 <variablelist>

  <varlistentry id="guc-recovery-user" xreflabel="recovery_user">
   <term><varname>recovery_user</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>recovery_user</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定用于执行在线恢复的 <productname>PostgreSQL</> 用户名。
     默认值为 <literal>''</literal>（空字符串）。
     <varname>recovery_user</varname> 必须指定超级用户，
     因为在在线恢复期间执行的 <function>pgpool_recovery</function> 函数需要超级用户权限。
    </para>
    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-recovery-password" xreflabel="recovery_password">
   <term><varname>recovery_password</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>recovery_password</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定 <xref linkend="guc-recovery-user"> 中配置的 <productname>PostgreSQL</> 用户名的密码，
     用于执行在线恢复。
    </para>
    <para>
     如果 <varname>recovery_password</varname> 留空，<productname>Pgpool-II</productname>
     将首先尝试从 <xref linkend="guc-pool-passwd"> 文件中获取
     <xref linkend="guc-recovery-user"> 的密码，然后再使用空密码。
    </para>
    <para>
     您也可以在 <varname>recovery_password</varname> 字段中指定 AES256-CBC 加密密码。
     要指定 <literal>AES</literal> 加密密码，密码字符串必须在使用
     <literal>aes-256-cbc</literal> 算法加密并编码为 <literal>base64</literal> 后，
     以 <literal>AES</literal> 为前缀。
    </para>
    <para>
     要指定未加密的明文密码，请在密码字符串前加上 <literal>TEXT</literal> 前缀。
     例如，如果您想将 <literal>mypass</literal> 设置为密码，则应在密码字段中指定
     <literal>TEXTmypass</literal>。如果没有有效的前缀，<productname>Pgpool-II</productname>
     将把该字符串视为明文密码。
    </para>
    <para>
     您也可以使用 <xref linkend="PG-ENC"> 工具来创建格式正确的
     <literal>AES</literal> 加密密码字符串。
      <note>
       <para>
        <productname>Pgpool-II</productname> 在启动时需要有效的解密密钥才能使用加密密码。
        有关向 <productname>Pgpool-II</productname> 提供解密密钥的更多详细信息，
        请参阅 <xref linkend="auth-aes-decryption-key">。
       </para>
      </note>
    </para>

    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-recovery-database" xreflabel="recovery_database">
   <term><varname>recovery_database</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>recovery_database</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定用于在线恢复的 PostgreSQL 数据库名称。
     默认值为 <literal>'postgres'</literal>。如果指定了空字符串（''），
     <productname>Pgpool-II</productname> 将尝试使用 <literal>"postgres"</literal> 数据库。
    </para>
    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
    <note>
     <para>
      要执行在线恢复，必须在 <xref linkend="guc-recovery-database"> 参数指定的数据库中
      创建 <varname>pgpool_recovery</varname>、<varname>pgpool_remote_start</varname>
      和 <varname>pgpool_switch_xlog</varname> 函数。
      要在默认数据库 <literal>"postgres"</literal> 中安装这些函数，请执行以下命令
      （<emphasis>如果配置了默认值以外的值，请将 <literal>"postgres"</literal>
      替换为 <varname>recovery_database</varname> 的设置</emphasis>）：
      <programlisting>
$ psql postgres -c "CREATE EXTENSION pgpool_recovery"
      </programlisting>
     </para>
    </note>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-recovery-1st-stage-command" xreflabel="recovery_1st_stage_command">
   <term><varname>recovery_1st_stage_command</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>recovery_1st_stage_command</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在线恢复第一阶段由主（主）节点运行的命令。出于安全原因，
     命令文件必须放置在数据库集群目录中。
     例如，如果 <varname>recovery_1st_stage_command</varname> = <literal>
      'sync-command'</literal>，那么 <productname>Pgpool-II</productname> 将在
     <literal>$PGDATA</literal> 目录中查找命令脚本，并尝试执行
     <command>$PGDATA/sync-command</command>。
    </para>
    <para>
     <varname>recovery_1st_stage_command</varname> 接收以下 7 个参数：
    </para>

    <itemizedlist>
     <listitem>
      <para>
       主（主）节点的数据库集群路径。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的后端节点的主机名。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的节点的数据库集群路径。
      </para>
     </listitem>
     <listitem>
      <para>
       主（主）节点的端口号（<productname>Pgpool-II</productname> 3.4 或之后）。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的节点编号（<productname>Pgpool-II</productname> 4.0 或之后）
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的端口号（<productname>Pgpool-II</productname> 4.1 或之后）
      </para>
     </listitem>
     <listitem>
      <para>
       主（主）节点的主机名
       （<productname>Pgpool-II</productname> 4.3 或之后）
      </para>
      <para>
       以前，主（主）节点的主机名是通过使用 <command>hostname</command> 命令获取的。
       这在大多数情况下是可以的，因为脚本毕竟是在主（主）节点上运行的。
       但是在某些系统中，通过 <command>hostname</command> 命令获取的主机名
       与 backend_hostname 配置参数中定义的主机名不同。
       这可能会导致 <xref linkend="guc-detach-false-primary"> 出现问题，
       因为它通过使用 <varname>recovery_1st_stage_command</varname> 生成的
       <varname>primary_conninfo</varname> 参数中的 <literal>host</literal> 参数来检查主节点和备节点之间的连接性。
       因此，强烈建议在 <varname>recovery_1st_stage_command</varname> 中使用此参数，
       而不是使用 <command>hostname</command> 命令来获取主节点的主机名。
      </para>
     </listitem>
    </itemizedlist>

    <note>
     <para>
      在执行 <varname>recovery_1st_stage command</varname> 期间，
      <productname>Pgpool-II</productname> 接受连接和查询，
      因此您可以检索和更新数据。
     </para>
    </note>

    <caution>
     <para>
      从 PostgreSQL 的角度来看，<varname>recovery_1st_stage command</varname>
      作为 <acronym>SQL</acronym> 命令运行。因此，如果 PostgreSQL 的
      <varname>statement_time_out</varname> 配置的值小于
      <varname>recovery_1st_stage_command</varname> 完成所需的时间，
      则 <varname>recovery_1st_stage command</varname> 可能会被 PostgreSQL 提前终止。
     </para>
     <para>
      这种情况下的典型错误是
      <programlisting>
       rsync used in the command is killed by signal 2 for example.
      </programlisting>
     </para>
    </caution>

    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-recovery-2nd-stage-command" xreflabel="recovery_2nd_stage_command">
   <term><varname>recovery_2nd_stage_command</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>recovery_2nd_stage_command</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>

    <para>
     指定在线恢复第二阶段由主节点运行的命令。此命令仅
     <xref linkend="guc-replication-mode"> 需要，其他模式不需要提供命令文件。
     出于安全原因，命令文件必须放置在数据库集群目录中。
     例如，如果 <varname>recovery_2nd_stage_command</varname> = <literal>
      'sync-command'</literal>，那么 <productname>Pgpool-II</productname> 将在
     <literal>$PGDATA</literal> 目录中查找命令脚本，并尝试执行
     <command>$PGDATA/sync-command</command>。
    </para>
    <para>
     <varname>recovery_2nd_stage_command</varname> 接收以下 7 个参数：
    </para>

    <itemizedlist>
     <listitem>
      <para>
       主（主）节点的数据库集群路径。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的后端节点的主机名。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的节点的数据库集群路径。
      </para>
     </listitem>
     <listitem>
      <para>
       主（主）节点的端口号（<productname>Pgpool-II</productname> 3.4 或之后）。
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的节点编号（<productname>Pgpool-II</productname> 4.0 或之后）
      </para>
     </listitem>
     <listitem>
      <para>
       要恢复的端口号（<productname>Pgpool-II</productname> 4.1 或之后）
      </para>
     </listitem>
     <listitem>
      <para>
       主（主）节点的主机名
       （<productname>Pgpool-II</productname> 4.3 或之后）
      </para>
     </listitem>
    </itemizedlist>

    <note>
     <para>
      <productname>Pgpool-II</productname> <emphasis>不</emphasis>接受
      在执行 <varname>recovery_2nd_stage_command</varname> 命令期间的客户端连接和查询，
      并在执行该命令之前等待现有客户端关闭其连接。
      因此，如果客户端长时间保持连接，<varname>recovery_2nd_stage_command</varname>
      可能无法执行。
     </para>
    </note>

    <caution>
     <para>
      从 PostgreSQL 的角度来看，<varname>recovery_2nd_stage command</varname>
      作为 <acronym>SQL</acronym> 命令运行。因此，如果 PostgreSQL 的
      <varname>statement_time_out</varname> 配置的值小于
      <varname>recovery_2nd_stage_command</varname> 完成所需的时间，
      则 <varname>recovery_2nd_stage command</varname> 可能会被 PostgreSQL 提前终止。
     </para>
    </caution>

    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-recovery-timeout" xreflabel="recovery_timeout">
   <term><varname>recovery_timeout</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>recovery_timeout</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定以秒为单位的超时时间，如果在线恢复未在此时间内完成，则取消该恢复。
     由于 <productname>Pgpool-II</productname> 在在线恢复的第二阶段不接受连接，
     此参数可用于取消在线恢复，以管理在线恢复期间的服务停机时间。
    </para>
    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-client-idle-limit-in-recovery" xreflabel="client_idle_limit_in_recovery">
   <term><varname>client_idle_limit_in_recovery</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>client_idle_limit_in_recovery</varname> 配置参数</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在在线恢复期间，如果客户端自上次查询后保持空闲状态，则断开该客户端的连接的时间（秒）。
     <varname>client_idle_limit_in_recovery</varname> 类似于
     <xref linkend="guc-client-idle-limit">，但仅在在线恢复的第二阶段生效。
    </para>
    <para>
     这对于防止 <productname>Pgpool-II</productname> 恢复被懒惰的客户端干扰，
     或者客户端与 <productname>Pgpool-II</productname> 之间的 TCP/IP 连接意外断开
     （例如电缆被切断）的情况非常有用。
    </para>

    <note>
     <para>
      <varname>client_idle_limit_in_recovery</varname> 必须小于
      <xref linkend="guc-recovery-timeout">。
      否则，<xref linkend="guc-recovery-timeout"> 会先触发，
      您将在执行在线恢复时看到以下错误：
      <programlisting>
       ERROR:  node recovery failed, waiting connection closed in the other pgpools timeout
      </programlisting>
     </para>
    </note>

    <para>
     如果设置为 -1，当在线恢复的第二阶段开始时，所有客户端将立即断开连接。
     默认值为 0，表示关闭此功能。
    </para>

    <para>
     此参数可以通过重新加载 <productname>Pgpool-II</> 配置来更改。
     您也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令为当前会话更改此参数的值。
    </para>
   </listitem>
  </varlistentry>

 </variablelist>
</sect1>