<!-- doc/src/sgml/healthcheck.sgml -->

<sect1 id="runtime-config-health-check">
 <title>健康检查</title>

 <para>
  <productname>Pgpool-II</productname> 定期连接到已配置的
  PostgreSQL 后端，以检测服务器或网络上是否出现任何错误。
  此错误检查过程称为"健康检查"。
 </para>
 <para>
  如果启用了 <link linkend="runtime-ssl">SSL</link>，健康检查
  进程可能会使用 SSL 连接。
 </para> 
 <para>
  如果检测到错误，<productname>Pgpool-II</productname> 会根据配置执行故障转移
  或降级。
  <caution>
   <para>
    健康检查需要为每个后端节点额外建立一个连接，
    因此需要相应地调整 <filename>postgresql.conf</filename> 中的
    <literal>max_connections</literal> 参数。
   </para>
  </caution>
 </para>

 <para>
  健康检查进程会收集各种统计数据，例如健康检查的总次数。要查看统计数据，
  请使用 <xref linkend="SQL-SHOW-POOL-HEALTH-CHECK-STATS">
  命令。请注意，数据存储在共享内存区域中，并且会在
  <productname>Pgpool-II</productname> 启动时被初始化。
 </para>

 <para>
  以下参数名称也可以在末尾带有数字后缀。
  后缀对应于后端 ID，该 ID 在后端信息中定义，例如
  <xref linkend="guc-backend-hostname">。
  例如，<varname>health_check_timeout0</varname> 应用于
  后端 0 的 <varname>health_check_timeout</varname> 值。
 </para>
 <para>
  如果没有带后缀的参数，则后端值取自不带后缀的参数名。
  从这个意义上说，不带后缀的参数名就像一个"全局变量"。
 </para>

 <variablelist>

  <varlistentry id="guc-health-check-timeout" xreflabel="health_check_timeout">
   <term><varname>health_check_timeout</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>health_check_timeout</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在 TCP 连接未能在此时间内成功时，放弃连接到后端
     <productname>PostgreSQL</> 的超时时间（秒）。
    </para>
    <para>
     此参数用于防止健康检查在网络电缆被拔掉时等待过长时间。
     默认值为 20。设置为 0 表示禁用超时（等待直到 TCP/IP 超时）。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-period" xreflabel="health_check_period">
   <term><varname>health_check_period</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>health_check_period</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定健康检查之间的间隔（秒）。
     默认为 0，表示禁用健康检查。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-user" xreflabel="health_check_user">
   <term><varname>health_check_user</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>health_check_user</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定执行健康检查的 <productname>PostgreSQL</> 用户名。
     默认为 <literal>''</literal>（空）。
     所有 <productname>PostgreSQL</> 后端都必须存在相同的用户。
     否则，健康检查会产生错误。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-password" xreflabel="health_check_password">
   <term><varname>health_check_password</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>health_check_password</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定为在 <xref linkend="guc-health-check-user"> 中配置的
     <productname>PostgreSQL</> 用户名执行健康检查而使用的密码。
     用户和密码在所有 <productname>PostgreSQL</> 后端中必须相同。
     否则，健康检查会产生错误。
    </para>
    <para>
     如果 <varname>health_check_password</varname> 留空，<productname>Pgpool-II</productname>
     将首先尝试从 <xref linkend="guc-pool-passwd"> 文件中获取
     <xref linkend="guc-health-check-user"> 的密码，然后才使用空密码。
    </para>
    <para>
     <productname>Pgpool-II</productname> 接受以下形式的
     密码，无论是在 <varname>health_check_password</varname>
     还是在 <xref linkend="guc-pool-passwd"> 文件中：
      <variablelist>

       <varlistentry>
	<term>AES256-CBC 加密密码</term>
	<listitem>
	 <para>
	  最安全且推荐的方式存储密码。密码字符串必须以
	  <literal>AES</literal> 为前缀。
	  您可以使用 <xref linkend="PG-ENC"> 实用程序来创建格式正确的
	   <literal>AES</literal> 加密密码字符串。
	   <productname>Pgpool-II</productname> 在启动时需要有效的解密密钥才能使用加密密码。
	   有关向 <productname>Pgpool-II</productname> 提供解密密钥的详细信息，
	   请参阅 <xref linkend="auth-aes-decryption-key">。
	 </para>
	</listitem>
       </varlistentry>

       <varlistentry>
	<term>MD5 哈希密码</term>
	<listitem>
	 <para>
	  安全性不如 AES256，但仍然比明文密码更好。密码字符串必须以
	  <literal>MD5</literal> 为前缀。请注意后端也必须配置
	  MD5 认证。您可以使用 <xref linkend="PG-MD5"> 实用程序来创建
	   格式正确的
	   <literal>MD5</literal> 哈希密码字符串。
	 </para>
	</listitem>
       </varlistentry>

       <varlistentry>
	<term>明文密码</term>
	<listitem>
	 <para>
	  未加密的明文密码。如果可能的话，应避免使用此方式。
	  密码字符串必须以 <literal>TEXT</literal> 为前缀。
	  例如，如果您要将 <literal>mypass</literal> 设置为密码，
	  则应在密码字段中指定 <literal>TEXTmypass</literal>。
	  在没有有效前缀的情况下，<productname>Pgpool-II</productname> 会将字符串视为明文密码。
	 </para>
	</listitem>
       </varlistentry>

      </variablelist>
    </para>

    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-database" xreflabel="health_check_database">
   <term><varname>health_check_database</varname> (<type>string</type>)
    <indexterm>
     <primary><varname>health_check_database</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定执行健康检查的 PostgreSQL 数据库名称。
     默认为 <literal>''</literal>（空），此时优先尝试 <literal>"postgres"</literal>
     数据库，然后是 <literal>"template1"</literal> 数据库，直到成功为止。
    </para>
    <para>
     <varname>health_check_database</varname> 在
     <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 中引入。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-max-retries" xreflabel="health_check_max_retries">
   <term><varname>health_check_max_retries</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>health_check_max_retries</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在放弃并触发故障转移之前，健康检查失败时的最大重试次数。
     <tip>
      <para>
       在不稳定的网络环境中，此设置很有用，因为即使主节点正常，
       健康检查偶尔也会失败。
      </para>
     </tip>
     <tip>
      <para>
       如果您想启用 <varname>health_check_max_retries</>，
       建议禁用 <xref linkend="guc-failover-on-backend-error">。
      </para>
     </tip>
     默认为 0，表示不重试。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-health-check-retry-delay" xreflabel="health_check_retry_delay">
   <term><varname>health_check_retry_delay</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>health_check_retry_delay</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在失败的健康检查重试之间休眠的秒数（除非 <xref linkend="guc-health-check-max-retries"> 大于 0，否则不使用）。
     如果为 0，则立即重试，无延迟。
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="guc-connect-timeout" xreflabel="connect_timeout">
   <term><varname>connect_timeout</varname> (<type>integer</type>)
    <indexterm>
     <primary><varname>connect_timeout</varname> configuration parameter</primary>
    </indexterm>
   </term>
   <listitem>
    <para>
     指定在使用 <function>connect()</> 系统调用连接到后端时，放弃连接前的毫秒数。
     默认值为 10000 毫秒（10 秒）。网络不稳定时可能需要增加此值。
     0 表示无超时。
     <note>
      <para>
       <varname>connect_timeout</varname> 的值不仅用于健康检查，
       还用于创建普通连接池。
      </para>
     </note>
    </para>
    <para>
     通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
    </para>
   </listitem>
  </varlistentry>

 </variablelist>
</sect1>
