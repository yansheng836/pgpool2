<!-- doc/src/sgml/config.sgml -->

<sect1 id="runtime-config-connection">
 <title>连接与认证</title>

 <sect2 id="runtime-config-connection-settings">
  <title>连接设置</title>

  <variablelist>

   <varlistentry id="guc-listen-addresses" xreflabel="listen_addresses">
    <term><varname>listen_addresses</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>listen_addresses</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定服务器在哪些 TCP/IP 地址上监听来自客户端应用程序的连接。
      该值的形式为逗号分隔的主机名和/或数字 IP 地址列表。<literal>*</literal>
      这个特殊条目对应所有可用的 IP 接口。<literal>0.0.0.0</literal>
      允许监听所有 IPv4 地址，<literal>::</literal> 允许监听所有 IPv6 地址。
      如果列表为空，则服务器不会在任何 IP 接口上进行监听，
      此时只能通过 Unix 域套接字进行连接。
      默认值是
      <systemitem class="systemname">localhost</systemitem>，仅
      允许本地 TCP/IP <quote>回环</quote> 连接。
      虽然客户端认证（<xref linkend="auth-pool-hba-conf">）允许对谁可以访问服务器进行细粒度控制，
      但 <varname>listen_addresses</varname> 控制哪些接口接受连接请求，
      这有助于防止在不安全的网络接口上出现重复的恶意连接请求。
      此参数仅在服务器启动时设置。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-port" xreflabel="port">
    <term><varname>port</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>port</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      <productname>Pgpool-II</productname> 用于监听连接所接受的端口号。
      默认值为 9999。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-unix-socket-directories" xreflabel="unix_socket_directories">
    <term><varname>unix_socket_directories</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>unix_socket_directories</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      创建接受 <productname>Pgpool-II</productname> 连接的 Unix 域套接字所在的目录。
      可以通过列出多个以逗号分隔的目录来创建多个套接字。
      每个设置值中的前导和尾随空格将被自动移除。
      默认为 <literal>/tmp</literal>。注意此套接字可能会被 cron 任务删除。
      建议将此值设置为 <literal>/var/run</literal> 或类似目录。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-unix-socket-group" xreflabel="unix_socket_group">
    <term><varname>unix_socket_group</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>unix_socket_group</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置 Unix 域套接字的所有者组。（套接字的所有者用户始终是启动服务器的用户。）
      结合 <varname>unix_socket_permissions</varname> 参数，可用作 Unix 域连接的额外访问控制机制。
      默认为空字符串，使用服务器用户的默认组。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-unix-socket-permissions" xreflabel="unix_socket_permissions">
    <term><varname>unix_socket_permissions</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>unix_socket_permissions</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置 Unix 域套接字的访问权限。Unix 域套接字使用标准的 Unix 文件系统权限集。
      参数值应为以 chmod 和 umask 系统调用接受的格式指定的数字模式。
      （要使用传统的八进制格式，数字必须以 <literal>0</literal>（零）开头。）
     </para>
     <para>
      默认权限为 <literal>0777</literal>，意味着任何人都可以连接。
      合理的替代值是 <literal>0770</literal>（仅用户和组，参见 <varname>unix_socket_group</varname>）
      和 <literal>0700</literal>（仅用户）。（注意对于 Unix 域套接字，
      只有写权限是重要的，因此设置或撤销读或执行权限没有意义。）
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-pcp-listen-addresses" xreflabel="pcp_listen_addresses">
    <term><varname>pcp_listen_addresses</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>pcp_listen_addresses</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 pcp 服务器在哪些 TCP/IP 地址上监听来自客户端应用程序的连接。
      该值的形式为逗号分隔的主机名和/或数字 IP 地址列表。
      <literal>*</literal> 这个特殊条目对应所有可用的 IP 接口。
      <literal>0.0.0.0</literal> 允许监听所有 IPv4 地址，
      <literal>::</literal> 允许监听所有 IPv6 地址。
      如果列表为空，则服务器不会在任何 IP 接口上进行监听，
      此时只能通过 Unix 域套接字进行连接。
      默认值是
      <systemitem class="systemname">localhost</systemitem>，仅
      允许本地 TCP/IP <quote>回环</quote> 连接。
      虽然客户端认证（<xref linkend="auth-pool-hba-conf">）允许对谁可以访问服务器进行细粒度控制，
      但 <varname>pcp_listen_addresses</varname> 控制哪些接口接受连接请求，
      这有助于防止在不安全的网络接口上出现重复的恶意连接请求。
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-pcp-port" xreflabel="pcp_port">
    <term><varname>pcp_port</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>pcp_port</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      PCP 进程用于监听连接所接受的端口号。
      默认值为 9898。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-pcp-socket-dir" xreflabel="pcp_socket_dir">
    <term><varname>pcp_socket_dir</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>pcp_socket_dir</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      创建接受 PCP 进程连接的 Unix 域套接字所在的目录。
      可以通过列出多个以逗号分隔的目录来创建多个套接字。
      每个设置值中的前导和尾随空格将被自动移除。
      默认为 <literal>/tmp</literal>。注意此套接字可能会被 cron 任务删除。
      建议将此值设置为 <literal>/var/run</literal> 或类似目录。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-num-init-children" xreflabel="num_init_children">
    <term><varname>num_init_children</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>num_init_children</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      预派生的 <productname>Pgpool-II</productname> 服务器进程数量。
      默认值为 32。num_init_children 也是客户端连接到
      <productname>Pgpool-II</productname> 的并发连接限制。
      如果超过 num_init_children 个客户端尝试连接到
      <productname>Pgpool-II</productname>，<emphasis>它们会被阻塞
       （不会像 <productname>PostgreSQL</productname> 那样被错误拒绝，
       而是直到某个 <productname>Pgpool-II</productname> 进程的连接被关闭）</emphasis>
      ，除非将 <xref linkend="guc-reserved-connections"> 设置为 1 或更大。
      最多可以排队 <xref linkend="guc-listen-backlog-multiplier">*
      num_init_children 个连接。
     </para>
     <para>
      队列位于内核中，称为"监听队列"。
      监听队列的长度称为"backlog"。
      某些系统对 backlog 有上限限制，
      如果 num_init_children * <xref linkend="guc-listen-backlog-multiplier">
      超过该数值，则需要提高 backlog 值。
      否则，在高负载系统中可能会出现以下问题：
      <itemizedlist>
	<listitem>
	 <para>
	  连接到 <productname>Pgpool-II</productname> 失败
	 </para>
	</listitem>
	<listitem>
	 <para>
	  由于内核中的重试，连接到 <productname>Pgpool-II</productname> 变慢。
	 </para>
	</listitem>
      </itemizedlist>
      可以使用 "netstat -s" 命令检查监听队列是否实际溢出。
      如果找到类似以下的输出：
      <programlisting>
	535 times the listen queue of a socket overflowed
      </programlisting>
      则监听队列确实已溢出。在这种情况下应增加 backlog 值（需要超级用户权限）。
      <programlisting>
	# sysctl net.core.somaxconn
	net.core.somaxconn = 128
	# sysctl -w net.core.somaxconn = 256
      </programlisting>
      或者可以向 /etc/sysctl.conf 中添加以下内容。
      <programlisting>
	net.core.somaxconn = 256
      </programlisting>
     </para>
     <para>
      每个 <productname>PostgreSQL</productname> 的连接数大致为
      max_pool * num_init_children。
     </para>

     <para>
      然而，取消查询会创建另一个到后端的连接；
      因此，如果所有连接都在使用中，则无法取消查询。
      如果要确保可以取消查询，请将此值设置为预期连接数的两倍。
     </para>
     <para>
      此外，<productname>PostgreSQL</productname> 允许非超级用户
      的并发连接数最多为 max_connections - superuser_reserved_connections。
     </para>
     <para>
      总之，max_pool、num_init_children、max_connections、
      superuser_reserved_connections 必须满足以下公式：
      <programlisting>
       max_pool*num_init_children &lt;= (max_connections - superuser_reserved_connections) （不需要取消查询）
       max_pool*num_init_children*2 &lt;= (max_connections - superuser_reserved_connections) （需要取消查询）
      </programlisting>
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-reserved-connections" xreflabel="reserved_connections">
    <term><varname>reserved_connections</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>reserved_connections</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当此参数设置为 1 或更大时，来自客户端的传入连接
      将不会以错误消息"Sorry, too many clients already"拒绝，
      而是被阻塞，如果当前来自客户端的连接数
      超过（<xref linkend="guc-num-init-children"> -
       <varname>reserved_connections</varname>）。
      例如，如果 <varname>reserved_connections</varname> = 1
      且 <xref linkend="guc-num-init-children"> = 32，则
      第 32 个来自客户端的连接将被拒绝。
      此行为类似于 <productname>PostgreSQL</productname>，
      适用于客户端连接数较大且每个会话可能占用较长时间的系统。
      在这种情况下，监听队列可能非常长，可能导致系统不稳定。
      在此情况下，将此参数设置为非 0 是一个好主意，
      以防止监听队列变得过长。
     </para>
     <para>
      如果将此参数设置为 0，则不会拒绝来自客户端的任何连接。
      默认值为 0。
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="runtime-config-authentication-settings">
  <title>认证设置</title>
  <variablelist>

   <varlistentry id="guc-enable-pool-hba" xreflabel="enable_pool_hba">
    <term><varname>enable_pool_hba</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>enable_pool_hba</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      如果为 <literal>true</literal>，<productname>Pgpool-II</productname> 将使用
      <filename>pool_hba.conf</filename> 文件进行客户端认证。
      有关如何为客户端认证配置 <filename>pool_hba.conf</filename> 的详细信息，
      请参阅 <xref linkend="auth-pool-hba-conf">。
      默认为 <literal>false</literal>。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-pool-passwd" xreflabel="pool_passwd">
    <term><varname>pool_passwd</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>pool_passwd</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于认证的密码文件路径（绝对或相对路径）。
      默认值为 <literal>"pool_passwd"</literal>。
      相对路径将相对于配置文件所在的目录进行解释。
      指定 <literal>''</literal>（空）将禁用密码文件的使用。
     </para>
     <para>
      可以使用三种格式将密码存储在 pool_passwd 文件中：
      AES256 加密格式、明文格式和 md5 格式。
      <productname>Pgpool-II</productname> 通过密码的前缀来识别密码格式类型，
      因此 pool_passwd 中的每个密码条目必须根据密码格式添加前缀。
     </para>
     <para>
      要以明文格式存储密码，请使用 <literal>TEXT</literal> 前缀。
      例如，要将纯文本密码字符串 <literal>"mypassword"</literal>
      存储在 pool_passwd 中，请在密码字符串前添加 <literal>TEXT</literal> 前缀。
      例如：<literal>TEXTmypassword</literal>
     </para>
     <para>
      类似地，md5 哈希密码必须以 <literal>md5</literal> 为前缀，
      AES256 加密密码类型可以使用 <literal>AES</literal> 前缀存储。
      有关使用 AES256 加密密码的更多详细信息，请参阅 <xref linkend="auth-aes-encrypted-password">。
     </para>

     <para>
      如果没有有效的有效前缀，<productname>Pgpool-II</productname> 将
      把该字符串视为明文密码。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-allow-clear-text-frontend-auth" xreflabel="allow_clear_text_frontend_auth">
    <term><varname>allow_clear_text_frontend_auth</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>allow_clear_text_frontend_auth</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      如果 <productname>PostgreSQL</productname> 后端服务器要求某些用户
      使用 <literal>md5</literal> 或 <literal>SCRAM</literal> 认证，
      但该用户的密码不在 <filename>"pool_passwd"</filename> 文件中，
      则启用 <literal>allow_clear_text_frontend_auth</literal> 将允许
      <productname>Pgpool-II</productname> 使用明文密码认证与前端客户端通信，
      从客户端获取明文形式的密码，并用于后端认证。
     </para>
     <para>
      默认为 <literal>false</literal>。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
     <note>
      <para>
       <literal>allow_clear_text_frontend_auth</literal> 仅在 <xref linkend="guc-enable-pool-hba"> 未启用时生效
      </para>
     </note>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-authentication-timeout" xreflabel="authentication_timeout">
    <term><varname>authentication_timeout</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>authentication_timeout</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname>
      认证的超时时间（秒）。设置为 0 表示禁用超时。
      默认值为 60。
     </para>
     <para>
      此参数仅在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>

 </sect2>
</sect1>

<sect1 id="runtime-config-process-management">
 <title>进程管理</title>

  <para>
  <variablelist>

   <varlistentry id="guc-process-management-mode" xreflabel="process_management_mode">
    <term><varname>process_management_mode</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>process_management_mode</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname> 子进程的空闲进程管理方式。
      有效选项：
      <table id="process-management-mode-table">
      <title>可能的进程管理模式</title>

      <tgroup cols="2">
       <tbody>
        <row>
         <entry>static</entry>
         <entry>
          所有子进程在启动时预派生。
	  子进程数量固定（<xref linkend="guc-num-init-children">）。
         </entry>
        </row>

       <row>
         <entry>dynamic</entry>
         <entry>按需派生子进程，最多到 <xref linkend="guc-num-init-children">。
          任意时刻的空闲子进程数量取决于
          min_spare_children 和 max_spare_children
         </entry>
        </row>

       </tbody>
      </tgroup>
     </table>
     </para>
     <para>
       默认为 <literal>static</literal>，与 <emphasis>V4.4</emphasis> 之前的版本兼容。
     </para>

     <para>
      <varname>process_management_mode</varname> 在
      <productname>Pgpool-II </productname><emphasis>V4.4</emphasis> 之前不可用。
     </para>

     <para>
      有关更多详细信息，请参阅 <xref linkend="process-management-mode">。
     </para>

    </listitem>
   </varlistentry>

   <varlistentry id="guc-process-management-strategy" xreflabel="process_management_strategy">
    <term><varname>process_management_strategy</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>process_management_strategy</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定满足空闲（idle）进程数量所需的进程管理策略。
      有效选项：
      <table id="process-management-strategy-table">
      <title>可能的进程管理策略</title>

      <tgroup cols="2">
       <tbody>
        <row>
         <entry>lazy</entry>
         <entry>采用此策略时，缩容操作渐进执行，
         仅当多余的空闲进程数量在超过 5 分钟后仍保持高位时才触发
         </entry>
        </row>

       <row>
         <entry>gentle</entry>
         <entry>采用此策略时，缩容操作渐进执行，
         仅当多余的空闲进程数量在超过 2 分钟后仍保持高位时才触发
         </entry>
        </row>

       <row>
         <entry>aggressive</entry>
         <entry>采用此策略时，缩容操作激进执行，
         空闲进程较高时会更频繁地触发。
         此模式使用更快且略不智能的进程选择标准，
         以识别可以被服务以满足 max_spare_children 的子进程
         </entry>
        </row>

       </tbody>
      </tgroup>
     </table>
     </para>
     <para>
       默认为 <literal>gentle</literal>。
     </para>
          <para>
       <varname>process_management_strategy</varname> 在
       <productname>Pgpool-II </productname><emphasis>V4.4</emphasis> 之前不可用。
      </para>

    </listitem>
   </varlistentry>

 <varlistentry id="guc-min-spare-children" xreflabel="min_spare_children">
    <term><varname>min_spare_children</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>min_spare_children</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定要保持的最小空闲（idle）子进程数量。
      如果空闲进程数量低于 min_spare_children，
      Pgpool-II 将派生新的子进程，除非达到允许的子进程总数上限（num_init_children）。
      默认值为 5。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
     <para>
      此参数仅适用于 <emphasis>dynamic</emphasis> 进程管理模式。
     </para>
    </listitem>
   </varlistentry>

 <varlistentry id="guc-max-spare-children" xreflabel="max_spare_children">
    <term><varname>max_spare_children</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>max_spare_children</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定要保持的最大空闲（idle）子进程数量。
      如果空闲进程数量超过 max_spare_children，Pgpool-II 将终止多余的子进程。
      选择标准是选择具有最少池化连接数的进程。
      默认值为 10。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
     <para>
      此参数仅适用于 <emphasis>dynamic</emphasis> 进程管理模式。
     </para>
    </listitem>
   </varlistentry>

    </variablelist>

 </para>
</sect1>

<sect1 id="runtime-config-running-mode">
 <title>集群模式</title>
 <para>
  <variablelist>
   <varlistentry id="guc-backend-clustering-mode" xreflabel="backend_clustering_mode">
    <term><varname>backend_clustering_mode</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>backend_clustering_mode</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      集群模式是同步 <productname>PostgreSQL</productname> 服务器的方法。
      要设置集群模式，可使用 <varname>backend_clustering_mode</varname>。
      在本节中我们将讨论如何设置集群模式。
      有关更多详细信息，请参阅 <xref linkend="planning-postgresql">。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </para>

 <sect2 id="runtime-config-streaming-replication-mode" xreflabel="streaming replication mode">
  <title>流复制模式</title>

  <para>
   此模式是最流行且推荐的集群模式。在此模式下，
   <productname>PostgreSQL</productname> 负责各服务器之间的复制。
   要启用此模式，请使用 streaming_replication 作为
   <varname>backend_clustering_mode</varname> 的值。
    <programlisting>
backend_clustering_mode = streaming_replication
    </programlisting>
   在此模式下，您可以拥有最多 127 个流复制备用服务器。
   也可以完全不设置备用服务器。
  </para>
  <para>
   此模式的缺点是，在分发读查询时必须考虑复制延迟。
   节点之间的可见性一致性也无法保证。
   为了缓解这些问题，准备了大量额外的参数，但这可能使管理任务更加复杂。
   此外，当主服务器宕机并晋升某个备用节点时，故障转移可能需要一些时间。
   如果您想避免这些问题，请考虑使用
   <link linkend="guc-snapshot-isolation-mode">快照隔离模式</link>。
  </para>
  <para>
   有关流复制模式的额外参数，请参阅 <xref linkend="runtime-streaming-replication-check">。
  </para>
 </sect2>

 <sect2 id="guc-replication-mode" xreflabel="native replication mode">
  <title>原生复制模式</title>

  <para>
   此模式使 <productname>Pgpool-II</productname> 在
   <productname>PostgreSQL</productname> 后端之间复制数据。
   要启用此模式，请使用 native_replication 作为
   <varname>backend_clustering_mode</varname> 的值。
    <programlisting>
backend_clustering_mode = native_replication
    </programlisting>
   在此模式下，您可以拥有最多 127 个备用复制服务器。
   也可以完全不设置备用服务器。
  </para>

  <para>
   复制模式还可以与负载均衡（参见 <xref linkend="runtime-config-load-balancing">）
   配合使用，以将负载分发到所附加的后端节点。
  </para>

  <para>
   此模式的缺点是，节点之间的可见性一致性无法保证。
   这可能导致节点间不一致并读取到不一致的数据。
   如果您想避免这些问题，请考虑使用
   <link linkend="guc-snapshot-isolation-mode">快照隔离模式</link>。
  </para>

  <para>
   以下选项影响 <productname>Pgpool-II</productname> 在复制模式下的行为。
  </para>

  <variablelist>

   <varlistentry id="guc-replication-stop-on-mismatch" xreflabel="replication_stop_on_mismatch">
    <term><varname>replication_stop_on_mismatch</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>replication_stop_on_mismatch</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当设置为 on 时，如果所有节点对发送到所有
      <productname>PostgreSQL</productname> 后端节点的查询
      返回相同类型的响应包，则正常；
      否则，响应不同于多数节点的后台节点将被
      <productname>Pgpool-II</productname> 降级。
      如果将 <varname>replication_stop_on_mismatch</varname> 设置为 off，
      且发生类似情况，则 <productname>Pgpool-II</productname> 仅终止当前用户会话，
      而不会降级后端节点。
     </para>

     <note>
      <para>
       <productname>Pgpool-II</productname> 不检查后端返回的数据，
       仅通过比较结果包类型来做出决策。
      </para>
     </note>

     <para>
      启用 <varname>replication_stop_on_mismatch</varname> 的一个典型用例是
      防止后端节点之间的数据不一致。例如，您可能希望
      在某个后端节点上 UPDATE 语句失败而在其他节点上成功时，
      降级该后端节点。
     </para>
     <para>
      默认为 off。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-failover-if-affected-tuples-mismatch" xreflabel="failover_if_affected_tuples_mismatch">
    <term><varname>failover_if_affected_tuples_mismatch</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>failover_if_affected_tuples_mismatch</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当设置为 on 时，如果所有节点对 INSERT/UPDATE/DELETE
      查询返回的受影响元组数不同，则响应不同于多数节点的后台节点将被
      <productname>Pgpool-II</productname> 降级。
      如果将 <varname>failover_if_affected_tuples_mismatch</varname>
      设置为 off，且发生类似情况，则
      <productname>Pgpool-II</productname> 仅终止当前用户会话，
      而不会降级后端节点。
     </para>

     <note>
      <para>
       在平局情况下，当两个或多个组的节点数相同时，
       包含主节点（具有最年轻 node id 的后端节点）的组获得优先权。
      </para>
     </note>

     <para>
      默认为 off。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-replicate-select" xreflabel="replicate_select">
    <term><varname>replicate_select</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>replicate_select</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当设置为 on 时，<productname>Pgpool-II</productname> 启用 SELECT 查询复制模式，
      即 SELECT 查询将发送到所有后端节点。
     </para>

     <table id="replicate-select-affect-table">
      <title>replicate_select 与 <xref linkend="guc-load-balance-mode"> 对 SELECT 路由的影响</title>
      <tgroup cols="6" align="center">
       <colspec colname="_1" colwidth="1*">
	<colspec colname="_2" colwidth="1*">
	 <colspec colname="_3" colwidth="1*">
	  <colspec colname="_4" colwidth="1*">
	   <colspec colname="_5" colwidth="1*">
	    <colspec colname="_6" colwidth="1*">

	     <tbody>
	      <row>
	       <entry>replicate_select 为 true</entry>
	       <entry align="center">Y</entry>
	       <entry align="center" nameend="_6" namest="_3">N</entry>
	      </row>

	      <row>
	       <entry>load_balance_mode 为 true</entry>
	       <entry>ANY</entry>
	       <entry align="center" nameend="_5" namest="_3">Y</entry>
	       <entry align="center">N</entry>
	      </row>

	      <row>
	       <entry>SELECT 在事务块内</entry>
	       <entry align="center">ANY</entry>
	       <entry nameend="_4" namest="_3" align="center"> Y </entry>
	       <entry align="center">N</entry>
	       <entry align="center">ANY</entry>
	      </row>

	      <row>
	       <entry>
		 事务隔离级别为 SERIALIZABLE 且事务已发出写查询
	       </entry>
	       <entry align="center">ANY</entry>
	       <entry align="center">Y</entry>
	       <entry align="center">N</entry>
	       <entry align="center">ANY</entry>
	       <entry align="center">ANY</entry>
	      </row>

	      <row>
	       <entry>
		 结果（R：复制，M：仅发送到主节点，L：负载均衡）
	       </entry>
	       <entry align="center">R</entry>
	       <entry align="center">M</entry>
	       <entry align="center">L</entry>
	       <entry align="center">L</entry>
	       <entry align="center">M</entry>
	      </row>

	     </tbody>
      </tgroup>
     </table>

     <para>
      默认为 off。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-insert-lock" xreflabel="insert_lock">
    <term><varname>insert_lock</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>insert_lock</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当设置为 on 时，<productname>Pgpool-II</productname> 将在发出 INSERT
      语句之前自动对 <productname>PostgreSQL</productname> 上的表加锁。
     </para>

     <para>
      在复制具有 SERIAL 数据类型的表时，SERIAL 列的值可能在不同后端上获得不同的值。
      解决此问题的方法是在发出 INSERT 之前显式锁表。
     </para>
     <para>
      因此，为了自动锁定表，<productname>Pgpool-II</productname> 会进行以下转换：
      <programlisting>
       INSERT INTO ...
      </programlisting>
      转换为
      <programlisting>
       BEGIN;
       LOCK TABLE ...
       INSERT INTO ...
       COMMIT;
      </programlisting>
     </para>
     <caution>
      <para>
       此方法会严重降低事务的并行性
      </para>
     </caution>

     <para>
      <productname>Pgpool-II</productname> <emphasis>V2.2</emphasis>
      或更高版本会自动检测表是否具有 SERIAL 列，
      因此如果没有 SERIAL 列则永远不会锁表。
     </para>

     <para>
      <productname>Pgpool-II</productname> <emphasis>V3.0</emphasis> 到
      <productname>Pgpool-II</productname> <emphasis>V3.0.4</emphasis>
      使用针对序列关系的行锁，而非表锁。这是为了
      最小化与 <acronym>VACUUM</acronym>（包括 autovacuum）的锁冲突。
      然而这可能导致另一个问题。事务回绕发生后，
      针对序列关系的行锁会导致 PostgreSQL 内部错误
      （更准确地说，是 pg_clog 的访问错误，pg_clog 用于保持事务状态）。
      为防止此问题，<productname>PostgreSQL</productname> 核心开发者决定
      禁止对序列进行行锁，这当然破坏了
      <productname>Pgpool-II</productname>（"修复"版本的 PostgreSQL 作为
      9.0.5、8.4.9、8.3.16 和 8.2.22 发布）。
     </para>

     <para>
      <productname>Pgpool-II</productname> <emphasis>V3.0.5</emphasis>
      或更高版本使用针对 <literal>pgpool_catalog.insert_lock</literal>
      表的行锁，因为新的 PostgreSQL 禁止对序列关系进行行锁。
      因此，需要在使用 <productname>Pgpool-II</productname> 访问的所有数据库
      中预先创建 insert_lock 表。
      有关更多详细信息，请参阅 <xref linkend="create-installlock-table">。
      如果 insert_lock 表不存在，<productname>Pgpool-II</productname> 将锁住
      插入目标表。此行为与
      <productname>Pgpool-II</productname> <emphasis>V2.2</emphasis>
      和 <emphasis>V2.3</emphasis> 系列相同。
     </para>
     <para>
      如果您想使用与旧版本兼容的 <varname>insert_lock</varname>，
      可以通过 configure 脚本指定锁方法。
      有关更多详细信息，请参阅 <xref linkend="install-pgpool">。
     </para>

     <para>
      精细（每条语句）控制：
     </para>

     <itemizedlist>
      <listitem>
       <para>
	将 insert_lock 设置为 true，并在您不想获取表锁的 INSERT
	语句开头添加 /*NO INSERT LOCK*/。
       </para>
      </listitem>
      <listitem>
       <para>
	将 insert_lock 设置为 false，并在您想获取表锁的 INSERT
	语句开头添加 /*INSERT LOCK*/。
       </para>
      </listitem>

     </itemizedlist>
     <note>
      <para>
       如果启用了 insert_lock，PostgreSQL 8.0 的回归测试会在
       transactions、privileges、rules 和 alter_table 中失败。
      </para>
      <para>
       原因是 <productname>Pgpool-II</productname> 尝试对规则测试的
       VIEW 进行 LOCK，并产生以下错误消息：
       <programlisting>
	! ERROR: current transaction is aborted, commands ignored until
	end of transaction block
       </programlisting>
       例如，transactions 测试尝试向不存在的表插入数据，
       而 <productname>Pgpool-II</productname> 导致
       <productname>PostgreSQL</productname> 获取该表的锁。
       这显然会导致错误。事务将被中止，后续的 INSERT 语句
       会产生上述错误消息。
      </para>
     </note>
     <para>
      默认为 on。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-lobj-lock-table" xreflabel="lobj_lock_table">
    <term><varname>lobj_lock_table</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>lobj_lock_table</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于大对象复制控制的表名。
      如果已指定，<productname>Pgpool-II</productname> 将锁定
      <varname>lobj_lock_table</varname> 指定的表，并通过查看
      <literal>pg_largeobject</literal> 系统目录来生成大对象 ID，
      然后调用 <literal>lo_create</literal> 来创建大对象。
      此过程保证 <productname>Pgpool-II</productname>
      在复制模式下在所有 DB 节点上获得相同的大对象 ID。
     </para>
     <note>
      <para>
       <productname>PostgreSQL</productname> 8.0 及更早版本
       没有 <literal>lo_create</literal>，因此此功能
       不适用于 PostgreSQL 8.0 及更早版本。
      </para>
     </note>
     <para>
      对 <literal>libpq</literal> 函数
      <literal>lo_creat()</literal> 的调用会触发此功能。
      此外，通过 <acronym>Java</acronym> API
      （<acronym>JDBC</acronym> 驱动程序）、<acronym>PHP</acronym>
      API（<literal>pg_lo_create</literal>，或 PHP 库中类似的 API 如 PDO）
      以及各种编程语言中的此相同 API 创建大对象的方式
      已知会使用类似的协议，因此应该可以正常工作。
     </para>

     <para>
      此功能不适用于以下大对象操作。
     </para>
     <itemizedlist>
      <listitem>
       <para>
	所有使用 <literal>lo_create</literal>、<literal>lo_import_with_oid</literal> 的 API。
       </para>
      </listitem>
      <listitem>
       <para>
	在 SELECT 中调用的后端 <literal>lo_import</literal> 函数。
       </para>
      </listitem>
      <listitem>
       <para>
	在 SELECT 中调用的后端 <literal>lo_create</literal> 函数。
       </para>
      </listitem>
     </itemizedlist>

     <note>
      <para>
       所有 <productname>PostgreSQL</productname> 用户必须对
       <varname>lobj_lock_table</varname> 具有写访问权限，
       且可以在任何 schema 中创建。
      </para>
     </note>

     <para>
      创建大对象锁表的示例：
      <programlisting>
       CREATE TABLE public.my_lock_table ();
       GRANT ALL ON public.my_lock_table TO PUBLIC;
      </programlisting>
     </para>
     <para>
      默认为 <literal>''</literal>（空），表示禁用此功能。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="guc-snapshot-isolation-mode" xreflabel="snapshot isolation mode">
  <title>快照隔离模式</title>

  <para>
   此模式类似于原生复制模式，不同之处在于它增加了节点间的可见性一致性。
   其实现基于研究论文 <xref linkend="mishima2009">。
   要启用此模式，请使用 snapshot_isolation 作为
   <varname>backend_clustering_mode</varname> 的值。
    <programlisting>
backend_clustering_mode = snapshot_isolation
    </programlisting>
   例如，您可以避免由可见性不一致引起的以下节点间不一致。
   此处 S1 和 S2 表示会话，N1 和 N2 分别表示 PostgreSQL 服务器 1 和 2。
   <programlisting>
S1/N1: BEGIN;
S1/N2: BEGIN;
S1/N1: UPDATE t1 SET i = i + 1;	-- i = 1
S1/N2: UPDATE t1 SET i = i + 1; -- i = 1
S1/N1: COMMIT;
S2/N1: BEGIN;
S2/N2: BEGIN;
S2/N2: DELETE FROM t1 WHERE i = 1; -- 删除 1 行，因为 S1/N2 尚未提交
S2/N1: DELETE FROM t1 WHERE i = 1; -- 不删除任何行，因为 S1/N1 已提交且 i 不再为 1
S1/N2: COMMIT;
S2/N1: COMMIT;
S2/N2: COMMIT;
   </programlisting>
   在快照隔离模式下，结果将是以下两者之一，且不会破坏数据库节点间的数据一致性。
   <programlisting>
S1/N1: BEGIN;
S1/N2: BEGIN;
S1/N1: UPDATE t1 SET i = i + 1;	-- i = 1
S1/N2: UPDATE t1 SET i = i + 1; -- i = 1
S2/N1: BEGIN;
S2/N2: BEGIN;
S1/N1: COMMIT;
S1/N2: COMMIT;
S2/N1: DELETE FROM t1 WHERE i = 1; -- 不删除任何行，因为 S1/N1 已提交且 i 不再为 1
S2/N2: DELETE FROM t1 WHERE i = 1; -- 不删除任何行，因为 S1/N2 已提交且 i 不再为 1
S2/N1: COMMIT;
S2/N2: COMMIT;
   </programlisting>

   <programlisting>
S1/N1: BEGIN;
S1/N2: BEGIN;
S1/N1: UPDATE t1 SET i = i + 1;	-- i = 1
S1/N2: UPDATE t1 SET i = i + 1; -- i = 1
S2/N1: BEGIN;
S2/N2: BEGIN;
S2/N1: DELETE FROM t1 WHERE i = 1; -- 删除 1 行，因为 S1/N1 尚未提交
S2/N2: DELETE FROM t1 WHERE i = 1; -- 删除 1 行，因为 S1/N2 尚未提交
S1/N1: COMMIT;
S1/N2: COMMIT;
S2/N1: COMMIT;
S2/N2: COMMIT;
   </programlisting>

  </para>
  <itemizedlist>
   <listitem>
    <para>
     必须将事务隔离级别设置为 REPEATABLE READ。
     这意味着您需要在 <filename>postgresql.conf</filename> 中这样设置：
     <programlisting>
default_transaction_isolation = 'repeatable read'
     </programlisting>
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2 id="runtime-config-logical-replication-mode">
  <title>逻辑复制模式</title>

  <para>
   在此模式下，<productname>PostgreSQL</productname> 负责各服务器之间的复制。
   要启用此模式，请使用 logical_replication 作为
   <varname>backend_clustering_mode</varname> 的值。
    <programlisting>
backend_clustering_mode = logical_replication
    </programlisting>
   在此模式下，您可以拥有最多 127 个逻辑复制备用服务器。
   也可以完全不设置备用服务器。
  </para>

  <para>
   此模式的缺点是，在分发读查询时必须考虑复制延迟。
   节点之间的可见性一致性也无法保证。
   此外，某些类型的对象（如 DDL 和大对象）不会被复制。
  </para>

 </sect2>

 <sect2 id="guc-raw-mode" xreflabel="raw mode">
  <title>原始模式</title>
  <para>
   在此模式下，<productname>Pgpool-II</> 不关心数据库同步。
   让用户负责使整个系统有意义地运行。
   在此模式下<emphasis>不支持</emphasis>负载均衡。
   要启用此模式，请将 raw 用作 <varname>backend_clustering_mode</varname> 的值。
   <programlisting>
backend_clustering_mode = raw
   </programlisting>
  </para>
 </sect2>

</sect1>

<sect1 id="runtime-config-backend-settings">
 <title>后端设置</title>

 <sect2 id="runtime-config-backend-connection-settings">
  <title>后端连接设置</title>

  <variablelist>

   <varlistentry id="guc-backend-hostname" xreflabel="backend_hostname">
    <term><varname>backend_hostname</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>backend_hostname</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_hostname</varname> 指定要连接的
      <productname>PostgreSQL</productname> 后端。
      <productname>Pgpool-II</productname> 使用此参数与服务器通信。
     </para>

     <para>
      对于 TCP/IP 通信，此参数可以接受主机名或 IP 地址。
      如果以斜杠（<literal>/</literal>）开头，则表示使用 Unix 域通信
      而非 TCP/IP；该值为存储套接字文件的目录名称。
      当 backend_hostname 为空（<literal>''</literal>）时的默认行为
      是连接到 <filename>/tmp</filename> 中的 Unix 域套接字。
     </para>

     <para>
      可以通过在参数名末尾添加数字（例如 backend_hostname0）来指定多个后端。
      此数字称为"DB 节点 ID"，从 0 开始。
      被赋予 DB 节点 ID 0 的后端称为"主节点"。
      当定义了多个后端时，即使主节点宕机，服务也可以继续（某些模式下不成立）。
      在这种情况下，存活的最年轻 DB 节点 ID 将成为新主节点。
     </para>

     <para>
      请注意，在以流复制模式运行时，ID 为 0 的 DB 节点没有特殊含义。
      相反，您应该关注该 DB 节点是否为"主节点"。
      有关更多详细信息，请参阅 <xref linkend="runtime-config-load-balancing">、
      <xref linkend="runtime-config-failover">、
      <xref linkend="runtime-streaming-replication-check">。
     </para>

     <para>
      在复制配置中，备用方 <literal>primary_conninfo</literal> 中指定的
      <literal>host</literal> 必须与 <productname>Pgpool-II</productname>
      用于标识主节点的 <varname>backend_hostname</varname> 值匹配。
      例如，如果主节点是 backend0（主机名为 <literal>host0</literal>），
      备用节点是 backend1 且 <varname>backend_application_name</varname>
      设置为 <literal>server1</literal>，则 backend1 上的
      <filename>postgresql.conf</filename> 应配置如下：
      <programlisting>
primary_conninfo = 'host=host0 port=5432 user=postgres application_name=''server1'''
      </programlisting>
      在这种情况下，对应于 backend0 的 <varname>backend_hostname</varname>
      也应设置为 <literal>host0</literal>。
      如果这些值不匹配，<productname>Pgpool-II</productname> 可能无法正确
      识别主节点，流复制检查等功能可能无法按预期工作。
     </para>

     <para>
      如果计划只使用一个
      <productname>PostgreSQL</productname> 服务器，
      请通过 backend_hostname0 指定它。
     </para>

     <para>
      可以通过添加参数行并重新加载配置文件来添加新节点。
      但是，无法更新现有值，因此在这种情况下必须重启
      <productname>Pgpool-II</productname>。
     </para>

    </listitem>

   </varlistentry>

   <varlistentry id="guc-backend-port" xreflabel="backend_port">
    <term><varname>backend_port</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>backend_port</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_port</varname> 指定后端的端口号。
      可以通过在参数名末尾添加数字（例如 backend_port0）来指定多个后端。
      如果计划只使用一个 <productname>PostgreSQL</productname> 服务器，
      请通过 backend_port0 指定它。
     </para>
     <para>
      可以通过添加参数行并重新加载配置文件来添加新的后端端口。
      但是，无法更新现有值，因此在这种情况下必须重启
      <productname>Pgpool-II</productname>。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-backend-weight" xreflabel="backend_weight">
    <term><varname>backend_weight</varname> (<type>floating point</type>)
     <indexterm>
      <primary><varname>backend_weight</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_weight</varname> 指定后端的负载均衡比例。
      可以设置为大于或等于零的任何整数或浮点值。
      可以通过在参数名末尾添加数字（例如 backend_weight0）来指定多个后端。
      如果计划只使用一个 PostgreSQL 服务器，请通过 backend_weight0 指定它。
     </para>
     <para>
      可以通过重新加载配置文件在此参数中新增 <varname>backend_weight</varname>。
      但此更改仅对新建立的客户端会话生效。
      <productname>Pgpool-II</productname> <emphasis>V2.2.6</emphasis>、<emphasis>V2.3</emphasis>
      或更高版本允许通过重新加载配置文件来更新值。
      如果您想在流复制模式、逻辑复制模式和 slony 模式下阻止任何发送到备用节点的查询
      执行某些管理工作，这将非常有用。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="runtime-config-backend-data">
  <title>后端数据设置</title>

  <variablelist>

   <varlistentry id="guc-backend-data-directory" xreflabel="backend_data_directory">
    <term><varname>backend_data_directory</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>backend_data_directory</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_data_directory</varname> 指定后端的数据库集群目录。
      可以通过在参数名末尾添加数字（例如 backend_data_directory0）来指定多个后端。
      如果计划只使用一个 PostgreSQL 服务器，请通过
      backend_data_directory0 指定它。此参数由在线恢复功能使用。
      如果您不使用在线恢复，则无需设置此参数。
     </para>
     <para>
      可以通过添加参数行并重新加载配置文件来添加新的 <varname>backend data_directory</varname>。
      但是，无法更新现有值，因此在这种情况下必须重启
      <productname>Pgpool-II</productname>。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-backend-flag" xreflabel="backend_flag">
    <term><varname>backend_flag</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>backend_flag</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_flag</varname> 控制各种后端行为。
      可以通过在参数名末尾添加数字（例如 backend_flag0）来指定多个后端。
      如果计划只使用一个 PostgreSQL 服务器，请通过 backend_flag0 指定它。
     </para>
     <para>
      可以通过添加参数行并重新加载配置文件来添加新的后端标志。
      当前支持以下标志。多个标志可以使用 "|" 指定。
     </para>

     <table id="backend-flag-table">
      <title>后端标志</title>
      <tgroup cols="2">
       <thead>
	<row>
	 <entry>标志</entry>
	 <entry>说明</entry>
	</row>
       </thead>

       <tbody>
	<row>
	 <entry><literal>ALLOW_TO_FAILOVER</literal></entry>
	 <entry>允许故障转移或分离后端。
	  这是默认值。不能与
	  DISALLOW_TO_FAILOVER 同时指定。</entry>
	</row>
	<row>
	 <entry><literal>DISALLOW_TO_FAILOVER</literal></entry>
	 <entry>禁止故障转移或分离后端。
	  当您使用 <productname>Heartbeat</productname>
	  或 <productname>Pacemaker</productname> 等 HA（高可用）软件
	  保护后端时，此标志非常有用。不能与
	  ALLOW_TO_FAILOVER 同时指定。
	 </entry>
	</row>

	<row>
	 <entry><literal>ALWAYS_PRIMARY</literal></entry>
	 <entry>此标志仅在流复制模式下有用。
	  有关流复制模式的详细信息，请参阅 <xref linkend="running-mode">。
	  如果将此标志设置为某个后端，
	  <productname>Pgpool-II</productname> 将不会通过检查后端来
	  查找主节点。相反，始终将设置了此标志的节点视为主节点。
	  这对于包含具有固定主服务器名称的
	  <productname>Amazon Aurora for PostgreSQL Compatibility</productname>
	  的系统非常有用。
	  有关示例配置，请参阅 <xref linkend="example-Aurora">。</entry>
	</row>

       </tbody>
      </tgroup>
     </table>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>

    </listitem>
   </varlistentry>

   <varlistentry id="guc-backend-application-name" xreflabel="backend_application_name">
    <term><varname>backend_application_name</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>backend_application_name</varname> configuration parameter</primary>
     </indexterm>
    </term>

    <listitem>
     <para>
      <varname>backend_application_name</varname> 指定 walreceiver
      的应用程序名称，walreceiver 从主节点接收 WAL 日志。
      因此，在流复制模式之外的模式下，无需设置此参数。
      此外，如果您想在 <xref linkend="SQL-SHOW-POOL-NODES">
      和 <xref linkend="PCP-NODE-INFO"> 命令中显示
      "replication_state" 和 "replication_sync_state" 列，
      也需要此参数。此外，<xref linkend="guc-delay-threshold-by-time">
      也需要此参数。
     </para>
     <para>
      例如，假设主节点是 backend0（其主机名为 "host0"），
      相关的备用节点是 backend1，且 backend1 的
      backend_application_name 为 "server1"，则
      <filename>postgresql.conf</filename> 的
      <literal>primary_conninfo</literal> 参数应如下所示：
      <programlisting>
primary_conninfo = 'host=host0 port=5432 user=postgres application_name='server1''
      </programlisting>
      <xref linkend="guc-recovery-1st-stage-command"> 应生成此参数。
      有关命令的完整示例，请参阅
      <xref linkend="example-cluster-pgpool-config-online-recovery">。
     </para>
     <para>
      此参数可以通过重新加载 <productname>Pgpool-II</productname> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>

 </sect2>

</sect1>
