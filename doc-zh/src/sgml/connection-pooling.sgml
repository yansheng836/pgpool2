<!-- doc/src/sgml/config.sgml -->

<sect1 id="runtime-config-connection-pooling">
 <title>连接池</title>

 <para>
  <productname>Pgpool-II</productname> 会保持与 PostgreSQL 服务器的已建立
  连接，并在有相同属性（即用户名、数据库、
  协议版本）的新连接到来时复用这些连接。这可以减少连接开销，
  并提高系统的整体吞吐量。
 </para>

 <sect2 id="runtime-config-connection-pooling-settings">
  <title>连接池设置</title>

  <variablelist>

   <varlistentry id="guc-connection-cache" xreflabel="connection_cache">
    <term><varname>connection_cache</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>connection_cache</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时缓存到后端的连接。默认为 on。
      <emphasis>但是，即使 <varname>connection_cache</> 为 on，
       连接到 <literal>template0</>、<literal>template1</>、
        <literal>postgres</> 和 <literal>regression</> 数据库的连接也不会被缓存。</emphasis>
     </para>
     <para>
      如果修改此值，需要重启 <productname>Pgpool-II</productname>。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-max-pool" xreflabel="max_pool">
    <term><varname>max_pool</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>max_pool</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      每个 <productname>Pgpool-II</productname> 子进程
      中缓存连接的最大数量。<productname>Pgpool-II</productname> 在传入连接
      与同一数据库、同一用户名以及相同运行期参数连接时使用缓存连接。
      如果不是，
      <productname>Pgpool-II</productname> 将创建一条新的
      后端连接。如果缓存连接数超过 max_pool，最旧的连接将被丢弃，
      并使用该位置来建立新连接。
     </para>
     <para>
      默认值为 4。请注意，<productname>Pgpool-II</productname> 进程到后端的连接数
      总计可能达到 num_init_children * max_pool。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-listen-backlog-multiplier" xreflabel="listen_backlog_multiplier">
    <term><varname>listen_backlog_multiplier</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>listen_backlog_multiplier</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定前端到
      <productname>Pgpool-II</productname> 的连接队列长度。队列长度（实际上是
      <literal>"backlog"</literal>
      系统调用的 <literal>"backlog"</literal> 参数）定义为
      <varname>listen_backlog_multiplier</varname> * <xref linkend="guc-num-init-children">。
     </para>
     <note>
      <para>
       某些系统对 <literal>listen()</literal> 系统调用的 backlog 参数有上限限制。
       有关更多详细信息，请参阅 <xref linkend="guc-num-init-children">。
      </para>
     </note>
     <para>
      默认为 2。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-serialize-accept" xreflabel="serialize_accept">
    <term><varname>serialize_accept</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>serialize_accept</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，<productname>Pgpool-II</productname> 会对传入客户端连接启用序列化。
      未启用序列化时，OS 内核会唤醒所有
      <productname>Pgpool-II</productname> 子进程来执行 <literal>accept()</>，其中一个会
      实际获得传入连接。这里的问题是，因为如此多的子进程在同一时间被唤醒，会导致严重的上下文切换，
      从而影响性能。
     </para>
     <para>
      这种现象是一个众所周知的经典问题，称为
      "惊群效应"（the thundering herd problem）。可以通过序列化
      <literal>accept()</> 调用来解决，使得只有一个
      <productname>Pgpool-II</productname> 进程被唤醒来处理传入连接的 <literal>accept()
      </literal>。
     </para>

     <para>
      但序列化本身也有开销，建议仅在 <xref linkend="guc-num-init-children"> 值较大时使用。
      对于 <xref linkend="guc-num-init-children"> 较小的情况，
      由于序列化开销，serialize_accept 反而可能降低性能。
     </para>

     <note>
      <para>
       建议在决定是否使用
       <varname>serialize_accept</varname> 之前进行基准测试，因为 <xref linkend="guc-num-init-children"> 与 <varname>serialize_accept</varname> 之间的关系在不同环境下可能有所不同。
      </para>
     </note>

     <example id="example-serialize-accept-pgbench">
      <title>使用 pgbench 判断是否应使用 serialize_accept</title>
      <para>
       运行 <command>pgbench</command> 的命令如下。
       <programlisting>
	pgbench -n -S -p 9999 -c 32 -C -S -T 300 test
       </programlisting>
       这里，<literal>-C</literal> 告诉 <command>pgbench</command> 每次事务执行时都连接到数据库。<literal>-c 32</literal>
       指定了并发连接到 <productname>Pgpool-II</productname> 的会话数。
       你应该根据系统需求调整此值。
       <command>pgbench</command> 完成后，从 "including connections establishing" 中检查数值。
      </para>
     </example>

     <note>
      <para>
       当 <xref linkend="guc-child-life-time"> 启用时，<varname>serialize_accept</varname>
       无效。如果你打算开启 <varname>serialize_accept</varname>，请确保将 <xref linkend="guc-child-life-time"> 设为 0。
        另外，如果你担心 <productname>Pgpool-II</productname> 进程内存泄漏
        或其他潜在问题，可以使用 <xref linkend="guc-child-max-connections"> 代替。
 	  这纯粹是实现的限制，未来可能会移除。
      </para>
     </note>

     <para>
      默认为 off。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-child-life-time" xreflabel="child_life_time">
    <term><varname>child_life_time</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>child_life_time</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定一个 <productname>Pgpool-II
      </productname> 子进程在被终止前的空闲时间（秒），
      即没有客户端连接到它时。当子进程因 <varname>child_life_time</varname> 被终止时，
      <productname>Pgpool-II</productname> 会立即生成一个新的子进程。
      <varname>child_life_time</varname> 是用于防止 <productname>Pgpool-II
      </productname> 子进程中内存泄漏和其他意外错误的措施。
     </para>
     <note>
      <para>
       <varname>child_life_time</varname> 不适用于尚未接受任何连接的进程。
      </para>
     </note>
     <note>
      <para>
       当 <varname>child_life_time</varname> 启用时，
      <xref linkend="guc-serialize-accept"> 将失效。
      </para>
     </note>
     <para>
      默认为 300（5 分钟），设为 0 则禁用该功能。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-client-idle-limit" xreflabel="client_idle_limit">
    <term><varname>client_idle_limit</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>client_idle_limit</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定最后一次查询后保持客户端空闲的超时时间（秒），超过该时间则断开客户端连接。
      这对于防止 <productname>Pgpool-II</productname>
      子进程被懒惰的客户端或客户端与 <productname>Pgpool-II</productname> 之间损坏的 TCP/IP 连接占用很有用。
     </para>
     <note>
      <para>
       在在线恢复的第二阶段会忽略 <varname>client_idle_limit</varname>。
      </para>
     </note>
     <para>
      默认为 0，表示关闭该功能。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-child-max-connections" xreflabel="child_max_connections">
    <term><varname>child_max_connections</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>child_max_connections</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname>
      子进程以客户端连接数为度量的生命周期。
      <productname>Pgpool-II</productname> 在服务的客户端连接数达到 <varname>child_max_connections</varname> 后将终止该子进程，
      并立即生成一个新的子进程接替其位置。
     </para>
     <para>
      <varname>child_max_connections</varname> 在非常繁忙的服务器上很有用，
      此时 <xref linkend="guc-child-life-time"> 和 <xref linkend="guc-connection-life-time">
      都不会触发。它也用于防止 <productname>PostgreSQL</> 服务器的连接数过大。
     </para>
     <para>
      默认为 0，表示关闭该功能。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-connection-life-time" xreflabel="connection_life_time">
    <term><varname>connection_life_time</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>connection_life_time</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定终止到 <productname>PostgreSQL</> 后端缓存连接的超时时间（秒）。
      这用作缓存连接的过期时间。
     </para>
     <para>
      如果客户端连接到持有缓存连接的进程，则在客户端断开连接之前不会计算
      <varname>connection_life_time</varname>。因此，缓存连接可能保持的时间长于该值。为避免这种情况，
      建议对 <xref linkend="guc-client-idle-limit"> 设置较短的值。
     </para>
     <para>
      默认为 0，表示缓存连接不会断开。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-reset-query-list" xreflabel="reset_query_list">
    <term><varname>reset_query_list</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>reset_query_list</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定退出用户会话时发送到后端以重置连接的 <acronym>SQL</acronym> 命令。
      多个命令可通过 <literal>";"</literal> 分隔来指定。
     </para>

     <para>
      可用的命令因 <productname>PostgreSQL</> 版本而异。
      以下是针对不同 <productname>PostgreSQL</> 版本的 <varname>reset_query_list</varname>
      推荐设置。
      注意，<literal>ABORT</literal> 命令应始终包含在内。
     </para>

     <table id="reset-query-list-suggestions-table">
      <title>不同 PostgreSQL 版本下 <varname>reset_query_list</varname> 的推荐设置</title>
      <tgroup cols="2">
       <thead>
	<row>
	 <entry>PostgreSQL 版本</entry>
	 <entry>reset_query_list</entry>
	</row>
       </thead>

       <tbody>
	<row>
	 <entry>7.1 及更早版本</entry>
	 <entry><literal>'ABORT'</literal></entry>
	</row>

	<row>
	 <entry>7.2 至 8.2</entry>
	 <entry><literal>'ABORT; RESET ALL; SET SESSION AUTHORIZATION DEFAULT'</literal></entry>
	</row>

	<row>
	 <entry>8.3 及更新版本</entry>
	 <entry><literal>'ABORT; DISCARD ALL'</literal></entry>
	</row>

       </tbody>
      </tgroup>
     </table>

     <note>
      <para>
       对于 7.4 及更新版本的 <productname>PostgreSQL</>，若非事务块状态则不发出 <literal>"ABORT"</literal>。
      </para>
     </note>
     <para>
      默认为 <literal>'ABORT; DISCARD ALL'</literal>。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>
</sect1>

<sect1 id="runtime-config-logging">
 <title>错误报告与日志</title>

 <sect2 id="runtime-config-logging-where-to-log">
  <title>日志位置</title>

  <variablelist>

   <varlistentry id="guc-log-destination" xreflabel="log_destination">
    <term><varname>log_destination</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>log_destination</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      <productname>Pgpool-II</productname> 支持两种记录
      <productname>Pgpool-II</productname> 消息的日志目标。
      支持的日志目标为 <literal>stderr</literal>
      和 <literal>syslog</literal>。如果你希望在多个目标上记录日志，
      也可以用逗号分隔的设置列表来指定该参数。
      <programlisting>
       # 例如同时记录到 syslog 和 stderr
       log_destination = 'syslog,stderr'
      </programlisting>
      默认为仅记录到 <literal>stderr</literal>。
     </para>
     <note>
      <para>
       在某些系统上，你需要修改系统的 <application>syslog</application> 守护进程的
       配置才能使用
       <varname>log_destination</varname> 的
       <literal>syslog</literal> 选项。<productname>Pgpool-II</productname>
       可以记录到 <application>syslog</application> 设施 LOCAL0 到 LOCAL7
       （参见 <xref linkend="guc-syslog-facility">），但大多数平台上的默认
       <application>syslog</application>
       配置会丢弃所有此类消息。
       你需要在 syslog 守护进程的配置文件中添加类似：
       <programlisting>
	local0.*    /var/log/pgpool.log
       </programlisting>
       使其生效。
      </para>
     </note>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-logging-collector" xreflabel="logging_collector">
    <term><varname>logging_collector</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>logging_collector</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 此参数启用日志收集器，这是一个后台进程，用于捕获发送到 stderr 的日志消息并将其重定向到日志文件。
     </para>
 	 <note>
 	 <para>
 	 不使用日志收集器也可以记录到 stderr；日志消息将发送到服务器 stderr 所指向的位置。但是，这种方法仅适用于日志量较低的情况，因为它无法提供方便的日志文件轮换方式。
 	 </para>
 	 </note>
 	 <para>
 	 此参数只能在 Pgpool-II 启动时设置。
 	 </para>
     <para>
      <varname>logging_collector</varname> 在
      <productname>Pgpool-II </productname><emphasis>V4.2</emphasis> 之前不可用。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-directory" xreflabel="log_directory">
    <term><varname>log_directory</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>log_directory</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector"> 启用时，此参数决定日志文件的创建目录。
     </para>
 	 <para>默认为 <literal>/tmp/pgpool_logs</literal>。</para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>

    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-filename" xreflabel="log_filename">
    <term><varname>log_filename</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>log_filename</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector"> 启用时，此参数设置创建日志文件的文件名。该值被视为
 	 <literal>strftime</literal> 模式，因此可使用 %-转义来指定随时间变化的文件名。
 	 支持的 %-转义与 Open Group
 	 <ulink url="https://pubs.opengroup.org/onlinepubs/009695399/functions/strftime.html">strftime</ulink> 规范中列出的类似。
     </para>
 	 <para>
 	 如果你指定了不含转义的文件名，应计划使用日志轮换工具，
 	 以避免最终填满整个磁盘。
 	 </para>
 	 <para>
 	 默认为 <literal>pgpool-%Y-%m-%d_%H%M%S.log</literal>。
 	 </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-file-mode" xreflabel="log_file_mode">
    <term><varname>log_file_mode</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>log_file_mode</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector">
 	 启用时，此参数设置日志文件的权限。参数值应为 <literal>chmod</literal> 和 <literal>umask</literal> 系统调用接受的格式的数值模式。
 	 </para>

 	 <note>
 	 <para>
 	 要使用常见的八进制格式，数字必须以 0（零）开头。
 	 </para>
 	 </note>

     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-rotation-age" xreflabel="log_rotation_age">
    <term><varname>log_rotation_age</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>log_rotation_age</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector"> 启用时，此参数决定单个日志文件的使用最长时长，
 	 超过该时长后将会创建新日志文件。如果未指定单位，
 	 则视为分钟。默认为 24 小时。
     </para>
 	 <para>
 	 设为 0 可禁用基于时间的日志文件创建。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-rotation-size" xreflabel="log_rotation_size">
    <term><varname>log_rotation_size</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>log_rotation_size</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector"> 启用时，此参数决定单个日志文件的最大大小。
 	 向日志文件写入超过指定千字节数后，将创建新的日志文件。
     </para>
 	 <para>
 	 设为 0 可禁用基于大小的日志文件创建。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-truncate-on-rotation" xreflabel="log_truncate_on_rotation">
    <term><varname>log_truncate_on_rotation</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_truncate_on_rotation</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
 	 当 <xref linkend="guc-logging-collector"> 启用时，
 	 此参数将使 <productname>Pgpool-II</> 截断（覆盖）任何同名现有日志文件，
 	 而不是追加到现有日志文件。
 	 但是，仅在因基于时间的轮换而打开新文件时才进行截断，而非在启动或基于大小的轮换时。
 	 关闭时，所有情况下都会追加到预存在的文件。
 	 例如，将此设置与 <xref linkend="guc-log-filename">
 	 设置为 pgpool-%H.log 组合使用，将生成二十四个逐时日志文件，然后循环覆盖它们。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-syslog-facility" xreflabel="syslog_facility">
    <term><varname>syslog_facility</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>syslog_facility</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      另请参阅你系统的 syslog 守护进程文档。
      启用向 <application>syslog</application> 记录日志时，
      此参数决定所使用的 <application>syslog</application>
      <literal>"facility"</literal>。
      可从 <literal>LOCAL0</>、<literal>LOCAL1</>、
      <literal>LOCAL2</>、<literal>LOCAL3</>、<literal>LOCAL4</>、
      <literal>LOCAL5</>、<literal>LOCAL6</>、<literal>LOCAL7</> 中选择；
      默认为 <literal>LOCAL0</>。
      另请参阅你系统的 <application>syslog</> 守护进程文档。
     </para>

     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-syslog-ident" xreflabel="syslog_ident">
    <term><varname>syslog_ident</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>syslog_ident</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      启用向 <application>syslog</> 记录日志时，此参数决定用于在 <application>syslog</> 日志中标识
      <productname>Pgpool-II</productname> 消息的程序名。默认为 <literal>pgpool</literal>。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="runtime-config-logging-when-to-log">
  <title>日志记录时机</title>

  <variablelist>

   <varlistentry id="guc-client-min-messages" xreflabel="client_min_messages">
    <term><varname>client_min_messages</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>client_min_messages</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      控制发送客户端的最小消息级别。
      有效值为 <literal>DEBUG5</>、<literal>DEBUG4</>、
      <literal>DEBUG3</>、<literal>DEBUG2</>、<literal>DEBUG1</>、
      <literal>LOG</>、<literal>NOTICE</>、<literal>WARNING</> 和
      <literal>ERROR</>。每个级别包含其后的所有级别。默认为 <literal>NOTICE</>。
     </para>

     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-min-messages" xreflabel="log_min_messages">
    <term><varname>log_min_messages</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>log_min_messages</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      默认为 WARNING。
      控制输出到日志的最小消息级别。
      有效值为 <literal>DEBUG5</>、<literal>DEBUG4</>、
      <literal>DEBUG3</>、<literal>DEBUG2</>、<literal>DEBUG1</>、
      <literal>INFO</>、<literal>NOTICE</>、<literal>WARNING</>、
      <literal>ERROR</>、<literal>LOG</>、<literal>FATAL</> 和
      <literal>PANIC</>。
      每个级别包含其后的所有级别。
      默认为 <literal>WARNING</>。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="runtime-config-logging-what-to-log">
  <title>日志记录内容</title>

  <variablelist>

   <varlistentry id="guc-log-statement" xreflabel="log_statement">
    <term><varname>log_statement</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_statement</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，将所有 SQL 语句打印到日志中。
     </para>

     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-per-node-statement" xreflabel="log_per_node_statement">
    <term><varname>log_per_node_statement</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_per_node_statement</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      类似于 <xref linkend="guc-log-statement">，但会为每个数据库节点分别打印
      日志。这对于确保复制或负载均衡是否正常工作很有用。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-notice-per-node-statement" xreflabel="notice_per_node_statement">
    <term><varname>notice_per_node_statement</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>notice_per_node_statement</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      类似于 <xref linkend="guc-log-per-node-statement">，但以
      NOTICE 消息的形式为每个数据库节点分别打印语句日志。使用
      默认的 <xref linkend="guc-client-min-messages"> 设置（即 NOTICE）时，
      日志消息也会打印到客户端终端上。这对客户端很方便，因为它无需访问
      <productname>Pgpool-II</productname> 日志文件。
      注意，
      与 <xref linkend="guc-log-per-node-statement"> 不同，<xref linkend="guc-notice-per-node-statement">
      不会打印内部查询（例如，系统目录查询）。
      这是因为此功能也用于测试。由于内部查询往往是非确定性的，
      在测试中打印它们并无帮助。出于同样的原因，
      后端进程 ID 也不会被打印。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-client-messages" xreflabel="log_client_messages">
    <term><varname>log_client_messages</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_client_messages</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，将客户端消息打印到日志中。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-backend-messages" xreflabel="log_backend_messages">
    <term><varname>log_backend_messages</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>log_backend_messages</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 <literal>terse</literal>
      或 <literal>verbose</literal> 时，将后端消息打印到
      日志中。使用 <literal>terse</literal> 时，相同类型的消息数量会被记录，
      并在发送不同类型消息时打印。以下为示例。
      <programlisting>
       LOG:  last DataRow message from backend 0 repeated 10 times
      </programlisting>
      因此，如果对应会话的进程被杀死，日志将不会打印。
      如果你希望在此种情况下也打印日志，请使用 <literal>verbose</literal> 选项。
      注意，使用该选项时每条重复消息都会被打印，产生大量日志行。默认为 <literal>none</literal>，
      这会禁用来自后端的日志消息打印。
     </para>
     <para>
      此参数可通过重新加载
      <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-hostname" xreflabel="log_hostname">
    <term><varname>log_hostname</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_hostname</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，在 <command>ps</> 命令结果和连接日志
      （当 <xref linkend="guc-log-connections"> 开启时）中以主机名代替 IP 地址。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-connections" xreflabel="log_connections">
    <term><varname>log_connections</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_connections</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，将所有客户端连接打印到日志中。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-disconnections" xreflabel="log_disconnections">
    <term><varname>log_disconnections</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_disconnections</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，将所有客户端连接终止打印到日志中。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-pcp-processes" xreflabel="log_pcp_processes">
    <term><varname>log_pcp_processes</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>log_pcp_processes</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，启用关于正常 PCP 进程
      fork 和退出状态的日志记录。默认为 off。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-error-verbosity" xreflabel="log_error_verbosity">
    <term><varname>log_error_verbosity</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>log_error_verbosity</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      控制每条日志消息输出的详细程度。
      有效值为 <literal>TERSE</>、<literal>DEFAULT</> 和 <literal>VERBOSE</>，
      每个级别在显示的消息中增加更多字段。<literal>TERSE</> 不包含 <literal>DETAIL</>、
      <literal>HINT</>、<literal>QUERY</>
      和 <literal>CONTEXT</> 错误信息。
     </para>
     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
      也可以使用 <xref linkend="SQL-PGPOOL-SET"> 命令来修改当前会话中
      此参数的值。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-log-line-prefix" xreflabel="log_line_prefix">
    <term><varname>log_line_prefix</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>log_line_prefix</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      这是一个 <function>printf</> 风格的字符串，在每行日志的开头输出。
      <literal>%</literal> 字符开始<quote>转义序列</>，这些序列会被替换为下方概述的信息。
      所有未识别的转义将被忽略。其他字符直接复制到日志行。默认为 '%m: %a pid %p: '，
      它打印时间戳、应用程序名称和进程 ID。
     </para>

     <table id="log-line-prefix-table">
      <title>log_line_prefix 转义选项</title>

      <tgroup cols="2">
       <thead>
	<row>
	 <entry>转义</entry>
	 <entry>效果</entry>
	</row>
       </thead>

       <tbody>
	<row>
	 <entry>%a</entry>
	 <entry>
	  应用程序名称。子进程（会话进程）的初始值为 "child"。
	  如果客户端在启动消息中或通过 SET 命令设置了应用程序名称，
	  应用程序名称将相应更改。在其他类型的进程中，应用程序名称是硬编码字符串。
	  参见 <xref linkend="application-name-table">。
	 </entry>
	</row>

	<row>
	 <entry>%p</entry>
	 <entry>进程 ID（PID）</entry>
	</row>

	<row>
	 <entry>%P</entry>
	 <entry>进程名称</entry>
	</row>

	<row>
	 <entry>%t</entry>
	 <entry>不含毫秒的时间戳</entry>
	</row>

	<row>
	 <entry>%m</entry>
	 <entry>含毫秒的时间戳</entry>
	</row>

	<row>
	 <entry>%d</entry>
	 <entry>数据库名称</entry>
	</row>

	<row>
	 <entry>%u</entry>
	 <entry>用户名</entry>
	</row>

	<row>
	 <entry>%l</entry>
	 <entry>每个进程的日志行号</entry>
	</row>

	<row>
	 <entry>%%</entry>
	 <entry>'%' 字符</entry>
	</row>

       </tbody>
      </tgroup>
     </table>

     <table id="application-name-table">
      <title>各进程中的应用名称</title>

      <tgroup cols="2">
       <thead>
	<row>
	 <entry>进程类型</entry>
	 <entry>应用程序名称</entry>
	</row>
       </thead>

       <tbody>

	<row>
	 <entry>main</entry>
	 <entry>main</entry>
	</row>

	<row>
	 <entry>child</entry>
	 <entry>child</entry>
	</row>

	<row>
	 <entry>流复制延迟检查工作进程</entry>
	 <entry>sr_check_worker</entry>
	</row>

	<row>
	 <entry>watchdog 心跳发送方</entry>
	 <entry>heart_beat_sender</entry>
	</row>

	<row>
	 <entry>watchdog 心跳接收方</entry>
	 <entry>heart_beat_receiver</entry>
	</row>

	<row>
	 <entry>watchdog</entry>
	 <entry>watchdog</entry>
	</row>

	<row>
	 <entry>watchdog 健康检查</entry>
	 <entry>life_check</entry>
	</row>

	<row>
	 <entry>跟随主节点子进程</entry>
	 <entry>follow_child</entry>
	</row>

	<row>
	 <entry>watchdog 工具进程</entry>
	 <entry>watchdog_utility</entry>
	</row>

	<row>
	 <entry>pcp main</entry>
	 <entry>pcp_main</entry>
	</row>

	<row>
	 <entry>pcp child</entry>
	 <entry>pcp_child</entry>
	</row>

	<row>
	 <entry>健康检查进程</entry>
	 <entry>health_check%d (%d 由后端节点 ID 替换)</entry>
	</row>

	<row>
	 <entry>日志记录进程</entry>
	 <entry>logger（注意：应用程序名称 "logger" 不会输出到 logger 进程管理的日志文件中）</entry>
	</row>

       </tbody>
      </tgroup>
     </table>

     <para>
      此参数可通过重新加载 <productname>Pgpool-II</> 配置来更改。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>
</sect1>
