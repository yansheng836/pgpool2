<!-- doc/src/sgml/runtime.sgml -->

<chapter id="runtime">
 <title>服务器配置与运行</title>

 <para>
  本章讨论如何配置和运行 <productname>Pgpool-II</> 服务器，以及它与操作系统的交互。
 </para>

 <sect1 id="kernel-resource">
  <title>管理内核资源</title>

  <indexterm>
   <primary>内核资源</primary>
  </indexterm>

  <para>
   要运行 <productname>Pgpool-II</productname>，需要使用 System V 共享内存和 System V 信号量。虽然 Linux 上通常提供了足够的这两种资源，但其他平台可能需要配置内核参数。
  </para>
  <para>
   System V 共享内存的最小需求约为 140 MB。如果您计划使用超过默认 64MB 的<link linkend="runtime-in-memory-query-cache">查询缓存</link>，您可能需要更多的共享内存。需要的共享内存段数量为 3。通常内核会提供足够多的共享内存段。
  </para>

  <para>
   System V 信号量的最小需求约为 10。需要的信号量集数量为 3。通常内核会提供足够多的信号量集。
  </para>
 </sect1>

 <sect1 id="pgpool-II-user">
  <title><productname>Pgpool-II</productname> 用户账户</title>

  <indexterm>
   <primary>Pgpool-II 用户</primary>
  </indexterm>

  <para>
   与任何可被外部访问的服务器守护进程一样，建议为 <productname>Pgpool-II</productname> 运行创建一个独立的用户账户。该用户账户应该只拥有服务器管理的数据，并且不应与其他守护进程共享。（例如，使用 <literal>nobody</literal> 用户是一个糟糕的想法。）不建议以该用户身份安装可执行文件，因为一旦系统被攻破，攻击者可能会修改自身的二进制文件。
  </para>

  <para>
   要在您的系统中添加 Unix 用户账户，请查找 <command>useradd</command> 或 <command>adduser</command> 命令。用户名通常使用 <systemitem>pgpool</systemitem>，本书中也是以此假设为准，但您也可以使用其他名称。
  </para>
 </sect1>

 <sect1 id="configuring-pcp-conf">
  <title>配置 pcp.conf</title>

  <indexterm>
   <primary>pcp 配置</primary>
  </indexterm>

  <para>
   <productname>Pgpool-II</productname> 为管理员提供了一个接口，用于执行管理操作，如获取 <productname>Pgpool-II</productname> 状态或远程终止 <productname>Pgpool-II</productname> 进程。<filename>pcp.conf</filename> 是该接口用于认证的密码文件。所有运行模式都需要设置 <filename>pcp.conf</filename> 文件。在安装 <productname>Pgpool-II</productname> 期间会创建一个 <filename>$prefix/etc/pcp.conf.sample</filename> 文件。将该文件复制为 <filename>$prefix/etc/pcp.conf</filename> 并添加您的用户名和密码。

   <screen>
    <prompt>$</> <userinput>cp $prefix/etc/pcp.conf.sample $prefix/etc/pcp.conf</userinput>
   </screen>

   空行或以 <literal>#</literal> 开头的行被视为注释，将被忽略。用户名及其关联的密码必须按照以下格式写在一行中：

   <synopsis>
    <replaceable>用户名</>:<replaceable>[md5 加密密码]</>
   </synopsis>

   <replaceable>[md5 加密密码]</> 可以通过 <filename>$prefix/bin/pg_md5</> 命令生成。

   <screen>
    <prompt>$</> <userinput>pg_md5 your_password</userinput>
    1060b7b46a3bd36b3a0d66e0127d0517
   </screen>

   如果不想将密码作为参数传递，请执行 <command>pg_md5 -p</command>。

   <screen>
    <prompt>$</> <userinput>pg_md5 -p</userinput>
    password: <userinput>your_password</userinput>
   </screen>

   <filename>pcp.conf</filename> 文件必须对执行 <productname>Pgpool-II</productname> 的用户可读。
  </para>

 </sect1>

 <sect1 id="configuring-pgpool">
  <title>配置 Pgpool-II</title>

  <indexterm>
   <primary>Pgpool-II 配置</primary>
  </indexterm>

  <sect2 id="configuring-pgpool-conf">
   <title>配置 pgpool.conf</title>

   <para>
    <filename>pgpool.conf</filename> 是 <productname>Pgpool-II</productname> 的主配置文件。启动 <productname>Pgpool-II</productname> 时，您需要使用 <option>-f</option> 选项指定文件路径。<filename>pgpool.conf</filename> 默认位于 <filename>$prefix/etc/pgpool.conf</filename>（从源代码安装时）。
   </para>

   <para>
    要指定 <productname>Pgpool-II</productname> 的集群模式，请将 <xref linkend="guc-backend-clustering-mode"> 参数设置为下面解释的值。
   </para>

   <table id="backend-clustering-mode-table">
    <title><xref linkend="guc-backend-clustering-mode"> 在 pgpool.conf 中的值</title>

    <tgroup cols="2">
     <thead>
      <row>
       <entry>集群模式</entry>
       <entry>值</entry>
      </row>
     </thead>

     <tbody>
      <row>
       <entry>流复制模式</entry>
       <entry><literal>streaming_replication</literal></entry>
      </row>
      <row>
       <entry>原生复制模式</entry>
       <entry><literal>native_replication</literal></entry>
      </row>
      <row>
       <entry>逻辑复制模式</entry>
       <entry><literal>logical_replication</literal> </entry>
      </row>
      <row>
       <entry>快照隔离模式</entry>
       <entry><literal>snapshot_isolation</literal></entry>
      </row>
      <row>
       <entry>原始模式</entry>
       <entry><literal>raw</literal> </entry>
      </row>
     </tbody>
    </tgroup>
   </table>

   <para>
    如果使用源代码进行默认安装，这些配置文件位于 /usr/local/etc。您可以将其中的一个复制为 <filename>pgpool.conf</filename>。（这可能需要 root 权限）
    <programlisting>
# cd /usr/local/etc
# cp pgpool.conf.sample pgpool.conf
    </programlisting>
   </para>
  </sect2>

  <sect2 id="running-mode">
   <title>Pgpool-II 的集群模式</title>

   <indexterm zone="running-mode">
    <primary>流复制模式</primary>
   </indexterm>

   <indexterm zone="running-mode">
    <primary>原生复制模式</primary>
   </indexterm>

   <indexterm zone="running-mode">
    <primary>主副本模式</primary>
   </indexterm>

   <indexterm zone="running-mode">
    <primary>逻辑复制模式</primary>
   </indexterm>

   <indexterm zone="running-mode">
    <primary>快照隔离模式</primary>
   </indexterm>

   <para>
    <productname>Pgpool-II</>: 中有六种不同的<link linkend="guc-backend-clustering-mode">集群模式</link>：流复制模式、逻辑复制模式、原生复制模式、原始模式和快照隔离模式。在任何模式下，<productname>Pgpool-II</> 都提供连接池和自动故障切换。在线恢复仅可与流复制模式、快照隔离模式和原生复制模式一起使用。有关在线恢复的更多详细信息，请参阅 <xref linkend="runtime-online-recovery">。
   </para>

   <para>
    这些模式互相排斥，启动服务器后无法更改。您应该在系统设计初期就决定使用哪种模式。如果您不确定，建议使用流复制模式或快照隔离模式。
   </para>

   <para>
    <firstterm>流复制模式</firstterm> 可与运行流复制的 <productname>PostgreSQL</> 服务器配合使用。在此模式下，<productname>PostgreSQL</> 负责同步数据库。这是一种广泛使用且最推荐的 <productname>Pgpool-II</> 使用方式。此模式支持负载均衡，但不保证节点间的可见性一致性。
   </para>

   <para>
    在<firstterm>快照隔离模式</firstterm>下，<productname>Pgpool-II</> 负责同步数据库。该模式的优势在于同步是以同步方式进行的：向数据库写入数据时，直到所有 <productname>PostgreSQL</> 服务器完成写入操作后才会返回。它还保证节点间的可见性一致性。简而言之，这意味着单服务器上的事务可见性规则同样适用于由多个服务器组成的集群。这是 <productname>Pgpool-II</productname> 中快照隔离模式的一个显著特性。事实上，<productname>Pgpool-II</productname> 中的快照隔离模式是目前唯一在不修改 <productname>PostgreSQL</> 的情况下保证节点间可见性一致性的系统。因此，应用程序无需感知它们使用的是由 <productname>PostgreSQL</productname> 服务器组成的集群，而非单个 <productname>PostgreSQL</productname> 系统。然而，在此模式下，事务隔离级别<emphasis>必须为 REPEATABLE READ</emphasis>。您需要这样设置 <filename>postgresql.conf</filename>：
    <programlisting>
default_transaction_isolation = 'repeatable read'
    </programlisting>
   </para>
   <para>
    还需要注意，由于保持事务一致性的开销，此模式下的性能可能不如流复制模式和原生复制模式。
   </para>

   <para>
    在<firstterm>原生复制模式</firstterm>下，<productname>Pgpool-II</> 负责同步数据库。该模式的优势在于同步是以同步方式进行的：向数据库写入数据时，直到所有 <productname>PostgreSQL</> 服务器完成写入操作后才会返回。由于不保证节点间的可见性一致性，除非您需要使用除 REPEATABLE READ 之外的隔离模式，否则建议使用快照隔离模式。此模式支持负载均衡。
   </para>

   <para>
    <firstterm>逻辑复制模式</firstterm> 可与运行逻辑复制的 <productname>PostgreSQL</> 服务器配合使用。在此模式下，<productname>PostgreSQL</> 负责同步表。此模式支持负载均衡。由于逻辑复制不会复制所有表，用户有责任确保需要负载均衡的表已被复制。<productname>Pgpool-II</> 会对所有表进行负载均衡。这意味着如果某个表未被复制，<productname>Pgpool-II</> 可能会在订阅方侧查询到过时的表数据。
   </para>

   <para>
    在<firstterm>原始模式</firstterm>下，<productname>Pgpool-II</> 不关心数据库同步。使整个系统有意义地工作是用户的责任。此模式<emphasis>不支持</emphasis>负载均衡。
   </para>
  </sect2>

  <sect2 id="process-management-mode">
   <title>进程管理模式</title>

   <indexterm zone="running-mode">
    <primary>动态进程管理</primary>
   </indexterm>

   <indexterm zone="running-mode">
    <primary>静态进程管理</primary>
   </indexterm>
     <para>
          <productname>Pgpool-II</> 实现了多进程架构，其中每个子进程在任何时候只能处理一个客户端连接。<productname>Pgpool-II</> 能够处理的并发客户端连接总数由<link linkend="guc-num-init-children">num_init_children</link> 配置参数配置。<productname>Pgpool-II</> 支持两种子进程管理模式：<firstterm>动态</firstterm> 和 <firstterm>静态</firstterm>。在静态进程管理模式下，<productname>Pgpool-II</> 在启动时预派生 num_init_children 数量的子进程，每个子进程持续监听传入的客户端连接。而在动态进程管理模式下，<productname>Pgpool-II</> 持续跟踪空闲进程，并通过派生或终止进程来将该数量保持在指定范围内。
     </para>
     <para>
       <xref linkend="guc-process-management-mode"> 在 <productname>Pgpool-II </productname><emphasis>V4.4</emphasis> 之前不可用。
      </para>
  </sect2>


 </sect1>

 <sect1 id="configuring-backend-info">
  <title>配置后端信息</title>

  <para>
   要使 <productname>Pgpool-II</productname> 识别 <productname>PostgreSQL</> 后端服务器，您需要在 <filename>pgpool.conf</filename> 中配置 <varname>backend*</varname>。刚开始使用时，至少需要设置 <xref linkend="guc-backend-hostname"> 和 <xref linkend="guc-backend-port"> 参数才能启动 <productname>Pgpool-II</> 服务器。
  </para>

  <sect2 id="backend-settings">
   <title>后端设置</title>

   <para>
    <productname>Pgpool-II</> 使用的后端 <productname>PostgreSQL</> 必须在 <filename>pgpool.conf</filename> 中指定。请参阅 <xref linkend="runtime-config-backend-settings">。
   </para>

   <!--
   <variablelist>

   <varlistentry id="guc-backend-hostname" xreflabel="backend_hostname">
   <term><varname>backend_hostname</varname> (<type>string</type>)
   <indexterm>
   <primary><varname>backend_hostname</varname> configuration parameter</primary>
  </indexterm>
  </term>

   <listitem>
   <para>
   <varname>backend_hostname</varname> specifies where to
   connect to the <productname>PostgreSQL</productname>
   backend. It is used
   by <productname>Pgpool-II</productname> to communicate
   with the server.
  </para>

   <para>
   For TCP/IP communication, this parameter can take a hostname
   or an IP address. If this begins with a slash, it specifies
   Unix-domain communication rather than TCP/IP; the value is
   the name of the directory in which the socket file is
   stored. The default behavior when backend_hostname is empty
   ('') is to connect to a Unix-domain socket in /tmp.
  </para>

   <para>
   Multiple backends can be specified by adding a number at the
   end of the parameter name (e.g.backend_hostname0). This
   number is referred to as "DB node ID", and it starts from
   0. The backend which was given the DB node ID of 0 will be
   called "Main DB". When multiple backends are defined, the
   service can be continued even if the Main DB is down (not
   true in some modes). In this case, the youngest DB node ID
   alive will be the new Main DB.
  </para>

   <para>
   Please note that the DB node which has id 0 has no special
   meaning if operated in streaming replication mode. Rather,
   you should care about if the DB node is the "primary node" or
   not. See Streaming Replication for more details.
  </para>

   <para>
   If you plan to use only one PostgreSQL server, specify it by
   backend_hostname0.
  </para>

   <para>
   New nodes can be added in this parameter by reloading a
   configuration file. However, values cannot be updated so
   you must restart <productname>Pgpool-II</productname> in
   that case.
  </para>

  </listitem>

  </varlistentry>

   <varlistentry id="guc-backend-port" xreflabel="backend_port">
   <term><varname>backend_port</varname> (<type>integer</type>)
   <indexterm>
   <primary><varname>backend_port</varname> configuration parameter</primary>
  </indexterm>
  </term>

   <listitem>
   <para>
   <varname>backend_port</varname> specifies the port number
   of the backends. Multiple backends can be specified by
   adding a number at the end of the parameter name
   (e.g. backend_port0). If you plan to use only one
   PostgreSQL server, specify it by backend_port0.
  </para>
   <para>
   New backend ports can be added in this parameter by
   reloading a configuration file. However, values cannot be
   updated so you must
   restart <productname>Pgpool-II</productname> in that case.
  </para>
  </listitem>
  </varlistentry>

   <varlistentry id="guc-backend-data-directory" xreflabel="backend_data_directory">
   <term><varname>backend_data_directory</varname> (<type>string</type>)
   <indexterm>
   <primary><varname>backend_data_directory</varname> configuration parameter</primary>
  </indexterm>
  </term>

   <listitem>
   <para>
   <varname>backend_data_directory</varname> specifies the
   database directory. Multiple backends can be
   specified by adding a number at the end of the parameter
   name (e.g. backend_data_directory0). If you plan to use
   only one PostgreSQL server, specify it by
   backend_data_directory0.
  </para>
   <para>
   New backend data_directories can be added in this parameter
   by reloading a configuration file. However, values cannot
   be updated so you must
   restart <productname>Pgpool-II</productname> in that case.
  </para>
  </listitem>
  </varlistentry>

   <varlistentry id="guc-backend-flag" xreflabel="backend_flag">
   <term><varname>backend_flag</varname> (<type>string</type>)
   <indexterm>
   <primary><varname>backend_flag</varname> configuration parameter</primary>
  </indexterm>
  </term>

   <listitem>
   <para>
   <varname>backend_flag</varname> controls various backend
   behavior. Multiple backends can be specified by adding a
   number at the end of the parameter name
   (e.g. backend_flag0). If you plan to use only one
   PostgreSQL server, specify it by backend_flag0.
  </para>
   <para>
   New backend flags can be added in this parameter by
   reloading a configuration file.  Currently followings are
   allowed. Multiple flags can be specified by using "|".
  </para>

   <table id="backend-flag-table">
   <title>Backend flags</title>
   <tgroup cols="2">
   <thead>
   <row>
   <entry>Flag</entry>
   <entry>Description</entry>
  </row>
  </thead>

   <tbody>
   <row>
   <entry><literal>ALLOW_TO_FAILOVER</literal></entry>
   <entry>Allow to failover or detaching backend. This
   is the default. You cannot specify with
   DISALLOW_TO_FAILOVER at a same time.</entry>
  </row>
   <row>
   <entry><literal>DISALLOW_TO_FAILOVER</literal></entry>
   <entry>This is useful when you protect backend by
   using HA (High Availability) softwares such as
   Heartbeat or Pacemaker. You cannot specify with
   ALLOW_TO_FAILOVER at a same time.
  </entry>
  </row>
  </tbody>
  </tgroup>
  </table>

  </listitem>
  </varlistentry>

  </variablelist>

   -->

  </sect2>

 </sect1>

 <sect1 id="server-start">
  <title>启动 Pgpool-II 和 PostgreSQL</title>

  <para>
   要启动 <productname>Pgpool-II</productname>，请执行：
<screen>
$ <userinput>pgpool -f /usr/local/etc/pgpool.conf -F /usr/local/etc/pcp.conf</userinput>
</screen>
   这将在后台启动服务器。"-f" 指定主 pgpool 配置文件的路径，"-F" 指定 pcp 服务器配置文件的路径，pcp 服务器是 <productname>Pgpool-II</productname> 的控制服务器。有关该命令的其他选项，请参阅 <xref
   linkend="pgpool"> 手册。
  </para>

  <para>
   在启动 <productname>Pgpool-II</productname> 之前，您必须先启动 <productname>PostgreSQL</productname>，因为如果 <productname>PostgreSQL</productname> 尚未启动，<productname>Pgpool-II</productname> 会触发故障切换流程，并将 <productname>PostgreSQL</productname> 标记为宕机状态。
  </para>

  <para>
   如果您在控制 <productname>PostgreSQL</productname> 启动顺序方面遇到困难，例如 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname> 安装在不同的服务器上，您可以将 <xref
   linkend="guc-search-primary-node-timeout"> 设置得更长（默认为 5 分钟），以便 <productname>Pgpool-II</productname> 等待 <productname>PostgreSQL</productname> 启动，直到 <xref
   linkend="guc-search-primary-node-timeout"> 超时。如果 <productname>PostgreSQL</productname> 在 <xref
   linkend="guc-search-primary-node-timeout"> 超时前启动，<productname>Pgpool-II</productname> 应该能正常启动。如果 <productname>PostgreSQL</productname> 在 <xref linkend="guc-search-primary-node-timeout"> 超时后才启动，将无法检测到主节点，这意味着您无法执行 DML/DDL 操作。在这种情况下，您需要重新启动 <productname>Pgpool-II</productname>。要确认主节点是否存在，可以使用 <xref linkend="sql-show-pool-nodes"> 命令。
  </para>

  <para>
   请注意，<xref linkend="guc-search-primary-node-timeout"> 只能在流复制模式下使用，因为该参数仅在此模式下有效。有关流复制模式的更多详细信息，请参阅 <xref
   linkend="running-mode">。对于其他模式，请调整健康检查（请参阅 <xref
   linkend="runtime-config-health-check">）参数，以便在 <productname>PostgreSQL</productname> 可用之前有足够的时间。
  </para>

  <para>
   如果健康检查在 <productname>Pgpool-II</productname> 启动之前就检测到 <productname>PostgreSQL</productname> 不可用，某些或全部 <productname>PostgreSQL</productname> 会被识别为"宕机"状态。在这种情况下，您需要使用 <xref linkend="pcp-attach-node"> 命令手动将 <productname>PostgreSQL</productname> 服务器设置为"运行中"状态。如果在 <productname>PostgreSQL</productname> 可用之前客户端尝试连接到 <productname>Pgpool-II</productname>，可能会触发故障切换。在这种情况下，您也需要执行 <xref
   linkend="pcp-attach-node"> 命令将 <productname>PostgreSQL</productname> 服务器设置为"运行中"状态。
  </para>
 </sect1>

 <sect1 id="server-stop">
  <title>停止 Pgpool-II 和 PostgreSQL</title>

  <indexterm zone="server-stop">
   <primary>关闭</primary>
  </indexterm>

  <para>
   要停止 <productname>Pgpool-II</productname>，请执行：
<screen>
$ <userinput>pgpool -f /usr/local/etc/pgpool.conf -F /usr/local/etc/pcp.conf -m fast stop</userinput>
</screen>
   "-m" 选项指定 <productname>Pgpool-II</productname> 的停止方式。"fast" 表示即使仍有来自客户端的现有连接，也立即关闭 <productname>Pgpool-II</productname>。您可以将此选项指定为 "smart"，这将强制 <productname>Pgpool-II</productname> 等待直到所有客户端都与 <productname>Pgpool-II</productname> 断开连接。但这可能导致 <productname>Pgpool-II</productname> 无限等待，可能会引发操作系统发送 SIGKILL 信号并留下垃圾数据，从而在下次启动 <productname>Pgpool-II</productname> 时带来麻烦。
  </para>

  <para>
   关闭 <productname>Pgpool-II</productname> 后，您可以关闭 <productname>PostgreSQL</productname>。
  </para>
 </sect1>

 <sect1 id="server-temporarily-shutdown">
  <title>临时关闭 PostgreSQL</title>

  <indexterm zone="server-temporarily-shutdown">
   <primary>关闭</primary>
  </indexterm>

  <para>
   有时您需要临时停止或重启 <productname>PostgreSQL</productname> 以进行维护或版本升级。本节将介绍如何以最小停机时间完成此任务。
  </para>

  <sect2 id="using-pcp-detach-node">
   <title>使用 pcp_detach_node 命令</title>
   <para>
    如果使用 <command>pg_ctl</command> 停止 <productname>PostgreSQL</productname>，则不会发生故障切换，直到 <productname>Pgpool-II</productname> 通过健康检查检测到它，这取决于健康检查设置，并且断开 <productname>PostgreSQL</productname> 连接需要一些时间。特别是如果启用了<link linkend="tutorial-watchdog">Watchdog</link>且 <xref linkend="guc-failover-require-consensus"> 处于开启状态，<productname>Pgpool-II</productname> 将在超过半数 watchdog 节点同意 <productname>PostgreSQL</productname> 已停止之前不会开始故障切换。如果您使用 <xref linkend="pcp-detach-node"> 断开节点连接，无论健康检查设置如何，故障切换将立即开始。请注意，被断开的 <productname>PostgreSQL</productname> 节点实际上并未停止，如果需要，您需要手动停止它。
   </para>
  </sect2>

  <sect2 id="using-backend-flag">
   <title>使用 backend_flag</title>
   <para>
    停止或重启 <productname>PostgreSQL</productname> 会导致故障切换。如果运行模式不是流复制模式，或者服务器是流复制模式中的备用服务器，这可能没什么大碍，因为客户端始终可以使用集群中的其他服务器。但是，如果服务器是主服务器，它将导致通过提升其中一个备用服务器来更改主服务器。此外，如果集群中只剩下一个服务器，则没有可提升的备用服务器或替代服务器。
   </para>

   <para>
    在这种情况下，您可以使用 <xref linkend="guc-backend-flag"> 来避免故障切换。在 pgpool.conf 中设置以下内容可以避免 backend0 的故障切换。
     <programlisting>
backend_flag0 = DISALLOW_TO_FAILOVER
     </programlisting>
     通过重新加载或重启 <productname>Pgpool-II</productname> 生效。如果设置了此标志，当后端不可用时，不会发生故障切换。在后端不可用期间，客户端将收到错误消息：
     <programlisting>
psql: error: could not connect to server: FATAL:  failed to create a backend connection
DETAIL:  executing failover on backend
     </programlisting>
     重启后端后，客户端可以正常连接。要再次允许在后端上故障切换，您可以设置：
     <programlisting>
backend_flag0 = ALLOW_TO_FAILOVER
     </programlisting>
     然后重新加载或重启 <productname>Pgpool-II</productname>。
   </para>
  </sect2>

 </sect1>

 <sect1 id="backup">
  <title>备份 PostgreSQL 数据库</title>

  <indexterm zone="backup">
   <primary>备份</primary>
  </indexterm>

  <para>
   如果您计划使用 <command>pg_dump</command>、<command>pg_basebackup</command> 或其他任何工具备份 <productname>PostgreSQL</productname> 数据库，我们强烈建议直接针对 <productname>PostgreSQL</productname> 运行这些命令。由于 <productname>Pgpool-II</productname> 是一个代理软件，它在转发消息包时会产生额外的开销。由于获取备份往往会产生大量数据包，除非数据库非常小，否则通过 <productname>Pgpool-II</productname> 执行备份的速度会比直接连接 <productname>PostgreSQL</productname> 慢。
  </para>
  <para>
   此外，并行 <command>pg_dump</command> 如果通过 <productname>Pgpool-II</productname> 执行会报错，因为该命令会处理快照 ID，而快照 ID 是依赖于数据库的对象。
  </para>
  <para>
   在大多数情况下，您应该选择主服务器作为备份目标。如果您想备份备用服务器，则在选择正确的 <productname>PostgreSQL</productname> 服务器获取备份时必须非常小心，因为如果数据已过时，您很可能得到的是过时的数据库备份。您可以使用 <xref linkend="SQL-SHOW-POOL-NODES"> 或 <xref linkend="PCP-NODE-INFO"> 来了解备用服务器与主服务器的同步情况。
  </para>
 </sect1>

</chapter>
