<!-- doc/src/sgml/advanced.sgml -->

<chapter id="tutorial-watchdog">
 <title>Watchdog（监控守护）</title>

 <sect1 id="tutorial-watchdog-intro">
  <title>简介</title>

  <para>
   <firstterm>Watchdog</firstterm> 是 <productname>Pgpool-II</productname> 的一个子进程，用于提供高可用性。Watchdog 通过协调多个 <productname>Pgpool-II</productname> 节点来解决单点故障问题。Watchdog 首次引入于 <productname>pgpool-II</productname>
   <emphasis>V3.2</emphasis> 版本，并在 <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 版本中得到显著增强，以确保始终维持法定人数（quorum）。Watchdog 的这一新增强使其在应对脑裂综合征和网络分区方面更加容错和健壮。此外，<emphasis>V3.7</emphasis> 引入了法定人数故障转移（参见 <xref
    linkend="config-watchdog-failover-behavior">），以减少误判 <productname>PostgreSQL</productname> 服务器故障的情况。<emphasis>为确保法定人数机制正常工作，<productname>Pgpool-II</productname> 节点的数量必须为奇数且大于等于 3。</emphasis>
  </para>

  <sect2 id="tutorial-watchdog-coordinating-nodes">
   <title>协调多个 <productname>Pgpool-II</productname> 节点</title>

   <indexterm zone="tutorial-watchdog-coordinating-nodes">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    Watchdog 通过相互交换信息来协调多个 <productname>Pgpool-II</productname> 节点。
   </para>
   <para>
    在启动时，如果 watchdog 已启用，<productname>Pgpool-II</productname> 节点会从主 watchdog 节点同步所有配置的后端节点状态。如果该节点自身成为主节点，则会在本地初始化后端状态。当后端节点状态因故障转移等发生变化时，watchdog 会将信息通知给其他 <productname>Pgpool-II</productname> 节点并进行同步。当在线恢复发生时，watchdog 会限制其他 <productname>Pgpool-II</productname>
    节点的客户端连接，以避免后端之间出现不一致。
   </para>

   <para>
    Watchdog 还与所有已连接的 <productname>Pgpool-II</productname> 节点进行协调，以确保故障恢复、故障转移和 follow_primary 命令只能在某一个 <productname>pgpool-II</productname> 节点上执行。
   </para>

  </sect2>

  <sect2 id="tutorial-watchdog-lifechecking">
   <title>对其他 <productname>Pgpool-II</productname> 节点的生命检测</title>

   <indexterm zone="tutorial-watchdog-lifechecking">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    Watchdog 生命检测是 watchdog 的一个子组件，用于监控参与 watchdog 集群的 <productname>Pgpool-II</productname> 节点的健康状况，以提供高可用性。传统的 <productname>Pgpool-II</productname> watchdog 提供两种远程节点健康检查方法：<literal>"heartbeat"</literal>（心跳）和 <literal>"query"</literal>（查询）模式。
    <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 的 watchdog 新增了一种 <literal>"external"</literal> 模式至 <xref linkend="guc-wd-lifecheck-method">，
     支持将外部第三方健康检查系统与 <productname>Pgpool-II</productname> watchdog 进行集成。
   </para>
   <para>
    除了远程节点健康检查外，watchdog 生命检测还可以通过监控到上游服务器的连接来检查其所在节点自身的健康状况。如果监控失败，watchdog 会将其视为本地 <productname>Pgpool-II</productname>
    节点故障。
   </para>

   <para>
    在 <literal>heartbeat</literal> 模式下，watchdog 通过使用 <literal>heartbeat</literal> 信号来监控其他 <productname>Pgpool-II</productname>
    进程。Watchdog 定期接收其他 <productname>Pgpool-II</productname>
    发送的心跳信号。如果在一定时间内没有收到信号，watchdog 会将其视为 <productname>Pgpool-II</productname> 故障。
    为了提高冗余性，可以在 <productname>Pgpool-II</productname> 节点之间使用多个网络连接来进行心跳交换。
    这是默认且推荐的健康检查模式。
   </para>

   <para>
    在 <literal>query</literal> 模式下，watchdog 监控的是 <productname>Pgpool-II</productname>
    服务而非进程。在此模式下，watchdog 向其他
    <productname>Pgpool-II</productname> 发送查询并检查响应。
    <note>
     <para>
      请注意，此方法需要来自其他 <productname>Pgpool-II</productname> 的连接，
      因此如果 <xref linkend="guc-num-init-children"> 参数不够大，监控将会失败。
       此模式已废弃，仅保留向后兼容。
     </para>
    </note>
   </para>

   <para>
    <literal>external</literal> 模式由 <productname>Pgpool-II</productname>
    <emphasis>V3.5</emphasis> 引入。此模式基本上禁用了 <productname>Pgpool-II</productname> watchdog 的内置生命检测，
    期望外部系统能够通知 watchdog 关于本地和参与 watchdog 集群的所有远程节点的健康状况。
   </para>

  </sect2>

  <sect2 id="tutorial-watchdog-consistency-of-config">
   <title>所有 <productname>Pgpool-II</productname> 节点上配置参数的一致性</title>

   <indexterm zone="tutorial-watchdog-consistency-of-config">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    启动时，watchdog 会验证本地节点的 <productname>Pgpool-II</productname>
    配置与主 watchdog 节点上的配置是否一致，并警告用户任何差异。
    这样可以消除因不同 <productname>Pgpool-II</productname> 节点上配置不同而导致的不期望行为的可能性。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-changing-active">
   <title>检测到特定故障时切换主/备状态</title>

   <indexterm zone="tutorial-watchdog-changing-active">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    当检测到 <productname>Pgpool-II</productname> 故障时，
    watchdog 会通知其他 watchdog。
    如果这是主 <productname>Pgpool-II</productname>，
    watchdog 会通过投票决定新的主 <productname>Pgpool-II</productname>
    并切换主/备状态。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-automatic-vip">
   <title>自动虚拟 IP 切换</title>

   <indexterm zone="tutorial-watchdog-automatic-vip">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    当备 <productname>Pgpool-II</productname> 服务器提升为主节点时，
    新的主服务器会启用虚拟 IP 接口。同时，之前的主服务器会关闭虚拟 IP 接口。这使得主
    <productname>Pgpool-II</productname> 在服务器切换后仍可使用相同的
    IP 地址工作。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-changing-automatic-register-in-recovery">
   <title>在恢复过程中自动将服务器注册为备用节点</title>

   <indexterm zone="tutorial-watchdog-changing-automatic-register-in-recovery">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    当故障服务器恢复或新服务器接入时，watchdog 进程
    会将此信息通知给集群中的其他 watchdog，并附上新服务器的信息，
    同时 watchdog 进程会接收主服务器和其他服务器的信息。然后，接入的服务器会被注册为备用节点。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-start-stop">
   <title>启动/停止 watchdog</title>

   <indexterm zone="tutorial-watchdog-start-stop">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    Watchdog 进程作为 <productname>Pgpool-II</productname> 的子进程自动启动和停止，因此没有专用的启动和停止 watchdog 的命令。
   </para>
   <para>
    Watchdog 控制虚拟 IP 接口，watchdog 用于启用和关闭 VIP 所需的命令需要
    root 权限。<productname>Pgpool-II</productname> 要求运行 <productname>Pgpool-II</productname> 的用户在启用 watchdog 并结合虚拟 IP 时具有 root
    权限。
    然而，以 root 用户身份运行
    <productname>Pgpool-II</productname> 并不是良好的安全实践，推荐的替代方式是
    以普通用户身份运行 <productname>Pgpool-II</productname>，
    并通过 <command>sudo</command> 使用自定义命令来执行
    <xref linkend="guc-if-up-cmd">、<xref linkend="guc-if-down-cmd">
      和 <xref linkend="guc-arping-cmd">，
       或者对 <literal>if_*</literal> 命令使用 <command>setuid</command>（"执行时设置用户 ID"）
   </para>
   <para>
    生命检测进程是 watchdog 的一个子组件，其职责是监控参与
    watchdog 集群的 <productname>Pgpool-II</productname> 节点的健康状况。当 watchdog 配置为使用内置生命检测时，
    生命检测进程会自动启动，它在 watchdog 主进程初始化完成后启动。
    然而，生命检测进程仅在所有配置的 watchdog
    节点加入集群并变为活跃状态后才会开始工作。如果在生命检测变为活跃之前某个远程节点发生故障，该故障将不会被生命检测捕获。
   </para>
  </sect2>
 </sect1>

 <sect1 id="tutorial-watchdog-integrating-external-lifecheck">
  <title>将外部生命检测集成到 watchdog</title>

  <para>
   <productname>Pgpool-II</productname> watchdog 进程使用
   <acronym>BSD</acronym> 套接字与所有
   <productname>Pgpool-II</productname> 进程进行通信，
   相同的 <acronym>BSD</acronym> 套接字也可由任何第三方
   系统用于为本地和远程
   <productname>Pgpool-II</productname> watchdog 节点提供生命检测功能。
   IPC 的 <acronym>BSD</acronym> 套接字文件名通过在
   <literal>"s.PGPOOLWD_CMD."</literal> 字符串后附加
   <productname>Pgpool-II</productname> wd_port 构建，套接字文件放置在
   <xref linkend="guc-wd-ipc-socket-dir"> 目录中。
  </para>

  <sect2 id="tutorial-watchdog-ipc-command-packet">
   <title>Watchdog IPC 命令数据包格式</title>

   <indexterm zone="tutorial-watchdog-ipc-command-packet">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    Watchdog IPC 命令数据包由三个字段组成。
    下表详细说明了消息字段及其描述。
   </para>

   <table id="wd-ipc-command-format-table">
    <title>Watchdog IPC 命令数据包格式</title>
    <tgroup cols="3">
     <thead>
      <row>
       <entry>字段</entry>
       <entry>类型</entry>
       <entry>描述</entry>
      </row>
     </thead>

     <tbody>
      <row>
       <entry>TYPE</entry>
       <entry>BYTE1</entry>
       <entry>命令类型</entry>
      </row>
      <row>
       <entry>LENGTH</entry>
       <entry>网络字节序的 INT32</entry>
       <entry>后续数据的长度</entry>
      </row>
      <row>
       <entry>DATA</entry>
       <entry><acronym>JSON</acronym> 格式的数据</entry>
       <entry><acronym>JSON</acronym> 格式的命令数据</entry>
      </row>

     </tbody>
    </tgroup>
   </table>
  </sect2>

  <sect2 id="tutorial-watchdog-ipc-result-packet">
   <title>Watchdog IPC 结果数据包格式</title>

   <indexterm zone="tutorial-watchdog-ipc-result-packet">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    Watchdog IPC 命令结果数据包由三个字段组成。
    下表详细说明了消息字段及其描述。
   </para>

   <table id="wd-ipc-result-format-table">
    <title>Watchdog IPC 结果数据包格式</title>
    <tgroup cols="3">
     <thead>
      <row>
       <entry>字段</entry>
       <entry>类型</entry>
       <entry>描述</entry>
      </row>
     </thead>

     <tbody>
      <row>
       <entry>TYPE</entry>
       <entry>BYTE1</entry>
       <entry>命令类型</entry>
      </row>
      <row>
       <entry>LENGTH</entry>
       <entry>网络字节序的 INT32</entry>
       <entry>后续数据的长度</entry>
      </row>
      <row>
       <entry>DATA</entry>
       <entry><acronym>JSON</acronym> 格式的数据</entry>
       <entry><acronym>JSON</acronym> 格式的命令结果数据</entry>
      </row>

     </tbody>
    </tgroup>
   </table>
  </sect2>

  <sect2 id="tutorial-watchdog-ipc-command-packet-types">
   <title>Watchdog IPC 命令数据包类型</title>

   <indexterm zone="tutorial-watchdog-ipc-command-packet-types">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    发送到 watchdog 进程的 IPC 命令数据包的第一字节，以及 watchdog 进程返回的结果，被标识为
    命令或命令结果类型。
    下表列出了所有有效类型及其含义。
   </para>

   <table id="wd-ipc-command-packet--types-table">
    <title>Watchdog IPC 命令数据包类型</title>
    <tgroup cols="4">
     <thead>
      <row>
       <entry>名称</entry>
       <entry>字节值</entry>
       <entry>类型</entry>
       <entry>描述</entry>
      </row>
     </thead>

     <tbody>
      <row>
       <entry>REGISTER FOR NOTIFICATIONS</entry>
       <entry>'0'</entry>
       <entry>命令数据包</entry>
       <entry>用于注册当前连接以接收 watchdog 通知的命令</entry>
      </row>
      <row>
       <entry>NODE STATUS CHANGE</entry>
       <entry>'2'</entry>
       <entry>命令数据包</entry>
       <entry>用于向 watchdog 通知 watchdog 节点状态变更的命令</entry>
      </row>
      <row>
       <entry>GET NODES LIST</entry>
       <entry>'3'</entry>
       <entry>命令数据包</entry>
       <entry>用于获取所有配置的 watchdog 节点列表的命令</entry>
      </row>
      <row>
       <entry>NODES LIST DATA</entry>
       <entry>'4'</entry>
       <entry>结果数据包</entry>
       <entry>数据包中的 <acronym>JSON</acronym> 数据包含所有配置的 watchdog 节点列表</entry>
      </row>
      <row>
       <entry>CLUSTER IN TRANSITION</entry>
       <entry>'7'</entry>
       <entry>结果数据包</entry>
       <entry>当集群正在转换导致无法处理命令时，watchdog 返回此数据包类型。</entry>
      </row>
      <row>
       <entry>RESULT BAD</entry>
       <entry>'8'</entry>
       <entry>结果数据包</entry>
       <entry>当 IPC 命令失败时，watchdog 返回此数据包类型</entry>
      </row>
      <row>
       <entry>RESULT OK</entry>
       <entry>'9'</entry>
       <entry>结果数据包</entry>
       <entry>当 IPC 命令成功时，watchdog 返回此数据包类型</entry>
      </row>

     </tbody>
    </tgroup>
   </table>
  </sect2>

  <sect2 id="tutorial-watchdog-external-lifecheck-ipc">
   <title>外部生命检测 IPC 数据包和数据</title>

   <indexterm zone="tutorial-watchdog-external-lifecheck-ipc">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    "GET NODES LIST"、"NODES LIST DATA" 和 "NODE STATUS CHANGE"
    watchdog IPC 消息可用于集成外部
    生命检测系统。请注意，pgpool 的内置生命检测也使用相同的通道和技术。
   </para>

   <sect3 id="tutorial-watchdog-external-lifecheck-get-nodes">
    <title>获取已配置的 watchdog 节点列表</title>

    <indexterm zone="tutorial-watchdog-external-lifecheck-get-nodes">
     <primary>WATCHDOG</primary>
    </indexterm>
    <para>
     任何第三方生命检测系统都可以在 watchdog IPC 套接字上发送 "GET NODES LIST"
     数据包，并附带包含授权键和值的 <acronym>JSON</acronym>
     数据（如果已设置 <xref linkend="guc-wd-authkey">），或在 <xref linkend="guc-wd-authkey">
      未配置时发送空数据包，以获取
       "NODES LIST DATA" 结果数据包。
    </para>
    <para>
     watchdog 返回的 "GET NODES LIST" 的结果数据包中将包含所有已配置的 watchdog 节点列表，
     用于对其进行健康检查，格式为 <acronym>JSON</acronym>。
     watchdog 节点的 <acronym>JSON</acronym> 包含
     <literal>"WatchdogNodes"</literal> 数组，其中包含所有 watchdog 节点。
     每个 watchdog <acronym>JSON</acronym> 节点包含
     <literal>"ID"</literal>、<literal>"NodeName"</literal>、
     <literal>"HostName"</literal>、<literal>"DelegateIP"</literal>、
     <literal>"WdPort"</literal> 和 <literal>"PgpoolPort"</literal>
     每个节点的信息。
    </para>
    <para>
     <programlisting>
      -- "NODES LIST DATA" 中包含的 JSON 数据示例

      {
      "NodeCount":3,
      "WatchdogNodes":
      [
      {
      "ID":0,
      "State":1,
      "NodeName":"Linux_ubuntu_9999",
      "HostName":"watchdog-host1",
      "DelegateIP":"172.16.5.133",
      "WdPort":9000,
      "PgpoolPort":9999
      },
      {
      "ID":1,
      "State":1,
      "NodeName":"Linux_ubuntu_9991",
      "HostName":"watchdog-host2",
      "DelegateIP":"172.16.5.133",
      "WdPort":9000,
      "PgpoolPort":9991
      },
      {
      "ID":2,
      "State":1,
      "NodeName":"Linux_ubuntu_9992",
      "HostName":"watchdog-host3",
      "DelegateIP":"172.16.5.133",
      "WdPort":9000,
      "PgpoolPort":9992
      }
      ]
      }

      -- 注意：ID 0 始终保留用于本地 watchdog 节点

     </programlisting>
    </para>
    <para>
     从 watchdog 获取已配置的 watchdog 节点信息后，外部生命检测系统可以继续对 watchdog 节点进行
     健康检查，当检测到任何节点的状态变更时，可以通过
     watchdog 的 "NODE STATUS CHANGE" IPC 消息将其通知给 watchdog。
     消息中的数据应包含带有节点 ID 的 <acronym>JSON</acronym>
     （节点 ID 必须与 watchdog 在 WatchdogNodes 列表中返回的该节点的 ID 相同）以及节点的新状态。
    </para>
    <para>
     <programlisting>
      -- 用于向 pgpool-II watchdog 通知节点 ID 为 1 的节点健康检查失败的 JSON 示例如下

      {
      "NodeID":1,
      "NodeStatus":1,
      "Message":"可选的消息字符串，watchdog 将记录此事件"
      "IPCAuthKey":"wd_authkey 配置参数值"
      }

      -- NodeStatus 值的含义如下
      NODE STATUS DEAD  =  1
      NODE STATUS ALIVE =  2

     </programlisting>
    </para>
   </sect3>
  </sect2>
 </sect1>
 <sect1 id="tutorial-watchdog-restrictions">
  <title>watchdog 的限制</title>

  <indexterm zone="tutorial-watchdog-restrictions">
   <primary>WATCHDOG</primary>
  </indexterm>

  <sect2 id="tutorial-watchdog-restrictions-query-mode">
   <title>查询模式生命检测下的 watchdog 限制</title>
   <indexterm zone="tutorial-watchdog-restrictions-query-mode">
    <primary>WATCHDOG</primary>
   </indexterm>

   <para>
    在查询模式下，当所有数据库节点因 PostgreSQL 服务器故障或发出 pcp_detach_node 命令而从
    <productname>Pgpool-II</productname> 中分离时，watchdog 认为
    <productname>Pgpool-II</productname> 服务处于停止状态，并关闭分配给 watchdog 的虚拟 IP。
    因此，<productname>Pgpool-II</productname> 的客户端无法再使用
    虚拟 IP 连接到 <productname>Pgpool-II</productname>。这是为了避免脑裂，
    即出现多个主 <productname>Pgpool-II</productname> 的情况。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-restrictions-down-watchdog-mode">
   <title>连接到 watchdog 状态为停止的 <productname>Pgpool-II</productname></title>
   <indexterm zone="tutorial-watchdog-restrictions-down-watchdog-mode">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    不要使用真实 IP 连接到处于停止状态的 <productname>Pgpool-II</productname>。因为处于停止状态的 <productname>Pgpool-II</productname>
    无法接收来自其他
    <productname>Pgpool-II</productname> watchdog 的信息，所以其后端状态可能与其他 <productname>Pgpool-II</productname> 不同。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-restrictions-down-watchdog-require-restart">
   <title>watchdog 状态为停止的 <productname>Pgpool-II</productname> 需要重启</title>
   <indexterm zone="tutorial-watchdog-restrictions-down-watchdog-require-restart">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    处于停止状态的 <productname>Pgpool-II</productname> 既不能变为活跃状态，也不能作为备用 <productname>Pgpool-II</productname>。
    从停止状态恢复需要重启 <productname>Pgpool-II</productname>。
   </para>
  </sect2>

  <sect2 id="tutorial-watchdog-restrictions-active-take-time">
   <title>watchdog 提升为活跃状态需要几秒钟</title>
   <indexterm zone="tutorial-watchdog-restrictions-active-take-time">
    <primary>WATCHDOG</primary>
   </indexterm>
   <para>
    在主 <productname>Pgpool-II</productname> 停止后，
    备 <productname>Pgpool-II</productname> 提升为新主节点需要几秒钟，
    以确保在向下状态通知数据包发送到其他
    <productname>Pgpool-II</productname> 之前，原有的虚拟 IP 已被关闭。
   </para>
  </sect2>
 </sect1>

 <sect1 id="tutorial-advanced-arch">
  <title>Watchdog 架构</title>

  <para>
   Watchdog 是 <productname>Pgpool-II</productname> 的一个子进程，
   通过协调多个 <productname>Pgpool-II</productname> 来提供高可用性并解决单点故障问题。
   Watchdog 进程在 <productname>Pgpool-II</productname> 启动时自动启动（如果已启用），
   由两个主要组件组成：Watchdog 核心和生命检测系统。
  </para>

  <sect2 id="tutorial-advanced-arch-wd-core">
   <title>Watchdog 核心</title>
   <para>
    Watchdog 核心（称为"watchdog"）是一个
    <productname>Pgpool-II</productname> 子进程，
    负责管理集群中所有 <productname>Pgpool-II</productname> 节点的 watchdog 相关通信，
    并与 <productname>Pgpool-II</productname> 父进程和生命检测进程进行通信。
   </para>
   <para>
    Watchdog 进程的核心是一个状态机，从初始状态（<literal>WD_LOADING</literal>）开始，
    转换到备用状态（<literal>WD_STANDBY</literal>）或主/协调状态（<literal>WD_COORDINATOR</literal>）。
    备用状态和主/协调状态都是 watchdog 状态机的稳定状态，
    节点保持在备用或主/协调状态，直到检测到本地
    <productname>Pgpool-II</productname> 节点出现问题或远程
    <productname>Pgpool-II</productname> 从集群断开连接。
   </para>
   <para>
    Watchdog 进程执行以下任务：
   </para>
   <itemizedlist>
    <listitem>
     <para>
      管理和协调本地节点的 watchdog 状态。
     </para>
    </listitem>

    <listitem>
     <para>
      与内置或外部生命检测系统交互，
      进行本地和远程 <productname>Pgpool-II</productname>
      节点的健康检查。
     </para>
    </listitem>

    <listitem>
     <para>
      与 <productname>Pgpool-II</productname> 主进程交互，
      并提供机制使
      <productname>Pgpool-II</productname> 父进程能够通过
      watchdog 通道执行集群命令。
     </para>
    </listitem>

    <listitem>
     <para>
      与所有参与的 <productname>Pgpool-II
      </productname> 节点通信，以协调主/协调节点的选择并确保集群中的法定人数。
     </para>
    </listitem>

    <listitem>
     <para>
      管理主/协调节点上的虚拟 IP，
      并允许用户提供自定义脚本来处理升级和降级。
     </para>
    </listitem>

    <listitem>
     <para>
      验证 <productname>Pgpool-II</productname>
      配置在 watchdog 集群中参与的所有 <productname>Pgpool-II
      </productname> 节点上的一致性。
     </para>
    </listitem>

    <listitem>
     <para>
      在启动时同步所有 PostgreSQL 后端的状态。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <productname>Pgpool-II</productname> 主进程
      提供分布式锁定功能，
      用于同步不同的故障转移命令。
     </para>
    </listitem>

   </itemizedlist>

   <sect3 id="tutorial-advanced-arch-wd-core-comm">
    <title>与集群中其他节点的通信</title>
    <para>
     Watchdog 使用 TCP/IP 套接字与所有其他节点进行通信。
     每个 watchdog 节点可以与每个节点建立两个套接字连接。一个是该节点创建并发起连接到远程节点的出站（客户端）套接字，另一个是监听由远程
     watchdog 节点发起的入站连接的监听套接字。一旦与远程节点的套接字连接成功，
     watchdog 就会在该套接字上发送添加节点消息（<literal>WD_ADD_NODE_MESSAGE</literal>）。
     收到添加节点消息后，
     watchdog 节点会验证消息中封装的节点信息与 Pgpool-II 对该节点的配置是否匹配，
     如果节点通过验证测试，则将其添加到集群中，否则关闭连接。
    </para>
   </sect3>

   <sect3 id="tutorial-advanced-arch-wd-ipc-data">
    <title>IPC 和数据格式</title>
    <para>
     Watchdog 进程暴露一个 <acronym>UNIX</acronym> 域套接字
     用于 IPC 通信，该套接字接受和提供
     <acronym>JSON</acronym> 格式的数据。所有内部的 <productname>Pgpool-II
     </productname> 进程，包括 <productname>Pgpool-II's</productname>
     内置生命检测和 <productname>Pgpool-II</productname> 主进程
     都使用此 IPC 套接字接口与 watchdog 交互。
     任何外部/第三方系统也可以使用此 IPC 套接字与 watchdog 交互。
    </para>
    <para>
     有关如何使用 watchdog IPC 接口集成外部/第三方系统的详细信息，
     请参见 <xref linkend="tutorial-watchdog-integrating-external-lifecheck">。
    </para>
   </sect3>
  </sect2>

  <sect2 id="tutorial-advanced-arch-wd-lifecheck">
   <title>Watchdog 生命检测</title>
   <para>
    Watchdog 生命检测是 watchdog 的一个子组件，用于监控参与 watchdog
    集群的 <productname>Pgpool-II</productname> 节点的健康状况。<productname>Pgpool-II</productname> watchdog 提供三种内置的远程节点健康检查方法，即"heartbeat"（心跳）、"query"（查询）和"external"（外部）模式。
   </para>
   <para>
    在"heartbeat"模式下，生命检测进程通过
    <acronym>UDP</acronym> 套接字发送和接收数据来检查远程节点的可用性，
    对于每个节点，父生命检测进程会生成两个子进程，一个用于
    发送心跳信号，另一个用于接收心跳。
    而在"query"模式下，生命检测进程使用 PostgreSQL libpq
    接口来查询远程 <productname>Pgpool-II</productname>。
    在此模式下，生命检测进程为每个健康检查查询创建一个新线程，查询完成后线程即被销毁。
    而在"external"模式下，此模式会禁用
    <productname>Pgpool-II</productname> 的内置生命检测，
    期望外部系统来监控本地和远程节点。
   </para>
   <para>
    除了远程节点健康检查外，watchdog 生命检测还可以通过监控到上游服务器的连接来检查其所在节点自身的健康状况。
    为了监控到上游服务器的连通性，<productname>Pgpool-II
    </productname> 生命检测使用 <literal>execv()</literal> 函数来执行
    <command>'ping -q -c3 hostname'</command> 命令。
    因此会为执行每个 ping 命令生成一个新的子进程。
    这意味着在每个健康检查周期内，会为每个配置的上游服务器创建并销毁一个子进程。
    例如，如果生命检测配置了两个上游服务器，并且以十秒为间隔进行健康检查，那么每次十秒的生命检测都会为每个上游服务器生成一个子进程，
    每个进程将持续到 ping 命令执行完毕。
   </para>
  </sect2>

 </sect1>

</chapter>
