<!-- doc/src/sgml/config.sgml -->

<sect1 id="runtime-ssl">
 <title>安全套接层 (SSL)</title>

 <sect2 id="runtime-config-ssl-settings">

  <title>SSL 设置</title>

  <variablelist>

   <varlistentry id="guc-ssl" xreflabel="ssl">
    <term><varname>ssl</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>ssl</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置为 on 时，<productname>Pgpool-II</productname> 将在前端和后端通信中启用 <acronym>SSL</acronym>。
      默认值为 off。
     </para>
     <note>
      <para>
       为了使 SSL 能够用于前端连接，还必须配置
	<xref linkend="guc-ssl-key"> 和 <xref linkend="guc-ssl-cert">。
      </para>
     </note>
     <note>
      <para>
       要使 SSL 正常工作，<productname>Pgpool-II</productname> 必须使用 OpenSSL 支持进行编译。
       有关编译 <productname>Pgpool-II</productname> 的详细信息，请参阅 <xref linkend="install-pgpool">。
      </para>
     </note>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-key" xreflabel="ssl_key">
    <term><varname>ssl_key</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_key</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于入站前端连接的私钥文件。相对路径是相对于
      <productname>Pgpool-II</productname> 配置目录的。
      此选项没有默认值，如果未设置，则入站前端连接的
      <acronym>SSL</acronym> 将被禁用。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-cert" xreflabel="ssl_cert">
    <term><varname>ssl_cert</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_cert</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于入站前端连接的公钥 x509 证书文件。
      相对路径是相对于 <productname>Pgpool-II</productname> 配置目录的。
      此选项没有默认值，如果未设置，则入站前端连接的
      <acronym>SSL</acronym> 将被禁用。
     </para>

     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-ca-cert" xreflabel="ssl_ca_cert">
    <term><varname>ssl_ca_cert</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_ca_cert</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <acronym>PEM</acronym> 格式的 <acronym>CA</acronym>
      证书文件，可用于验证后端服务器证书。
      相对路径是相对于 <productname>Pgpool-II</productname> 配置目录的。
      这类似于 <command>OpenSSL verify(1)</command> 命令的
      <command>-CApath</command> 选项。
     </para>

     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-ca-cert-dir" xreflabel="ssl_ca_cert_dir">
    <term><varname>ssl_ca_cert_dir</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_ca_cert_dir</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定包含 <acronym>PEM</acronym> 格式 <acronym>CA</acronym>
      证书文件的目录路径，可用于验证后端服务器证书。
      这类似于 <command>OpenSSL verify(1)</command> 命令的
      <command>-CApath</command> 选项。
     </para>
     <para>
      此选项的默认值为未设置，这意味着不进行任何验证。
      如果未设置此选项但为 <xref linkend="guc-ssl-ca-cert"> 提供了值，
      则仍会进行验证。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-crl-file" xreflabel="ssl_crl_file">
    <term><varname>ssl_crl_file</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_crl_file</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定包含 SSL 服务器证书吊销列表 (CRL) 的文件。
      相对路径是相对于 <productname>Pgpool-II</productname> 配置目录的。
      默认为空，表示不加载任何 <acronym>CRL</acronym> 文件。
     </para>

     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-ciphers" xreflabel="ssl_ciphers">
    <term><varname>ssl_ciphers</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_ciphers</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 SSL 连接允许使用的 <acronym>SSL</acronym> 密码套件列表。
      有关此设置的语法和支持值的列表，请参阅
      <application>OpenSSL</application> 包中的
      <citerefentry><refentrytitle>ciphers</refentrytitle></citerefentry>
      手册页。
      仅影响使用 TLS 1.2 及更低版本的连接。
      目前没有控制 TLS 1.3 连接使用的密码选择的设置。
      默认值为 <literal>HIGH:MEDIUM:+3DES:!aNULL</literal>，与
      <productname>PostgreSQL</productname> 相同。
      有关为何选择该值，请参阅 <productname>PostgreSQL</productname> 手册。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-prefer-server-ciphers" xreflabel="ssl_prefer_server_ciphers">
    <term><varname>ssl_prefer_server_ciphers</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>ssl_prefer_server_ciphers</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定是否使用服务器的 <acronym>SSL</acronym>
      密码偏好设置，而不是客户端的。
      默认值为 false。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-ecdh-curve" xreflabel="ssl_ecdh_curve">
    <term><varname>ssl_ecdh_curve</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_ecdh_curve</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定在 <acronym>ECDH</acronym> 密钥交换中使用的曲线名称。
      所有连接的客户端都必须支持该曲线。
      它不需要与服务器椭圆曲线密钥使用的曲线相同。
      默认值为 <literal>prime256v1</literal>。
     </para>
     <para>
      最常见曲线的 OpenSSL 名称为：
      <literal>prime256v1</literal> (NIST P-256)，
      <literal>secp384r1</literal> (NIST P-384)，
      <literal>secp521r1</literal> (NIST P-521)。
      可使用命令 <command>openssl ecparam -list_curves</command> 显示所有可用曲线的完整列表，
      但它们并非全部都能在 <acronym>TLS</acronym> 中使用。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-dh-params-file" xreflabel="ssl_dh_params_file">
    <term><varname>ssl_dh_params_file</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_dh_params_file</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定包含 Diffie-Hellman 参数的文件名，
      这些参数用于所谓的一次性 DH 系列的 SSL 密码。默认为空。
      如果为空或指定文件不正确，则使用编译内置的默认 DH 参数。
      使用自定义 DH 参数可以降低攻击者破解知名编译内置 DH 参数时的风险。
      可使用命令 <command>openssl -out dhparams.pem 2048</command> 创建自己的
      DH 参数文件。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ssl-passphrase-command" xreflabel="ssl_passphrase_command">
    <term><varname>ssl_passphrase_command</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ssl_passphrase_command</varname> 配置参数</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      设置一个外部命令，当需要获取解密 SSL 文件（如私钥）的密码短语时，将调用该命令。
      默认情况下，此参数为空，这意味着如果需要密码短语则不会加载 SSL 文件。
     </para>
     <para>
      该命令必须将密码短语打印到标准输出，并以代码 0 退出。
      在参数值中，%p 将被替换为提示字符串。（写入 %% 表示字面量 %。）
      注意，提示字符串可能包含空格，因此请确保适当引用。
      如果存在换行符，则从输出的末尾去除单个换行符。
     </para>
     <para>
      该命令实际上不一定需要向用户提示输入密码短语。
      它可以读取密码短语来自文件、从钥匙串设施获取，或以类似方式获取。
      确保所选机制足够安全是用户的责任。
     </para>
     <para>
      通过重新加载 <productname>Pgpool-II</> 配置可以更改此参数。
      但是，如果新参数无效，则在重新启动 <productname>Pgpool-II</>
      之前将保留旧值，即使在 "pgpool show" 中显示了新参数也是如此。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="runtime-g-connection-pooling-settings">

  <title>生成 SSL 证书</title>

  <para>
   证书处理不在本文档范围内。<ulink url="http://developer.postgresql.org/pgdocs/postgres/ssl-tcp.html">
    Secure TCP/IP Connections with SSL</> 页面提供了有关如何生成自签名
   证书的示例命令和指引。
  </para>

 </sect2>

</sect1>