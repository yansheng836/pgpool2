<sect1 id="release-4-7-2">
 <title>版本 4.7.2</title>
 <note>
  <title>发布日期</title>
  <simpara>2026-06-04</simpara>
 </note>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-05-26 [14cd8d073]
    -->
    <para>允许 <literal>pgproto</literal> 接受换行数据。（石井達夫）</para>
    <para>现在 <literal>"\n"</literal> 被识别为换行符。这对于 <command>COPY</command> 命令是必要的。</para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [c8642fa27]
    -->
    <para>当主 watchdog 报告初始 -2 哨兵值时，保留本地 primary_node_id。（石井達夫）</para>
    <para>修复所有 pgpool 节点同时重启后的 watchdog 同步问题：备用节点可能用初始 -2 哨兵值覆盖有效的 primary_node_id，并无限期停留在未初始化状态。修复方法是：当领导者报告初始 -2 哨兵值时，保留本地 primary_node_id。</para>
    <para>问题由 Emond Papegaaij 和 Claude Code 报告。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/CAGXsc%2BZmBoLs3Mz%3DG-Bdm4JJG%2BfH1NpHfR3qVJVwW4eBKWwStQ%40mail.gmail.com">干净启动时的主节点检测竞争</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [a483e0315]
    -->
    <para>修复在 <function>elog(DEBUG)</function> 中调用 <function>pool_get_transaction_isolation()</function> 的问题。（石井達夫）</para>
    <para>修复 <function>where_to_send_main_replica()</function> 和 <function>where_to_send_native_replication()</function> 中 DEBUG 日志导致的已中止事务中的 FATAL 错误。DEBUG 日志调用了 <function>pool_get_transaction_isolation()</function>，该函数内部通过 <function>do_query()</function> 执行查询，可能在已中止事务中引发 FATAL 错误。修复方法是：从 DEBUG 日志中移除这些调用。</para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [a4aa28c46]
    -->
    <para>修复已中止事务中的空查询情况。（石井達夫）</para>
    <para>修复已中止事务中空查询错误地引发错误的问题。空查询在已中止事务中应该是允许的，但之前的提交导致 <function>check_transaction_state_and_abort()</function> 拒绝了它们。修复方法是：在 <function>SimpleQuery()</function> 中检测空查询并绕过检查。</para>
    <para>问题由 Ronaldo Raivil 报告。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/161">pgpool >= 4.3.2 在已中止保存点内的驱动健康检查探测中合成 25P02 错误，破坏了需要调用 ROLLBACK TO SAVEPOINT 的 ORM</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-06 [eab08304d]
    -->
    <para>修复关于 <varname>ssl_dh_params_file</varname> 的描述。（石井達夫）</para>
    <para>它实际上不仅在参数为空（默认值）时回退到编译内置的默认值，而且在指定的参数不正确时也会回退。</para>
   </listitem>

   <listitem>
    <!--
    2026-04-27 [4b4df1365]
    -->
    <para>修复 PCP 主进程在重启时记住子进程 PID 的问题。（石井達夫）</para>
    <para>
     在故障转移/故障恢复时，pgpool 主进程向 PCP 主进程发送信号请求重启，PCP 主进程随后重启。问题是，当 PCP 主进程重启时，它会忘记其子 PCP 工作进程的 PID 列表。

     结果，诸如 <command>pcp_promote_node</command> 之类的命令可能会无限期挂起。修复方法是：将 PCP 工作进程 PID 存储在共享内存中，以便它们在 PCP 主进程重启后仍然存在。
    </para>
    <para>问题由 Taiki Koshino 报告。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260425.223051.1207744844622514060.ishii%40postgresql.org">PCP 进程问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-07 [22d332216]
    -->
    <para>修复查询缓存：在使用 <command>MERGE</command> 语句时使缓存失效。（石井達夫）</para>
    <para><command>MERGE</command> 是在 <productname>PostgreSQL</productname> 15 中引入的，<productname>Pgpool-II</productname> 4.4 导入了 <productname>PostgreSQL</productname> 15 的解析器。然而，查询缓存子模块中存在一个疏忽：<command>MERGE</command> 语句没有触发查询缓存的失效。</para>
    <para>问题由 Nadav Shatz 报告。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260407.090802.1335576118037412011.ishii%40postgresql.org">回复：提案：内存中最近变更表的跟踪</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-06 [ae5aea968]
    -->
    <para>修复 SSL 模块中的内存泄漏。（石井達夫）</para>
    <para>修复 <function>load_dh_file()</function> 在提供无效 DH 参数时的内存泄漏。该函数未能释放 <function>PEM_read_DHparams()</function> 返回的内存。同时更新同一文件中的版权声明，以保留原始的 <productname>PostgreSQL</productname> 版权。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260403.091842.102589579921239540.ishii%40postgresql.org">SSL 模块中的内存泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-31 [3355c0ce4]
    -->
    <para>修复 <command>SET transaction_read_only = 0</command> 的路由错误。（Bo Peng）</para>
    <para>修复 <command>SET transaction_read_only = 0</command> 错误路由到备用节点导致的查询错误。同时将不正确的 bool ret = POOL_BOTH 替换为适当的枚举类型。</para>
    <para>问题由 liujinyang-highgo 报告。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/155">set 命令 \'set transaction_read_only = 0;\' 路由到备用节点并执行失败。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-25 [3c777f213]
    -->
    <para>移除 where_to_send_main_replica() 中不必要的 STREAM 检查。（石井達夫）</para>
    <para>该检查已在 check_replication_delay() 中完成。</para>
   </listitem>

   <listitem>
    <!--
    2026-03-20 [62343bcdb]
    -->
    <para>修复会话本地关系缓存情况错误。（石井達夫）</para>
    <para>修复 <function>pool_search_relcache()</function> 中会话本地关系缓存的不正确处理。临时表信息可能被错误地注册到共享关系缓存中，导致后续查询使用过期的缓存条目并可能错误地路由查询。修复方法是：避免将会话本地缓存条目的临时表信息注册到共享关系缓存中。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/154">查询临时表被路由到备用节点。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-19 [c6bc87955]
    -->
    <para>修复 <function>pool_search_relcache()</function> 中的锁释放泄漏。（石井達夫）</para>
    <para>修复 <function>pool_search_relcache()</function> 中的锁释放泄漏。在共享关系缓存更新期间，重新获取排他锁后锁状态没有正确重置，可能导致共享内存锁一直持有。同时将容易混淆的变量 <varname>locked</varname> 重命名为 <varname>locked_by_others</varname>，并修复不必要的 <varname>per_node_statement_log</varname> 输出。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260317.182615.1046517700474336117.ishii%40postgresql.org">pool_search_relcache 中的锁释放泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-09 [9f918c15e]
    -->
    <para>在 fork 之前关闭监听套接字。（石井達夫）</para>
    <para>修复 fork 的子进程中继承了不必要的监听套接字。以前，pcp_main、健康检查和生命周期检查等子进程继承了它们不使用的 pgpool 和/或 PCP 监听套接字，这可能会阻止后续 pgpool 启动时正确绑定端口。修复方法是：使用 <function>close_listening_sockets()</function> 在 fork 后关闭不必要的监听套接字。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260302.100028.1346768433787074248.ishii%40postgresql.org">在 fork 之前关闭监听套接字</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-05 [7e629039b]
    -->
    <para>降低 <command>pcp_proc_info</command> 和 <command>show pool_pools</command> 等中使用的调试日志级别。（石井達夫）</para>
    <para>之前提交意外添加的调试日志已从 LOG 降级为 DEBUG5。</para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-03-04 [bc1ff3d31]
    -->
    <para>文档：修复 <varname>ignore_leading_white_space</varname> 和 <varname>allow_sql_comments</varname> 的文档。（石井達夫）</para>
    <para>它们缺少关于默认值的描述。</para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-03-25 [7c769d67a]
    -->
    <para>测试：为之前提交补充 001.load_balance 测试。（石井達夫）</para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>
<sect1 id="release-4-7-1">
 <title>版本 4.7.1</title>
 <note>
  <title>发布日期</title>
  <simpara>2026-02-26</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-02-24 [7bfc5e989]
    -->
    <para>添加参数以指定 watchdog/心跳监听地址和端口。（Taiki Koshino）</para>
    <para>通过指定 <xref linkend="guc-wd-listen-address"> 和 <xref linkend="guc-wd-listen-port">，watchdog 将仅监听指定的地址和端口。您现在还可以使用 <xref linkend="guc-wd-heartbeat-listen-addresses"> 和 <xref linkend="guc-wd-heartbeat-listen-port"> 指定心跳进程的监听端口。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/CAGXsc%2BYo7zOYmqLwcf_uoFUFn65jpySUU-QAseKn7rzr_zAmLA%40mail.gmail.com">回复：Pgpool-II 4.7.0 发布。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-29 [118f61932]
    -->
    <para>允许并行 <command>make</command>。（石井達夫）</para>
    <para>以前，并行 <command>make</command> 会失败并显示：<programlisting>
In file included from parser.c:27:
../../src/include/parser/gramparse.h:33:10: fatal error: gram.h: No such file or directory
33 | #include "gram.h"
     </programlisting>
    </para>
    <para>补丁由 anthonyroussel 创建，石井達夫审阅。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/pull/146">修复并行构建</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-26 [d638018ce]
    -->
    <para>添加关于 <xref linkend="guc-backend-hostname"> 和 <varname>primary_conninfo</varname> 主机不匹配的详细日志。（Taiki Koshino）</para>
    <para>如果 <filename>pgpool.conf</filename> 中设置的 <xref linkend="guc-backend-hostname"> 与 <productname>PostgreSQL</productname> 的 <varname>primary_conninfo</varname> 中的主机设置不匹配，将输出以下日志消息。<programlisting>
sr_check_worker pid 25099: DETAIL: pgpool.conf backend_hostname: "pgpool0" does not match PostgreSQL primary_conninfo host: "pgpool_logtest"
     </programlisting>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-12-24 [ef33aca38]
    -->
    <para>明确声明支持的最低 OpenSSL 版本。（石井達夫）</para>
    <para>可以通过在 configure.ac 中使用以下方式实现：<programlisting>
AC_DEFINE(OPENSSL_API_COMPAT, [0x10002000L],
          [Define to the OpenSSL API version in use. This avoids deprecation warnings from newer OpenSSL versions.])
     </programlisting>支持的最低版本是 1.0.2，这与 <productname>PostgreSQL</productname> 目前相同。（实际上 AC_DEFINE 是从 <productname>PostgreSQL</productname> 引入的）。这也可以避免如果您的 OpenSSL 版本较新（如 3.0）时出现的弃用警告。此外，在安装文档中添加了说明，要求 OpenSSL 的最低版本为 1.0.2。</para>
   </listitem>
   <listitem>
    <!--
    2026-01-29 [940f9e848]
    -->
    <para>将 libpcp 重命名为 libpgpoolpcp 以避免与 RHEL PCP 库冲突。（Bo Peng）</para>
    <para><productname>Pgpool-II</productname> 提供的 libpcp.so 库与 RHEL 附带的 libpcp.so 冲突。为避免此冲突，<productname>Pgpool-II</productname> 的 libpcp 库已重命名为 libpgpoolpcp。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/142">由于与 RHEL 提供的库命名冲突，安装 pgpool-II 失败</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-02-12 [170c2dc65]
    -->
    <para>修复当既未配置 <varname>logdir</varname> 也未配置 <xref linkend="guc-work-dir"> 时的启动失败。（Taiki Koshino）</para>
    <para>问题由 Luca Ferrari 报告，Taiki Koshino 审阅。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/144">work_dir 和/或 logdir 在 4.7 上未设置为默认值？</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-31 [55ee45801]
    -->
    <para>修复 <function>check_transaction_state_and_abort()</function> 中的疏忽。（石井達夫）</para>
    <para>该函数未考虑前端为 NULL 的情况。</para>
    <para>问题由 Coverity 指出。</para>
   </listitem>

   <listitem>
    <!--
    2026-01-22 [c0f2cb883]
    -->
    <para>修复 <function>walker()</function> 函数使其提前返回。（石井達夫）</para>
    <para>
     以前，<function>walker()</function> 函数一旦找到目标对象就返回 false。但可以进行短路遍历，本应如此实现。

     同时从 <productname>PostgreSQL</productname> 导入注释以阐明 walker 函数设计。
    </para>
    <para>问题由 liujinyang-highgo 报告，石井達夫审阅。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/143">函数 view_walker() 中的不当处理。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-09 [7b00d24de]
    -->
    <para>修复 <literal>scram-sha-256</literal> 认证中的内存泄漏。（石井達夫）</para>
    <para>问题由 Coverity 指出。</para>
   </listitem>

   <listitem>
    <!--
    2026-01-07 [81e8d3ec6]
    -->
    <para>修复检查 <function>malloc()</function> 返回值的问题。（石井達夫）</para>
    <para><function>create_inet_domain_sockets()</function> 在为套接字 fd 分配内存时忘记检查 <function>malloc()</function> 的返回值。</para>
   </listitem>

   <listitem>
    <!--
    2026-01-06 [2218b5e56]
    -->
    <para>修复健康检查进程不启动的问题。（石井達夫）</para>
    <para>
     以前，如果后端节点宕机，<function>pgpool_main()</function> 不会启动健康检查进程。这阻止了 auto_failback 工作。当未使用 <option>-D</option> 选项时，Pgpool 从 <varname>pool_status</varname> 文件读取状态。如果文件中记录的状态是"down"，pgpool 将不会为该节点启动健康检查进程。由于 auto_failback 是从健康检查进程调用的，这意味着 auto_failback 对该后端节点不起作用。

     修复方法是：无条件启动健康检查进程。
    </para>
    <para>问题由 Meng Feilong 报告，石井達夫审阅。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/140">处于宕机状态的节点无法进行故障恢复</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-24 [9acb78c01]
    -->
    <para>修复 nodes.h 中重复的 NodeTag 枚举。（石井達夫）</para>
    <para>修复了从 <productname>PostgreSQL</productname> 导入的 nodes.h 中的 NodeTag 定义与 Pgpool-II 自身定义之间的重复。</para>
    <para>问题由 liujinyang-highgo 报告和诊断，石井達夫审阅。</para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/138">关于 NodeTag 定义的问题。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-23 [77096f651]
    -->
    <para>修复 <function>pcp_connect()</function> 中的资源泄漏。（石井達夫）</para>
    <para>该函数在某些地方没有调用 <function>freeaddrinfo()</function>。</para>
   </listitem>

   <listitem>
    <!--
    2025-12-23 [515d5fe85]
    -->
    <para>修复可能的整数溢出。（石井達夫）</para>
    <para>在 <function>pool_write_flush()</function> 和 <function>pool_flus_it()</function> 中使用了 <function>write(2)</function>。<function>write(2)</function> 返回 ssize_t，但在代码中有些地方将返回值赋给了 int 变量。结果可能导致返回值被求值为负值，从而引发错误。</para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>
<sect1 id="release-4-7-0">
 <title>版本 4.7.0</title>
 <note>
  <title>发布日期</title>
  <simpara>2025-12-23</simpara>
 </note>

 <sect2>
  <title>概述</title>
  <para>本版本的 <productname>Pgpool-II</productname> 包含多项增强功能，例如移除遗留的 Slony 模式、加强安全性、增强前端/后端协议兼容性以及提高操作透明度。</para>

  <para>主要增强功能如下：</para>

  <itemizedlist>

   <listitem>
    <para>弃用 Slony 模式。</para>
   </listitem>

   <listitem>
    <para>将 <varname>logdir</varname> 参数重命名为 <varname>work_dir</varname>。</para>
   </listitem>

   <listitem>
    <para>添加对前端/后端协议版本 3.2 的支持。</para>
   </listitem>

   <listitem>
    <para>增强 watchdog 和心跳接收器的安全性。</para>
   </listitem>

   <listitem>
    <para>向 <command>pcp_proc_info</command> 和 <command>show pool_pools</command> 添加新字段以显示客户端信息，并添加 <function>pgpool_adm_pcp_proc_info</function> 扩展函数。</para>
   </listitem>

   <listitem>
    <para>使在线恢复数据库可配置。</para>
   </listitem>

   <listitem>
    <para>导入 PostgreSQL 18 的 SQL 解析器。</para>
   </listitem>

  </itemizedlist>
 </sect2>
<sect2 id="migration-4-7-0">
  <title>迁移到版本 4.7</title>
  <itemizedlist>

   <listitem>
    <!--
    2025-10-29 [b402dbc09]
    -->
    <para>弃用 <literal>Slony</literal> 模式。（石井達夫）</para>
    <para>以前，<productname>Pgpool-II</productname> 接受 <literal>slony</literal> 模式作为 <xref linkend="guc-backend-clustering-mode"> 之一以支持 <literal>Slony-I</literal> (https://www.slony.info/)。然而最新的 <literal>Slony-I</literal> 发布于 2022 年，至今已近 3 年。我们没有收到任何用户关于弃用 <literal>Slony</literal> 模式的反馈。这表明没有活跃的 <literal>Slony-I</literal> 模式用户。因此，我们决定移除 <literal>Slony-I</literal> 支持。</para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-May/009489.html">[pgpool-general: 9486] 弃用 slony 模式</ulink>
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20250722.153130.1007226654125839063.ishii%40postgresql.org">https://www.postgresql.org/message-id/20250722.153130.1007226654125839063.ishii%40postgresql.org</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-15 [579306dbc]
    -->
    <para>将 <xref linkend="guc-log-pcp-processes"> 默认值更改为 <literal>off</literal>。（石井達夫）</para>
    <para>以前，<varname>log_pcp_processes</varname> 的默认值是 <literal>on</literal>，如果管理员频繁使用 pcp 命令监控 pgpool 状态，可能会产生过多的日志行。此外，该日志属于调试信息，因此最好将其默认值改为 <literal>off</literal>。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20250815.111100.2261166502134199972.ishii%40postgresql.org">https://www.postgresql.org/message-id/20250815.111100.2261166502134199972.ishii%40postgresql.org</ulink>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-07-17 [3cc3d2d84]
    2025-07-25 [a16b63b8d]
    -->
    <para>使在线恢复数据库可配置。（Bo Peng）</para>
    <para>新增了一个配置参数 <xref linkend="guc-recovery-database">，允许用户指定用于在线恢复的数据库。以前，该值被硬编码为 <literal>template1</literal>。现在默认设置为 <literal>postgres</literal>。</para>
    <para>对于从 4.6 之前版本迁移的用户，可以通过设置 <varname>recovery_database = 'template1'</varname> 或在 <literal>postgres</literal> 数据库中创建在线恢复所需的扩展来保持兼容性。</para>
   </listitem>
   <listitem>
    <!--
    2025-10-14 [b286ce29a]
    2025-10-14 [25ad5e466]
    -->
    <para>将 <varname>logdir</varname> 参数重命名为 <xref linkend="guc-work-dir">。（Taiki Koshino）</para>
    <para>以前，存储 <filename>pgpool_status</filename> 和锁文件的目录由 <varname>logdir</varname> 参数指定。但是，由于名称 <varname>logdir</varname> 具有误导性，为清晰起见，该参数已重命名为 <varname>work_dir</varname>。默认值为 <literal>/tmp</literal>，与之前的参数 <varname>logdir</varname> 相同。</para>
    <para>为了向后兼容，仍然支持旧的 <varname>logdir</varname> 参数。如果使用了旧的 <varname>logdir</varname> 参数，<productname>Pgpool-II</productname> 将将其值设置为 <varname>work_dir</varname> 并发出警告消息。</para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>变更</title>
  <itemizedlist>

   <listitem>
    <!--
    2025-10-03 [df2771637]
    2025-10-05 [020ee7084]
    -->
    <para>导入 PostgreSQL 18 的新解析器。（Bo Peng）</para>
    <para>PostgreSQL 18 解析器的主要变更包括：</para>
    <itemizedlist>
     <listitem>
      <para>允许生成列成为虚拟列</para>
     </listitem>
     <listitem>
      <para>在 DML 查询的 RETURNING 中添加 OLD/NEW 支持</para>
     </listitem>
     <listitem>
      <para>添加各种约束（例如 WITHOUT OVERLAPS、ENFORCED / NOT ENFORCED）</para>
     </listitem>
     <listitem>
      <para>等。</para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <!--
    2025-08-22 [94b62a93c]
    2025-09-01 [f15c8a302]
    2025-09-01 [b9e7b4245]
    -->
    <para>限制 watchdog 和心跳接收器仅监听配置的地址。（Bo Peng）</para>
    <para>以前，watchdog 和心跳接收器进程都监听所有接口。出于安全原因，它们现在仅监听 <varname>hostname</varname> 和 <varname>heartbeat_hostname</varname> 指定的地址。</para>
   </listitem>

   <listitem>
    <!--
    2025-08-16 [dfffcddd6]
    -->
    <para>清理 watchdog 源代码。（石井達夫）</para>
    <para>
    <itemizedlist>
     <listitem>
      <para>从 wd_utils.h 中移除 wd_is_upper_ok()，因为实际上没有它的定义。</para>
     </listitem>
     <listitem>
      <para>将 wd_get_ping_result() 和 wd_issue_ping_command() 改为静态函数。它们只在 wd_ping.c 中使用。</para>
     </listitem>
    </itemizedlist>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-13 [a85b848cc]
    -->
    <para>如果生命周期检查尚未启动，则发出警告。（石井達夫）</para>
    <para>目前，lifecheck 仅在生命周期检查就绪时输出日志。这对管理员来说很不方便，因为他们需要持续查看日志文件直到生命周期检查就绪。此提交使生命周期检查进程定期发出额外警告，直到生命周期检查启动。警告的间隔为 wd_interval * 10 秒。但在 lifecheck 首次启动时不会发出警告，因为此时 lifecheck 很可能尚未就绪。</para>
   </listitem>

   <listitem>
    <!--
    2025-07-09 [766e73811]
    2025-07-15 [523b3d94d]
    -->
    <para>添加对前端/后端协议 3.2 的支持。（石井達夫）</para>
    <itemizedlist>
     <listitem>
      <para>实现 NegotiateProtocolVersion 消息。</para>
      <para>当前端请求协议版本 3.2（即 PostgreSQL 18+ 或兼容客户端）而后端仍然仅支持 3.0（即后端为 PostgreSQL 17 或更早版本）时，实现该消息是必要的。</para>
      <para>此提交处理该消息，使得当不存在连接缓存时，该消息从后端转发到前端。</para>
      <para>如果存在连接缓存，pgpool 将发送创建连接缓存时保存的消息给前端。</para>
      <para>
       讨论：<ulink url="https://www.postgresql.org/message-id/20250708.112133.1324153277751075866.ishii%40postgresql.org">https://www.postgresql.org/message-id/20250708.112133.1324153277751075866.ishii%40postgresql.org</ulink>
      </para>
     </listitem>

     <listitem>
      <para>实现协议版本 3.2 的 BackendKeyData 和查询取消消息。</para>
      <para>从 PostgreSQL 18 开始，前端/后端协议已更改为 3.2。在此变更中，BackendKeyData 和查询取消消息被修改为允许可变长度的取消密钥。</para>
      <para>此提交实现了这些更改，现在我们可以使用 3.2 协议连接到 PostgreSQL 前端和后端。</para>
      <programlisting>
Example session is:
PGMAXPROTOCOLVERSION="3.2" psql -p 11000 test
      </programlisting>
      <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20250714.155710.1706961744888449986.ishii%40postgresql.org">https://www.postgresql.org/message-id/20250714.155710.1706961744888449986.ishii%40postgresql.org</ulink>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <!--
    2025-06-24 [706ca859c]
    2025-07-15 [0e80ac4e5]
    2025-07-15 [f4cfd6188]
    -->
    <para>添加 <xref linkend="pgpool-adm-pcp-proc-info">。（石井達夫）</para>
    <para>此提交添加了新的 pgpool_adm 扩展函数：pcp_proc_info。同时向 pcp_proc_info 和 "show pool_pools" 添加了新字段：client_host、client_port 和 SQL 语句。通过这些添加，现在可以跟踪 pgpool 的客户端、pgpool 本身和 PostgreSQL 之间的关系。</para>
    <para>此外，此提交允许通过 pcp_proc_info 了解最后执行的命令（语句）。以前，除非查看 pgpool 日志，否则无法实现。</para>
    <para>lipcp.so 版本从 2.0.0 升级到 3.0.0。</para>
   </listitem>

   <listitem>
    <!--
    2025-06-14 [9fc568f00]
    -->
    <para>增强 lifecheck 日志。（石井達夫）</para>
    <para>以前，当 wd_lifecheck_method = 'query' 时，如果在 log_line_prefix 中指定了 "%a"，生命周期检查会打印不带应用程序名称的 SQL。此提交添加了 application_name "lifecheck_ping" 以使日志更美观。由于这会改变用户可见的行为，因此不将其应用于稳定分支。</para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-June/004604.html">[pgpool-hackers: 4603] life check 日志不美观</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-05 [1a68c6eb0]
    -->
    <para>用 pg_prng 随机函数替换 random()。（Martijn van Duren, 石井達夫）</para>
    <para>以前我们使用 random() 来选择负载均衡节点。然而 PostgreSQL 有更好的随机数生成器：pg_prng.c。此提交导入该文件并使用 pg_prng_double() 生成 [0.0, 1.0) 范围内的随机数。种子使用 pg_strong_random() 生成。</para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004589.html">[pgpool-hackers: 4588] 重新整理随机函数并使用更好的随机数</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-03 [cea80281d]
    2025-06-04 [e058c9334]
    -->
    <para>在 watchdog 接收套接字上重试绑定。（石井達夫）</para>
    <para>偶尔 028.watchdog_enable_consensus_with_half_votes 会因为绑定 watchdog 接收套接字失败而超时。此提交尝试通过重试绑定来缓解该问题。目前最多重试 5 次，每次重试间隔 1 秒。</para>
   </listitem>

   <listitem>
    <!--
    2025-05-28 [ffd3a5224]
    -->
    <para>从 PostgreSQL 导入 likely/unlikely 宏。（石井達夫）</para>
    <para>这些宏不仅有助于提高性能（如果正确使用），而且使从 PostgreSQL 移植代码到 pgpool 变得更加容易，因为这些宏在代码中偶尔会用到。</para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004600.html">[pgpool-hackers: 4599] 移植 likely/unlikely</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-20 [7abdd48ec]
    2025-05-20 [e14feea1d]
    -->
    <para>用 pg_strong_random() 替换 PostmasterRandom()。（石井達夫）</para>
    <para>我们的 PostmasterRandmon() 是很久以前（2016 年）从 PostgreSQL 导入的。同年，PostgreSQL 用 pg_strong_random()（src/port/pg_strong_random.c）替换了 PostmasterRandmon()。此提交跟随这一变更。</para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004589.html">[pgpool-hackers: 4588] 重新整理随机函数并使用更好的随机数</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-08 [5f7d06d87]
    -->
    <para>如果从 <filename>.pcppass</filename> 文件读取密码失败，则回退到提示输入密码。（Bo Peng）</para>
    <para>如果从 <filename>.pcppass</filename> 文件读取密码失败，应回退到提示用户输入，类似于 PostgreSQL 处理 <filename>.pgpass</filename> 的方式。</para>
    <para>此提交还更改了以下消息，使其无需 <option>-d</option> 选项即可显示：<programlisting>
WARNING: password file \"%s\" is not a plain file
WARNING: password file \"%s\" has group or world access; permissions should be u=rw (0600) or less
     </programlisting>
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004590.html">[pgpool-hackers: 4589] 如果从 .pcppass 文件读取密码失败，则尝试从提示符读取。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-14 [bbe93d86b]
    -->
    <para>移除或降低 pgpool 启动时不适当的日志消息级别。（石井達夫）</para>
   </listitem>

   <listitem>
    <!--
    2025-05-02 [5c41d5338]
    -->
    <para>将主版本信息添加到配置文件中。（Bo Peng）</para>
   </listitem>

  </itemizedlist>
 </sect2>
<sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-07-15 [2a236be3e]
    -->
    <para>修复被报告为 <literal>strings</literal> 的 <literal>enum</literal> 参数的文档。（Taiki Koshino）</para>
    <para>修复以下 6 个参数的文档：<programlisting>
log_standby_delay
log_backend_messages
wd_lifecheck_method
memqcache_method
disable_load_balance_on_write
backend_clustering_mode
     </programlisting>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-05-09 [174772d6c]
    -->
    <para>增强查询缓存文档。（石井達夫）</para>
    <para>Pgpool 拒绝缓存调用返回 <literal>TIMESTAMP WITH TIMEZONE</literal>、<literal>TIME WITH TIMEZONE</literal> 的函数的查询。如果有多个同名函数，其中某个返回 <literal>TIMESTAMP WITH TIMEZONE</literal>、<literal>TIME WITH TIMEZONE</literal>，即使其他函数不返回这些数据类型，pgpool 也会拒绝缓存。因此，添加了关于此问题的说明及解决方法。</para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>测试工具</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-08-22 [534e04a0e]
    2025-08-22 [384c17eab]
    -->
    <para>允许在 <xref linkend="watchdog-setup"> 中指定 VIP。（石井達夫）</para>
    <para>此提交允许使用新选项 <option>-vip [ip]</option> 在 <command>watchdog_setup</command> 中指定 VIP 参数（<varname>delegate_ip</varname>）。如果省略 ip，则假定为 <literal>127.0.0.1</literal>。即使指定了 vip 选项，pgpool 也不会实际将 VIP 设置到系统：<xref linkend="guc-if-up-cmd">、<xref linkend="guc-if-down-cmd"> 仅设置为 echo 命令，除了输出日志外不做任何操作。此选项旨在跟踪 <productname>Pgpool-II</productname> 关于 VIP 处理的操作。</para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20250820.151646.1640218512808280876.ishii%40postgresql.org">https://www.postgresql.org/message-id/20250820.151646.1640218512808280876.ishii%40postgresql.org</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
<sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-06-25 [04e09df17]
    2025-06-27 [b76c6876d]
    -->
    <para>修复 038.pcp_commands 回归测试。（石井達夫）</para>
    <para>测试结果显示了本地主机 IP。虽然 IP 可以是 IPv4 或 IPv6，但测试脚本没有考虑到这一点。为修复此问题，现在 test.sh 将 IPv4 和 IPv6 IP 转换为 "localhost"。</para>
   </listitem>

   <listitem>
    <!--
    2025-06-08 [e30bb2ead]
    -->
    <para>稳定 029.cert_passphrase 回归测试。（石井達夫）</para>
    <para>当 ssl_passphrase_command 无效时，错误消息通常是 "bad decrypt"，但有时似乎是 "wrong tag"。</para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>
