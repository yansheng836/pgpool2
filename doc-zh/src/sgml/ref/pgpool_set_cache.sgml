<!--
doc/src/sgml/ref/set.sgml
PostgreSQL documentation
-->

<refentry id="SQL-PGPOOL-SET-CACHE">
 <indexterm zone="sql-pgpool-set-cache">
  <primary>PGPOOL SET</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>PGPOOL SET CACHE</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>PGPOOL SET CACHE</refname>
  <refpurpose>删除查询缓存</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <synopsis>
   PGPOOL SET CACHE DELETE <replaceable class="PARAMETER">'query'</replaceable>
  </synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   <command>PGPOOL SET CACHE DELETE</command> 命令会删除
   <link linkend="runtime-in-memory-query-cache">查询缓存</link>
   中先前由该查询所创建的缓存。查询字符串必须与之前的查询完全
   一致，包括末尾的 <literal>';'</literal>。然而，如果查询
   中包含 <literal>'</literal>（单引号），则需要像下面这样用
   <literal>'</literal> 进行转义：
   <programlisting>
PGPOOL SET CACHE DELETE 'SELECT ''FOO'';';
   </programlisting>
   对于因查询未引用任何表而不会被
   <link linkend="guc-memqcache-auto-cache-invalidation">自动
   缓存失效特性</link> 所失效的查询来说，此命令尤其有用。
  </para>
  <para>
   如果您不记得之前的查询，可以使用
   <xref linkend="PCP-INVALIDATE-QUERY-CACHE"> 删除包括您想要删除的缓存在内的所有
   查询缓存。但这需要 <productname>Pgpool-II</productname> 管理员权限。
  </para>
  <para>
   成功删除查询缓存后，此命令会输出一条提示消息：
   <programlisting>
NOTICE:  query cache deleted. query: "<literal>query string</literal>"
   </programlisting>
   如果缓存不存在或未启用查询缓存，它会输出
   一条提示消息：
   <programlisting>
NOTICE:  query cache does not exist for query: "<literal>query string</literal>"
   </programlisting>
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>

  <para>
   意外创建了一个多余的查询缓存。请使用该命令将其删除。
    <programlisting>
test=# /*FORCE QUERY CACHE*/SELECT current_timestamp;
       current_timestamp
-------------------------------
 2024-10-18 18:25:07.826423+09
(1 row)

test=# PGPOOL SET CACHE DELETE '/*FORCE QUERY CACHE*/SELECT current_timestamp;';
NOTICE:  query cache deleted. query: "/*FORCE QUERY CACHE*/SELECT current_timestamp;"
SET
    </programlisting>
  </para>

 </refsect1>

 <refsect1>
  <title>另请参阅</title>

  <simplelist type="inline">
   <member><xref linkend="PCP-INVALIDATE-QUERY-CACHE"></member>
  </simplelist>
 </refsect1>

</refentry>