 <!--
doc/src/sgml/ref/pg_enc.sgml
Pgpool-II documentation
-->

<refentry id="PG-ENC">
 <indexterm zone="pg-enc">
  <primary>pg_enc</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>pg_enc</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>其他命令</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pg_enc</refname>
  <refpurpose>
   AES256 密码加密工具
  </refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pg_enc</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
   <arg choice="plain"><replaceable>-p</replaceable></arg>
  </cmdsynopsis>
  <cmdsynopsis>
   <command>pg_enc</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
   <arg choice="plain"><replaceable>password</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-PG-ENC-1">
  <title>描述</title>
  <para>
   <command>pg_enc</command>
   AES256 密码加密工具。
  </para>
 </refsect1>

 <refsect1>
  <title>选项</title>
  <para>
   <variablelist>

    <varlistentry>
     <term><option>-k <replaceable class="parameter">KEY_FILE</replaceable></option></term>
     <term><option>--key-file=<replaceable class="parameter">KEY_FILE</replaceable></option></term>
     <listitem>
      <para>
       设置加密密钥文件的路径。默认为用户主目录下的 <literal>.pgpoolkey</literal> 文件，
       可以通过环境变量 <varname>PGPOOLKEYFILE</varname> 覆盖。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-K <replaceable class="parameter">ENCRYPTION_KEY</replaceable></option></term>
     <term><option>--enc-key=<replaceable class="parameter">ENCRYPTION_KEY</replaceable></option></term>
     <listitem>
      <para>
       用于加密数据库密码的加密密钥。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-f <replaceable class="parameter">CONFIG_FILE</replaceable></option></term>
     <term><option>--config-file=<replaceable class="parameter">CONFIG_FILE</replaceable></option></term>
     <listitem>
      <para>
       指定 <literal>pgpool.conf</literal> 文件。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-i <replaceable class="parameter">INPUT_FILE</replaceable></option></term>
     <term><option>--input-file=<replaceable class="parameter">INPUT_FILE</replaceable></option></term>
     <listitem>
      <para>
       指定包含用户名和密码配对的文件。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-p</option></term>
     <term><option>--prompt</option></term>
     <listitem>
      <para>
       通过标准输入提示输入数据库密码。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-P</option></term>
     <term><option>--prompt-for-key</option></term>
     <listitem>
      <para>
       通过标准输入提示输入加密密钥。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-m</option></term>
     <term><option>--update-pass</option></term>
     <listitem>
      <para>
       在 <literal>pool_passwd</literal> 文件中创建加密密码条目。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-u <replaceable class="parameter">your_username</replaceable></option></term>
     <term><option>--username=<replaceable class="parameter">your_username</replaceable></option></term>
     <listitem>
      <para>
       为名为 <literal>your_username</literal> 的数据库用户创建
       <literal>pool_passwd</literal> 条目。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-h</option></term>
     <term><option>--help</option></term>
     <listitem>
      <para>
       打印 <literal>pg_enc</literal> 的帮助信息。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>

    <note>
     <para>
      如果将密码作为参数提供给 pg_enc 且密码中包含 "$" 字符，
      则必须对其进行转义。
     </para>
     <para>
      例如，如果你想使用 "ABC$123" 作为密码，
      <programlisting>
pg_enc -m -f /path/to/pgpool.conf -u username ABC\$123
      </programlisting>
     </para>
    </note>
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>
  <para>
   以下是一个示例输出：
   <programlisting>
    pg_enc -p
    db password: [your password]
   </programlisting>
  </para>
  <para>
   或
  </para>
  <programlisting>
   ./pg_enc foo
   trying to read key from file /home/pgpool/.pgpoolkey

   jglid1QRgiCl/vfhHUDyVA==
   pool_passwd string: AESjglid1QRgiCl/vfhHUDyVA==
  </programlisting>
  <para>
   <literal>pg_enc</literal> 可用于 <literal>pool_passwd</literal> 密码，用法如下：
   <programlisting>
    pg_enc -m -f /path/to/pgpool.conf -u username -p
    db password: [your password]
   </programlisting>
   带有给定密码的用户名 <literal>username</literal> 条目将被添加。
  </para>
  <para>
   如果不使用参数提示或命令行参数提供密码，
   <literal>pg_enc</literal> 可以从文件中读取用户名:密码配对。
   它会将所有用户名和加密后的密码添加到
   <xref linkend="guc-pool-passwd"> 认证文件中。
   <programlisting>
    $ cat users.txt
    username1:secretpassword1
    username2:secretpassword2

    $ pg_enc -m -f /path/to/pgpool.conf -i users.txt
    trying to read key from file /home/pgpool/.pgpoolkey
    trying to read user:password pairs from file users.text

    $ cat /path/to/pool_passwd
    username1:AESrx5QdpGyW/+4CB80KWtwhg==
    username2:AESUAdohy7nCUhWliRI9WiYQA==
   </programlisting>
  </para>

 </refsect1>

</refentry>
