<!--
    doc/src/sgml/ref/show_pool_cache.sgml
    Pgpool-II documentation
  -->

<refentry id="SQL-SHOW-POOL-CACHE">
 <indexterm zone="sql-show-pool-cache">
  <primary>SHOW</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>SHOW POOL_CACHE</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>SQL - Language Statements</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>SHOW POOL_CACHE</refname>
  <refpurpose>
   显示缓存存储统计信息
  </refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <synopsis>
   SHOW POOL_CACHE
  </synopsis>
 </refsynopsisdiv>

 <refsect1>
  <title>描述</title>

  <para>
   如果启用了内存查询缓存，则 <command>SHOW POOL_CACHE</command>
   将显示 <link linkend="runtime-in-memory-query-cache">内存查询缓存</link> 的统计信息。以下是一个示例会话：
   各项内容的说明参见 <xref linkend="show-pool-cache-table">。
   <programlisting>
    test=# \x
    \x
    Expanded display is on.
    test=# show pool_cache;
    show pool_cache;
    -[ RECORD 1 ]---------------+---------
    num_cache_hits              | 891703
    num_selects                 | 99995
    cache_hit_ratio             | 0.90
    num_hash_entries            | 131072
    used_hash_entries           | 99992
    num_cache_entries           | 99992
    used_cache_entries_size     | 12482600
    free_cache_entries_size     | 54626264
    fragment_cache_entries_size | 0
   </programlisting>

  </para>

  <note>
   <para>
    如果缓存存储为 memcached，则除 num_cache_hits、num_selects 和 cache_hit_ratio 之外的所有列的值均显示为 0。
   </para>
  </note>

  <table id="show-pool-cache-table">
   <title>show pool_cache 显示的项目</title>
   <tgroup cols="2">
    <thead>
     <row>
      <entry>名称</entry>
      <entry>说明</entry>
     </row>
    </thead>

    <tbody>

     <row>
      <entry><literal>num_cache_hits</literal></entry>
      <entry>对查询缓存的命中次数。</entry>
     </row>

     <row>
      <entry><literal>num_selects</literal></entry>
      <entry>未命中查询缓存的 SELECT 语句数量。</entry>
     </row>

     <row>
      <entry><literal>cache_hit_ratio</literal></entry>
      <entry>缓存命中率。
       计算公式为 num_cache_hits/(num_cache_hits+num_selects)。</entry>
     </row>

     <row>
      <entry><literal>num_hash_entries</literal></entry>
      <entry>用于管理缓存的哈希表中的条目数量。为了管理大量的缓存，<productname>Pgpool-II</productname> 使用哈希表。
       哈希条目数会自动调整为大于 <xref linkend="guc-memqcache-max-num-cache"> 的最接近的 2 的幂。
       例如，<xref linkend="guc-memqcache-max-num-cache"> 的默认值 100,000 会被调整为 131,072（2 的 17 次方）。
      </entry>
     </row>

     <row>
      <entry><literal>used_hash_entries</literal></entry>
      <entry>
       已使用的哈希表条目数量。如果该值接近 <literal>num_hash_entries</literal>，
       建议增加 <literal>num_hash_entries</literal>。
       即使使用了所有哈希表条目，也不会报错。但是，由于哈希表条目和缓存会被复用以注册新的缓存条目，性能会受到影响。
      </entry>
     </row>

     <row>
      <entry><literal>num_cache_entries</literal></entry>
      <entry>
       已使用的缓存条目数量。在当前实现中，该数值应与 <literal>used_hash_entries</literal> 相同。
      </entry>
     </row>

     <row>
      <entry><literal>free_cache_entries_size</literal></entry>
      <entry>
       未使用缓存的大小（以字节为单位）。当该值趋近于 0 时，系统会移除已注册的缓存并注册新的缓存，
       这不会导致错误，但会降低性能。请考虑增加 <xref linkend="guc-memqcache-total-size">。
      </entry>
     </row>

     <row>
      <entry><literal>fragment_cache_entries_size</literal></entry>
      <entry>
       碎片化缓存的大小（以字节为单位）。当已注册的缓存被驱逐时，空间会变为碎片状态，直到下次复用该块。
       <productname>Pgpool-II</productname> 按照 <xref linkend="guc-memqcache-cache-block-size"> 指定的固定大小块写入缓存。
       当已注册的缓存被驱逐时，空间会变为碎片状态，直到下次复用该块。
       <literal>fragment_cache_entries_size</literal> 显示此类碎片区域的总大小。
      </entry>
     </row>

    </tbody>
   </tgroup>
  </table>

 </refsect1>

</refentry>
