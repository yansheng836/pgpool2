<!--
doc/src/sgml/ref/pcp_promote_node.sgml
Pgpool-II documentation
-->

<refentry id="PCP-PROMOTE-NODE">
 <indexterm zone="pcp-promote-node">
  <primary>pcp_promote_node</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>pcp_promote_node</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>PCP Command</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pcp_promote_node</refname>
  <refpurpose>
   将指定节点提升为 Pgpool-II 的新主节点</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pcp_promote_node</command>
   <arg rep="repeat"><replaceable>options</replaceable></arg>
   <arg><replaceable>node_id</replaceable></arg>
   <arg><replaceable>gracefully</replaceable></arg>
   <arg><replaceable>switchover</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-PCP-PROMOTE-NODE-1">
  <title>描述</title>
  <para>
   <command>pcp_promote_node</command> 将指定节点提升为
   <productname>Pgpool-II</productname> 的新主节点。仅适用于流复制模式。
   请注意，除非指定了 <literal>switchover</literal> 选项，否则此命令
   实际上并不会提升备用 <productname>PostgreSQL</productname> 后端：
   它仅更改 <productname>Pgpool-II</productname> 的内部状态并触发故障转移，
   用户必须在 <productname>Pgpool-II</productname> 外部手动提升
   备用 <productname>PostgreSQL</productname>。
  </para>

  <para>
   如果指定了 <literal>switchover</literal>，
   <productname>Pgpool-II</productname> 将分离当前主节点（将其内部状态更改为 down），
   并执行 <xref linkend="guc-failover-command">，将新主节点参数设置为指定的节点 ID。
   由于大多数故障转移脚本会提升新主节点，因此指定节点将成为新主节点。
   需要正确设置 <xref linkend="guc-follow-primary-command"> 以将原主节点转换为备节点。
  </para>

  <para>
   如果未指定 <literal>switchover</literal>，<command>pcp_promote_node</command>
   将执行以下操作。请注意，如果设置了 <xref linkend="guc-follow-primary-command">，
   该命令将被执行。建议在执行此命令前禁用 <xref linkend="guc-follow-primary-command">。

   <orderedlist>

    <listitem>
     <para>
      将备用 <productname>PostgreSQL</productname> 的状态从备节点更改为主节点。
      这仅更改 <productname>Pgpool-II</productname> 的内部状态，
      并不会实际提升 <productname>PostgreSQL</productname> 备服务器。
     </para>
    </listitem>

    <listitem>
     <para>
      将未被此命令参数指定的 <productname>PostgreSQL</productname> 节点状态更改为 down。
      这仅更改 <productname>Pgpool-II</productname> 的内部状态，
      并不会实际使 <productname>PostgreSQL</productname> 备服务器停止运行。
     </para>
    </listitem>

    <listitem>
     <para>
      如果设置了 <xref linkend="guc-follow-primary-command">，则对
      <productname>PostgreSQL</productname> 执行 <xref linkend="guc-follow-primary-command">。
     </para>
    </listitem>

   </orderedlist>
  </para>

  <para>
   如果指定了 <literal>switchover</literal>，<command>pcp_promote_node</command>
   将执行以下操作。如果设置了 <xref linkend="guc-follow-primary-command">，
   该命令将被执行。在执行此命令前需要设置 <xref
   linkend="guc-follow-primary-command">，因为故障转移脚本将创建新主节点，
   其他节点需要被转换为备节点。

   <orderedlist>

    <listitem>
     <para>
      将主 <productname>PostgreSQL</productname> 的状态从 up 更改为 down。
      这将触发 <xref linkend="guc-failover-command"> 执行，将新主节点参数设置为
      指定的节点 ID。由于大多数故障转移脚本会提升新主节点，因此指定节点将成为
      新主节点。
     </para>
    </listitem>

    <listitem>
     <para>
      将未被此命令参数指定的备用 <productname>PostgreSQL</productname> 节点状态
      更改为 down。这仅更改 <productname>Pgpool-II</productname> 的内部状态，
      并不会实际使 <productname>PostgreSQL</productname> 备服务器停止运行。
     </para>
    </listitem>

    <listitem>
     <para>
      如果设置了 <xref linkend="guc-follow-primary-command">，则对
      <productname>PostgreSQL</productname> 执行 <xref linkend="guc-follow-primary-command">。
     </para>
    </listitem>

   </orderedlist>
  </para>

 </refsect1>

 <refsect1>
  <title>选项</title>
  <para>
   <variablelist>

    <varlistentry>
     <term><option>-n <replaceable class="parameter">node_id</replaceable></option></term>
     <term><option>--node-id=<replaceable class="parameter">node_id</replaceable></option></term>
     <listitem>
      <para>
       要提升为新主节点的后端节点索引。
       指定节点必须处于 "up" 或 "waiting" 状态。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-g </option></term>
     <term><option>--gracefully</option></term>
     <listitem>
      <para>
       等待所有客户端断开连接（除非 client_idle_limit_in_recovery 为 -1 或 recovery_timeout 超时）。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-s </option></term>
     <term><option>--switchover</option></term>
     <listitem>
      <para>
       通过触发 <xref linkend="guc-failover-command"> 来实际提升指定节点。
       同时更改当前主节点状态为 down。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>其他选项 </option></term>
     <listitem>
      <para>
       参见 <xref linkend="pcp-common-options">。
      </para>
     </listitem>
    </varlistentry>

   </variablelist>
  </para>
 </refsect1>

</refentry>
