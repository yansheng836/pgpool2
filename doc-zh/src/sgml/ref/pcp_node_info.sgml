<!--
doc/src/sgml/ref/pcp_node_info.sgml
Pgpool-II documentation
-->

<refentry id="PCP-NODE-INFO">
 <indexterm zone="pcp-node-info">
  <primary>pcp_node_info</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>pcp_node_info</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>PCP Command</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pcp_node_info</refname>
  <refpurpose>
   显示指定节点 ID 的信息</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pcp_node_info</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
   <arg><replaceable>node_id</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-PCP-NODE-INFO-1">
  <title>描述</title>
  <para>
   <command>pcp_node_info</command>
   显示指定节点 ID 的信息。
  </para>
 </refsect1>

 <refsect1>
  <title>选项</title>
  <para>
   <variablelist>

    <varlistentry>
     <term><option>-n <replaceable class="parameter">node_id</replaceable></option></term>
     <term><option>--node-id=<replaceable class="parameter">node_id</replaceable></option></term>
     <listitem>
      <para>
       要获取信息的后端节点的索引。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-a</option></term>
     <term><option>--all</option></term>
     <listitem>
      <para>
       显示所有后端节点的信息。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>其他选项 </option></term>
     <listitem>
      <para>
       参见<xref linkend="pcp-common-options">。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>
  <para>
   以下是示例输出：
   <programlisting>
$ pcp_node_info -w -p 11001 -n 1
/tmp 11003 1 0.500000 waiting up standby standby 0 streaming async 2021-02-27 14:51:30
   </programlisting>
  </para>
  <para>
   结果按以下顺序显示：
   <literallayout class="monospaced">
1. hostname
2. port number
3. status
4. load balance weight
5. status name
6. actual backend status (obtained using PQpingParams. Pgpool-II 4.3 or later)
7. backend role
8. actual backend role (obtained using pg_is_in_recovery. Pgpool-II 4.3 or later)
9. replication delay
10. replication state (taken from pg_stat_replication. Pgpool-II 4.1 or later)
11. sync replication state (taken from pg_stat_replication. Pgpool-II 4.1 or later)
12. last status change time
   </literallayout>
  </para>
  <para>
   <emphasis>3 (status)</> 用 [0 到 3] 之间的数字表示。
   <itemizedlist>
    <listitem><para>0 - 此状态仅在初始化期间使用。PCP 不会显示它。 </para></listitem>
    <listitem><para>1 - 节点已启动。尚无连接。 </para></listitem>
    <listitem><para>2 - 节点已启动。连接已池化。 </para></listitem>
    <listitem><para>3 - 节点已关闭。 </para></listitem>
   </itemizedlist>
  </para>
  <para>
   <emphasis>4 (load balance weight)</> 以归一化格式（0 - 1）显示。
  </para>
  <para>
   <emphasis>5 (status name)</> 表示
   <productname>Pgpool-II</productname> 所识别的
   <productname>PostgreSQL</productname> 节点的状态。它对应
   "<emphasis>3 (status)</>" 中显示的数值，并以人类可读的格式表示
   <productname>Pgpool-II</productname> 维护的节点内部状态。
  </para>
  <para>
   <emphasis>6 (actual backend status)</> 实时显示后端状态。该信息在调用命令时通过调用
   <function>PQpingParams</function> 获取。<function>PQpingParams</function> 仅在
   <productname>PostgreSQL</productname> 9.1 或更高版本中可用。如果
   <productname>Pgpool-II</productname> 使用
   <productname>PostgreSQL</productname> 9.0 或更早版本构建，则该列显示
   "unknown"。此外，如果
   <link linkend="runtime-config-health-check">健康检查</link>
   被禁用，也会显示 "unknown"。
   当通过 <xref
   linkend="pcp-detach-node"> 分离后端节点时，
   <productname>Pgpool-II</productname> 管理的状态将为 "down"，而实际后端状态为
   "up"。因此可能出现 5 与 6 不匹配的情况。但不应出现 5 为 "up" 而 6
   为 "down" 的情况。
  </para>
  <para>
   <emphasis>7 (backend role)</> 从
   <productname>Pgpool-II</productname> 的角度，基于其内部维护的信息，
   指示 <productname>PostgreSQL</productname>
   节点的角色（primary 或 standby）。
  </para>
  <para>
   <emphasis>8 (actual backend role)</> 实时显示后端状态。结果将为
   "primary" 或 "standby"，如果信息检索失败则可能为
   "unknown"。由于 <productname>Pgpool-II</productname>
   按节点 ID 顺序搜索后端节点，并假定最后找到的节点为 primary，
   因此当因错误操作导致存在多个非 standby 节点时，
   7 可能与 8 不匹配（此命令有助于发现此类情况）。
   在非流复制模式下，状态将为
   "main" 或 "replica"。与流复制模式不同，
   不会调用 <function>pg_is_in_recovery</function>，7 和 8 的值将始终相同。
  </para>
  <para>
   要正确显示 <emphasis>9, 10 和 11</>，<xref linkend="guc-sr-check-period">
   不能为 0。
   此外，要正确显示 <emphasis>10 和 11</>，需要以下
   设置。
   <itemizedlist>
    <listitem>
     <para>
      <xref linkend="guc-backend-application-name"> 必须配置。
     </para>
     <para>
      每个 <varname>backend_application_nameX</varname>
      设置的值必须与对应节点的
      <varname>primary_conninfo</varname> 中的 <varname>application_name</varname>
      指定的值匹配。
     </para>
     <para>
      特别是，当使用 Pgpool-II 的
      <xref linkend="pcp-recovery-node"> 命令和社区提供的
      示例脚本创建 standby 节点时，请确保
      <varname>backend_application_nameX</varname> 的值与
      <varname>backend_hostnameX</varname> 的值匹配。有关具体的配置示例，
      请参阅 <xref linkend="example-cluster-pgpool-config-backend-settings">。
     </para>
    </listitem>
    <listitem>
     <para>
      <xref linkend="guc-sr-check-user"> 必须是
      <productname>PostgreSQL</productname> 超级用户，或者已被授予
      <literal>pg_monitor</literal> 角色的权限。
     </para>
    </listitem>
   </itemizedlist>
   <note>
    <para>
     要向
     <xref linkend="guc-sr-check-user"> 授予 <literal>pg_monitor</literal> 角色的权限，
     请由 <productname>PostgreSQL</productname> 超级用户执行以下 SQL 命令
     （将 "sr_check_user" 替换为 <xref linkend="guc-sr-check-user"> 的设置值）：
     <programlisting>
GRANT pg_monitor TO sr_check_user;
     </programlisting>
     对于 <productname>PostgreSQL</productname> 9.6，没有
     pg_monitor 组，<xref linkend="guc-sr-check-user"> 必须
     是 <productname>PostgreSQL</productname> 超级用户。
    </para>
   </note>
  </para>

  <para>
    从 <productname>Pgpool-II</productname> 4.4 开始，9（replication
    delay）以字节或秒为单位显示。有关更多
    详细信息，请参见 <xref linkend="guc-delay-threshold-by-time">。
  </para>

  <para>
   <option>-a</option> 或 <option>--all</option> 选项列出所有后端节点的信息。
  </para>
  <programlisting>
$ pcp_node_info -w -p 11001 -a
/tmp 11002 1 0.500000 waiting up primary primary 0 none none 2021-02-27 14:51:30
/tmp 11003 1 0.500000 waiting up standby standby 0 streaming async 2021-02-27 14:51:30
  </programlisting>

  <para>
   <option>--verbose</option> 选项有助于理解输出。例如：
  </para>
  <programlisting>
$ pcp_node_info -w -p 11001 --verbose 1
Hostname               : /tmp
Port                   : 11003
Status                 : 1
Weight                 : 0.500000
Status Name            : waiting
Backend Status Name    : up
Role                   : standby
Backend Role           : standby
Replication Delay      : 0
Replication State      : streaming
Replication Sync State : async
Last Status Change     : 2021-02-27 14:51:30
  </programlisting>
 </refsect1>

</refentry>
