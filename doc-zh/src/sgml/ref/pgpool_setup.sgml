<!--
doc/src/sgml/ref/pgpool_setup.sgml
Pgpool-II 文档
-->

<refentry id="PGPOOL-SETUP">
 <indexterm zone="pgpool-setup">
  <primary>pgpool_setup</primary>
 </indexterm>

 <refmeta>
  <refentrytitle>pgpool_setup</refentrytitle>
  <manvolnum>1</manvolnum>
  <refmiscinfo>其他命令</refmiscinfo>
 </refmeta>

 <refnamediv>
  <refname>pgpool_setup</refname>
  <refpurpose>
   创建 Pgpool-II 集群的临时安装</refpurpose>
 </refnamediv>

 <refsynopsisdiv>
  <cmdsynopsis>
   <command>pgpool_setup</command>
   <arg rep="repeat"><replaceable>option</replaceable></arg>
  </cmdsynopsis>
 </refsynopsisdiv>

 <refsect1 id="R1-PGPOOL-SETUP-1">
  <title>
   描述
  </title>
  <para>
   <application>pgpool_setup</application> 在当前目录下创建
   <productname>Pgpool-II</productname> 集群的临时安装，包括
   <productname>Pgpool-II</productname> 安装和指定数量的
   <productname>PostgreSQL</productname> 安装。
   运行 <application>pgpool_setup</application> 之前当前目录必须为空。
  </para>
  <para>
   <application>pgpool_setup</application> 仅供测试使用，
   不应用于创建生产安装。
  </para>

  <para>
   <application>pgpool_setup</application> 会对 localhost 执行
   <application>ssh</application>。您需要配置
   <application>ssh</application> 以便无需密码即可登录 localhost。
  </para>

  <para>
   目前 <application>pgpool_setup</application> 支持流复制模式、原生复制模式、原始模式、
   逻辑复制模式和快照隔离模式。要支持 watchdog，
   请参阅 <xref linkend="WATCHDOG-SETUP"> 了解详情。
  </para>
 </refsect1>

 <refsect1>
  <title>选项</title>
  <para>
   <application>pgpool_setup</application> 接受以下命令行参数：

   <variablelist>

    <varlistentry>
     <term><option>-m <replaceable class="parameter">mode</replaceable></option></term>
     <listitem>
      <para>
       指定运行模式。<replaceable>mode</replaceable> 可以是
       <literal>r</literal>（原生复制模式）、<literal>s</literal>（流复制模式）、
       <literal>n</literal>（原始模式）、<literal>l</literal>
       （逻辑复制模式）或 <literal>i</literal>（快照隔离模式）。如果省略此项，
       默认使用 <literal>s</literal>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-n <replaceable class="parameter">num_clusters</replaceable></option></term>
     <listitem>
      <para>
       指定 PostgreSQL 安装的数量。
       如果省略此项，默认使用 <literal>2</literal>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-p <replaceable class="parameter">base_port</replaceable></option></term>
     <listitem>
      <para>
       指定 Pgpool-II 和 PostgreSQL 使用的基准端口号。
       Pgpool-II 端口为 base_port，pcp 端口为 base_port + 1。
       第一个 PostgreSQL 节点的端口为 base_port + 2，第二个
       PostgreSQL 节点的端口为 base_port + 3，以此类推。
      </para>
      <para>
       如果指定了 -pg 选项，则第一个 PostgreSQL 节点的端口为
       pg_base_port，第二个 PostgreSQL 节点的端口为
       pg_base_port + 1，以此类推。
      </para>
      <para>
       如果省略此项，默认使用 <literal>11000</literal>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-pg <replaceable class="parameter">pg_base_port</replaceable></option></term>
     <listitem>
      <para>
       指定 PostgreSQL 使用的基准端口号。
       第一个 PostgreSQL 节点的端口为 base_port + 2，第二个
       PostgreSQL 节点的端口为 base_port + 3，以此类推。
      </para>
      <para>
       如果省略此项，默认使用 <literal>base_port</literal>+2。
      </para>

     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>--no-stop</option></term>
     <listitem>
      <para>
       完成后不停止 pgpool 和 PostgreSQL。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-d</option></term>
     <listitem>
      <para>
       以调试模式启动 pgpool。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-s</option></term>
     <listitem>
      <para>
       在流复制模式下，使用复制槽（replication slot）而非归档目录。
       由于归档目录被所有 <productname>PostgreSQL</productname> 集群共享，
       如果一个备用节点晋升为主节点，归档目录中的时间线将发生变化，
       其他备用节点将会停止。使用复制槽可以避免此问题，
       如果您可以使用支持复制槽的 <productname>PostgreSQL</productname> 9.4 或更高版本，
       则应始终使用复制槽。<application>pgpool_setup</application>
       使用的复制槽名称为 <literal>pgpool_setup_slot</literal>。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-r</option></term>
     <listitem>
      <para>
       在恢复脚本（basebackup.sh）中使用 <command>pg_rewind</command> 命令。
       如果该命令失败，则切换为使用普通 rsync 命令。在某些情况下，
       使用 <command>pg_rewind</command> 进行恢复比 rsync 快得多，
       因为它不会复制整个数据库集群。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-e</option></term>
     <listitem>
      <para>
       不创建 <productname>PostgreSQL</productname> 数据库。
       此选项供 <command>watchdog_setup</command> 使用，
       对其他用途可能没有实际意义。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-t</option></term>
     <listitem>
      <para>
       设置一些额外的参数到 pgpool.conf 中，以便回归测试
       能够针对现有安装执行测试。目前设置的参数是
       <xref linkend="guc-health-check-test">。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>-c</option></term>
     <listitem>
      <para>
       使用安装在 <filename>/etc/pgpool-II/</filename> 目录中的示例脚本和配置文件。
      </para>
      <para>
       此选项是为了测试 <productname>Pgpool-II</productname> RPM 包中包含的
       示例脚本和配置文件而添加的。在指定此选项之前，请确保已将
       示例脚本和配置文件安装到 <filename>/etc/pgpool-II/</filename> 目录中。
      </para>
     </listitem>
    </varlistentry>

   </variablelist>
  </para>
 </refsect1>

 <refsect1>
  <title>环境变量</title>
  <para>
   <application>pgpool_setup</application> 识别以下环境变量：

   <variablelist>

    <varlistentry>
     <term><option>PGPOOL_INSTALL_DIR</option></term>
     <listitem>
      <para>
       指定 Pgpool-II 的安装目录。Pgpool-II 二进制文件应位于
       PGPOOL_INSTALL_DIR/bin 下，pgpool.conf 和 pool_hba.conf 等配置文件
       应位于 PGPOOL_INSTALL_DIR/etc 下。默认值为 /usr/local。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>PGPOOLDIR</option></term>
     <listitem>
      <para>
       指定 Pgpool-II 配置文件的路径。
       默认值为 PGPOOL_INSTALL_DIR/etc。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>PGBIN</option></term>
     <listitem>
      <para>
       指定 PostgreSQL 命令（如 initdb、pg_ctl 和 psql）的路径。
       默认值为 /usr/local/pgsql/bin。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>PGLIB</option></term>
     <listitem>
      <para>
       指定 PostgreSQL 共享库的路径。
       默认值为 /usr/local/pgsql/lib。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>PGSOCKET_DIR</option></term>
     <listitem>
      <para>
       指定 Unix socket 目录的路径。
       默认值为 /tmp。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>INITDBARG</option></term>
     <listitem>
      <para>
       指定 initdb 命令的参数。
       默认值为 "--no-locale -E UTF_8"。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>USE_REPLICATION_SLOT</option></term>
     <listitem>
      <para>
       如果为 "true"，则在流复制模式下使用复制槽而非归档目录。
       这与指定了 "-s" 选项具有相同的效果。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>USE_PG_REWIND</option></term>
     <listitem>
      <para>
       如果为 "true"，则在流复制模式下在 basebackup.sh 脚本中使用
       <command>pg_rewind</command>。这与指定了 "-r" 选项具有相同的效果。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>ENABLE_TEST</option></term>
     <listitem>
      <para>
       如果为 "true"，则相当于指定了 "-t" 选项。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term><option>TEST_SAMPLES</option></term>
     <listitem>
      <para>
       如果为 "true"，则相当于指定了 "-c" 选项。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>

  </para>
 </refsect1>

 <refsect1>
  <title>示例</title>
  <para>
   <screen>
$ pgpool_setup 
PostgreSQL major version: 124
Starting set up in streaming replication mode
creating startall and shutdownall
creating failover script
creating database cluster /tmp/test/data0...done.
update postgresql.conf
creating pgpool_remote_start
creating basebackup.sh
creating recovery.conf
creating database cluster /tmp/test/data1...done.
update postgresql.conf
creating pgpool_remote_start
creating basebackup.sh
creating recovery.conf
temporarily start data0 cluster to create extensions
temporarily start pgpool-II to create standby nodes
 node_id | hostname | port  | status | lb_weight |  role   | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change  
---------+----------+-------+--------+-----------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | /tmp     | 11002 | up     | 0.500000  | primary | 0          | true              | 0                 |                   |                        | 2020-08-18 13:50:19
 1       | /tmp     | 11003 | down   | 0.500000  | standby | 0          | false             | 0                 |                   |                        | 2020-08-18 13:50:18
(2 rows)

recovery node 1...pcp_recovery_node -- Command Successful
done.
creating follow primary script
 node_id | hostname | port  | status | lb_weight |  role   | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change  
---------+----------+-------+--------+-----------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | /tmp     | 11002 | up     | 0.500000  | primary | 0          | true              | 0                 |                   |                        | 2020-08-18 13:50:19
 1       | /tmp     | 11003 | up     | 0.500000  | standby | 0          | false             | 0                 |                   |                        | 2020-08-18 13:50:23
(2 rows)

shutdown all

pgpool-II setting for streaming replication mode is done.
To start the whole system, use /tmp/test/startall.
To shutdown the whole system, use /tmp/test/shutdownall.
pcp command user name is "t-ishii", password is "t-ishii".
Each PostgreSQL, pgpool-II and pcp port is as follows:
#1 port is 11002
#2 port is 11003
pgpool port is 11000
pcp port is 11001
The info above is in README.port.
t-ishii$ ./startall 
waiting for server to start....5744 2020-08-18 13:50:27 JST LOG:  starting PostgreSQL 12.4 on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 7.5.0-3ubuntu1~18.04) 7.5.0, 64-bit
5744 2020-08-18 13:50:27 JST LOG:  listening on IPv4 address "0.0.0.0", port 11002
5744 2020-08-18 13:50:27 JST LOG:  listening on IPv6 address "::", port 11002
5744 2020-08-18 13:50:27 JST LOG:  listening on Unix socket "/tmp/.s.PGSQL.11002"
5744 2020-08-18 13:50:27 JST LOG:  redirecting log output to logging collector process
5744 2020-08-18 13:50:27 JST HINT:  Future log output will appear in directory "log".
 done
server started
waiting for server to start....5757 2020-08-18 13:50:27 JST LOG:  starting PostgreSQL 12.4 on x86_64-pc-linux-gnu, compiled by gcc (Ubuntu 7.5.0-3ubuntu1~18.04) 7.5.0, 64-bit
5757 2020-08-18 13:50:27 JST LOG:  listening on IPv4 address "0.0.0.0", port 11003
5757 2020-08-18 13:50:27 JST LOG:  listening on IPv6 address "::", port 11003
5757 2020-08-18 13:50:27 JST LOG:  listening on Unix socket "/tmp/.s.PGSQL.11003"
5757 2020-08-18 13:50:27 JST LOG:  redirecting log output to logging collector process
5757 2020-08-18 13:50:27 JST HINT:  Future log output will appear in directory "log".
 done
server started
t-ishii$ psql -p 11000 test
psql (12.4)
Type "help" for help.

test=# show pool_nodes;
 node_id | hostname | port  | status | lb_weight |  role   | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change  
---------+----------+-------+--------+-----------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | /tmp     | 11002 | up     | 0.500000  | primary | 0          | true              | 0                 |                   |                        | 2020-08-18 13:50:32
 1       | /tmp     | 11003 | up     | 0.500000  | standby | 0          | false             | 0                 | streaming         | async                  | 2020-08-18 13:50:32
(2 rows)

   </screen>
  </para>
 </refsect1>

</refentry>
