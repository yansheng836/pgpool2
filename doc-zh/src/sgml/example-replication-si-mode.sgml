<sect1 id="example-replication-mode">
 <title>复制模式与快照隔离模式配置示例</title>
 <para>
  本节展示了如何配置 <productname>Pgpool-II</productname> 的复制模式与快照隔离模式的示例。
 </para>
 <para>
  在 <xref linkend="EXAMPLE-CLUSTER"> 中描述的流复制模式下，复制由
  PostgreSQL 的流复制功能完成。然而，在原生复制模式下，
  <productname>Pgpool-II</productname> 通过将写查询路由到所有
  <productname>PostgreSQL</productname> 服务器来执行复制。
 </para>
 <para>
  快照隔离模式与原生复制模式类似，不同之处在于它增加了各节点之间的
  可见性一致性。
 </para>
 <para>
  本配置示例使用了 <productname>PostgreSQL</productname> 18。
 </para>

 <sect2 id="example-replication-mode-structure">
  <title>集群结构</title>
  <para>
   在本示例中，我们使用一台 <productname>Pgpool-II</productname> 和三台
   PostgreSQL 服务器来说明如何配置和使用 Pgpool-II 的复制功能。
  </para>
  <para>
   在本示例中，我们使用 3 台安装了 Rocky Linux 10 的服务器。
   假设这些服务器分别为 <literal>server1</literal>、
   <literal>server2</literal>、<literal>server3</literal>。
   我们在所有服务器上安装 <productname>PostgreSQL</productname>，并在
   server1 上安装 <productname>Pgpool-II</productname>。
  </para>
  <para>
   在本示例中，我们使用最少的配置来设置复制。
   在生产环境中，建议启用 Watchdog 以避免单点故障。
   关于 Watchdog 配置的更多详情，请参阅 <xref linkend="EXAMPLE-CLUSTER-PGPOOL-CONFIG-WATCHDOG">。
  </para>

  <table id="example-replication-mode-table-ip">
   <title>主机名和 IP 地址</title>
   <tgroup cols="3">
    <thead>
     <row>
      <entry>主机名</entry>
      <entry>IP 地址</entry>
      <entry>虚拟 IP</entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>server1</entry>
      <entry>192.168.137.101</entry>
      <entry>PostgreSQL 节点0、Pgpool-II</entry>
     </row>
     <row>
      <entry>server2</entry>
      <entry>192.168.137.102</entry>
      <entry>PostgreSQL 节点1</entry>
     </row>
     <row>
      <entry>server3</entry>
      <entry>192.168.137.103</entry>
      <entry>PostgreSQL 节点2</entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <table id="example-replication-mode-table-postgresql-config">
   <title>PostgreSQL 版本与配置</title>
   <tgroup cols="3">
    <thead>
     <row>
      <entry>项目</entry>
      <entry>值</entry>
      <entry>说明</entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>PostgreSQL 版本</entry>
      <entry>18.0</entry>
      <entry>-</entry>
     </row>
     <row>
      <entry>port</entry>
      <entry>5432</entry>
      <entry>-</entry>
     </row>
     <row>
      <entry>$PGDATA</entry>
      <entry>/var/lib/pgsql/18/data</entry>
      <entry>-</entry>
     </row>
     <row>
      <entry>归档模式</entry>
      <entry>on</entry>
      <entry>/var/lib/pgsql/archivedir</entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <table id="example-replication-mode-table-pgpool-config">
   <title>Pgpool-II 版本与配置</title>
   <tgroup cols="3">
    <thead>
     <row>
      <entry>项目</entry>
      <entry>值</entry>
      <entry>说明</entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>Pgpool-II 版本</entry>
      <entry>4.3.0</entry>
      <entry>-</entry>
     </row>
     <row>
      <entry morerows='1'>port</entry>
      <entry>9999</entry>
      <entry>Pgpool-II 接受连接</entry>
     </row>
     <row>
      <entry>9898</entry>
      <entry>PCP 进程接受连接</entry>
     </row>
     <row>
      <entry>配置文件</entry>
      <entry>/etc/pgpool-II/pgpool.conf</entry>
      <entry>Pgpool-II 配置文件</entry>
     </row>
     <row>
      <entry>Pgpool-II 启动用户</entry>
      <entry>postgres（Pgpool-II 4.1 或更高版本）</entry>
      <entry>Pgpool-II 4.0 或更早版本，默认启动用户为 root</entry>
     </row>
     <row>
      <entry morerows='1'>集群模式</entry>
      <entry>原生复制模式</entry>
      <entry>-</entry>
     </row>
     <row>
      <entry>快照隔离模式</entry>
      <entry>-</entry>
     </row>
    </tbody>
   </tgroup>
  </table>
 </sect2>

 <sect2 id="example-replication-mode-installation">
  <title>安装</title>
  <para>
   在本示例中，我们通过 YUM 安装 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname> RPM 软件包。
  </para>
  <para>
   从 <productname>PostgreSQL</productname> YUM 仓库安装 <productname>PostgreSQL</productname>。
  </para>
  <programlisting>
[all servers]# yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-10-x86_64/pgdg-redhat-repo-latest.noarch.rpm
[all servers]# yum install -y postgresql18-server
  </programlisting>

  <para>
   由于 <productname>PostgreSQL</productname> YUM 仓库中也包含
   <productname>Pgpool-II</productname> 相关软件包，因此请在
   <filename>/etc/yum.repos.d/pgdg-redhat-all.repo</filename> 中添加
   "exclude" 设置，以避免从 <productname>PostgreSQL</productname> YUM
   仓库安装 <productname>Pgpool-II</productname>。
  </para>
  <programlisting>
[all servers]# vi /etc/yum.repos.d/pgdg-redhat-all.repo
  </programlisting>
  <para>
   以下是 <filename>/etc/yum.repos.d/pgdg-redhat-all.repo</filename> 的设置示例。
  </para>
  <programlisting>
[pgdg-common]
...
exclude=pgpool*

[pgdg18]
...
exclude=pgpool*

[pgdg17]
...
exclude=pgpool*

[pgdg16]
...
exclude=pgpool*

[pgdg15]
...
exclude=pgpool*

[pgdg14]
...
exclude=pgpool*

[pgdg13]
...
exclude=pgpool*
...
exclude=pgpool*
  </programlisting>
  <para>
   使用 Pgpool-II YUM 仓库安装 <productname>Pgpool-II</productname>。
  </para>
  <programlisting>
[all servers]# yum install -y https://www.pgpool.net/yum/rpms/4.6/redhat/rhel-10-x86_64/pgpool-II-release-4.6-1.noarch.rpm
[all servers]# yum install -y pgpool-II-pg18-*
  </programlisting>
 </sect2>

 <sect2 id="example-replication-mode-pre-setup">
  <title>开始之前</title>
  <para>
   在开始配置过程之前，请确认以下前提条件。
  </para>

  <sect3 id="example-replication-mode-ssh">
   <title>配置免密码 SSH 登录</title>
   <para>
    要使用 <productname>Pgpool-II</productname> 的在线恢复功能，
    需要配置允许对所有服务器进行 <emphasis>免密码</emphasis> SSH 访问
    的设置。请在所有服务器上执行以下命令来设置免密码
    <literal>SSH</literal>。
    生成的密钥文件名称为 <literal>id_rsa_pgpool</literal>。
   </para>
   <programlisting>
[all servers]# su - postgres
[all servers]$ mkdir ~/.ssh && chmod 700 ~/.ssh
[all servers]$ ssh-keygen -t rsa -f ~/.ssh/id_rsa_pgpool
   </programlisting>
   <para>
    然后，将公钥 <filename>id_rsa_pgpool.pub</filename> 添加到每台服务器上的
    <filename>/var/lib/pgsql/.ssh/authorized_keys</filename> 文件中。
   </para>
   <para>
    设置好 SSH 后，请确认以 <literal>postgres</literal> 用户身份执行
    <command>ssh postgres@serverX -i ~/.ssh/id_rsa_pgpool</command> 命令
    能够无需输入密码即可登录到每台服务器。
   </para>

   <note>
    <para>
     如果使用 SSH 公钥认证登录失败，请检查以下各项：
     <itemizedlist>
      <listitem>
       <para>
        确认 <filename>/etc/ssh/sshd_config</filename> 中允许公钥认证选项 <literal>PubkeyAuthentication</literal>：
       </para>
      </listitem>
     </itemizedlist>
     <programlisting>
PubkeyAuthentication yes
     </programlisting>
     <itemizedlist>
      <listitem>
       <para>
        如果启用了 SELinux，SSH 公钥认证（免密码 SSH）可能会失败。
        您需要在所有服务器上执行以下命令。
       </para>
      </listitem>
     </itemizedlist>
    <programlisting>
[all servers]# su - postgres
[all servers]$ restorecon -Rv ~/.ssh
    </programlisting>
    </para>
   </note>
  </sect3>

  <sect3 id="example-replication-mode-pgpass">
   <title>创建 .pgpass</title>
   <para>
    为了允许 <literal>repl</literal> 用户在不指定密码的情况下执行在线恢复脚本，
    我们在每台服务器的 <literal>postgres</literal> 用户主目录下创建
    <filename>.pgpass</filename> 文件，并将权限更改为 <literal>600</literal>。
   </para>
   <programlisting>
[all servers]# su - postgres
[all servers]$ vi ~/.pgpass
server1:5432:replication:repl:&lt;repl user password&gt;
server2:5432:replication:repl:&lt;repl user password&gt;
server3:5432:replication:repl:&lt;repl user password&gt;
server1:5432:postgres:postgres:&lt;postgres user password&gt;
server2:5432:postgres:postgres:&lt;postgres user password&gt;
server3:5432:postgres:postgres:&lt;postgres user password&gt;
[all servers]$ chmod 600 ~/.pgpass
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-firewall">
   <title>配置防火墙</title>
   <para>
    当连接到 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname> 服务器时，必须通过启用防火墙管理软件使目标端口可访问。以下是 <systemitem>Rocky Linux 10</systemitem> 的示例。
   </para>
   <programlisting>
[all servers]# firewall-cmd --permanent --zone=public --add-service=postgresql
[all servers]# firewall-cmd --permanent --zone=public --add-port=9999/tcp --add-port=9898/tcp
[all servers]# firewall-cmd --reload
   </programlisting>
   <para>
    如果启用了 Watchdog，还需要开放端口 9000 和 9694。
   </para>
   <programlisting>
[all servers]# firewall-cmd --permanent --zone=public --add-port=9000/tcp  --add-port=9694/udp
   </programlisting>
  </sect3>
 </sect2>

 <sect2 id="example-replication-mode-postgresql-config">
  <title><productname>PostgreSQL</productname> 配置</title>
  <para>
   本节介绍如何创建和配置 PostgreSQL 服务器。
  </para>
  <para>
   在本示例中，我们使用 WAL 归档。
   首先，我们在所有服务器上创建目录 <filename>/var/lib/pgsql/archivedir</filename>
   来存放 <acronym>WAL</acronym> 段。
  </para>
  <programlisting>
[all servers]# su - postgres
[all servers]$ mkdir /var/lib/pgsql/archivedir
  </programlisting>
  <para>
   只在 server1 上创建一个 PostgreSQL 服务器。
   另外两个 PostgreSQL 服务器通过 <xref linkend="example-replication-mode-create-replicas"> 中所述的
   Pgpool-II 在线恢复功能创建。
  </para>
  <para>
   执行以下命令在 server1 上创建 PostgreSQL 数据库集群。
  </para>
  <programlisting>
[server1]# su - postgres
[server1]$ /usr/pgsql-18/bin/initdb -E UTF8 --no-locale
  </programlisting>
  <para>
   然后编辑 server1 上的 <filename>$PGDATA/postgresql.conf</filename>。
  </para>
  <programlisting>
[server1]$ vi $PGDATA/postgresql.conf
listen_addresses = '*'
archive_mode = on
archive_command = 'cp "%p" "/var/lib/pgsql/archivedir/%f"'
  </programlisting>

  <para>
   假设所有 <productname>Pgpool-II</productname> 服务器和
   <productname>PostgreSQL</productname> 服务器位于同一子网内，并编辑
   <filename>pg_hba.conf</filename> 以启用 <literal>scram-sha-256</literal> 认证方法。
  </para>
  <programlisting>
[server1]$ vi $PGDATA/pg_hba.conf
host    all             all             samenet                 scram-sha-256
host    replication     all             samenet                 scram-sha-256
  </programlisting>

  <para>
   执行以下命令启动 PostgreSQL 服务器。
  </para>
  <programlisting>
[server1]$ /usr/pgsql-18/bin/pg_ctl start
  </programlisting>

  <para>
   创建 PostgreSQL 用户。
  </para>
  <table id="example-replication-mode-user">
   <title>PostgreSQL 用户</title>
   <tgroup cols="3">
    <thead>
     <row>
	  <entry>用户名</entry>
	  <entry>密码</entry>
	  <entry>说明</entry>
     </row>
    </thead>
    <tbody>
     <row>
      <entry>repl</entry>
      <entry>repl</entry>
	  <entry>PostgreSQL 复制用户</entry>
     </row>
     <row>
      <entry>pgpool</entry>
      <entry>pgpool</entry>
      <entry>
       执行健康检查的用户（<xref linkend="GUC-HEALTH-CHECK-USER">）
      </entry>
     </row>
     <row>
      <entry>postgres</entry>
      <entry>postgres</entry>
      <entry>执行在线恢复的用户</entry>
     </row>
    </tbody>
   </tgroup>
  </table>

  <programlisting>
[server1]$ psql -U postgres -p 5432
postgres=# SET password_encryption = 'scram-sha-256';
postgres=# CREATE ROLE pgpool WITH LOGIN;
postgres=# CREATE ROLE repl WITH REPLICATION LOGIN;
postgres=# \password pgpool
postgres=# \password repl
postgres=# \password postgres
  </programlisting>

  <sect3 id="example-replication-mode-si-mode">
   <title>快照隔离模式的设置</title>
   <para>
    只有在 PostgreSQL 的事务隔离级别为"<emphasis>可重复读</emphasis>"时，
    快照隔离模式才可用。如果要使用快照隔离模式，请在
    <filename>postgresql.conf</filename> 中设置
    <varname>default_transaction_isolation ='repeatable read'</varname>。
   </para>
   <programlisting>
[server1]$ vi $PGDATA/postgresql.conf
default_transaction_isolation = 'repeatable read'
   </programlisting>
  </sect3>
 </sect2><sect2 id="example-replication-mode-pgpool-config">
  <title>配置 <productname>Pgpool-II</productname></title>
  <para>
   使用 RPM 安装 <productname>Pgpool-II</productname> 时，
   <productname>Pgpool-II</productname> 的配置示例文件位于
   <filename>/etc/pgpool-II</filename>。
  </para>

  <sect3 id="example-replication-mode-clustering-mode">
   <title>集群模式</title>
   <para>
    首先，在 <xref linkend="GUC-BACKEND-CLUSTERING-MODE"> 中指定 <productname>Pgpool-II</productname> 的集群模式。
   </para>
   <itemizedlist>
    <listitem>
     <para>
      原生复制模式
     </para>
     <programlisting>
backend_clustering_mode = native_replication
     </programlisting>
    </listitem>
    <listitem>
     <para>
      快照隔离模式
     </para>
     <programlisting>
backend_clustering_mode = snapshot_isolation
     </programlisting>
    </listitem>
   </itemizedlist>
  </sect3>

  <sect3 id="example-replication-mode-listen-addresses">
   <title>listen_addresses</title>
   <para>
    为了让 Pgpool-II 接受所有传入连接，我们设置 <varname>listen_addresses = '*'</varname>。
   </para>
   <programlisting>
listen_addresses = '*'
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode--health-check">
   <title>健康检查</title>
   <para>
    启用健康检查，以便让 <productname>Pgpool-II</> 检测 PostgreSQL 故障。
    此外，如果网络不稳定，即使后端正常运行，健康检查也可能会失败，
    从而导致故障切换或降级操作的发生。
    为了防止这种健康检查的误检，我们设置 <varname>health_check_max_retries = 3</varname>。
    指定 <xref linkend="GUC-HEALTH-CHECK-USER"> 和 <xref linkend="GUC-HEALTH-CHECK-PASSWORD">。
    在本示例中，我们将 <xref linkend="GUC-SR-CHECK-PASSWORD"> 留空，并在
    <xref linkend="GUC-POOL-PASSWD"> 中创建条目。
    关于如何在 <xref linkend="GUC-POOL-PASSWD"> 中创建条目，请参阅 <xref linkend="example-replication-mode-auth">。
    从 <productname>Pgpool-II</productname> 4.0 开始，如果这些参数留空，
    <productname>Pgpool-II</productname> 会先从 <xref linkend="GUC-POOL-PASSWD"> 文件中获取该
    特定用户的密码，而不是使用空密码。
   </para>
   <programlisting>
health_check_period = 5
health_check_timeout = 30
health_check_user = 'pgpool'
health_check_password = ''
health_check_max_retries = 3
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-backend-settings">
   <title>后端设置</title>
   <para>
    指定 <productname>PostgreSQL</productname> 后端信息。
    可以通过在参数名称末尾添加数字来指定多个后端。
   </para>
   <programlisting>
# - Backend Connection Settings -

backend_hostname0 = 'server1'
backend_port0 = 5432
backend_weight0 = 1
backend_data_directory0 = '/var/lib/pgsql/18/data'
backend_flag0 = 'ALLOW_TO_FAILOVER'

backend_hostname1 = 'server2'
backend_port1 = 5432
backend_weight1 = 1
backend_data_directory1 = '/var/lib/pgsql/18/data'
backend_flag1 = 'ALLOW_TO_FAILOVER'

backend_hostname2 = 'server3'
backend_port2 = 5432
backend_weight2 = 1
backend_data_directory2 = '/var/lib/pgsql/18/data'
backend_flag2 = 'ALLOW_TO_FAILOVER'
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-online-recovery">
   <title>在线恢复</title>
   <para>
    接下来，为了执行在线恢复，我们指定 <productname>PostgreSQL</productname>
    用户名以及在线恢复命令 <command>recovery_1st_stage_command</command> 和
    <command>recovery_2nd_stage_command</command>。
    由于执行在线恢复需要 <productname>PostgreSQL</productname> 的
    <emphasis>超级用户</emphasis> 权限，因此我们在 <xref linkend="GUC-RECOVERY-USER"> 中指定 <literal>postgres</literal> 用户。
   <programlisting>
recovery_user = 'postgres'
recovery_password = ''

recovery_1st_stage_command = 'recovery_1st_stage'
recovery_2nd_stage_command = 'recovery_2nd_stage'
   </programlisting>
    示例脚本
    <ulink url="https://raw.githubusercontent.com/pgpool/pgpool2/refs/heads/master/src/sample/scripts/replication_mode_recovery_1st_stage.sample">replication_mode_recovery_1st_stage.sample</ulink>、
    <ulink url="https://raw.githubusercontent.com/pgpool/pgpool2/refs/heads/master/src/sample/scripts/replication_mode_recovery_2nd_stage.sample">replication_mode_recovery_2nd_stage.sample</ulink>
    以及
    <ulink url="https://raw.githubusercontent.com/pgpool/pgpool2/refs/heads/master/src/sample/scripts/pgpool_remote_start.sample">pgpool_remote_start.sample</ulink>
    安装在 <filename>/etc/pgpool-II/</filename> 中。
    请根据示例脚本创建在线恢复命令所使用的脚本，并将这些文件复制到数据库集群目录中。
   </para>
   <programlisting>
[server1]# cp -p /etc/pgpool-II/sample_scripts/replication_mode_recovery_1st_stage.sample /var/lib/pgsql/18/data/recovery_1st_stage
[server1]# cp -p /etc/pgpool-II/sample_scripts/replication_mode_recovery_2nd_stage.sample /var/lib/pgsql/18/data/recovery_2nd_stage
[server1]# cp -p /etc/pgpool-II/sample_scripts/pgpool_remote_start.sample /var/lib/pgsql/18/data/pgpool_remote_start
[server1]# chown postgres:postgres /var/lib/pgsql/18/data/{recovery_1st_stage,recovery_2nd_stage,pgpool_remote_start}
   </programlisting>
   <para>
    基本上，只要根据 PostgreSQL 的安装目录修改 <emphasis>PGHOME</emphasis>，就应该可以正常工作。
   </para>
   <programlisting>
[server1]# vi /var/lib/pgsql/18/data/recovery_1st_stage
...
PGHOME=/usr/pgsql-18
...

[server1]# vi /var/lib/pgsql/18/data/recovery_2nd_stage
...
PGHOME=/usr/pgsql-18
...

[server1]# vi /var/lib/pgsql/18/data/pgpool_remote_start
...
PGHOME=/usr/pgsql-18
...
   </programlisting>  </sect3>

  <sect3 id="example-replication-mode-auth">
   <title>客户端认证配置</title>
   <para>
    启用客户端与 <productname>Pgpool-II</productname> 之间的客户端认证。
    使用 RPM 安装时，<productname>Pgpool-II</productname> 的配置文件
    <filename>pool_hba.conf</filename> 位于 <filename>/etc/pgpool-II</filename>。
    默认情况下，pool_hba 认证是禁用的，设置 <varname>enable_pool_hba = on</varname>
    以启用它。
   </para>
   <programlisting>
enable_pool_hba = on
   </programlisting>
   <para>
    <filename>pool_hba.conf</filename> 文件的格式与 PostgreSQL 的
    <filename>pg_hba.conf</filename> 格式非常接近。请将 <literal>pgpool</literal> 和 <literal>postgres</literal> 用户的认证方法设置为 <literal>scram-sha-256</literal>。
   </para>
   <programlisting>
[server1]# vi /etc/pgpool-II/pool_hba.conf
host    all         pgpool           0.0.0.0/0          scram-sha-256
host    all         postgres         0.0.0.0/0          scram-sha-256
   </programlisting>
   <note>
    <para>
     请注意，在 <productname>Pgpool-II</productname> 4.0 中，
     <filename>pgpool.conf</filename> 中的 <xref linkend="guc-health-check-password">、
     <xref linkend="guc-sr-check-password">、
     <xref linkend="guc-wd-lifecheck-password">、<xref linkend="guc-recovery-password">
     只能指定 AES 加密密码或明文密码。
    </para>
   </note>
   <para>
    认证所使用的默认密码文件名称为 <xref linkend="GUC-POOL-PASSWD">。
    要使用 <literal>scram-sha-256</literal> 认证，需要用于解密密码的
    解密密钥。我们在 <productname>Pgpool-II</productname> 启动用户
    <literal>postgres</literal>（<productname>Pgpool-II</productname> 4.1 或更高版本）的主目录中创建
    <literal>.pgpoolkey</literal> 文件。
    （<productname>Pgpool-II</productname> 4.0 或更早版本，默认情况下
    <productname>Pgpool-II</productname> 以 <literal>root</literal> 身份启动）
     <programlisting>
[server1]# su - postgres
[server1]$ echo 'some string' > ~/.pgpoolkey
[server1]$ chmod 600 ~/.pgpoolkey
     </programlisting>
   </para>
   <para>
    执行命令 <command>pg_enc -m -k /path/to/.pgpoolkey -u username -p</command>
    将用户名和 <literal>AES</literal> 加密密码注册到文件 <filename>pool_passwd</filename> 中。
    如果 <filename>pool_passwd</filename> 尚不存在，它会在与
    <filename>pgpool.conf</filename> 相同的目录中创建。
   </para>
   <programlisting>
[server1]# su - postgres
[server1]$ pg_enc -m -k ~/.pgpoolkey -u pgpool -p
db password: [pgpool user's password]
[server1]$ pg_enc -m -k ~/.pgpoolkey -u postgres -p
db password: [postgres user's password]

[server1]$ cat /etc/pgpool-II/pool_passwd
pgpool:AESheq2ZMZjynddMWk5sKP/Rw==
postgres:AESHs/pWL5rtXy2IwuzroHfqg==
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-pcp-password">
   <title>PCP 密码</title>
   <para>
    由于使用 <literal>PCP</literal> 命令需要用户认证，
    因此我们需要在 <filename>pcp.conf</filename> 中以
    "<literal>用户名:加密密码</literal>" 的格式指定用户名和 md5 加密密码。
   </para>
   <para>
    我们使用 <xref linkend="PG-MD5"> 为 <literal>pgpool</literal> 用户创建加密密码条目，如下所示：
   </para>
   <programlisting>
[server1]# echo 'pgpool:'`pg_md5 PCP password` &gt;&gt; /etc/pgpool-II/pcp.conf
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-log">
   <title>日志记录</title>
   <para>
    自 Pgpool-II 4.2 起，实现了日志收集器进程。
    在本示例中，我们启用日志收集器。
   </para>
   <programlisting>
log_destination = 'stderr'
logging_collector = on
log_directory = '/var/log/pgpool'
log_filename = 'pgpool-%Y-%m-%d_%H%M%S.log'
log_truncate_on_rotation = on
log_rotation_age = 1d
log_rotation_size = 10MB
   </programlisting>
   <para>
    在 server1 上创建日志目录。
   </para>
   <programlisting>
[server1]# mkdir /var/log/pgpool_log/
[server1]# chown postgres:postgres /var/log/pgpool_log/
   </programlisting>
  </sect3>
 </sect2><sect2 id="example-replication-mode-start-stop">
  <title>启动/停止 Pgpool-II</title>
  <para>
   在启动 <productname>Pgpool-II</productname> 之前，请先启动
   <productname>PostgreSQL</productname> 服务器。
   另外，在停止 <productname>PostgreSQL</productname> 时，
   需要先停止 Pgpool-II。
   执行以下命令来启动或停止 <productname>Pgpool-II</productname>。
  </para>
  <itemizedlist>
   <listitem>
    <para>
     启动 <productname>Pgpool-II</productname>
    </para>
    <programlisting>
# systemctl start pgpool.service
    </programlisting>
   </listitem>
   <listitem>
    <para>
     停止 <productname>Pgpool-II</productname>
    </para>
    <programlisting>
# systemctl stop pgpool.service
    </programlisting>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2 id="example-replication-mode-try">
  <title>使用方法</title>
  <para>
   配置完成后，让我们开始使用 <productname>Pgpool-II</productname>。
  </para>
  <para>
   首先，让我们启动 <productname>Pgpool-II</productname>。
  </para>
  <programlisting>
[server1]# systemctl start pgpool.service
  </programlisting>

  <sect3 id="example-replication-mode-create-replicas">
   <title>使用在线恢复创建 PostgreSQL 服务器</title>
   <para>
    然后，我们使用在线恢复创建 PostgreSQL 节点1 和节点2。
    请确保 <command>pcp_recovery_node</command> 命令所使用的
    <filename>recovery_1st_stage</filename>、
    <filename>recovery_2nd_stage</filename> 和 <filename>pgpool_remote_start</filename>
    脚本位于 server1 上的数据库集群目录中。
   </para>
   <programlisting>
[server1]# pcp_recovery_node -h server1 -p 9898 -U pgpool -n 1
Password:
pcp_recovery_node -- Command Successful

[server1]# pcp_recovery_node -h server1 -p 9898 -U pgpool -n 2
Password:
pcp_recovery_node -- Command Successful
   </programlisting>
   <para>
    如果 <command>pcp_recovery_node</command> 已成功运行，
    请确认 PostgreSQL 节点0 以主节点身份启动，
    节点1 和节点2 以副本节点身份启动。
   </para>
   <programlisting>
# psql -h server1 -p 9999 -U pgpool postgres -c "show pool_nodes"
Password for user pgpool:
 node_id | hostname | port | status | pg_status | lb_weight |  role   | pg_role | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change
 ---------+----------+------+--------+-----------+-----------+---------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | server1  | 5432 | up     | up        | 0.333333  | main    | main    | 0          | true              | 0                 |                   |                        | 2021-12-02 16:48:21
 1       | server2  | 5432 | up     | up        | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:48:21
 2       | server3  | 5432 | up     | up        | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:48:21
(3 rows)
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-verify-replication">
   <title>验证复制</title>
   <para>
    接下来，让我们使用基准测试工具 pgbench 验证复制功能。
   </para>
   <programlisting>
[server1]# /usr/pgsql-18/bin/createdb test -U postgres -p 9999
[server1]# /usr/pgsql-18/bin/pgbench -h server1 -U postgres -i -p 9999 test
   </programlisting>
   <para>
    为了检查复制是否正常工作，请直接连接到每台 PostgreSQL
    服务器，查看它们是否返回相同的结果。
   </para>
   <programlisting>
[server1]# /usr/pgsql-18/bin/psql -h server1 -U postgres -p 5432 test
test=# \d
              List of relations
 Schema |       Name       | Type  |  Owner
--------+------------------+-------+----------
 public | pgbench_accounts | table | postgres
 public | pgbench_branches | table | postgres
 public | pgbench_history  | table | postgres
 public | pgbench_tellers  | table | postgres
(4 rows)

[server1]# /usr/pgsql-18/bin/psql -h server2 -U postgres -p 5432 test
test=# \d
              List of relations
 Schema |       Name       | Type  |  Owner
--------+------------------+-------+----------
 public | pgbench_accounts | table | postgres
 public | pgbench_branches | table | postgres
 public | pgbench_history  | table | postgres
 public | pgbench_tellers  | table | postgres
(4 rows)

[server1]# /usr/pgsql-18/bin/psql -h server3 -U postgres -p 5432 test
test=# \d
              List of relations
 Schema |       Name       | Type  |  Owner
--------+------------------+-------+----------
 public | pgbench_accounts | table | postgres
 public | pgbench_branches | table | postgres
 public | pgbench_history  | table | postgres
 public | pgbench_tellers  | table | postgres
(4 rows)
   </programlisting>
   <para>
    server1、server2 和 server3 返回相同的结果。
   </para>
   <para>
    接下来，让我们运行一段时间 pgbench 并检查结果。
   </para>
   <programlisting>
[server1]# /usr/pgsql-18/bin/pgbench -h server1 -U postgres -p 9999 -T 10 test
   </programlisting>
   <para>
    所有 PostgreSQL 服务器都返回相同的结果。
   </para>
   <programlisting>
[server1]# /usr/pgsql-18/bin/psql -h server1 -U postgres -p 5432 test -c "SELECT sum(abalance) FROM pgbench_accounts"
Password for user postgres:
  sum
--------
 -99710
(1 row)

[server1]# /usr/pgsql-18/bin/psql -h server2 -U postgres -p 5432 test -c "SELECT sum(abalance) FROM pgbench_accounts"
Password for user postgres:
  sum
--------
 -99710
(1 row)

[server1]# /usr/pgsql-18/bin/psql -h server3 -U postgres -p 5432 test -c "SELECT sum(abalance) FROM pgbench_accounts"
Password for user postgres:
  sum
--------
 -99710
(1 row)
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-verify-node-failure">
   <title>PostgreSQL 故障</title>
   <para>
    接下来，停止 server1 上的 PostgreSQL 主节点，
    并验证主节点的切换。
   </para>
   <programlisting>
[server1]# su - postgres -c "/usr/pgsql-18/bin/pg_ctl -m i stop"
   </programlisting>
   <para>
    停止 <literal>server1</literal> 上的 <productname>PostgreSQL</productname> 后，
    会发生切换，<literal>server2</literal> 上的
    <productname>PostgreSQL</productname> 将成为新的主节点。
   </para>
   <programlisting>
[server1]# psql -h server1 -p 9999 -U pgpool postgres -c "show pool_nodes"
Password for user pgpool:
 node_id | hostname | port | status | pg_status | lb_weight |  role   | pg_role | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change
---------+----------+------+--------+-----------+-----------+---------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | server1  | 5432 | down   | down      | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:57:45
 1       | server2  | 5432 | up     | up        | 0.333333  | main    | main    | 1          | true              | 0                 |                   |                        | 2021-12-02 16:48:21
 2       | server3  | 5432 | up     | up        | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:48:21
(3 rows)
   </programlisting>
  </sect3>

  <sect3 id="example-replication-mode-try-online-recovery">
   <title>在线恢复</title>
   <para>
    在这里，我们使用 <productname>Pgpool-II</productname> 的在线恢复功能来
    恢复 <literal>server1</literal> 上的 PostgreSQL 节点0。
   </para>
   <programlisting>
# pcp_recovery_node -h server1 -p 9898 -U pgpool -n 0
Password:
pcp_recovery_node -- Command Successful
   </programlisting>
   <para>
    然后确认 <literal>server1</literal> 以主节点身份启动。
   </para>
   <programlisting>
# psql -h server1 -p 9999 -U pgpool postgres -c "show pool_nodes"
Password for user pgpool:
 node_id | hostname | port | status | pg_status | lb_weight |  role   | pg_role | select_cnt | load_balance_node | replication_delay | replication_state | replication_sync_state | last_status_change
---------+----------+------+--------+-----------+-----------+---------+---------+------------+-------------------+-------------------+-------------------+------------------------+---------------------
 0       | server1  | 5432 | up     | up        | 0.333333  | main    | main    | 0          | true              | 0                 |                   |                        | 2021-12-02 16:57:45
 1       | server2  | 5432 | up     | up        | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:48:21
 2       | server3  | 5432 | up     | up        | 0.333333  | replica | replica | 0          | false             | 0                 |                   |                        | 2021-12-02 16:48:21
(3 rows)
   </programlisting>
  </sect3>
 </sect2>
</sect1>