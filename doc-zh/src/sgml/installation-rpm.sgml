<!-- doc/src/sgml/installation-rpm.sgml -->

 <sect1 id="install-rpm">
 <title>从 RPM 安装</title>
 <para>
  本章介绍如何从 RPM 包安装 <productname>Pgpool-II</productname>。
  如果您计划从源码安装，请参考 <xref linkend="install-source">。
 </para>
 <para>
  <productname>Pgpool-II</productname> 社区为 RHEL10/9/8 及兼容 RHEL 的操作系统提供 RPM 包。
  您可以从
  <ulink url="https://www.pgpool.net/yum/">Pgpool-II 官方仓库</ulink>下载软件包文件。
 </para>
 <para>
   <productname>Pgpool-II</productname> 官方仓库包含以下软件包：
 </para>
 <table id="rpm-packages">
  <title>Pgpool-II RPM 软件包</title>
  <tgroup cols="2">
   <thead>
    <row>
     <entry>软件包</entry>
     <entry>描述</entry>
    </row>
   </thead>
   <tbody>
    <row>
     <entry>pgpool-II-pgXX</entry>
     <entry>运行 Pgpool-II 所需的库和二进制文件</entry>
    </row>
    <row>
     <entry>pgpool-II-pgXX-extensions</entry>
     <entry>该包必须安装在所有 PostgreSQL 服务器上才能使用在线恢复功能</entry>
    </row>
    <row>
     <entry>pgpool-II-pgXX-debuginfo</entry>
     <entry>用于调试的调试符号</entry>
    </row>
    <row>
     <entry>pgpool-II-pgXX-debugsource</entry>
     <entry>仅适用于 RHEL10/9/8。用于调试的调试符号</entry>
    </row>
    <row>
     <entry>pgpool-II-pgXX-extensions-debuginfo</entry>
     <entry>仅适用于 RHEL10/9/8。用于调试的调试符号</entry>
    </row>
    <row>
     <entry>pgpool-II-pgXX-devel</entry>
     <entry>供开发者使用的头文件</entry>
    </row>
   </tbody>
  </tgroup>
 </table>

 <para>
  <productname>Pgpool-II</productname> 需要 <productname>PostgreSQL</productname> 的
  库和扩展目录。由于不同版本的 <productname>PostgreSQL</productname> 目录路径不同，
  <productname>Pgpool-II</productname> 为每个 PostgreSQL 版本提供独立的软件包。
  上述软件包名称中的 "XX" 是一个两位数字，代表 PostgreSQL 的版本号。
  请根据您的 PostgreSQL 版本选择对应的 <productname>Pgpool-II</productname> RPM 包。
  （例如，如果您使用的是 PostgreSQL 18，则需要安装 <filename>pgpool-II-pg18</filename>）
 </para>

 <sect2 id="before-installing">
  <title>安装前准备</title>
  <para>
   由于 <productname>Pgpool-II</productname> 相关软件包也包含在
   <productname>PostgreSQL</productname> YUM 仓库中，
   如果已安装 PostgreSQL 仓库包，
   请在 <filename>/etc/yum.repos.d/pgdg-redhat-all.repo</filename> 中添加 "exclude" 设置，
   以免 <productname>Pgpool-II</productname> 从
   <productname>PostgreSQL</productname> YUM 仓库被安装。
   如果 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname>
   安装在不同的服务器上，可以跳过本节。
  </para>
  <programlisting>
vi /etc/yum.repos.d/pgdg-redhat-all.repo
  </programlisting>
  <para>
   以下是 <filename>/etc/yum.repos.d/pgdg-redhat-all.repo</filename> 的设置示例。
  </para>
  <programlisting>
[pgdg-common]
...
exclude=pgpool*

[pgdg18]
...
exclude=pgpool*

[pgdg17]
...
exclude=pgpool*

[pgdg16]
...
exclude=pgpool*

[pgdg15]
...
exclude=pgpool*

[pgdg14]
...
exclude=pgpool*

[pgdg13]
...
exclude=pgpool*
  </programlisting>
 </sect2>

 <sect2 id="installing-rpm">
  <title>安装 RPM</title>
  <para>
   本节介绍如何使用
   <ulink url="https://www.pgpool.net/yum/">Pgpool-II 官方 YUM 仓库</ulink>
   安装 <productname>Pgpool-II</productname>。
  </para>

  <para>
   以下命令以在 RHEL10 上为 PostgreSQL 18 安装 Pgpool-II 4.6.x 为例。
   如果您使用其他版本，请将 "pgXX" 替换为您的 PostgreSQL 版本号。
  </para>
  <para>
   首先，安装与您 <productname>Pgpool-II</productname>
   版本和发行版对应的仓库。
   对于 REHL9/8，请参阅 <ulink url="https://www.pgpool.net/download/yum/">此处</ulink>。
  </para>
  <programlisting>
dnf install https://www.pgpool.net/yum/rpms/4.6/redhat/rhel-10-x86_64/pgpool-II-release-4.6-1.noarch.rpm
  </programlisting>
  <para>
   然后，安装 <productname>Pgpool-II</productname>。
  </para>
  <programlisting>
dnf install pgpool-II-pg18
  </programlisting>
  <para>
   如需使用在线恢复功能，请在<emphasis>所有 PostgreSQL 服务器</emphasis>上安装
   <filename>pgpool-II-pg18-extensions</filename>。
   由于 <filename>pgpool-II-pgXX-extensions</filename> 依赖于
   <filename>pgpool-II-pgXX</filename> 软件包，
   如果 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname>
   安装在不同的服务器上，则也需要在 PostgreSQL 服务器上安装
   <filename>pgpool-II-pgXX</filename>。
  </para>
  <note>
   <para>
    <filename>pgpool-II-pgXX-extensions</filename> 需要安装在
    <emphasis>PostgreSQL 服务器上</emphasis>。
    如果 <productname>Pgpool-II</productname> 和 <productname>PostgreSQL</productname>
    安装在不同的服务器上，
    则不需要在运行 <productname>Pgpool-II</productname> 的服务器上安装它。
   </para>
  </note>
  <programlisting>
dnf install pgpool-II-pg18-extensions pgpool-II-pg18
  </programlisting>

  <para>
   如有需要，您还可以选择安装调试信息和开发包。
  </para>
  <programlisting>
dnf install pgpool-II-pg18-debuginfo pgpool-II-pg18-devel
  </programlisting>
 </sect2>

 <sect2 id="configure-rpm">
  <title>配置 Pgpool-II</title>
  <para>
   所有 <productname>Pgpool-II</productname> 配置文件
   均安装在 <filename>/etc/pgpool-II</filename> 目录下。有关如何配置
   文件的说明，请参考 <xref linkend="configuring-pgpool">。
  </para>
 </sect2>

 <sect2 id="start-rpm">
  <title>启动/停止 Pgpool-II</title>
  <para>
   在 RHEL10/9/8 上，如果设置了 <productname>Pgpool-II</productname> 的自动启动，
   只需执行一次以下操作。
   <programlisting>
systemctl enable pgpool.service
   </programlisting>

   此后，要启动 <productname>Pgpool-II</productname>，
   运行以下命令或重启整个系统。
   请注意，在此之前 PostgreSQL 服务器必须已经启动。

   <programlisting>
systemctl start pgpool.service
   </programlisting>

   要停止 <productname>Pgpool-II</productname>，执行以下操作。
   请注意，必须在停止 <productname>PostgreSQL</productname> 之前
   先停止 <productname>Pgpool-II</productname>。
   <programlisting>
systemctl stop pgpool.service
   </programlisting>

   此后，您可以停止 <productname>PostgreSQL</productname>
   服务器。
  </para>
 </sect2>
</sect1>
