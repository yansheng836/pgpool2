<!-- doc/src/sgml/intro.sgml -->

<preface id="preface">
 <title>前言</title>

 <para>
  本书是 <productname>Pgpool-II</productname> 的官方文档。它由
  <productname>Pgpool-II</productname> 开发团队及其他志愿者与
  <productname>Pgpool-II</productname> 软件开发并行编写。本书描述了当前版本
  <productname>Pgpool-II</productname> 正式支持的所有功能。
 </para>

 <para>
  为了方便查阅有关 <productname>Pgpool-II</productname> 的大量信息，本书按
  多个部分组织。每一部分面向不同的用户群体，或面向处于不同
  <productname>Pgpool-II</productname> 使用阶段的读者：

  <itemizedlist>
   <listitem>
    <para>
     <xref linkend="tutorial"> 为新手提供了一个非正式的入门介绍。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="admin"> 描述了服务器的安装与
      管理。任何运行
      <productname>Pgpool-II</productname> 服务器的用户，无论是个人使用还是为他人提供服务，都应阅读此部分。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="examples"> 解释了若干配置示例，
      以便用户可以选择适合自己实际系统的起点。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="reference"> 包含了关于 SQL 命令、客户端和服务端程序的参考信息。此部分以命令或程序为分类提供结构化的信息，辅助其他部分。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="appendixes"> 是附录部分，包含版本发布说明等信息。
    </para>
   </listitem>

   <!--
   <listitem>
   <para>
   <xref linkend="internals"> contains assorted information that might be of
   use to <productname>PostgreSQL</> developers.
  </para>
  </listitem>
   -->

  </itemizedlist>
 </para>

 <sect1 id="intro-whatis">
  <title><productname>Pgpool-II</productname> 是什么？</title>

  <para>
   <productname>Pgpool-II</productname> 管理一个
   <firstterm>连接池（pool）</firstterm>，其中包含多个
   <productname>PostgreSQL</productname> 服务器，以实现单实例
   <productname>PostgreSQL</productname> 安装所不具备的一些功能。这些功能包括：

   <variablelist>

    <varlistentry>
     <term>高可用性（High Availability）</term>
     <listitem>
      <para>
       <productname>Pgpool-II</productname> 通过使用多个
       <productname>PostgreSQL</productname> 服务器来提供高可用（HA）功能，
       能够自动将故障服务器从服务器池中移除，从而继续执行数据库任务。这被称为
       <firstterm>自动故障转移（automatic failover）</firstterm>。此外，
       <productname>Pgpool-II</productname> 还为自身提供了一个 HA 功能，称为
       <firstterm>Watchdog（看门狗）</firstterm>（详见 <xref
       linkend="tutorial-watchdog">）。同时，
       <productname>Pgpool-II</productname> 采用了 sophisticated
       <firstterm>法定人数（quorum）</firstterm> 算法，以避免误判和脑裂问题，
       使整个 HA 系统高度可靠。详见 <xref
       linkend="config-watchdog-failover-behavior">。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>负载均衡（Load balancing）</term>
     <listitem>
      <para>
       <productname>Pgpool-II</productname> 将读查询分发到多个
       <productname>PostgreSQL</productname> 服务器上，以获得更高的性能。此功能称为
       <firstterm>负载均衡（load balancing）</firstterm>。写查询被发送到主服务器
       （在流复制模式下）或所有服务器（在原生成复制模式和快照隔离模式下）。
       关于这些模式的更多细节，请参见 <xref linkend="running-mode">。
       无论如何，<productname>Pgpool-II</productname> 都能自动区分读查询和写查询。
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
  <para>
   除了上述基本功能外，<productname>Pgpool-II</productname> 还提供了以下有用的功能：
  </para>
  <para>
   <variablelist>

    <varlistentry>
     <term>连接池（Connection Pooling）</term>
     <listitem>
      <para>
       <productname>Pgpool-II</productname> 维护与
       <productname>PostgreSQL</productname>
       服务器的已建立连接，并在具有相同属性（即用户名、数据库、协议版本以及其他连接参数）的新连接到来时重用这些连接。
       它降低了连接开销，并提高了系统整体吞吐量。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>在线恢复（Online Recovery）</term>
     <listitem>
      <para>
       <productname>Pgpool-II</productname> 可以执行一条命令来完成数据库节点的在线恢复。
       当在线恢复与自动故障转移配合使用时，故障转移后脱机的节点可以自动作为备用节点重新加入。
       也可以同步并附加新的
       <productname>PostgreSQL</productname> 服务器。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>限制超限连接（Limiting Exceeding Connections）</term>
     <listitem>
      <para>
       <productname>PostgreSQL</productname> 对并发连接的最大数量有限制，
       达到该上限后会拒绝新连接。提高最大连接数会增加资源消耗，并对整体系统
       性能产生负面影响。<productname>Pgpool-II</productname> 也有最大连接数限制，
       但超出限制的额外连接会被排队等待，而不是立即返回错误。
       不过，您可以配置为在超出连接限制时返回错误（4.1 及之后版本）。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Watchdog（看门狗）</term>
     <listitem>
      <para>
       Watchdog 可以协调多个 <productname>Pgpool-II</productname>，
       构建健壮的集群系统，避免单点故障和脑裂问题。
       为避免脑裂，至少需要 3 个 <productname>Pgpool-II</productname> 节点。
       Watchdog 可以对其他 <productname>Pgpool-II</productname> 节点执行存活检查（lifecheck），
       以检测 <productname>Pgpool-II</productname> 的故障。
       如果活动的 <productname>Pgpool-II</productname> 宕机，备用的
       <productname>Pgpool-II</productname> 可以提升为活动节点，并接管虚拟 IP。
      </para>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>内存查询缓存（In Memory Query Cache）</term>
     <listitem>
      <para>
       内存查询缓存允许保存 SELECT 语句及其结果的键值对。
       当相同的 SELECT 查询再次到来时，<productname>Pgpool-II</productname> 直接
       从缓存中返回结果。由于不涉及 SQL 解析，也不访问
       <productname>PostgreSQL</productname>，使用内存缓存的速度极快。
       另一方面，在某些情况下它可能比正常路径慢，因为存储缓存数据会引入一些开销。
      </para>
     </listitem>
    </varlistentry>

   </variablelist>

  </para>

  <para>
   <productname>Pgpool-II</productname> 遵循 PostgreSQL 的后端和前/后端协议，
   并在后端与前/前端之间中继消息。因此，数据库应用程序（前/前端）认为
   <productname>Pgpool-II</productname> 就是实际的
   <productname>PostgreSQL</productname> 服务器，而
   服务器（后端）则将 <productname>Pgpool-II</productname> 视为其客户端之一。
   因为
   <productname>Pgpool-II</productname> 对服务器和客户端都是透明的，
   现有的数据库应用程序几乎无需修改源代码即可与 <productname>Pgpool-II</productname> 一起使用。
  </para>

  <para>
   <productname>Pgpool-II</productname> 运行于 Linux、FreeBSD 以及大多数类 UNIX 架构上。
   Windows 不受支持。支持的 PostgreSQL 服务器版本为 7.4 及以上
   （部分功能可能与较旧版本的
   <productname>PostgreSQL</productname> 不兼容）。您还必须确保所有
   <productname>PostgreSQL</productname> 服务器使用相同的主版本。
   此外，我们不建议混合使用不同编译选项构建的
   <productname>PostgreSQL</productname> 安装：例如是否支持 SSL、
   是否使用 --disable-integer-datetimes、不同的 block size。
   这些差异可能会影响 <productname>Pgpool-II</productname> 的部分功能。
   <productname>PostgreSQL</productname> 的小版本差异通常不是问题。
   但我们未对每个小版本都进行测试，因此建议使用完全相同的
   <productname>PostgreSQL</productname> 小版本。
  </para>

  <para>
   通过 <productname>Pgpool-II</productname> 使用 SQL 存在一些限制。
   详情请参见 <link linkend="restrictions">限制</link>。
  </para>

 </sect1>

 &history;
 &notation;
 &info;
 &restrictions;
 &problems;

</preface>
