<!-- doc/src/sgml/performance.sgml -->

<chapter id="performance">
 <title>性能考虑</title>

 <indexterm>
  <primary>性能</primary>
  <secondary>服务器</secondary>
 </indexterm>

 <para>
  有许多配置参数会影响
  <productname>Pgpool-II</productname> 的性能。
  在本章中，我们将介绍如何调整这些参数。
 </para>

 <sect1 id="resource-requirement">
  <title>资源需求</title>

  <para>
   <productname>Pgpool-II</productname> 不会消耗过多的资源。不过，它仍有最低的资源需求。在本节中，我们将逐一说明。
  </para>

  <sect2 id="memory-requirement">
   <title>内存需求</title>

   <para>
    在 <productname>Pgpool-II</productname> 中有两种类型的内存使用：共享内存和进程私有内存。前者在 <productname>Pgpool-II</productname> 主服务器进程启动时分配，直到整个 <productname>Pgpool-II</productname> 服务器关闭前不会释放。后者在每个 <productname>Pgpool-II</productname> 子进程中分配，并在进程结束时释放。
   </para>

   <sect3 id="shared-memory-requirement">
    <title>共享内存需求</title>

    <para>
     以下是计算共享内存需求的公式。
     <programlisting>
      Shared memory requirement (in mega bytes) = 10 + <xref linkend="guc-num-init-children"> * <xref linkend="guc-max-pool"> * 0.02
     </programlisting>
     例如，如果您有 <varname>num_init_children</varname> = 32（默认值）和 <varname>max_pool</varname> = 4（默认值），那么您将需要 10 + 32 * 4 * 0.02 = 12.6 MB。
    </para>

    <para>
     如果您计划在共享内存中使用内存查询缓存（详见 <xref linkend="runtime-in-memory-query-cache">），则需要更多的 RAM。参见 <xref linkend="guc-memqcache-total-size"> 和 <xref linkend="guc-memqcache-max-num-cache"> 了解所需 RAM 大小。
    </para>

    <para>
     需要注意的是，在 <productname>Pgpool-II</productname> 4.1 或更高版本中，即使未启用内存查询缓存，如果 <xref linkend="guc-enable-shared-relcache"> 已启用（默认启用），它也会额外消耗 128MB 的共享内存。
    </para>
   </sect3>
   
   <sect3 id="process-memory-requirement">
    <title>进程内存需求</title>
    <para>

     以下是计算进程内存需求的公式。
     <programlisting>
      Process memory requirement in total (in mega bytes) = <xref linkend="guc-num-init-children"> * 0.16
     </programlisting>
     例如，如果您有 <varname>num_init_children</varname> = 32（默认值），则需要 5.12MB。请注意，这是在 <productname>Pgpool-II</productname> 子进程启动时的最小内存需求。一旦进程运行，它将根据消息包大小和其他因素消耗更多内存。建议在投入生产使用前，测量进程实际使用的内存量。
    </para>
   </sect3>
  </sect2>

  <sect2 id="disk-requirement">
   <title>磁盘需求</title>
   <para>
    <productname>Pgpool-II</productname> 不会占用太多磁盘空间。另外，它也不需要高速磁盘，因为 <productname>Pgpool-II</productname> 引起的磁盘 I/O 流量很小。但是，如果您计划生成大量日志，当然需要相应的磁盘空间。
   </para>
  </sect2>
 </sect1>

 <sect1 id="managing-client-connections">
  <title>管理客户端连接</title>
  <para>
   随着接受的客户端连接数不断增长，能够接受来自客户端新连接的 <productname>Pgpool-II</productname> 子进程数量会减少，最终降为 0。在这种情况下，新客户端需要等待，直到某个子进程变为空闲。在重负载下，等待客户端的队列长度可能会越来越长，最终达到系统的限制（您可能会看到 "535 times the listen queue of a socket overflowed" 错误）。在这种情况下，您需要增加队列限制。有几种方法可以解决这个问题。
  </para>

  <sect2 id="controlling-num-init-children">
   <title>控制 num_init_children</title>
   <para>
    解决此问题的明显方法是增加子进程的数量。这可以通过调整 <xref linkend="guc-num-init-children"> 来实现。然而，增加子进程需要更多的 CPU 和内存资源。另外，您必须非常注意 <productname>PostgreSQL</productname> 的 max_connections 参数，因为一旦子进程数量大于 max_connections，<productname>PostgreSQL</productname> 就会拒绝接受新连接，并触发故障转移。
   </para>
   <para>
    增加 num_init_children 的另一个缺点是所谓的"惊群问题"（thundering herd problem）。当新连接请求进入时，内核会唤醒任意一个睡眠的子进程来发起 accept() 系统调用。这会引发多个进程争夺套接字，可能给系统带来沉重负载。为了缓解这个问题，您可以将 serialize_accept 设置为 on，这样只有一个进程来抓取接受套接字。不过请注意，当并发客户端数量较少时，性能可能会下降。
   </para>
   <para>
    在 <productname>Pgpool-II</productname> 4.4 或更高版本中，可以使用 <xref linkend="guc-process-management-mode"> 进行更高效的管理。通过将 <varname>process-management-mode</varname> 设置为 <literal>dynamic</literal>，当并发客户端数量较少时，<productname>Pgpool-II</productname> 的子进程数量可以减少，从而节省资源消耗。另一方面，当并发客户端数量变大时，子进程数量会增加，从而能够响应更多的连接需求。但是，请注意由于需要启动新进程以获得更多子进程，建立连接的时间可能会增加。
   </para>
   <para>
    有关理解 <varname>process-management-mode</varname> 的更多信息，参见 <xref linkend="process-management-mode">。
   </para>
  </sect2>

  <sect2 id="controlling-listen-backlog-multiplier">
   <title>控制 listen_backlog_multiplier</title>
   <para>
    另一种解决方案是增加连接请求队列。这可以通过增加 <xref linkend="guc-listen-backlog-multiplier"> 来实现。
   </para>
  </sect2>

  <sect2 id="when-to-use-reserved-connections">
   <title>何时使用 reserved_connections</title>
   <para>
    然而，上述解决方案均无法保证连接接受队列不会被填满。如果客户端连接请求的到达速度超过处理查询的速度，队列终有一天会被填满。例如，如果有一些执行时间较长的重查询，就极易触发该问题。
   </para>
   <para>
    解决方案是设置 <xref linkend="guc-reserved-connections">，使溢出的连接请求被拒绝，就像 <productname>PostgreSQL</productname> 已经做的那样。这会向应用程序给出可见的错误（"Sorry max_connections already"），并强制它们重试。因此，该解决方案应仅用于无法预知系统负载上限的情况。
   </para>
  </sect2>

 </sect1>

 <sect1 id="read-query-load-balancing">
  <title>读查询负载均衡</title>
  <para>
   如果存在多个 <productname>PostgreSQL</productname> 节点，并且 <productname>Pgpool-II</productname> 运行在流复制模式、逻辑复制模式或复制模式下（有关这些运行模式的详细信息，参见 <xref linkend="running-mode">），则可以将读查询分布在那些数据库节点上，以获得更高的吞吐量，因为每个数据库节点处理较少数量的查询。要启用此功能，您需要开启 <xref linkend="guc-load-balance-mode">。
  </para>

  <para>
   目前绝大多数系统都使用流复制模式，因此从现在开始我们聚焦于该模式。
  </para>

  <sect2 id="session-level-load-balancing-vs-statement-level-load-balancing">
   <title>会话级负载均衡与语句级负载均衡</title>
   <para>
    默认情况下负载均衡模式为"会话级"，这意味着客户端连接到 <productname>Pgpool-II</productname> 时确定读查询发送到哪个节点。例如，如果我们有两个节点（节点 0 和节点 1），每次创建新会话时都会随机选择其中一个节点。从长远来看，选择某个节点的概率会越来越接近由 <xref linkend="guc-backend-weight">0 和 <xref linkend="guc-backend-weight">1 指定的比例。如果这两个值相等，则选择每个节点的概率将是均等的。
   </para>

   <para>
    另一方面，如果 <xref linkend="guc-statement-level-load-balance"> 设置为 on，则在每个查询开始时确定负载均衡节点。这在应用程序拥有自己的连接池并持续连接到 <productname>Pgpool-II</productname> 的情况下非常有用，并且一旦应用程序启动，负载均衡节点将不再改变。另一个用例是批处理应用程序。它会发出大量查询，但只有 1 个会话。通过语句级负载均衡，它可以利用多台服务器。
   </para>
  </sect2>

  <sect2 id="creating-specific-purpose-database-node">
   <title>创建专用数据库节点</title>
   <para>
    在 OLAP 环境中，有时希望为一个特定用途准备一个大型只读数据库。通过使用流复制创建副本数据库可以实现这一点。在这种情况下，有两种方式可以将读查询重定向到该数据库：指定数据库名称或指定应用程序名称。对于前者，使用 <xref linkend="guc-database-redirect-preference-list">。对于后者，使用 <xref linkend="guc-app-name-redirect-preference-list">。
   </para>
  </sect2>

 </sect1>

 <sect1 id="in-memory-query-caching">
  <title>内存查询缓存</title>
  <para>
   <productname>Pgpool-II</productname> 允许缓存读查询结果以供后续使用。这对于多次发出相同读查询的应用程序类型将带来巨大好处。如果有两个查询，且查询字符串（如果有，则为预准备语句的参数）相同，则两个查询被视为"相同"。第一次发送该查询时，<productname>Pgpool-II</productname> 会保存查询结果，并在第二次查询时直接使用，而无需向 <productname>PostgreSQL</productname> 发出任何请求。该技术将在 <xref linkend="runtime-in-memory-query-cache"> 中加以说明。
  </para>

  <sect2 id="when-not-to-use-in-memory-query-caching">
   <title>何时不使用内存查询缓存</title>
   <para>
    当表被修改时，针对该表的查询结果可能会发生变化。为了避免不一致，<productname>Pgpool-II</productname> 会在相应表被修改时丢弃查询缓存数据。因此，频繁更新的数据库不适合使用内存查询缓存。您可以使用 <xref linkend="SQL-SHOW-POOL-CACHE"> 来检查您的数据库是否适合使用查询缓存。如果查询缓存命中率低于 70%，您可能应该避免使用查询缓存。
   </para>
  </sect2>
 </sect1>

 <sect1 id="relation-cache">
  <title>关系缓存</title>
  <para>
   在非 raw 模式（参见 <xref linkend="running-mode">）或 <xref linkend="guc-load-balance-mode"> 未设置为 off 的情况下，<productname>Pgpool-II</productname> 有时需要向 <productname>PostgreSQL</productname> 请求元信息，例如某张表是否为临时表等。为了获取这些信息，<productname>Pgpool-II</productname> 会向主 <productname>PostgreSQL</productname> 发送查询，最多可达 10 个查询（在 4.1 或更高版本中，查询数量有所减少，但仍不为零）。为了减少开销，<productname>Pgpool-II</productname> 维护着"关系缓存"。下次查询中包含相同的表时，<productname>Pgpool-II</productname> 会从缓存中提取信息。
  </para>
  <para>
   有一些参数用于配置关系缓存。参见 <xref linkend="guc-relcache-expire">、<xref linkend="guc-relcache-size">、<xref linkend="guc-check-temp-table">、<xref linkend="guc-check-unlogged-table"> 了解更多详情。
  </para>

  <sect2 id="shared-relation-cache">
   <title>共享关系缓存</title>
   <para>
    关系缓存本质上存在于进程私有内存中，与进程绑定。因此，即使某个进程为某张表创建了关系缓存，在其他进程中关系缓存可能尚未创建。总之，在所有进程中都创建关系缓存条目之前，查询会继续发送到 <productname>PostgreSQL</productname>。<productname>Pgpool-II</productname> 4.1 通过在共享内存中创建关系缓存来解决此问题。如果某个会话在共享内存中创建了关系缓存条目，其他会话将通过查看共享关系缓存来获得缓存结果。有关更多详情，参见 <xref linkend="guc-enable-shared-relcache"> 配置参数部分。此功能非常有效，我们建议启用此功能。
   </para>
  </sect2>
 </sect1>

 <sect1 id="other-performance-considerations">
  <title>其他性能考虑</title>
  <para>
   本节介绍一些其他性能相关的考虑事项。
  </para>

  <sect2 id="thundering-herd-problem">
   <title>惊群问题</title>
   <para>
    如果 <xref linkend="guc-num-init-children"> 较大，可能会导致大量 <productname>Pgpool-II</productname> 进程被唤醒并发生频繁的上下文切换。这会导致系统负载升高，影响整体系统性能。此问题被称为"惊群问题"。启用 <xref linkend="guc-serialize-accept"> 可以解决此问题。请注意，对于较小的 <xref linkend="guc-num-init-children">，<xref linkend="guc-serialize-accept"> 可能会使系统性能变差。请查看 <xref linkend="guc-serialize-accept"> 章节中的指导。
   </para>
  </sect2>

  <sect2 id="disaster-recovery-settings">
   <title>灾难恢复设置</title>
   <para>
    要建立灾难恢复设置，可以在地理位置相距较远的地方部署一台 <productname>Pgpool-II</productname> 加 <productname>PostgreSQL</productname> 主服务器，以及另一台 <productname>Pgpool-II</productname> 加待机 <productname>PostgreSQL</productname> 服务器。靠近待机服务器的客户端将只读查询发送到靠近待机服务器的 <productname>Pgpool-II</productname>。然而，由于待机 <productname>Pgpool-II</productname> 会向主 <productname>PostgreSQL</productname> 服务器的系统目录发送内部查询，查询性能可能会下降。为避免此问题，可以使用 <xref linkend="guc-relcache-query-target"> 使得此类查询发送到待机服务器。参见 <xref linkend="guc-relcache-query-target"> 了解更多详情。
   </para>
  </sect2>
 </sect1>
</chapter>
