<!-- doc/src/sgml/config.sgml -->

<chapter id="runtime-config">
 <title>服务器配置</title>

 <indexterm>
  <primary>configuration</primary>
  <secondary>of the server</secondary>
 </indexterm>

 <para>
  <productname>Pgpool-II</productname> 有许多影响其行为配置参数。本章第一节介绍
  如何与配置参数交互。后续各节将逐一详细介绍每个参数。
 </para>

 <sect1 id="config-setting">
  <title>设置参数</title>

  <sect2 id="config-setting-names-values">
   <title>参数名称和值</title>

   <para>
    所有参数名称均不区分大小写。每个参数接受五种类型之一的值：布尔型、字符串型、整数型、浮点型
    或枚举型（enum）。类型决定了设置参数的语法：
   </para>

   <itemizedlist>
    <listitem>
     <para>
      <emphasis>布尔型：</emphasis>
      值可以写作
      <literal>on</literal>、
      <literal>off</literal>、
      <literal>true</literal>、
      <literal>false</literal>、
      <literal>yes</literal>、
      <literal>no</literal>、
      <literal>1</literal>、
      <literal>0</literal>
      （均不区分大小写）或其任意不模糊的前缀。
     </para>
    </listitem>

    <listitem>
     <para>
      <emphasis>字符串型：</emphasis>
      通常用单引号将值括起来，值内部的单引号需要加倍。
      但如果值是简单数字或标识符，通常可以省略引号。
     </para>
    </listitem>

    <listitem>
     <para>
      <emphasis>数值型（整数和浮点数）：</emphasis>
      小数点仅允许用于浮点参数。不要使用千位分隔符。不需要引号。
     </para>
    </listitem>

    <listitem>
     <para>
      <emphasis>带单位的数值：</emphasis>
      用于描述内存或时间数量的数值参数具有隐式单位。
      单位可能是千字节、兆字节、毫秒、秒或分钟。
      例如：

      <programlisting>
       search_primary_node_timeout = 6min
       memqcache_total_size = 64MB
      </programlisting>
      对于这些设置，不带单位的纯数值将使用参数的默认单位。
     </para>
     <para>
      单位名称区分大小写，数值和单位之间可以有空格。
     </para>
     <para>有效的内存单位为 kB（千字节）、MB（兆字节）、GB（吉字节）和 TB（太字节）。</para>
     <para>有效的时间单位为 ms（毫秒）、s（秒）、min（分钟）、h（小时）和 d（天）。</para>

    </listitem>

    <listitem>
     <para>
      <emphasis>枚举型：</emphasis>
      枚举类型参数的书写方式与字符串参数相同，但仅限于一组有限的值。
      枚举参数值不区分大小写。
     </para>
    </listitem>
   </itemizedlist>
  </sect2>

  <sect2 id="config-setting-configuration-file">
   <title>通过配置文件交互参数</title>

   <para>
    设置这些参数最基本的方式是编辑
    <filename>pgpool.conf</filename><indexterm><primary>pgpool.conf</></> 文件，
    如果从源代码安装，该文件位于 <literal>$prefix/etc/pgpool.conf</literal>。
    该文件的内容可能如下所示：
    <programlisting>
     # 这是一行注释
     listen_addresses = 'localhost'
     port = 9999
     serialize_accept = off
     reset_query_list = 'ABORT; DISCARD ALL'
    </programlisting>

    每行指定一个参数。名称和值之间的等号是可选的。
    空格不重要（引号内的参数值除外），空白行会被忽略。
    哈希符号（<literal>#</literal>）表示该行剩余部分为注释。
    不是简单标识符或数字的参数值必须用单引号括起来。
    要在参数值中嵌入单引号，写入两个单引号（推荐）或反斜杠加单引号。
   </para>

   <para>
    以此方式设置的参数为集群提供默认值。活动会话看到的设置即为这些值，
    除非被覆盖。以下各节介绍管理员或用户可以覆盖这些默认值的方式。
   </para>

   <para>
    <indexterm>
     <primary>SIGHUP</primary>
    </indexterm>
    每当主服务器进程收到 <systemitem>SIGHUP</> 信号时，都会重新读取
    配置文件；该信号最方便通过命令行运行 <literal>pgpool reload</> 发送。
    主 pgpool 进程还会将此信号传播到其所有子进程，以便下一个会话也采用新值。
    某些参数只能在服务器启动时设置；对这些参数在配置文件中进行的任何更改
    都将在服务器重启前被忽略。配置文件中无效的参数设置在处理
    <systemitem>SIGHUP</> 时同样会被忽略（但会记录到日志中）。
   </para>
  </sect2>

  <sect2 id="config-setting-sql-command-interaction">
   <title>通过 SQL 客户端交互参数</title>

   <para>
    <productname>Pgpool-II</productname> 还提供了两种 SQL 风格的命令
    来与会话本地配置设置交互。
   </para>

   <itemizedlist>
    <listitem>
     <para>
      <xref linkend="SQL-PGPOOL-SHOW"> 命令允许查看所有参数的当前值。
     </para>
    </listitem>

    <listitem>
     <para>
      <xref linkend="SQL-PGPOOL-SET"> 命令允许修改可以设置为会话局部的
      那些参数的当前值；对其他会话没有影响。
     </para>
    </listitem>
   </itemizedlist>

  </sect2>
 </sect1>

