<!-- doc/src/sgml/release-4.4.sgml -->
<!-- 关于典型标记，请参见 release.sgml 中的头部注释 --><sect1 id="release-4-4-17">
 <title>Release 4.4.17</title>
 <note>
  <title>发布日期</title>
  <simpara>2026-06-04</simpara>
 </note>

 <sect2>
  <title>错误修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-05-26 [4eb1fe84a]
    -->
    <para>
     允许 <literal>pgproto</literal> 接受新行数据。 (Tatsuo Ishii)
    </para>
    <para>
     现在 <literal>"\n"</literal> 被识别为新行。这对于 <command>COPY</command> 命令是必需的。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [0cfb6ebdc]
    -->
    <para>
     当主节点 Watchdog 报告初始 -2 哨兵值时，保留本地的 primary_node_id。 (Tatsuo Ishii)
    </para>
    <para>
     修复在所有 pgpool 节点同时重启后的 Watchdog 同步问题，
     其中备用节点可能会用初始 -2 哨兵值覆盖有效的 primary_node_id，
     并无限期停留在未初始化状态。修复方法是在主节点报告初始 -2 哨兵值时，保留本地的
     primary_node_id。
    </para>
    <para>
     问题报告者：Emond Papegaaij 和 Claude Code。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/CAGXsc%2BZmBoLs3Mz%3DG-Bdm4JJG%2BfH1NpHfR3qVJVwW4eBKWwStQ%40mail.gmail.com">清理启动时的主节点检测竞态条件</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [6ec39ceb1]
    -->
    <para>
     修复中止事务中空查询的情况。 (Tatsuo Ishii)
    </para>
    <para>
     修复中止事务中的空查询错误地引发错误的问题。
     中止事务中应允许空查询，
     但之前的提交导致 <function>check_transaction_state_and_abort()</function> 拒绝了它们。
     修复方法是在 <function>SimpleQuery()</function> 中检测空查询并绕过检查。
    </para>
    <para>
     问题报告者：Ronaldo Raivil。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/161">pgpool >= 4.3.2 在中止的保存点内的驱动程序健康检查探测中合成 25P02，破坏需要调用 ROLLBACK TO SAVEPOINT 的 ORM</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-06 [479c71033]
    -->
    <para>
     修复关于 <varname>ssl_dh_params_file</varname> 的描述。 (Tatsuo Ishii)
    </para>
    <para>
     实际上，不仅当参数为空（默认值）时，而且当指定的参数不正确时，
     它也会回退到编译时默认值。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-27 [571e1f917]
    -->
    <para>
     修复 PCP 主进程在重启时记住子进程 PID。 (Tatsuo Ishii)
    </para>
    <para>
     在故障切换/故障恢复时，pgpool 主进程向 PCP 主
     进程发送信号以请求重启，PCP 主进程进行重启。问题是，当
     PCP 主进程重启时，它会忘记其子 PCP 工作进程 PID 列表。

     结果，像 <command>pcp_promote_node</command> 这样的命令可能会无限期挂起。
     修复方法是将 PCP 工作进程 PID 存储在共享内存中，使其能够生存过 PCP 主进程重启。
    </para>
    <para>
     问题报告者：Taiki Koshino。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260425.223051.1207744844622514060.ishii%40postgresql.org">pcp 进程问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-09 [4cdd766e1]
    -->
    <para>
     修复版本不匹配：为 4.4.16 重新生成 configure。 (Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-07 [fbcbfe5b0]
    -->
    <para>
     修复查询缓存在使用 <command>MERGE</command> 语句时不过期缓存的问题。 (Tatsuo Ishii)
    </para>
    <para>
     <command>MERGE</command> 是在 <productname>PostgreSQL</productname> 15 中引入的，
     <productname>Pgpool-II</productname> 4.4 引入了 <productname>PostgreSQL</productname> 15 的解析器。
     然而，查询缓存子模块存在疏漏：
     <command>MERGE</command> 语句未触发查询缓存的无效化。
    </para>
    <para>
     问题报告者：Nadav Shatz。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260407.090802.1335576118037412011.ishii%40postgresql.org">Re: Proposal: Recent mutated table tracking in memory</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-06 [205d1c439]
    -->
    <para>
     修复 SSL 模块中的内存泄漏。 (Tatsuo Ishii)
    </para>
    <para>
     修复 <function>load_dh_file()</function> 中当提供无效的 DH 参数时的内存泄漏。
     该函数未能释放由 <function>PEM_read_DHparams()</function> 返回的内存。
     此外，更新同一文件中的版权声明，以保留原始的 <productname>PostgreSQL</productname> 版权声明。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260403.091842.102589579921239540.ishii%40postgresql.org">SSL 模块中的内存泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-31 [4812ec5b6]
    -->
    <para>
     修复 <command>SET transaction_read_only = 0</command> 的路由错误。 (Bo Peng)
    </para>
    <para>
     修复将 <command>SET transaction_read_only = 0</command> 错误地路由到备用节点的问题，这会导致查询错误。
     此外，将不正确的 bool ret = POOL_BOTH 替换为适当的枚举类型。
    </para>
    <para>
     问题报告者：liujinyang-highgo。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/155">set command 'set transaction_read_only = 0;' routed to standby node and execute failed.</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-20 [7e96543c7]
    -->
    <para>
     修复会话级关系缓存的情况错误。 (Tatsuo Ishii)
    </para>
    <para>
     修复 <function>pool_search_relcache()</function> 中会话级关系缓存的处理不正确问题。
     临时表信息可能会错误地注册到共享关系缓存中，导致后续查询
     使用过时的缓存条目，并可能错误地路由查询。
     修复方法是为会话级缓存条目避免在共享关系缓存中注册临时表信息。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/154">query temp table is routed to standby node.</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-19 [4095caec0]
    -->
    <para>
     修复 <function>pool_search_relcache()</function> 中的锁释放泄漏。 (Tatsuo Ishii)
    </para>
    <para>
     修复 <function>pool_search_relcache()</function> 中的锁释放泄漏。
     在更新共享关系缓存期间，在重新获取排他锁后，锁状态未正确重置，
     可能导致共享内存锁一直处于持有状态。此外，将令人困惑的变量 <varname>locked</varname> 重命名为 <varname>locked_by_others</varname>
     并修复不必要的 <varname>per_node_statement_log</varname> 输出。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260317.182615.1046517700474336117.ishii%40postgresql.org">pool_search_relcache 中的锁释放泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-09 [293e9cae9]
    -->
    <para>
     在分叉前关闭监听套接字。 (Tatsuo Ishii)
    </para>
    <para>
     修复分叉子进程中继承的不必要的监听套接字。
     以前，pcp_main、健康检查和 lifecheck 等子进程继承了它们不使用的 pgpool 和/或 PCP 监听套接字，
     这可能阻止后续的 pgpool 启动正确绑定端口。
     修复方法是在分叉后使用 <function>close_listening_sockets()</function> 关闭不必要的监听套接字。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260302.100028.1346768433787074248.ishii%40postgresql.org">在分叉前关闭监听套接字</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-03-04 [c9a12b93c]
    -->
    <para>
     文档：修复 <varname>ignore_leading_white_space</varname> 和 <varname>allow_sql_comments</varname> 的文档。 (Tatsuo Ishii)
    </para>
    <para>
     它们缺乏对默认值的描述。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-16">
 <title>Release 4.4.16</title>
 <note>
  <title>Release Date</title>
  <simpara>2026-02-26</simpara>
 </note>

 <sect2>
  <title>Changes</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-01-29 [118f61932]
    -->
    <para>
     允许并行执行<command>make</command>。 (Tatsuo Ishii)
    </para>
    <para>
     此前，并行执行<command>make</command>时会失败，报错如下：
     <programlisting>
In file included from parser.c:27:
../../src/include/parser/gramparse.h:33:10: fatal error: gram.h: No such file or directory
33 | #include "gram.h"
     </programlisting>
    </para>
    <para>
     该补丁由 anthonyroussel 创建，Tatsuo Ishii 审核。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/pull/146">Fix parallel build</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-26 [d638018ce]
    -->
    <para>
     添加关于<xref linkend="guc-backend-hostname">与<varname>primary_conninfo</varname>主机之间不匹配的详细信息日志。 (Taiki Koshino)
    </para>
    <para>
     如果在<filename>pgpool.conf</filename>中设置的<xref linkend="guc-backend-hostname">与<productname>PostgreSQL</productname>的<varname>primary_conninfo</varname>中的主机设置不匹配，
     将输出以下日志消息。
     <programlisting>
sr_check_worker pid 25099: DETAIL: pgpool.conf backend_hostname: "pgpool0" does not match PostgreSQL primary_conninfo host: "pgpool_logtest"
     </programlisting>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-12-24 [ef33aca38]
    -->
    <para>
     明确指定支持的最低 OpenSSL 版本。 (Tatsuo Ishii)
    </para>
    <para>
     可以通过在 configure.ac 中使用以下方式实现：
     <programlisting>
AC_DEFINE(OPENSSL_API_COMPAT, [0x10002000L],
          [Define to the OpenSSL API version in use. This avoids deprecation warnings from newer OpenSSL versions.])
     </programlisting>

     支持的最低版本为 1.0.2，与当前<productname>PostgreSQL</productname>相同。（实际上，AC_DEFINE 是从<productname>PostgreSQL</productname>引入的。）此外，如果您安装的 OpenSSL 较新（如 3.0），这还可以避免 OpenSSL 的弃用警告。
     另外，在安装文档中补充说明 OpenSSL 所需的最低版本为 1.0.2。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug fixes</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-01-31 [55ee45801]
    -->
    <para>
     修复<function>check_transaction_state_and_abort()</function>中的疏漏。 (Tatsuo Ishii)
    </para>
    <para>
     该函数未考虑前端为 NULL 的情况。
    </para>
    <para>
     问题由 Coverity 指出。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-22 [c0f2cb883]
    -->
    <para>
     修复<function>walker()</function>函数使其提前返回。 (Tatsuo Ishii)
    </para>
    <para>
     此前，<function>walker()</function>函数在找到目标对象后返回 false。但可以短路遍历过程，本应如此处理。

     此外，从<productname>PostgreSQL</productname>导入注释，以澄清 walker 函数的设计。
    </para>
    <para>
     问题由 liujinyang-highgo 报告，Tatsuo Ishii 审核。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/143">Improper handling in the function view_walker().</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-09 [7b00d24de]
    -->
    <para>
     修复<literal>scram-sha-256</literal>认证中的内存泄漏。 (Tatsuo Ishii)
    </para>
    <para>
     问题由 Coverity 指出。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-07 [81e8d3ec6]
    -->
    <para>
     修复对<function>malloc()</function>返回值的检查。 (Tatsuo Ishii)
    </para>
    <para>
     <function>create_inet_domain_sockets()</function>在为 socket fd 分配内存时忘记检查<function>malloc()</function>的返回值。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-06 [2218b5e56]
    -->
    <para>
     修复健康检查进程无法正常启动的问题。 (Tatsuo Ishii)
    </para>
    <para>
     此前，如果后端节点处于宕机状态，<function>pgpool_main()</function>不会启动健康检查进程。这会导致 auto_failback 无法正常工作。
     Pgpool 在缺少<option>-D</option>选项时从<varname>pool_status</varname>文件读取状态。如果文件中记录的状态为“down”，则 pgpool 不会为该节点启动健康检查进程。由于 auto_failback 是从健康检查进程调用的，这意味着 auto_failback 对该后端节点不生效。

     修复方法：无条件启动健康检查进程。
    </para>
    <para>
     问题由 Meng Feilong 报告，Tatsuo Ishii 审核。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/140">Nodes in the down state cannot failback </ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-23 [515d5fe85]
    -->
    <para>
     修复可能的整数溢出。 (Tatsuo Ishii)
    </para>
    <para>
     在<function>pool_write_flush()</function>和<function>pool_flus_it()</function>中使用了<function>write(2)</function>。<function>write(2)</function>返回 ssize_t 类型，而代码中有部分地方将返回值赋值给了 int 类型变量。结果可能导致返回值被求值为负数，从而引发错误。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-15">
 <title>版本 4.4.15</title>
 <note>
  <title>发布日期</title>
  <simpara>2025-12-15</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-12-08 [5bd10177c]
    -->
    <para>
     在 pgpool_setup 中使用 "grep -E" 替代已弃用的 "egrep"。 (Bo Peng)
    </para>
    <para>
     将 "egrep" 替换为 "grep -E"，以避免在较新的 GNU grep 版本中出现弃用警告。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-12-08 [8deccc15e]
    -->
    <para>
     修复 show pool_status 中的缓冲区溢出错误。 (Tatsuo Ishii)
    </para>
    <para>
     "show pool_status"、"pgpool show" 和 pcp_pool_status 可能导致缓冲区溢出错误。如果 backend_flag 设置为 "ALWAYS_PRIMARY"，负责生成 backend_flag 可打印格式的 pool_flag_to_str() 会将数据写入静态缓冲区的末尾之外。
    </para>
    <para>
     问题由 zam bak 报告，并由 Bo Peng 审核。
    </para>
    <para>
     讨论： <ulink url="https://www.postgresql.org/message-id/20251202.140205.427777414210613577.ishii%40postgresql.org">Re: "buffer overflow detected" when running SHOW POOL_STATUS</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-02 [c8865c703]
    -->
    <para>
     修复 "show pool_status" 中显示的 memqcache_stats_start_time。 (Tatsuo Ishii)
    </para>
    <para>
     get_config() 使用 ctime() 生成 memqcache_stats_start_time 的可打印形式。但它没有考虑到 ctime() 会在结果末尾添加换行符。因此，不仅 memqcache_stats_start_time 的输出带有不必要的换行符，而且下一行打印了空项。
    </para>
    <para>
     讨论： <ulink url="https://www.postgresql.org/message-id/20251130.102712.131456481338876013.ishii%40postgresql.org">Re: "buffer overflow detected" when running SHOW POOL_STATUS</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-14">
 <title>发布 4.4.14</title>
 <note>
  <title>发布日期</title>
  <simpara>2025-11-25</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-11-07 [4fabca331]
    -->
    <para>
     移除不必要的 <varname>application_name</varname> 处理。(Tatsuo Ishii)
    </para>
    <para>
     之前的提交添加了一个在重用现有连接时设置 <varname>application_name</varname> 的步骤。
     然而，这是不必要的，因为 DISCARD ALL 会自动重置 <varname>application_name</varname>，
     并且 <function>send_params()</function> 已经将所有必要的参数状态消息（包括 <varname>application_name</varname>）发送到前端。
     移除这个冗余步骤还可以提高性能，特别是对于地理距离较远的后端节点。
    </para>
    <para>
     讨论： <ulink url="https://github.com/pgpool/pgpool2/issues/130">地理分布式配置中 pgPool 性能严重下降</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-11-16 [b113027dd]
    -->
    <para>
     修复 <command>CopyOut</command> 时的段错误。(Tatsuo Ishii)
    </para>
    <para>
     在扩展查询协议模式下执行 <command>COPY relname TO STDOUT</command> 时，pgpool 发生段错误。
    </para>
    <para>
     此问题由 <ulink url="https://github.com/tetesh">https://github.com/tetesh</ulink> 报告。
    </para>
    <para>
     讨论： <ulink url="https://github.com/pgpool/pgpool2/issues/133">datanymizer 和 pg-pool 分段错误</ulink>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-10-03 [b84553bf0]
    -->
    <para>
     在某些边缘情况下防止 watchdog 脑裂场景。(Tatsuo Ishii)
    </para>
    <para>
     Watchdog 同时使用信标消息和心跳进行通信。
     如果心跳尚未激活，信标消息丢失超过 30 秒可能导致脑裂场景，
     即旧领导者保持活动状态，同时选举出新领导者。
     此更改通过撤销旧领导者来防止两个领导者同时存在。
     当节点检测到它从领导者处丢失信标消息超过两次时，
     领导者被设置为 LOST，并通过 set_state(WD_JOINING) 触发新的领导者选举。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-10-03 [ef8ccb9e7]
    -->
    <para>
     防止在给定不存在的预处理语句时出现 FATAL 错误。(Tatsuo Ishii)
    </para>
    <para>
     以前，当使用不存在的预处理语句时，<function>Bind()</function> 会引发 FATAL 错误，这与 PostgreSQL 不同。
     此更改使 <function>Bind()</function> 检查语句是否存在，如果不存在则向前端发送 ERROR 消息。
     请注意，不会写入日志，因为目前无法记录此情况。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-24 [59a359eef]
    -->
    <para>
     修复同时提供 -p 和 -P 时 <xref linkend="pg-enc"> 不起作用的问题。(Tatsuo Ishii)
    </para>
    <para>
     原始报告显示，运行 pg_enc -p -P 会提示输入密码和加密密钥，
     但如果未提供密钥则会失败。
    </para>
    <para>
     讨论： <ulink url="https://www.postgresql.org/message-id/7f18c30b.237.1997555ca11.Coremail.liujy%40highgo.com">pg_enc</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-18 [f20f7041c]
    -->
    <para>
     修复 32 位环境下的编译问题。(Tatsuo Ishii)
    </para>
    <para>
     在 32 位系统上，编译 src/parser/snprintf.c 会因未定义的函数 <function>isnan()</function> 和 <function>isinf()</function> 而失败，这些函数来自 math.h。
     此更改包含了 math.h，并重新排序了包含文件以提高清晰度。
     该文件最初从 PostgreSQL 导入，其中已包含 math.h。
     此修复通过拉取请求提供： https://github.com/pgpool/pgpool2/pull/128
    </para>
    <para>
     讨论： <ulink url="https://www.postgresql.org/message-id/20250917.194736.353755422175293639.ishii%40postgresql.org">32 位目标的编译问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-10 [0a6868d40]
    -->
    <para>
     修复 recovery_1st_stage.sample 中 Bash 更安全的目录删除。(Taiki Koshino)
    </para>
    <para>
     在 rm 命令中引用变量以避免意外删除：
     <command>rm -rf "${DEST_NODE_PGDATA}"</command>

     如果变量为空，rm 可能会删除意外的文件或目录。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-05 [441f338ab]
    -->
    <para>
     修复未使用 <option>--with-memcached</option> 选项构建时的编译错误。(Tatsuo Ishii)
    </para>
    <para>
     在未配置 <option>--with-memcached</option> 时发生编译器错误。
     问题在于编译器无法确定 ptr 只会在启用 memcached 时被释放。
     为了解决这个问题，相关代码块被包裹在 #ifdef USE_MEMCACHED 中。
    </para>
    <para>
     问题由 Bo Peng 报告。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-09-04 [9e0324575]
    -->
    <para>
     修复查询缓存锁文件处理。(Tatsuo Ishii)
    </para>
    <para>
     查询缓存模块在 <varname>logdir</varname> 中创建一个锁文件用于并发控制，
     但存在两个错误：主 pgpool 进程可能创建一个多余的 "QUERY_CACHE_LOCK_FILE"，
     并且锁文件在关闭时未被移除。此更改修复了这两个问题。
    </para>
    <para>
     问题由 Bo Peng 报告和分析。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-09-17 [1a975b539]
    -->
    <para>
     更新 <ulink url="https://www.pgpool.net/docs/latest/ja/html/example-cluster.html">8.2. Pgpool-II + Watchdog 设置示例</ulink> 中关于 "设置 PostgreSQL 备用服务器" 的文档。(Taiki Koshino)
    </para>
    <para>
     添加一条说明：在不使用在线恢复设置备用服务器时，不要将 <varname>primary_conninfo</varname> 写入 postgresql.auto.conf。
    </para>
    <para>
     讨论： <ulink url="https://github.com/pgpool/pgpool2/issues/67">Follow primary 命令未修复 postgresql.auto.conf</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-22 [5bd535792]
    -->
    <para>
     修复 watchdog_setup 手册。(Tatsuo Ishii)
    </para>
    <para>
     它错误地指出 watchdog_setup 中未设置心跳。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-09-30 [3ddbc64f1]
    -->
    <para>
     使 023.ssl_connection 适应 PostgreSQL 18。(Tatsuo Ishii)
    </para>
    <para>
     PostgreSQL 18 大幅更改了 psql 的 \conninfo 输出格式，这
     导致测试失败，因为测试依赖于 \conninfo。此更改
     使测试脚本能够识别 PostgreSQL 版本以解决该问题。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-29 [b4043e6c3]
    -->
    <para>
     修复 010.rewrite_timestamp 中的 ruby 脚本。(Tatsuo Ishii)
    </para>
    <para>
     ruby 脚本使用了 "File.exists"，据说在较新版本的 Ruby 中已弃用。
     将其替换为 "File.exist"。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-29 [9c901e60a]
    -->
    <para>
     调整 034 和 075 测试中的超时时间。(Tatsuo Ishii)
    </para>
    <para>
     034.promote_node 和 075.detach_primary_left_down_node 将完成 follow primary 脚本的超时时间设置为 60 秒。
     事实证明这些超时值不够长，有时会导致误报错误。因此将它们从 60 秒改为 120 秒。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-27 [42b177614]
    -->
    <para>
     在 023.ssl_connection 中添加 ssl_ecdh_curve 测试。(Tatsuo Ishii)
    </para>
    <para>
     023.ssl_connection 未涵盖 ssl_ecdh_curve 的测试。此
     提交使用错误的 ssl_ecdh_curve 参数进行测试，以查看前端和 pgpool 之间的连接是否失败。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-13">
 <title>发布 4.4.13</title>
 <note>
  <title>发布日期</title>
  <simpara>2025-08-21</simpara>
 </note>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-07-25 [75d905ff9]
    -->
    <para>
     修复看门狗打印不适当的 NOTICE 消息的问题。(Tatsuo Ishii)
    </para>
    <para>
     <function>read_ipc_socket_and_process()</function> 每次向 IPC 套接字写入命令时都会打印一条通知消息，即使写入成功也是如此。修复此问题，使其仅在写入失败时打印通知消息。

     此 bug 未被发现的原因是，该消息仅在 <varname>log_min_messages</varname> 设置为 notice 或更高级别时才会出现。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/121">pgpool IPC 套接字连接问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-07-19 [d05de5c73]
    -->
    <!--
    2025-07-19 [ed57af34e]
    -->
    <para>
     修复读取启动数据包时的资源泄漏问题。(Tatsuo Ishii)
    </para>
    <para>
     修复 pool_push_pending_data 中的资源泄漏问题。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-30 [bac455b2f]
    -->
    <para>
     修复大端机器上 scram-sha-256 认证失效的问题。(Tatsuo Ishii)
    </para>
    <para>
     当执行 <literal>scram-sha-256</literal> 认证时，会使用哈希函数 <function>pg_sha_256_final</function>。该函数从 <productname>PostgreSQL</productname> 导入，并使用预处理器定义 WORDS_BIGENDIAN 来判断主机字节序。虽然 WORDS_BIGENDIAN 应在 configure 时定义，但在从 <productname>PostgreSQL</productname> 导入 <function>pg_sha_256_final</function>（及其他函数）时遗漏了此部分。结果，<literal>scram-sha-256</literal> 仅在小端机器上正常工作。通过向 configure.ac 添加 AC_C_BIGENDIAN 宏修复了此问题。
    </para>
    <para>
     问题由 Christoph Berg 报告，并由 pranavkaruvally 分析。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/106">4.6.1 在大端 s390x 上失败：后端响应类型为 'E'，期望 'R'</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-23 [2721c7b76]
    -->
    <para>
     修复源代码中的拼写错误。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-09 [4305c42d6]
    -->
    <para>
     修复心跳设备处理问题。(Tatsuo Ishii)
    </para>
    <para>
     在处理 <filename>pgpool.conf</filename> 时，<varname>heartbeat_device</varname> 被错误处理，导致第一个设备被忽略。
    </para>
    <para>
     问题由 Bo Peng 分析。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-02 [06ba7dde4]
    -->
    <para>
     修复 <filename>pgpool.conf</filename> 注释中的拼写错误。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-08-15 [a54f5d1ad]
    -->
    <para>
     增强“8.3. 复制模式与快照隔离模式配置示例”文档。(Taiki Koshino)
    </para>
    <para>
     更新了示例脚本的链接以及“8.3.3. 开始之前”部分的说明。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-04 [e2f24fec8]
    -->
    <para>
     修复关于 reload 不生效参数的文档。(Taiki Koshino)
    </para>
    <para>
     “authentication_timeout”和“memqcache_oiddir”不会因 reload 而生效。文档已更改为“此参数只能在服务器启动时设置。”。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-07-24 [bc5fa5e7e]
    -->
    <!--
    2025-07-23 [1e8963554]
    -->
    <para>
     统一看门狗领导者术语（后续）。 (Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-06 [31eee18d7]
    -->
    <!--
    2025-07-16 [34b07c2d5]
    -->
    <para>
     修复示例脚本链接。(Taiki Koshino)
    </para>
    <para>
     修改了“<ulink url="https://www.pgpool.net/docs/46/en/html/example-cluster.html">8.2. Pgpool-II + Watchdog 设置示例</ulink>”部分中的示例脚本。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-18 [f4ae9f39e]
    2025-06-18 [7a20dc419]
    -->
    <para>
     修复负载均衡说明中遗漏逻辑复制模式和 Slony 模式的问题。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-18 [9d2e4e692]
    -->
    <para>
     增强 <xref linkend="pcp-node-info"> 文档。(Bo Peng)
    </para>
    <para>
     澄清每个 <varname>backend_application_nameX</varname> 必须与 <varname>primary_conninfo</varname> 的 <varname>application_name</varname> 中指定的值匹配，才能正确显示“<varname>replication_state</varname>”和“<varname>replication_sync_state</varname>”。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-08 [59f9df005]
    -->
    <para>
     添加内核资源章节。(Tatsuo Ishii)
    </para>
    <para>
     <productname>Pgpool-II</productname> 使用 System V 共享内存和信号量。最好在文档中描述这些要求。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-07 [40617fd90]
    -->
    <para>
     为 configure 的 <option>--with-ldap</option> 选项添加说明。(Tatsuo Ishii)
    </para>
    <para>
     在 v4.2 中引入 LDAP 支持时遗漏了此说明。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-04 [ff60bf930]
    -->
    <para>
     澄清 <productname>Pgpool-II</productname> 支持的平台。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-03 [cc4da5979]
    -->
    <para>
     增强 <xref linkend="guc-child-life-time"> 文档。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-06-08 [78e45decf]
    -->
    <para>
     稳定 029.cert_passphrase 回归测试。(Tatsuo Ishii)
    </para>
    <para>
     当 <varname>ssl_passphrase_command</varname> 无效时，错误消息通常是“bad decrypt”，但有时似乎是“wrong tag”。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-12">
 <title>发布版本 4.4.12</title>
 <note>
  <title>发布日期</title>
  <simpara>2025-05-15</simpara>
 </note>

 <sect2>
  <title>概述</title>
  <para>
   此版本包含一个安全修复。
  </para>
  <para>
   <productname>Pgpool-II</productname> 的客户端认证机制中存在一个认证绕过漏洞。
   在 <productname>Pgpool-II</productname> 中，即使本应强制执行认证，也可能被绕过。
   因此，攻击者可以以任何用户身份登录，可能导致信息泄露、数据篡改，甚至数据库完全关闭。(CVE-2025-46801)
  </para>
  <para>
   此漏洞影响认证配置符合以下任一模式的系统：
  </para>
  <itemizedlist>
   <listitem>
    <para>
     模式 1：当满足以下所有条件时，此漏洞会发生：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       在 <filename>pool_hba.conf</filename> 中使用了 <literal>password</literal> 认证方法
      </para>
     </listitem>
     <listitem>
      <para>
       allow_clear_text_frontend_auth = off
      </para>
     </listitem>
     <listitem>
      <para>
       用户的密码未在 <filename>pool_passwd</filename> 中设置
      </para>
     </listitem>
     <listitem>
      <para>
       在 <filename>pg_hba.conf</filename> 中使用了 <literal>scram-sha-256</literal> 或 <literal>md5</literal> 认证方法
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <para>
     模式 2：当满足以下所有条件时，此漏洞会发生：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       enable_pool_hba = off
      </para>
     </listitem>
     <listitem>
      <para>
       在 pg_hba.conf 中使用了以下认证方法之一：
       <literal>password</literal>、<literal>pam</literal> 或 <literal>ldap</literal>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <para>
     模式 3：当满足以下所有条件时，此漏洞会发生：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       使用了原始模式 (backend_clustering_mode = 'raw')
      </para>
     </listitem>
     <listitem>
      <para>
       在 <filename>pool_hba.conf</filename> 中使用了 <literal>md5</literal> 认证方法
      </para>
     </listitem>
     <listitem>
      <para>
       allow_clear_text_frontend_auth = off
      </para>
     </listitem>
     <listitem>
      <para>
       用户的密码以明文或 AES 格式注册在 <filename>pool_passwd</filename> 中
      </para>
     </listitem>
     <listitem>
      <para>
       在 <filename>pg_hba.conf</filename> 中使用了以下认证方法之一：
       <literal>password</literal>、<literal>pam</literal> 或 <literal>ldap</literal>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>
  </itemizedlist>
  <para>
   Pgpool-II 4.0 和 4.1 系列的所有版本，以及 4.2.0 至 4.2.21、4.3.0 至 4.3.14、
   4.4.0 至 4.4.11、4.5.0 至 4.5.6 和 4.6.0 均受此漏洞影响。
   强烈建议升级到 <productname>Pgpool-II</productname>
   4.6.1、4.5.7、4.4.12、4.3.15 和 4.2.22 或更高版本。
   或者，您可以修改设置，使其不匹配任何易受攻击的配置模式。
  </para>
 </sect2>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-02 [32dee13dd]
    -->
    <para>
     向配置文件添加主版本信息。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-13 [bcc6727]
    -->
    <para>
     修复某些情况下不正确的客户端认证。(Tatsuo Ishii)
    </para>
    <para>
     如果 enable_pool_hba = on，其认证方法是 "password"，密码未在 pool_passwd 中注册，并且 pg_hba.conf 中的认证方法是 "scram-sha-256" 或 "md5"，那么当客户端首次连接到 pgpool 时，认证会按预期执行。但是，如果客户端连接到缓存的连接，则会接受来自客户端的任何密码。
    </para>
    <para>
     此外，如果 enable_pool_hba = off，在某些情况下，客户端首次连接时不会被要求输入密码，或者当客户端连接到缓存的连接时，即使应该要求输入密码，也不会被要求。
    </para>
    <para>
     除了修复上述问题外，还进行了以下更改：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       移除单一的 PostgreSQL 代码路径以简化认证代码。
      </para>
     </listitem>
     <listitem>
      <para>
       移除前端和后端的 crypt 认证支持。该功能从未被记录且从未经过测试。此外，crypt 认证在很久以前（PostgreSQL 8.4，2009 年）已被移除。
      </para>
     </listitem>
     <listitem>
      <para>
       添加新的回归测试 "040.client_auth"。该测试使用 CSV 格式的测试规范文件执行详尽的客户端认证测试。
      </para>
     </listitem>
    </itemizedlist>
   </listitem>
   <listitem>
    <!--
    2025-05-08 [e3451b560]
    -->
    <para>
     修复查询缓存中长期存在的 bind 错误。(Tatsuo Ishii)
    </para>
    <para>
     当准备了一个命名语句时，可以在没有解析消息的情况下进行绑定然后执行。问题是，用于在执行或 COMMIT 时使查询缓存失效所需的表 OID 仅在解析消息处理过程 (Parse()) 中收集。因此，如果在之前的执行之后没有解析就执行了绑定，则不会收集表 OID，导致 pgpool 无法使查询缓存失效。修复方法是在绑定时也收集表 OID。向 006.memqcache 添加回归测试。
    </para>
    <para>
     问题由 Achilleas Mantzios 报告，测试程序由他提供。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-April/009430.html">[pgpool-general: 9427] 关于查询结果缓存可见性的说明</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-02 [1dfacffed]
    -->
    <para>
     修复查询缓存失效错误。(Tatsuo Ishii)
    </para>
    <para>
     当收到执行消息时，pgpool 会检查其最大行数参数。如果该参数不为零，pgpool 会设置 "partial_fetch" 标志，以指示 pool_handle_query_cache() 不创建查询缓存。问题是，提交 2a99aa5d1 忽略了即使 INSERT/UPDATE/DELETE 也会将执行消息参数设置为非零值（通常为 1），并且 pgpool 甚至为非 SELECT 语句设置了该标志。这导致无法使查询缓存失效，因为如果该标志为真，pool_handle_query_cache() 中的后续代码会跳过缓存失效。为了解决这个问题，修改 Execute() 以在设置该标志之前检查查询是否为只读 SELECT。同时向 006.memqcache 添加测试。
    </para>
    <para>
     问题由 Achilleas Mantzios 报告，测试程序由他提供。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-April/009430.html">[pgpool-general: 9427] 关于查询结果缓存可见性的说明</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-05 [5947ba418]
    -->
    <para>
     修复在 OpenBSD 上的可移植性。(Tatsuo Ishii)
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004583.html">[pgpool-hackers: 4582] 使 pgpool 在 OpenBSD 上构建</ulink>
    </para>
    <para>
     补丁由 Martijn van Duren 创建。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-05-01 [14c94473b]
    -->
    <para>
     修复 json_writer 未正确编码特殊字符的问题。(Bo Peng)
    </para>
    <para>
     如果 <xref linkend="guc-wd-authkey"> 包含特殊字符（例如反斜杠），启用 watchdog 时 <productname>Pgpool-II</productname> 会崩溃。
    </para>
    <para>
     补丁由 Martijn van Duren 创建，并由 Bo Peng 稍作修改。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-03-04 [e1e32536f]
    -->
    <para>
     修复 sr 检查和健康检查在重新加载时未重新打开 <xref linkend="guc-pool-passwd"> 的问题。(Tatsuo Ishii)
    </para>
    <para>
     流复制检查和健康检查进程在重新加载时忘记重新打开 <xref linkend="guc-pool-passwd">。如果 <xref linkend="guc-sr-check-password"> 或 <xref linkend="guc-health-check-password"> 是空字符串，则密码从 <xref linkend="guc-pool-passwd"> 获取。因此，这些进程在重新加载时读取了 <xref linkend="guc-pool-passwd"> 的过期内容。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-04-24 [e50114280]
    -->
    <para>
     增强关于 <xref linkend="guc-connection-life-time"> 的描述 (Tatsuo Ishii)
    </para>
    <para>
     <xref linkend="guc-connection-life-time"> 是一个配置值，用于确定到 <productname>PostgreSQL</productname> 后端的缓存连接的生命周期。当前文档缺少一个描述，即过期计算实际上是在客户端断开与持有缓存连接的进程的连接时进行的。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-April/004578.html">[pgpool-hackers: 4577] 文档：增强关于 connection_life_time 的描述</ulink>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-03-05 [a3086943f]
    -->
    <para>
     增强关于 <xref linkend="guc-sr-check-user"> 的解释。(Tatsuo Ishii)
    </para>
    <para>
     它必须是超级用户或属于 <literal>pg_monitor</literal> 组。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-11">
 <title>版本 4.4.11</title>
  <note>
   <title>发布日期</title>
   <simpara>2025-02-28</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-02-26 [b6d160f]
    -->
    <para>
     从示例跟随主节点脚本中移除 <command>pg_basebackup</command>。（Bo Peng）
    </para>
    <para>
     如果 <command>pg_rewind</command> 失败，对用户来说最安全的方式是手动恢复。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-01-31 [3536f80]
    -->
    <para>
     修复 <function>per_node_error_log()</function> 错误消息中打印了两个冒号的问题。（Bo Peng）
    </para>
    <para>
     补丁由 Umar Hayat 创建。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-01-12 [15c0a5b]
    -->
    <para>
     修复 <function>pool_signal</function>。（Tatsuo Ishii）
    </para>
    <para>
     之前 pool_signal 没有设置 SA_RESTART 标志。因此任何被信号中断的系统调用都不会重启。我们的一些代码已经做好了准备，以便在系统调用被信号中断时重启。但不确定所有地方都已做好准备。因此添加该标志。注意，<productname>PostgreSQL</productname> 始终使用该标志。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-12-09 [8d4fb84]
    -->
    <para>
     修复了当在 <xref linkend="guc-pool-passwd"> 参数中指定了非默认值的文件时，<command>pg_md5</command> 和 <command>pg_enc</command> 不会更新密码文件的问题。（Bo Peng）
    </para>
    <para>
     此问题由 Sadhuprasad Patro 报告。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-12-04 [6489bb0]
    2024-12-11 [2a99aa5]
    -->
    <para>
     修复流复制模式下的几个查询缓存 bug。（Tatsuo Ishii）
    </para>
    <itemizedlist>
     <listitem>
      <para>
       当查询缓存启用且从前端发送执行消息时，如果查询缓存数据可用，<productname>Pgpool-II</productname> 会将查询缓存数据注入后端消息缓冲区。但缓存消息可能被插入到不适当的位置，导致 <productname>Pgpool-II</productname> 错误地引发“kind mismatch”错误。
      </para>
     </listitem>
     <listitem>
      <para>
       如果查询缓存启用，查询以扩展查询模式运行，且 pgpool 以流复制模式运行，执行消息可能返回不正确的结果。
      </para>
     </listitem>
     <listitem>
      <para>
       另一个问题是当连续发送多个执行消息时。在这种情况下，Pgpool-II 为每个执行消息返回完全相同的查询缓存结果。这是错误的，因为第二次或后续的执行应返回 0 行。
      </para>
      <para>
       讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-December/004548.html">[pgpool-hackers: 4547] 查询缓存中的 Bug</ulink>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-01-17 [3db1305]
    -->
    <para>
     增强客户端认证章节。（Tatsuo Ishii）
    </para>
    <para>
     添加关于 pool_passwd 的介绍。之前概览页面中只有关于 pool_hba.conf 的描述。关于 <xref linkend="guc-pool-passwd"> 的通用指南将帮助用户理解本章节。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-01-14 [0a38c5d]
    -->
    <para>
     稳定 032.dml_adaptive_loadbalance 测试。（Tatsuo Ishii）
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-10">
 <title>发布版本 4.4.10</title>
  <note>
   <title>发布日期</title>
   <simpara>2024-11-28</simpara>
  </note>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-11-26 [48502ce]
    -->
    <para>
     如果后端发送错误消息，则中止 SSL 协商。(Tatsuo Ishii)
    </para>
    <para>
     在 SSL 协商的客户端实现 (pool_ssl_negotiate_clientserver()) 中，中间人攻击者有可能在 SSL 协商阶段发送一条长错误消息来混淆 Pgpool-II 或客户端。此提交立即拒绝协商（发出 FATAL 错误）并退出会话以防止此类攻击。
    </para>
    <para>
     这类似于 PostgreSQL 的 CVE-2024-10977。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-25 [81ff539]
    -->
    <para>
     修复 watchdog 进程不重新加载配置的问题。(Bo Peng)
    </para>
    <para>
     <productname>Pgpool-II</productname> 中的 <function>reload_config()</function> 函数应向 watchdog 进程发送 SIGHUP 信号。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-15 [ffabbed]
    -->
    <para>
     修复 <function>do_query</function> 中的错误。(Tatsuo Ishii)
    </para>
    <para>
     修复 <function>do_query</function> 以使用 ntohs/ntohl 代替 htons/htonl。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-13 [c81e354]
    -->
    <para>
     修复更新 <filename>pool_passwd</filename> 并重新加载后的身份验证失败问题。(Tatsuo Ishii)
    </para>
    <para>
     如果 <filename>pool_hba.conf</filename> 被禁用，即使执行了 <productname>Pgpool-II</productname> 重新加载，<productname>Pgpool-II</productname> 子进程也无法识别对 pool_passwd 的更新。重新加载处理函数 check_config_reload() 错误地认为仅当 <xref linkend="guc-enable-pool-hba"> 开启时才需要重新打开 pool_passwd。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-November/001944.html">https://www.pgpool.net/pipermail/pgpool-general/2024-November/001944.html"</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-11 [f3a98a4]
    2024-11-12 [897147b]
    -->
    <para>
     修复 <command>COPY</command> 挂起问题。(<ulink url="https://github.com/pgpool/pgpool2/issues/79">#79</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     在 COPY IN 状态（即 COPY FROM STDIN）期间，前端可以发送 Flush 或 Sync 消息。根据 F/B 协议规范，这些消息应被忽略，但 Pgpool-II 将其视为无效消息，导致 COPY 挂起。
    </para>
    <para>
     此外，为此问题添加了一个回归测试。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-09 [a690ce0]
    -->
    <para>
     修复 <productname>Pgpool-II</productname> 子进程在关闭期间崩溃的问题。(Tatsuo Ishii)
    </para>
    <para>
     实际崩溃发生在 <function>close_all_backend_connections()</function> 中。由于 on_system_exit 注册了 <function>child_will_go_down()</function>，因此调用了 <function>close_all_backend_connections()</function>。当时 pgpool 子进程似乎刚刚启动并正在执行 <function>pool_init_cp()</function>。连接池对象尚未完全初始化，这是崩溃的原因。
    </para>
    <para>
     问题由 Emond Papegaaij 报告并分析。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-November/001938.html">https://www.pgpool.net/pipermail/pgpool-general/2024-November/001938.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-10-26 [33a2f2c]
    -->
    <para>
     修复由于身份验证失败导致的偶发性健康检查失败。(Tatsuo Ishii)
    </para>
    <para>
     当 <xref linkend="guc-health-check-password"> 为空字符串时，健康检查进程会从 pool_passwd 文件中查找密码。问题在于，该文件的文件描述符是从父 pgpool 进程继承的。这意味着 pgpool 主进程和健康检查进程（可能多个进程）共享同一个描述符，这会导致各种问题，包括此处报告的问题。为解决此问题，在健康检查进程启动时重新打开文件，以便每个健康检查进程拥有自己的文件描述符。
    </para>
    <para>
     请注意，pgpool 子进程（负责前端会话）已经重新打开了文件描述符，它们与此问题无关。
    </para>
    <para>
     问题由 Emond Papegaaij 报告并分析。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-October/001913.html">https://www.pgpool.net/pipermail/pgpool-general/2024-October/001913.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-10-23 [1a66900]
    -->
    <para>
     优化 <literal>ALTER ROLE</literal> 的查询缓存失效。(Tatsuo Ishii)
    </para>
    <para>
     提交 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=commit;h=6b7d585eb1c693e4ffb5b8e6ed9aa0f067fa1b89">6b7d585eb1c693e4ffb5b8e6ed9aa0f067fa1b89</ulink> 在使用任何 <literal>ALTER ROLE/USER</literal> 语句时会使查询缓存失效。实际上这有些过度。因为以下查询不会影响角色的权限。
    </para>
    <programlisting>
ALTER ROLE user WITH [ENCRYPTED] PASSWORD
ALTER ROLE user WITH CONNECTION LIMIT
    </programlisting>
    <para>
     因此，如果使用这些命令，请不要使查询缓存失效。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-October/004532.html">https://www.pgpool.net/pipermail/pgpool-hackers/2024-October/004532.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-09-18 [04142b4]
    -->
    <para>
     修复 pgpool 子进程退出时 <productname>Pgpool-II</productname> 崩溃的问题。(Tatsuo Ishii)
    </para>
    <para>
     当 pgpool 子进程退出时，会调用 <function>close_all_backend_connections()</function>，该函数负责关闭连接池中所有到后端的连接。它错误地使用了 MAIN_CONNECTION 宏，这对于当前活动连接是可行的，但对于池化连接则不好，因为主节点在创建连接池时可能不同。修复方法是改用 <function>in_use_backend()</function>。
    </para>
    <para>
     报告者：Emond Papegaaij
    </para>
   </listitem>

   <listitem>
    <!--
    2024-09-14 [794adad]
    -->
    <para>
     修复复制/快照隔离模式中的错误。(<ulink url="https://github.com/pgpool/pgpool2/issues/69">#69</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     当接收到 <command>INSERT</command> 命令时，pgpool 会自动向目标表发出表 LOCK 命令，但忘记将该命令发送给主节点以外的其他节点。这仅发生在扩展查询模式下。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-09-14 [10333d5]
    -->
    <para>
     修复原生复制/快照隔离模式中的另一个错误。(Tatsuo Ishii)
    </para>
    <para>
     insert_lock() 忘记将行锁命令（lock_kind == 3 情况）发送给主节点以外的其他节点。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-09-14 [12e1b78]
    2024-09-10 [f60aaf4]
    -->
    <para>
     修复 <filename>pool_memqcache.c</filename> 中的资源泄漏。(Tatsuo Ishii)
    </para>
    <para>
     这些泄漏是由提交 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=commit;h=6fdba5c33">6fdba5c33</ulink> 引入的。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-10-03 [bf46b3d]
    -->
    <para>
     修复文档和示例配置文件中的拼写错误。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-11-25 [8b7af6a]
    2024-11-25 [36100bd]
    -->
    <para>
     使 024.cert_auth 测试适配 OpenSSL 3.2。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-9">
 <title>发布版本 4.4.9</title>
  <note>
   <title>发布日期</title>
   <simpara>2024-09-09</simpara>
  </note>

 <sect2>
  <title>概述</title>
  <para>
   此版本包含一个安全修复。
  </para>
  <para>
   当启用查询缓存（<xref linkend="runtime-in-memory-query-cache">）功能时，
   数据库用户可能通过查询缓存读取到本不应对其可见的表中的行（CVE-2024-45624）。
  </para>
  <para>
   所有早于 4.5.4、4.4.9、4.3.12、4.2.19、4.1.22 的 <productname>Pgpool-II</productname> 版本，
   以及所有具有查询缓存功能的更早版本（查询缓存功能自 3.2 版本起实现）均受此漏洞影响。
  </para>

  <itemizedlist>
   <listitem>
    <para>
     如果为某个用户针对某张表创建了查询缓存，然后通过 <command>REVOKE</command> 命令撤销了该用户对该表的访问权限，
     该用户仍然可能通过查询缓存访问其没有访问权限的表。
    </para>
   </listitem>

   <listitem>
    <para>
     如果为用户 A 针对某张表创建了查询缓存，然后其他用户 B 在同一会话中通过 <command>SET ROLE</command> 或
     <command>SET SESSION_AUTHORIZATION</command> 访问该表，用户 B 仍然可能通过查询缓存访问其没有访问权限的表。
    </para>
   </listitem>

   <listitem>
    <para>
     如果为启用了行级安全的表为用户 A 创建了查询缓存，然后其他用户 B 在同一会话中通过 <command>SET ROLE</command>
     或 <command>SET SESSION_AUTHORIZATION</command> 访问该表，用户 B 可能通过查询缓存检索到本不应对其可见的行。
    </para>
   </listitem>

  </itemizedlist>
  <para>
   强烈建议升级到 <productname>Pgpool-II</productname> 4.5.4、4.4.9、4.3.12、4.2.19 和 4.1.22 或更高版本。
   或者您最好关闭查询缓存功能。
  </para>

  <para>
   请注意，为修复此漏洞，某些命令（ALTER DATABASE、ALTER ROLE、ALTER TABLE、REVOKE）现在会使整个查询缓存数据失效。
   这可能会影响使用查询缓存功能时的性能。
  </para>
 </sect2>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-08-15 [730acde]
    -->
    <para>
     反向移植 <xref linkend="guc-notice-per-node-statement">。（Tatsuo Ishii）
    </para>
    <para>
     这主要用于测试。反向移植版本：v4.1
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-August/004505.html">[pgpool-hackers: 4504]</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-08-09 [1f200d7]
    2024-08-11 [ce7ed50]
    -->
    <para>
     修复复制模式和快照隔离模式中的错误。（Tatsuo Ishii）
    </para>
    <para>
     修复了以下错误：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       在复制模式和快照隔离模式下，当命令完成时，<productname>Pgpool-II</productname> 会等待查询就绪消息，
       但忘记某些命令（例如 <command>SET ROLE</command>）会产生参数状态消息。
       结果导致 <productname>Pgpool-II</productname> 报错，提示其他消息在查询就绪消息之前到达。
      </para>
     </listitem>

     <listitem>
      <para>
       如果前一个查询产生了参数状态消息，后续的 <function>parse()</function> 需要读取并处理它，
       因为它想要读取应跟随在参数状态消息之后的查询就绪消息。
       然而，当调用 <function>ParameterStatus()</function> 时，查询进行中的标志被设置，
       如果在此 <function>parse()</function> 调用中处理的查询是负载均衡的，则可能只处理了来自一个后端的参数状态消息。
       参数状态消息很可能来自所有活跃的后端，因为它们是由 <command>SET</command> 命令生成的，
       而在复制模式和快照隔离模式下，<command>SET</command> 命令会发送到所有活跃的后端。
       因此，在调用 <function>ParameterStatus()</function> 之前取消设置查询进行中的标志。
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-8">
 <title>Release 4.4.8</title>
  <note>
   <title>发布日期</title>
   <simpara>2024-08-15</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-08-02 [97485f6]
    -->
    <para>
     在示例脚本中，使用 <command>psql -V</command> 替代 <command>initdb -V</command>。(Bo Peng)
    </para>
    <para>
     在示例脚本中，使用 <command>psql -V</command> 替代 <command>initdb -V</command>，因为在某些情况下可能未安装 postgresqlxx-server。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug修复</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-07-30 [251e422]
    -->
    <para>
     修复接收 flush 消息后的挂起问题。(<ulink url="https://github.com/pgpool/pgpool2/issues/59">#59</ulink>) (Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-07-22 [c1dd550]
    -->
    <para>
     修复段错误。 (Tatsuo Ishii)
    </para>
    <para>
     有报告指出 pgpool 子进程在 pool_do_auth 中发生段错误。原因是
     MAIN_CONNECTION() 返回了 NULL。看起来 my_main_node_id 被设置为了
     错误的节点 ID 0，而该节点实际处于宕机状态，因此 cp->slots[0] 中没有连接。
    </para>
    <para>
     问题由 Emond Papegaaij 报告。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-July/001852.html">[pgpool-general: 9175]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-07-19 [49340b9]
    -->
    <para>
     修复动态进程管理。 (Tatsuo Ishii)
    </para>
    <para>
     用于进程驱逐算法的 pooled_connection 计算不正确。结果始终为
     max_pool。此外还增加了一些注释。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-July/004491.html">[pgpool-hackers: 4490]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-28 [8ea6f7a]
    -->
    <para>
     修复解析配置文件时的段错误。 (Tatsuo Ishii)
    </para>
    <para>
     此提交修复了在解析 pgpool.conf 时，如果设置值未用单引号括起来则发生的段错误。
    </para>
    <para>
     补丁由 Carlos Chapi 创建，Tatsuo Ishii 审查并修改。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-21 [7ca9b23]
    -->
    <para>
     修复不使用 MAIN_NODE 宏的段错误。 (Tatsuo Ishii)
    </para>
    <para>
     某些函数（close_idle_connection()、new_connection() 和
     pool_create_cp()）在不适当的地方使用了 MAIN* 和 VALID_BACKEND。
     MAIN* 和 VALID_BACKEND 仅适用于当前到后端的连接，而不适用于池化连接，
     因为在池化连接中，哪个后端是主节点或正在运行必然与当前到后端的连接相同。
     这些宏的错误使用有时会导致段错误。
    </para>
    <para>
     问题由 Emond Papegaaij 报告。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-June/009176.html">[pgpool-general: 9114]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-21 [152c65d]
    -->
    <para>
     修复 MAIN_NODE 宏（实际上是 pool_virtual_main_db_node_id()）。 (Tatsuo Ishii)
    </para>
    <para>
     该宏在没有会话上下文时会返回 REAL_MAIN_NODE_ID。
     这是错误的，因为当发生故障转移/回退时，REAL_MAIN_NODE_ID 随时可能更改。
    </para>
    <para>
     假设 REAL_MAIN_NODE_ID == my_main_node_id == 1。
     然后由于故障回退，REAL_MAIN_NODE_ID 变为 0。
     接着 MAIN_CONNECTION(cp) 将返回 NULL，对其任何引用都会导致段错误。为防止此问题，
     我们应返回 my_main_node_id。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-14 [9488d9f]
    -->
    <para>
     修复 <command>show pool_processes</command> 避免重复显示行描述。 (Tatsuo Ishii)
    </para>
    <para>
     processes_reporting() 意外同时调用了 send_row_description()
     和 send_row_description_and_data_rows()。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-June/004472.html">[pgpool-hackers: 4471]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-11 [8c18899]
    -->
    <para>
     修复子进程中的段错误。 (Tatsuo Ishii)
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-May/001780.html">[pgpool-general: 9104]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-11 [93ba28b]
    -->
    <para>
     网络短暂中断时 Watchdog 死亡并导致 pgpool2 崩溃。(<ulink url="https://www.pgpool.net/mantisbt/view.php?id=823">#823</ulink>) (Muhammad Usama)
    </para>
    <para>
     启用网络监控后，如果 <productname>Pgpool-II</productname>
     节点丢失了所有网络接口或分配的 IP 地址，它会立即关闭，
     通过快速将非通信节点从集群中移除来提供额外保护。
    </para>
    <para>
     问题是 <productname>Pgpool-II</productname> 在网络监控
     禁用时也对网络中断事件做出了响应。此修复确保在网络监控未启用时不打开
     网络监控套接字，从而防止不必要的关闭。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-10 [c90ec3b]
    -->
    <para>
     修复启用 watchdog 时 <command>pgpool reset</command> 命令不工作的问题。 (Tatsuo Ishii)
    </para>
    <para>
     由 keiseo 报告问题并提供问题分析。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-June/004466.html">[pgpool-hackers: 4465]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-04 [0574a5d]
    -->
    <para>
     修复 <literal>psql_scan</literal> 崩溃。(<ulink url="https://github.com/pgpool/pgpool2/issues/54">#54</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     有报告指出，在判断长查询中的字符串是否为 psql 变量（即以 ":" 开头）时，
     <literal>psql_scan</literal> 发生崩溃。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-06-07 [cb4ecf0]
    -->
    <para>
     修复文档和程序源代码中的大量拼写错误。 (Tatsuo Ishii)
    </para>
    <para>
     补丁由 Umar Hayat 创建。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-08-01 [d9fd431]
    -->
    <para>
     将选择要终止进程的标准添加到 <xref linkend="guc-max-spare-children">。 (Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-07-30 [d8f8cca]
    -->
    <para>
     增强故障转移文档。 (Tatsuo Ishii)
    </para>
    <para>
     阐明启用 <xref linkend="guc-failover-on-backend-shutdown"> 时的故障转移条件。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-19 [36112da]
    -->
    <para>
     补充虚拟 IP 相关参数的缺失默认值。 (Bo Peng)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-7">
 <title>发布版本 4.4.7</title>
  <note>
   <title>发布日期</title>
   <simpara>2024-05-16</simpara>
  </note>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-05-01 [afa710e]
    -->
    <para>
     修复使用 <literal>-Werror=implicit-function-declaration</literal> 时的编译器错误。(Bo Peng)
    </para>
    <para>
     添加 <literal>LDAP_DEPRECATED</literal> 以包含已弃用 ldap 函数的原型。
     同时在 autoconf 检查中添加缺失的头文件。
    </para>
    <para>
     补丁由 Vladimir Petko 创建。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-04-04 [ad42306]
    2024-04-04 [9dee833]
    -->
    <para>
     修复多种导致段错误的原因。(Tatsuo Ishii)
    </para>
    <para>
     据报告，在某些涉及故障转移的情况下，pgpool 及其子进程会发生段错误。
    </para>
    <para>
     问题由 Emond Papegaaij 报告并分析。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-April/009131.html">https://www.pgpool.net/pipermail/pgpool-general/2024-April/009131.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-04-03 [cd4482d]
    -->
    <para>
     修复未初始化内存错误。(Tatsuo Ishii)
    </para>
    <para>
     据报告，valgrind 发现了多个错误，包括 read_startup_packet 中的未初始化内存错误。该函数在取消或 SSL 请求的情况下使用 palloc 为启动包中的用户名分配内存，随后该内存被 pstrdup 使用。由于 palloc 分配的内存是未定义的，此处本应使用 palloc0。
    </para>
    <para>
     问题由 Emond Papegaaij 报告并分析。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-April/009126.html">https://www.pgpool.net/pipermail/pgpool-general/2024-April/009126.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-04-02 [ba6142f]
    -->
    <para>
     修复当 load_balance_mode 关闭时的错误/挂起问题。(Tatsuo Ishii)
    </para>
    <para>
     提交 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=commit;h=3f3c1656">3f3c1656</ulink>
     导致当 load_balance_mode 关闭、主节点 ID 不为 0 且查询为 BEGIN 等时出现错误/挂起。
    </para>
    <para>
     错误由 Emond Papegaaij 发现并分析。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-March/009113.html">https://www.pgpool.net/pipermail/pgpool-general/2024-March/009113.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-25 [93ee49f]
    -->
    <para>
     修复使用特定 CFLAGS 时的编译错误。(Tatsuo Ishii)
    </para>
    <para>
     <ulink url="https://github.com/pgpool/pgpool2/issues/42">https://github.com/pgpool/pgpool2/issues/42</ulink>
     报告称，使用 CFLAGS
     -flto=4 -Werror=odr -Werror=lto-type-mismatch -Werror=strict-aliasing
     时 gcc 会报错。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-March/004443.html">https://www.pgpool.net/pipermail/pgpool-hackers/2024-March/004443.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-25 [1a15495]
    -->
    <para>
     修复示例 pgpool.conf 中的注释。(Bo Peng)
    </para>
    <para>
     <varname>sr_check_period</varname> 的默认值应为 10 秒。
     同时修复了注释中的一些拼写错误。
    </para>
    <para>
     补丁由 hiroin 创建，并由 Bo Peng 修改。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-21 [9054387]
    -->
    <para>
     允许即使扩展查询未结束时也能运行重置查询。(Tatsuo Ishii)
    </para>
    <para>
     提交 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=commit;h=240c668d">240c668d</ulink>
     导致如果扩展查询消息未结束时，重置查询会失败。此提交通过在 SimpleQuery() 中检查是否正在运行重置查询来修复该问题。同时为此添加了测试用例。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-21 [710875a]
    2024-03-20 [2a7a38b]
    -->
    <para>
     修复 Coverity 指出的内存泄漏。(Takuma Hoshiai)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-18 [02452a6]
    -->
    <para>
     防范不适当的协议数据。(Tatsuo Ishii)
    </para>
    <para>
     如果在扩展查询消息序列结束之前到达一条简单查询消息，pgpool 可能会挂起。这是因为会话上下文中用于简单查询的查询上下文被扩展查询消息的查询上下文覆盖了。
    </para>
    <para>
     此提交通过在 SimpleQuery() 中检查扩展查询协议消息是否已结束来实现防护。如果它们未结束，则引发 FATAL 错误。此检查检测到的一个已知示例是 JDBC 驱动程序的 "autosave=always" 选项。这意味着在此提交之后，pgpool 将不接受该选项，直到 JDBC 驱动程序端修复该问题（在结束扩展查询消息协议之前发送简单协议消息）。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-February/004428.html">https://www.pgpool.net/pipermail/pgpool-hackers/2024-February/004428.html</ulink>
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-04-03 [8986a8b]
    -->
    <para>
     增强“上游服务器连接”文档。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-28 [062d4c9]
    -->
    <para>
     日语文档的语言清理。(Masaya Kawamoto)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-03-27 [0b8f377]
    -->
    <para>
     添加关于在两个 postgres 节点时使用 <command>pcp_promote_node</command> 的说明。(Masaya Kawamoto)
    </para>
    <para>
     即使只有两个 postgres 节点，也存在需要设置 <varname>follow_primary_command</varname> 的情况。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-03-16 [927d7ae]
    2024-03-15 [8e17321]
    -->
    <para>
     修复回归测试 005.jdbc 以避免 Java 21 弃用警告。(Bo Peng)
    </para>
    <para>
     将 Runtime.exec(String) 替换为 Runtime.exec(String[]) 以避免 Java 21 弃用警告。
     补丁由 Vladimir Petko 创建，并由 Bo Peng 修改。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-6">
 <title>版本 4.4.6</title>
  <note>
   <title>发布日期</title>
   <simpara>2024-02-29</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-02-27 [aea5a51]
    -->
    <para>
     从示例脚本中移除了 restore_command。（Bo Peng）
    </para>
    <para>
     由于启用了复制槽，不需要 restore_command。它偶尔会导致故障转移失败。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-02-26 [5a25a29]
    -->
    <para>
     修复了 <xref linkend="guc-recovery-user">、
     <xref linkend="guc-failover-on-backend-shutdown"> 和
     <xref linkend="guc-insert-lock"> 的默认值。（Bo Peng）
    </para>
    <para>
     因为 <varname>recovery_user</varname> 的默认值应该与
     <xref linkend="guc-health-check-user"> 和
     <xref linkend="guc-sr-check-user"> 相同，我将默认值从 '' 改为 'nobody'。
    </para>
    <para>
     <varname>failover_on_backend_shutdown</varname> 的正确默认值应为 "false"，
     而 <varname>insert_lock</varname> 的正确默认值应为 "on"。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-02-10 [85570e1]
    -->
    <para>
     修复了 <xref linkend="guc-statement-level-load-balance"> 与 BEGIN 等语句的问题。（Tatsuo Ishii）
    </para>
    <para>
     当启用了 <varname>statement_level_load_balance</varname> 时，
     BEGIN/END/COMMIT/ABORT/SET/SAVEPOINT/RELEASE SAVEPOINT/DEALLOCATE
     ALL/DISCARD 会被发送到主节点和所有备用节点，即使
     <xref linkend="guc-load-balance-mode"> 处于关闭状态。
     这不仅完全错误，而且如果某个备用节点位于远程网络中，还会导致性能下降。
    </para>
    <para>
     修复方法：当 <varname>load_balance_mode</varname> 关闭时，pgpool 仅将此类查询发送到主节点。
    </para>
    <para>
     报告：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2024-January/009059.html">https://www.pgpool.net/pipermail/pgpool-general/2024-January/009059.html</ulink>
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2024-February/004423.html">https://www.pgpool.net/pipermail/pgpool-hackers/2024-February/004423.html</ulink>
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2024-02-26 [ed49149]
    -->
    <para>
     修复了日文 watchdog 文档。（Tatsuo Ishii）
    </para>
    <para>
     它错误地使用了 "master" watchdog 节点，而不是 "leader"。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-02-26 [99c657f]
    -->
    <para>
     修复了英文 watchdog 文档。（Tatsuo Ishii）
    </para>
    <para>
     它错误地使用了 "main" PostgreSQL 节点，而不是 "primary"。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-5">
 <title>发布 4.4.5</title>
  <note>
   <title>发布日期</title>
   <simpara>2023-11-30</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-11-17 [823cfd9]
    -->
    <para>
     修改示例脚本中的复制槽名称转换，以支持主机名大写。(Bo Peng)
    </para>
    <para>
     在故障转移或在线恢复期间执行的脚本中，复制槽会自动创建或删除。
     由于复制槽名称不能使用大写字母，因此修改这些脚本以将主机名转换为小写。
    </para>
    <para>
     补丁由 Sheikh Wasiu Al Hasib 创建，并由 Bo Peng 修改。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-10-06 [5c9b377]
    -->
    <para>
     在示例脚本 <filename>escalation.sh.sample</filename> 中添加 SSH 超时设置，
     该脚本在 <xref linkend="guc-wd-escalation-command"> 参数中设置。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-10-04 [6075d59]
    -->
    <para>
     移除不必要的日志行。(Tatsuo Ishii)
    </para>
    <para>
     调试消息 "unset_tx_started_by_multi_statement_query is called in CommandComplete" 被意外遗留。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-09-29 [72a171c]
    2023-09-29 [2036eac]
    -->
    <para>
     修复 <function>Describe()</function> 使其不会因 <command>DECLARE</command> 创建的 portal 而中止。(Tatsuo Ishii)
    </para>
    <para>
     当执行 <command>DECLARE foo CURSOR FOR</command> 时，
     PostgreSQL 会自动创建一个名为 "foo" 的 portal。
     由于该 portal 不受 Pgpool-II 管理，
     "Describe foo" 消息会失败并报错 "unable to execute Describe.
     unable to get the bind message"。
    </para>
    <para>
     为解决此问题，使 <function>Describe()</function> 创建一个虚拟的写查询上下文，
     以便将 describe 消息发送到主节点（流复制模式）或所有节点（复制/快照隔离模式）。
    </para>
    <para>
     同时为此提交添加测试用例 080。
    </para>
    <para>
     此问题由 Heather Lapointe 报告并分析。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2023-September/008995.html">https://www.pgpool.net/pipermail/pgpool-general/2023-September/008995.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-10-03 [9b95270]
    2023-10-07 [f6b8228]
    2023-09-20 [3763441]
    -->
    <para>
     移除 <varname>NAMEDATALEN</varname> 的重复定义。(Tatsuo Ishii)
    </para>
    <para>
     <varname>NAMEDATALEN</varname> 的定义现在仅在
     <filename>src/include/pg_config_manual.h</filename> 中。
     同时将 <filename>src/utils/pool_select_walker.c</filename> 中的
     <varname>POOL_NAMEDATALEN</varname> 替换为
     <varname>NAMEDATALEN</varname>。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-August/004366.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-August/004366.html</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-11-29 [42dc580]
    -->
    <para>
     修改 "Pgpool-II + Watchdog 设置示例" 以提及
     <varname>pcp_listen_addresses = '*'</varname> 是必需的，
     以允许 PCP 监听所有地址。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-09-19 [fff663e]
    -->
    <para>
     修复 <xref linkend="pcp-node-info"> 文档。(Tatsuo Ishii)
    </para>
    <para>
     实际后端状态是通过 PQpingParams 获取的，而不是 pg_isready。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-08-21 [d7256e1]
    -->
    <para>
     增强 <xref linkend="guc-follow-primary-command"> 手册。(Tatsuo Ishii)
    </para>
    <para>
     提及如果未使用 <xref linkend="pcp-recovery-node">，
     则有必要运行 <xref linkend="pcp-attach-node">。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-08-21 [957f983]
    -->
    <para>
     增强 <command>pgpool_setup</command>。(Tatsuo Ishii)
    </para>
    <para>
     增强故障转移脚本生成和主节点跟随脚本生成，以便它们创建更好的日志记录。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-4">
 <title>发布版本 4.4.4</title>
  <note>
   <title>发布日期</title>
   <simpara>2023-08-17</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-07-23 [2cbc61e]
    -->
    <para>
     仅在共享内存初始化后安装系统退出回调函数。(Muhammad Usama)
    </para>
    <para>
     当由于获取共享内存失败而调用退出回调函数时，清理函数在访问位于共享内存中的 process_info 时可能会产生段错误。
     虽然我们也可以通过当 process_info 为 NULL 时从退出回调中退出来修复此问题，但在共享内存成功初始化后安装该函数是更好的方法，因为系统的其余部分始终假定 process_info 永远不会为 NULL，并且在子进程生成之前也没有需要清理的内容。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-23 [379b24e]
    2023-07-22 [e59d034]
    -->
    <para>
     当 <xref linkend="guc-process-management-mode"> 设置为 "dynamic" 时，禁止将 <xref linkend="guc-max-spare-children"> 设置为大于 <xref linkend="guc-num-init-children">。(Muhammad Usama)
    </para>
    <para>
     配置后处理阶段现在会在 <varname>num_init_children</varname> 小于 <varname>max_spare_children</varname> 时抛出错误。
     该提交还根据配置的进程管理策略调整了启动时生成的子进程数量。
     对于 Aggressive 策略，会生成 <varname>max_spare_children</varname> 数量的进程；对于其他两种策略，启动时会创建 <varname>min_spare_children</varname> 数量的子进程。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-29 [13e73bc]
    -->
    <para>
     降低日志消息级别。(Tatsuo Ishii)
    </para>
    <para>
     在 <ulink url="https://www.pgpool.net/mantisbt/view.php?id=806">https://www.pgpool.net/mantisbt/view.php?id=806</ulink> 中指出日志文件中存在以下日志条目：
	</para>
	<para>
	 LOG: pool_pending_message_set_flush_request: msg: Parse
	</para>
	<para>
	 这些消息用于调试，日志级别本应为 DEBUG*。因此我将日志级别从 LOG 更改为 DEBUG5。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-12 [7ff642f]
    -->
    <para>
     为某些系统目录查询函数添加模式限定。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-08-09 [30db44e]
    -->
    <para>
     修复 Coverity 警告。(Tatsuo Ishii)
    </para>
    <para>
     修复查询缓存模块将 time_t 值赋值给 int32 变量的问题。将变量类型更改为 int64。根据 Coverity 报告。同时使用 difftime() 计算 time_t 差值。这是推荐的做法。
     <ulink url="https://www.jpcert.or.jp/sc-rules/c-msc05-c.html">https://www.jpcert.or.jp/sc-rules/c-msc05-c.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-08-04 [1633f61]
    -->
    <para>
     修复 find_primary_node_repeatedly 在 <xref linkend="guc-search-primary-node-timeout"> 超时内未终止的问题。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-17 [cd03751]
    -->
    <para>
     修复使用 v2 协议时的崩溃问题。(<ulink url="https://www.pgpool.net/mantisbt/view.php?id=807">bug 807</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     当 read_kind_from_backend() 累积统计数据时，未针对 v2 协议情况进行准备。
    </para>
    <para>
	 来自 MCanivez 的错误报告和补丁。由我稍作修改。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-12 [1f5b7c8]
    -->
    <para>
     修复多语句情况下的 <literal>PREPARE</literal>。(Tatsuo Ishii)
    </para>
    <para>
     如果多语句查询在第二个或后续位置包含 <literal>PREPARE</literal>，并且后续的绑定消息使用了该预备语句，则会因 "unable to bind" 错误而失败，因为该预备语句未保存在已发送消息中。
    </para>
    <para>
	 为解决此问题，如果在解析语句后发现此类情况，将为该命名语句创建一个查询上下文，并将其添加到已发送消息列表中。
    </para>
    <para>
	 讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2023-July/008931.html">https://www.pgpool.net/pipermail/pgpool-general/2023-July/008931.html</ulink>
    </para>
    <para>
	 为此新增了回归测试 079..multi_prepare。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-09 [e62c5a7]
    -->
    <para>
     修复 pgproto 以支持带参数的绑定消息。(Tatsuo Ishii)
    </para>
    <para>
     之前 pgproto 只能处理不带参数的绑定消息。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-05 [5049e59]
    -->
    <para>
     修复日志消息中的拼写错误。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-23 [571fbd9]
    -->
    <para>
     修复缺失的 CTE SEARCH 和 CYCLE 重写。(Bo Peng)
    </para>
    <para>
     在原生复制模式下，<productname>Pgpool-II</productname> 需要在写查询中将日期/时间函数重写为时间戳。CTE SEARCH 和 CYCLE 的重写缺失了。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-19 [8e4bf20]
    -->
    <para>
     移除 src/sample/pgpool.conf.sample-stream 中重复的 pcp_listen_address 设置。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-14 [a091bcb]
    -->
    <para>
     防止在共享锁下更新查询缓存。(Tatsuo Ishii)
    </para>
    <para>
     在 <ulink url="https://www.pgpool.net/mantisbt/view.php?id=795">bug 795</ulink> 中指出，过期的查询缓存条目可能在共享锁下被修改。这可能导致查询缓存使用的共享内存损坏。为解决此问题，我们暂时释放共享锁，然后在修改缓存条目之前获取排他锁。这可能会创建一个时间窗口，我们需要重新获取缓存条目元数据，以确保元数据未被其他人修改。
    </para>
    <para>
	 回溯到引入查询缓存共享锁的 V4.4 稳定版。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-05 [6aebf24]
    -->
    <para>
     修复 <xref linkend="guc-delay-threshold-by-time"> 和 <xref linkend="guc-prefer-lower-delay-standby">。(Tatsuo Ishii)
    </para>
	<para>
     自 4.4 版本引入 <varname>delay_threshold_by_time</varname> 以来，它们一直存在问题。
	</para>
    <itemizedlist>
	 <listitem>
	  <para>
	   <varname>delay_threshold_by_time</varname> 未在 where_to_send 中被检查。这导致复制延迟时负载均衡失效。
	  </para>
	 </listitem>
    </itemizedlist>
    <itemizedlist>
	 <listitem>
	  <para>
	   如果同时启用了 <varname>delay_threshold_by_time</varname> 和 <varname>prefer_lower_standby_delay</varname>，select_load_balancing_node 会失效。
	  </para>
	 </listitem>
    </itemizedlist>
	<para>
	 为解决此问题，修复了 where_to_send 和 select_load_balancing_node。同时为 <varname>delay_threshold_by_time</varname> 添加了测试用例，已添加到 033.prefer_lower_standby_delay。
    </para>
    <para>
	 讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2023-June/008864.html">https://www.pgpool.net/pipermail/pgpool-general/2023-June/008864.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-05-30 [f388140]
    -->
    <para>
     修复 replication_mode_recovery_2nd_stage.sample 示例脚本中错误的变量名。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-08-03 [5bf4ede]
    -->
    <para>
     修复缺失的 "logger" 应用程序名称。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-08-01 [65e2760]
    -->
    <para>
     阐明停止模式的含义。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-26 [dda631f]
    -->
    <para>
     从 4.2.10 版本发布说明中移除错误信息。(Bo Peng)
    </para>
    <para>
     从 4.2.10 版本发布说明中移除 "修复 pgpool_recovery 扩展脚本。(Tatsuo Ishii)"。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-07-11 [ddbcead]
    -->
    <para>
     增强 "8.2. Pgpool-II + Watchdog 设置示例" 中的 SSH 公钥认证设置部分。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-29 [fcac3fb]
    2023-06-28 [9adee42]
    -->
    <para>
     修复负载均衡模式章节。(Tatsuo Ishii)
    </para>
    <para>
     在某些地方，"流复制模式" 被写成了 "原生复制模式"。同时增强了关于流复制模式下负载均衡额外要求的描述。添加了一些索引。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-06-27 [fc0747a]
    -->
    <para>
     增强在线恢复文档。(Bo Peng)
    </para>
    <para>
     提到在线恢复的第二阶段仅在未启用 watchdog 的多个 pgpool 节点上无法正常工作。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-05-22 [29f7861]
    -->
    <para>
     修复偶发的 069.memory_leak_extended 测试失败。(Tatsuo Ishii)
    </para>
    <para>
     经查明，偶发测试失败的原因是，在 pgbench 完成后，运行 ps 命令之前 pgpool 子进程已经消失。原因是某种类型不匹配的 FATAL 错误："DISCARD ALL cannot be executed within a pipeline"。为解决此问题，在后台运行 pgbench，并在 pgbench 完成之前获取进程大小。
    </para>
    <para>
	 讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-May/004338.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-May/004338.html</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-3">
 <title>版本 4.4.3</title>
  <note>
   <title>发布日期</title>
   <simpara>2023-05-18</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-03-25 [77f1e64]
    -->
    <para>
     将 <literal>psqlscan</literal> 源代码导入 <productname>Pgpool-II</productname>，用于判断多语句查询。(Tatsuo Ishii)
    </para>
    <para>
     <literal>Psqlscan</literal> 是 PostgreSQL 源代码树中的一个模块。
     它本质上是 <productname>PostgreSQL</productname> SQL 扫描器的一个子集，
     但专门用于检测每条 SQL 语句的结束位置。因此，我们可以通过它来统计查询字符串中的 SQL 语句数量。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/004291.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/04291.html</ulink>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-April/004320.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-April/004320.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-12 [5f912d3]
    2023-04-09 [7aca88c]
    -->
    <para>
     允许广泛使用多语句。(Tatsuo Ishii)
    </para>
    <para>
     此提交尝试消除 pgpool 长期以来关于多语句（multi-statements）的限制。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/004287.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/004287.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-16 [ad4d966]
    2023-03-30 [8ce860d]
    2023-03-28 [722d4bd]
    -->
    <para>
     对内部查询使用模式限定。(Tatsuo Ishii)
    </para>
    <para>
     某些对象（如函数和类型转换）未使用 "pg_catalog." 模式限定。
     这虽然不会立即导致安全问题，但使用模式限定始终是一个好习惯。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-04-30 [eeea973]
    -->
    <para>
     修复 <xref linkend="guc-prefer-lower-delay-standby"> 的 bug。
     (<ulink url="https://www.pgpool.net/mantisbt/view.php?id=798">bug 798</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     当客户端连接到 pgpool 时，会选择一个备用节点作为负载均衡节点。
     如果在会话持续期间备用节点延迟超过 delay_threshold，
     <varname>prefer_lower_delay_standby</varname> 会选择延迟最低的备用节点作为新的负载均衡节点，
     并将目标后端设置为此节点。不幸的是，此决定是在检查 SELECT 查询是否包含写入函数等之前做出的，
     导致 <productname>Pgpool-II</productname> 会愉快地发送无法在备用节点上执行的 SELECT 语句。
     为了解决此问题，<varname>prefer_lower_delay_standby</varname> 的处理被移至写入函数等检查之后。
    </para>
    <para>
     同时修改 033.prefer_lower_standby_delay 回归测试以包含上述情况。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-03-22 [1618f0f]
    -->
    <para>
     修复在使用 musl libc 的系统上的编译错误。
     (<ulink url="https://www.pgpool.net/mantisbt/view.php?id=790">bug 790</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     补丁由 leimaohui 提供。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-05 [33655c3]
    -->
    <para>
     修复多个查询缓存的 bug。(Tatsuo Ishii)
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-January/004259.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-January/004259.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-05 [95ab912]
    -->
    <para>
     修复 sr worker 在极端情况下不向备用服务器发送错误查询的问题。(Tatsuo Ishii)
    </para>
    <para>
     当设置了 ALWAYS_PRIMARY 标志时，即使主节点宕机，PRIMARY_NODE_ID 宏也会返回节点 ID 而非 -1。
     在这种情况下，流复制延迟检查工作进程会向备用节点发送
     <literal>SELECT pg_current_wal_lsn()</literal> 或
     <literal>SELECT pg_current_xlog_location()</literal>
     （取决于 <productname>PostgreSQL</productname> 的版本），这当然会引发错误。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/004279.html">https://www.pgpool.net/pipermail/pgpool-hackers/2023-February/004279.html</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-31 [38225a7]
    -->
    <para>
     修复 <literal>DEALLOCATE</literal> 的类型不匹配错误。
     (<ulink url="https://www.pgpool.net/mantisbt/view.php?id=780">bug 780</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     当以下条件全部满足时，会发生类型不匹配错误：
     <itemizedlist>
      <listitem>
       <para>
        流复制模式
       </para>
      </listitem>
     </itemizedlist>
     <itemizedlist>
      <listitem>
       <para>
        负载均衡节点不是主节点
       </para>
      </listitem>
     </itemizedlist>
     <itemizedlist>
      <listitem>
       <para>
        在多语句查询中使用了 <literal>PREPARE</literal>
       </para>
      </listitem>
     </itemizedlist>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-25 [1c623f7]
    -->
    <para>
     修复回归测试的编译错误 (Takuma Hoshiai)
    </para>
    <para>
     该错误由使用 gcc10 的时间戳测试引起。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-30 [93f5585]
    -->
    <para>
     在提交共享 relcache 时获取更强的锁。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-18 [1f503e1]
    -->
    <para>
     修复 <literal>show pool_version</literal> 显示两次行描述的问题。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-04-25 [171a57c]
    -->
    <para>
     为 <xref linkend="guc-wd-priority"> 添加说明。(Chen Ningwei)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-19 [a57cc11]
    -->
    <para>
     添加关于 <literal>PREPARE/EXECUTE/DEALLOCATE</literal> 的限制说明。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-19 [a9b4f30]
    -->
    <para>
     添加使用 pgpool 时使用 <literal>-D</literal> 选项的注意事项。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-14 [7a540cc]
    -->
    <para>
     增强“从 RPM 安装”部分。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-13 [bbe5236]
    -->
    <para>
     在“服务器设置与操作”章节中添加“备份 PostgreSQL 数据库”部分。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-13 [eb338ff]
    -->
    <para>
     从“8.2. Pgpool-II + Watchdog 设置示例”中移除 "-D" 启动 OPTS 的配置。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-09 [336f50f]
    -->
    <para>
     修复限制部分。(Tatsuo Ishii)
    </para>
    <para>
     <itemizedlist>
      <listitem>
       <para>
        添加关于 <literal>SCRAM-SHA-256</literal> 认证的说明
       </para>
      </listitem>
     </itemizedlist>
     <itemizedlist>
      <listitem>
       <para>
        添加关于快照隔离模式的说明
       </para>
      </listitem>
     </itemizedlist>
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-04 [33c6313]
    2023-04-03 [75e8870]
    -->
    <para>
     添加当 pg 节点状态显示为“unknown”时的说明。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-03-22 [366e8e6]
    -->
    <para>
     提到执行 <xref linkend="pcp-promote-node"> 时，要提升的目标节点必须处于运行状态。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-22 [85994ce]
    -->
    <para>
     提到 AES256 支持需要 <option>--with-openssl</option> 选项。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-21 [e813ed6]
    -->
    <para>
     明确说明使用 .pcppass 需要 pcp 命令的 -w 选项。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-15 [6bf97fd]
    -->
    <para>
     修复“2.8 创建插入锁表”部分。(Tatsuo Ishii)
    </para>
    <para>
     它仅引用了原生复制，而本应引用快照隔离模式。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-02-01 [6d39a7e]
    -->
    <para>
     增强 <xref linkend="sql-show-pool-cache">。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>测试工具</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-03-28 [89b5129]
    -->
    <para>
     修改 <command>pgpool_setup</command>，将一些参数追加到配置文件末尾。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2023-01-28 [baedc51]
    -->
    <para>
     重构 071.execute_and_deallocate/test.sh。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-05-12 [8c3250e]
    2023-04-29 [2376ab7]
    2023-04-16 [c9e45b6]
    2023-04-15 [1e25fd2]
    -->
    <para>
     增强回归测试。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-04-06 [89c0159]
    -->
    <para>
     修复偶发的 005.jdbc 测试失败。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-2">
 <title>版本 4.4.2</title>
  <note>
   <title>发布日期</title>
   <simpara>2023-01-23</simpara>
  </note>

 <sect2>
  <title>概述</title>
  <para>
   此版本包含一个安全修复。
  </para>
  <para>
   如果同时满足以下条件，<xref linkend="guc-wd-lifecheck-user"> 的密码会
   被 <xref linkend="SQL-SHOW-POOL-STATUS"> 命令暴露。
   任何能够连接到 <productname>Pgpool-II</productname> 的用户都可以执行该命令。(CVE-2023-22332)
  </para>

  <itemizedlist>
   <listitem>
    <para>
     版本 3.3 或更高版本。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="guc-use-watchdog"> 设置为 <literal>on</literal>。
    </para>
   </listitem>

   <listitem>
    <para>
     <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>query</literal>。
    </para>
   </listitem>

   <listitem>
    <para>
     一个明文密码被设置
     到 <xref linkend="guc-wd-lifecheck-password">。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   在这种情况下，强烈建议升级到此版本
   （我们不再在 show pool_status 命令中暴露 <xref linkend="guc-wd-lifecheck-password">），
   或者使用以下一种变通方法。
  </para>

  <para>
   针对 4.0.x 到 4.4.x 用户的变通方法：
  </para>

  <itemizedlist>
   <listitem>
    <para>
    禁用 watchdog。将 <xref linkend="guc-use-watchdog">
    设置为 <literal>off</literal>。
    </para>
   </listitem>

   <listitem>
    <para>
    将 <xref linkend="guc-wd-lifecheck-method">
    更改为 <literal>heartbeat</literal>。
    </para>
   </listitem>

   <listitem>
    <para>
    将空字符串设置
    到 <xref linkend="guc-wd-lifecheck-password">。这将使用
    <filename>pool_passwd</filename> 文件中的密码。
    </para>
   </listitem>

   <listitem>
    <para>
    将 AES 加密密码设置
    到 <xref linkend="guc-wd-lifecheck-password">。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   在任何情况下，我们都建议
   在 <productname>PostgreSQL</productname> 中
   更改 <xref linkend="guc-wd-lifecheck-password">。
  </para>

  <para>
   针对 3.0.x 到 3.7.x 用户的变通方法：
  </para>

  <itemizedlist>
   <listitem>
    <para>
    禁用 watchdog。将 <xref linkend="guc-use-watchdog">
    设置为 <literal>off</literal>。
    </para>
   </listitem>

   <listitem>
    <para>
     将 <xref linkend="guc-wd-lifecheck-method">
     更改为 <literal>heartbeat</literal>。
    </para>
   </listitem>
  </itemizedlist>

  <para>
   在任何情况下，我们都建议
   在 <productname>PostgreSQL</productname> 中
   更改 <xref linkend="guc-wd-lifecheck-password">。
  </para>

  <para>
   请注意，<productname>Pgpool-II</productname> 3.7.x 或
   更早版本已停止支持，并且不再为这些版本提供次要更新。
  </para>
 </sect2>

 <sect2>
  <title>变更</title>
  <itemizedlist>

   <listitem>
    <!--
    2023-01-21 [123ceb46]
      -->
    <para>
     不在 <xref linkend="SQL-SHOW-POOL-STATUS">、
     <xref linkend="PCP-POOL-STATUS"> 和
     <xref linkend="PGPOOL-ADM-PCP-POOL-STATUS"> 命令中暴露
     <xref linkend="guc-wd-lifecheck-password">。(CVE-2023-22332) (Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-21 [4e0cb44b]
      -->
    <para>
     将 <xref linkend="guc-wd-lifecheck-password"> 的默认值
     更改为文档中所述的空字符串。(Bo Peng)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>

   <listitem>
    <!--
	2023-01-16 [65865834]
      -->
    <para>
     修复了 pgpool_adm: pcp_pool_status 在与 <productname>PostgreSQL</productname> 15 一起使用时出现的错误。(Tatsuo Ishii)
    </para>
    <para>
     pcp_pool_status 实际上返回 SETOF record，但之前被声明为
     "returns record"。这种不严谨在
     <productname>PostgreSQL</productname> 14 之前一直被容忍，但 <productname>PostgreSQL</productname> 15
     变得更加严格。为了解决这个问题，我们需要将
     pcp_pool_status 的返回类型从 "RETURNS record" 更改为 "RETURNS SETOF record"。
    </para>
    <para>
     并且 pgpool_adm 扩展版本已升级到 1.5。
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-13 [ac752fd6]
      -->
    <para>
     修复了流复制检查中的整数溢出问题。(Tatsuo Ishii)
    </para>
    <para>
     当 <xref linkend="guc-delay-threshold-by-time"> 启用且
     复制延迟超过约 36 分钟时，会记录整数溢出错误，并且复制检查失败。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-24 [b31e6c94]
      -->
    <para>
     修复了 <filename>pgpool-regclass.c</filename> 中的编译器警告。(Florian Weimer,
     Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-10 [b8698f5f]
      -->
    <para>
     修复了 Coverity 指出的未初始化变量。(Takuma Hoshiai)
    </para>
    </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>

   <listitem>
    <!--
    2023-01-06 [2d002b7d]
      -->
    <para>
     文档：增强并修复 <link linkend="runtime-in-memory-query-cache-shmem-config">使用共享内存的配置</link> 章节中的一些错误。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2023-01-03 [1a259a28]
      -->
    <para>
     文档：增强对 <xref linkend="guc-delay-threshold-by-time"> 的解释。(zam bak, Tatsuo
     Ishii)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-1">
 <title>发布版本 4.4.1</title>
  <note>
   <title>发布日期</title>
   <simpara>2022-12-22</simpara>
  </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2022-12-16 [6311565]
    -->
    <para>
     将流复制 LOG 消息 "standby_delay: 54" 降级为 DEBUG1 级别。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-12 [dfcc21e]
    -->
    <para>
     将 LOG 消息 "we have hit the ceiling" 降级为 DEBUG5 级别。(Tatsuo Ishii)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>

   <listitem>
    <!--
    2022-12-20 [f6cbfe2]
    -->
    <para>
     修复时间戳重写错误。(Tatsuo Ishii)
    </para>
    <para>
     在 <productname>Pgpool-II</productname> 4.4 的 outfuncs 模块中，布尔常量被忽略并在重写的查询字符串中变为空字符串，从而导致语法错误。
    </para>
    <para>
     此问题由 Michiel van Leening 报告。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2022-December/008581.html">[pgpool-general: 8521]</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-20 [7bc3176]
    -->
    <para>
     修复与 <xref linkend="guc-failover-require-consensus"> 相关的问题。(Muhammad Usama)
    </para>
    <para>
     修复方法是根据整个 watchdog 集群中健康检查参数的最大值，动态设置故障转移命令的超时时间。
    </para>
    <para>
     由 Tatsuo Ishii 审核和测试。
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2022-November/004228.html">[pgpool-hackers: 4227]</ulink>
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>

   <listitem>
    <!--
    2022-12-19 [8e5f5eb]
    -->
    <para>
     文档：增强性能部分。(Tatsuo Ishii)
    </para>
    <para>
     提及动态进程管理。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-14 [5d5d4a2]
    -->
    <para>
     文档：修复 Aurora 示例中的拼写错误。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-12 [83dcd83]
    -->
    <para>
     文档：增强动态进程管理文档。(Tatsuo Ishii)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>

   <listitem>
    <!--
    2022-12-17 [2985853]
    -->
    <para>
     允许使用环境变量定义 PGPOOLDIR。
     (<ulink url="https://www.pgpool.net/mantisbt/view.php?id=766">bug 766</ulink>) (Bo Peng)
    </para>
   </listitem>

  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-4-0">
 <title>版本 4.4.0</title>
 <note>
  <title>发布日期</title>
  <simpara>2022-12-06</simpara>
 </note>

 <sect2>
  <title>概述</title>
  <para>
   此版本增加了许多增强功能，以便更轻松地进行配置和管理。新增了动态备用进程管理模式。
   流复制延迟可以通过时间（而非 WAL 字节数）来控制。
   <productname>Pgpool-II</productname> 和 PCP 的监听地址支持多个地址。
   现在可以完整配置 Unix 套接字目录。
   允许自定义 <varname>trusted_servers</varname> 用于检查上游连接的命令。
   引入了新的 <productname>PostgreSQL</productname> 15 <literal>SQL</literal>
   解析器。查询缓存的性能也得到了改进。
  </para>

  <para>
   主要增强功能如下：
  </para>

  <itemizedlist>
   <listitem>
    <para>
     新增动态备用进程管理功能。
     此功能允许在静态和动态进程管理模式之间进行选择。
    </para>
   </listitem>

   <listitem>
    <para>
     允许在流复制模式下按时间指定复制延迟。为此，引入了新的
     参数 <xref linkend="guc-delay-threshold-by-time">。
    </para>
   </listitem>

   <listitem>
    <para>
     正如 <productname>PostgreSQL</productname> 已经做的那样，
     <productname>Pgpool-II</productname> 现在支持
     <xref linkend="guc-unix-socket-directories">、<xref linkend="guc-unix-socket-group">
     和 <xref linkend="guc-unix-socket-permissions">，以便更灵活、更精确地控制 UNIX 域套接字。
    </para>
   </listitem>

   <listitem>
    <para>
     允许在 <xref linkend="guc-listen-addresses">
     和 <xref linkend="guc-pcp-listen-addresses"> 中使用逗号分隔的多个监听地址。它们控制
     哪些接口接受连接尝试，这有助于防止在不安全的网络接口上重复的恶意连接请求。
    </para>
   </listitem>

   <listitem>
    <para>
     允许自定义 <xref linkend="guc-trusted-servers"> 用于检查上游连接的命令。
     为此，引入了新的参数 <xref linkend="guc-trusted-server-command">。
    </para>
   </listitem>

   <listitem>
    <para>
     导入 PostgreSQL 15 的 SQL 解析器。
    </para>
   </listitem>

   <listitem>
    <para>
     通过减少锁竞争来加速 <link linkend="runtime-in-memory-query-cache">查询
     缓存</link>。这使得并发运行的客户端能够更快地获取缓存内容。
    </para>
   </listitem>
  </itemizedlist>

 </sect2>

 <sect2 id="migration-4-4-0">
  <title>迁移到版本 4.4</title>
  <itemizedlist>

   <listitem>
    <para>
     将配置参数 <varname>socket_dir</varname> 重命名为 <xref linkend="guc-unix-socket-directories">。
    </para>
   </listitem>

   <listitem>
    <para>
     配置参数 <literal>delegate_IP</literal> 被重命名
     为 <xref linkend="guc-delegate-ip">。为了与旧版本兼容，旧的参数 delegate_IP 仍然可以工作。如果
     使用了旧的参数 delegate_IP，<productname>Pgpool-II</productname> 将把值设置到
     delegate_ip 并发出警告消息。
    </para>
   </listitem>

   <listitem>
    <para>
     来自后端的参数状态消息现在会转发到前端。当参数状态消息从后端到达时，
     <productname>Pgpool-II</productname> 会记住它，但不会转发到前端。现在它们会被转发到前端。
     使用 <productname>Pgpool-II</productname> 的应用程序应该准备好接受该消息。但是
     由于 <productname>PostgreSQL</productname> 已经在生成参数状态消息时将其发送到前端，
     那些与 <productname>PostgreSQL</productname> 一起工作的应用程序应该不会对此更改有任何问题。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>更改</title>
  <itemizedlist>

   <listitem>
    <!--
	2022-11-09 [ce5e529c]
      -->
    <para>
     新增动态备用进程管理功能。(Jianshen Zhou, Muhammad Usama)
    </para>
    <para>
     此功能允许在静态和动态进程管理模式之间进行选择。
     静态进程管理与 <productname>Pgpool-II</productname> 的现有行为相同，
     即在启动时生成所有子进程。新的动态模式会跟踪空闲进程，
     并派生或终止进程以将该数量保持在指定范围内。
    </para>
    <para>
     新增了四个设置，<xref linkend="guc-process-management-mode">、
     <xref linkend="guc-process-management-strategy">、
     <xref linkend="guc-min-spare-children"> 和 <xref linkend="guc-max-spare-children">
     用于配置进程管理行为，而 <varname>process_management_strategy</varname>
     允许在三种可能的缩减策略之间进行选择。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-01-28 [832212b2]
    2022-01-29 [515860f3]
    -->
    <para>
     允许按时间指定复制延迟。(Tatsuo Ishii)
    </para>
    <para>
     <xref linkend="guc-delay-threshold"> 以字节为单位指定复制延迟
     上限。添加了一个类似的参数，称为
     <xref linkend="guc-delay-threshold-by-time">，以便可以通过时间（秒）来指定限制。新参数在大于 0 时生效。
     在这种情况下，<xref linkend="SQL-SHOW-POOL-NODES"> 和
     <xref linkend="pcp-node-info"> 以秒为单位显示复制延迟。
     如果该参数设置为 0，则 <xref linkend="guc-delay-threshold-by-time">
     被忽略，并回退到 delay_threshold 模式。
    </para>
    <para>
     为此，在共享内存数据 Backendinfo 中添加了新成员 standby_delay_by_time，以区分复制延迟是以字节（standby_delay_by_time = false）还是秒（standby_delay_by_time = true）为单位测量的。如果 standby_delay_by_time 为 true，则 standby_delay 以秒 * 1000000 为单位测量，因此精度为小数点后 6 位。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-07-20 [bc03514b]
    -->
    <para>
     支持 unix_socket_directories 及相关参数。(Masaya Kawamoto)
    </para>
    <para>
     将配置参数 <varname>socket_dir</varname> 重命名为 <xref linkend="guc-unix-socket-directories">，
     并添加新的配置参数 <xref linkend="guc-unix-socket-group"> 和 <xref linkend="guc-unix-socket-permissions">。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-11-04 [b7bcf0d7]
    -->
    <para>
     添加 <xref linkend="guc-trusted-server-command"> 配置参数。(Takuma Hoshiai)
    </para>
    <para>
     此功能能够指定由 <xref linkend="guc-trusted-servers"> 用于检查上游连接的命令。
     以前，只使用 ping 命令，并且是硬编码的。
    </para>
    <para>
     默认值为 'ping -q -c3 %h'，与之前相同。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-02-23 [fd0efcea]
    2022-03-05 [9f727c1e]
    -->
    <para>
     实现逗号分隔的多个监听地址。(Tatsuo Ishii)
    </para>
    <para>
     以前，<productname>Pgpool-II</productname> 只允许将单个主机名、IP 或 '*' 设置到
     <xref linkend="guc-listen-addresses"> 参数。现在我们可以像
     <productname>PostgreSQL</productname> 已经做的那样，将多个监听地址设置到该参数。
    </para>
    <para>
     此外，<xref linkend="guc-pcp-listen-addresses"> 现在也接受逗号分隔的多个监听地址。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-09-22 [424e68c1]
    -->
    <para>
     导入 PostgreSQL 15 的 SQL 解析器。(Bo Peng, Tatsuo Ishii)
    </para>
    <para>
     PostgreSQL 15 解析器的主要更改包括：
    </para>
    <itemizedlist>

     <listitem>
      <para>
       添加新的 SQL MERGE 命令
      </para>
      <programlisting>
MERGE INTO ... USING ...
      </programlisting>
     </listitem>

     <listitem>
      <para>
       为 COPY FROM 添加新的 HEADER MATCH 选项
      </para>
      <programlisting>
COPY ... FROM stdin WITH (HEADER MATCH);
      </programlisting>
     </listitem>

     <listitem>
      <para>
       允许外键 ON DELETE SET 操作
      </para>
      <programlisting>
CREATE TABLE t1 (
...
FOREIGN KEY (c1, c2) REFERENCES t2 ON DELETE SET NULL (c2)
);
      </programlisting>
     </listitem>

     <listitem>
      <para>
       允许在 ALTER TABLE 中设置 ACCESS METHOD
      </para>
      <programlisting>
ALTER TABLE ... SET ACCESS METHOD ...;
      </programlisting>
     </listitem>

    </itemizedlist>
   </listitem>

   <listitem>
    <!--
    2022-09-19 [dc559c07]
    -->
    <para>
     将查询缓存中的排他锁替换为共享锁。(Tatsuo Ishii)
    </para>
    <para>
     以前，查询缓存模块使用信号量来保护查询缓存模块中的关键区域。然而，这有一个严重的缺点，因为使用信号量的锁定是排他锁。这会导致缓存读取客户端不必要的等待，并且在存在许多并发客户端时导致性能下降。
    </para>
    <para>
     为了解决这个问题，将锁定实现替换为 <function>flock(2)</function>，它允许共享锁定。现在，缓存读取客户端不需要互相争抢锁，从而提高了并发性。
    </para>
    <para>
     为此，pgpool 主进程创建一个虚拟文件作为 <filename>logdir/memq_lock_file</filename> 并打开它。该文件描述符由所有子进程继承，以便它们可以对该文件发出 <function>flock(2)</function>。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-02-10 [7ec74730]
    -->
    <para>
     添加对 <xref linkend="guc-wd-lifecheck-password">
     和 <xref linkend="guc-recovery-password"> 格式的验证。(Masaya Kawamoto)
    </para>
   </listitem>

   <listitem>
    <!--
    2022-01-31 [4bcba525]
    -->
    <para>
     增强参数状态处理。(Tatsuo Ishii)
    </para>
    <para>
     当参数状态消息从后端到达时，Pgpool-II 会记住它，但不会转发到前端。此提交允许将参数状态消息转发到前端。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-01-29 [515860f3]
    -->
    <para>
     更改在指定 <xref linkend="guc-delay-threshold-by-time"> 时获取复制延迟的方式。(Tatsuo Ishii)
    </para>
    <para>
     使用 pg_stat_replication.replay_lag。这种方式使代码更简单，并且可以获得更精确的复制延迟。唯一的缺点是 pg_stat_replication.replay_lag 仅在 PostgreSQL 10 或更高版本中可用（以前的方法可以在 9.5 或更高版本中使用）。
    </para>
   </listitem>

   <listitem>
    <!--
	2022-01-18 [f9521fe4]
      -->
    <para>
     实现 flush 跟踪功能。(Tatsuo Ishii)
    </para>
    <para>
     当从前端收到 flush 消息时，任何来自后端的待处理消息都应该被 flush 并发送到前端。为此，此提交实现了“flush 跟踪”功能。即，当 flush 消息到达时，pgpool 在每个待处理消息中设置“flush pending”标志。如果来自后端的响应消息对应于设置了 flush pending 标志的待处理消息，则该消息会立即被 flush 到前端，而不是缓冲。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-01-18 [d9e4bef8]
    2022-01-12 [a14fe59a]
    -->
    <para>
     增强 pgproto。(Tatsuo Ishii)
    </para>
    <para>
     允许显示 ParameterStatus 的参数名称和值。
    </para>
    <para>
     添加新命令 'z'。这与 'y' 类似，只是 'z' 只读取 1 条消息，并且不等待“ready for query”到达（如果 1 秒内没有消息到达则超时）。
    </para>
   </listitem>

   <listitem>
    <!--
    2022-12-03 [0b31fdc3]
    -->
    <para>
     将 LOG 消息“new IPC connection received”降级为 DEBUG1。
     (<ulink url="https://www.pgpool.net/mantisbt/view.php?id=775">bug 775</ulink>) (Bo Peng)
    </para>
    <para>
     此消息会定期输出，不应记录为 LOG。
     补丁由 pstef 创建，并由 Bo Peng 审查。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>Bug 修复</title>
  <itemizedlist>
   <listitem>
    <para>
     在此版本中，已经应用了与 <productname>Pgpool-II</productname> 4.3.4 相同的 bug 修复。有关这些修复的更多详细信息，请参阅 <xref linkend="release-4-3-4">。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档更改</title>
  <itemizedlist>
   <listitem>
    <!--
    2022-10-27 [89d1235]
    -->
    <para>
     文档：更新 AWS 配置示例文档，并为 AWS 添加示例脚本。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

</sect1>