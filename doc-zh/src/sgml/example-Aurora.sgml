<sect1 id="example-Aurora">
 <title>Aurora 配置示例</title>

 <para>
  <productname>Amazon Aurora for PostgreSQL
   Compatibility</productname>（Aurora）是一项针对
  <productname>PostgreSQL</productname> 的托管服务。从用户的角度来看，<productname>Aurora</productname> 可以视为一个具有若干例外情况的流式复制集群。首先，
  故障转移和在线恢复由 <productname>Aurora</productname> 管理。因此无需设置
  <xref linkend="guc-failover-command">、<xref linkend="guc-follow-primary-command">
  以及与恢复相关的参数。本节我们将介绍如何为 <productname>Aurora</productname> 配置 <productname>Pgpool-II</productname>。
 </para>

 <sect2 id="example-Aurora-config">
  <title>为 Aurora 配置 pgpool.conf</title>
  <para>
   <itemizedlist>
    <listitem>
     <para>
      从 <filename>pgpool.conf.sample</filename> 创建 <filename>pgpool.conf</filename>。
      确保 <filename>pgpool.conf</filename> 中包含以下行：
      <programlisting>
backend_clustering_mode = streaming_replication
      </programlisting>
     </para>
    </listitem>
    <listitem>
     <para>
      将 <xref linkend="guc-sr-check-period"> 设置为 0
       以禁用流式复制延迟检查。这是
       因为 <productname>Aurora</productname> 不提供
       用于检查复制延迟所需的功能。
     </para>
     <programlisting>
sr_check_period = 0
     </programlisting>
    </listitem>
    <listitem>
     <para>
      将 <xref linkend="guc-enable-pool-hba"> 设置为 on，
       以启用 md5 认证
       （<productname>Aurora</productname> 始终使用 md5
       认证）。
     </para>
     <programlisting>
enable_pool_hba = on
     </programlisting>
    </listitem>
    <listitem>
     <para>
      创建 <filename>pool_password</filename>。详见 <xref linkend="auth-md5">。
     </para>
    </listitem>
    <listitem>
     <para>
      将 <xref linkend="guc-backend-hostname">0 设置为 Aurora 集群端点。
	  将 <xref linkend="guc-backend-hostname">1 设置为 Aurora 读取端点。
	  按照通常方式设置合适的 <xref linkend="guc-backend-weight">。
	  无需设置 <xref linkend="guc-backend-data-directory">。
     </para>
     <programlisting>
backend_hostname0 = 'cluster endpoint'
backend_hostname1 = 'reader endpoint'
     </programlisting>
    </listitem>
    <listitem>
     <para>
      将 <varname>ALWAYS_PRIMARY</varname> 标志
      设置为 <xref linkend="guc-backend-flag">
      对应于 <xref linkend="guc-backend-hostname">0。
     </para>
    </listitem>
    <listitem>
     <para>
      由于故障转移由 Aurora 管理，将 <varname>DISALLOW_TO_FAILOVER</varname> 标志
      设置为 <xref linkend="guc-backend-flag">
      对应于 <xref linkend="guc-backend-hostname">0 和 <xref linkend="guc-backend-hostname">1。
     </para>
     <programlisting>
backend_flag0 = 'ALWAYS_PRIMARY|DISALLOW_TO_FAILOVER'
backend_flag1 = 'DISALLOW_TO_FAILOVER'
     </programlisting>
    </listitem>
    <listitem>
     <para>
      将 <xref linkend="guc-health-check-period"> 设置为 0 以禁用健康检查。
     </para>
     <programlisting>
health_check_period = 0
     </programlisting>
    </listitem>
    <listitem>
     <para>
      禁用 <xref linkend="guc-failover-on-backend-error">
       以避免在连接到后端或
       执行查询时在后端侧检测到错误时触发故障转移，
       原因同上。
     </para>
    </listitem>
   </itemizedlist>
  </para>
 </sect2>
</sect1>
