<!-- doc/src/sgml/restrictions.sgml -->

<sect1 id="restrictions" xreflabel="restrictions">
 <title>限制</title>

 <para>
  本节描述了 <productname>Pgpool-II</productname> 当前的限制。

  <variablelist>

   <varlistentry>
    <term>使用 pg_terminate_backend</term>
    <listitem>
     <para>
      如果使用 <function>pg_terminate_backend()</function> 停止后端进程，这将触发故障转移。发生这种情况的原因是 <productname>PostgreSQL</productname> 为终止的后端进程发送的消息与完全关闭 postmaster 时发送的消息完全相同。在 3.6 版本之前没有解决方法。从 3.6 版本开始，此限制已得到缓解。如果该函数的参数（即进程 ID）是常量，则可以安全地使用该函数。但在扩展协议模式下，您无法使用该函数。在 4.3 或更高版本中，您可以通过设置 <xref linkend="guc-failover-on-backend-shutdown"> 为 <literal>off</literal> 来完全防止由 <function>pg_terminate_backend()</function> 引起的故障转移，但这也会防止由 postmaster 终止引起的故障转移。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>负载均衡</term>
    <listitem>
     <para>
      多语句查询（单行中的多个 SQL 命令）始终发送到主节点（在流复制模式下）或主节点（在其他模式下）。通常 <productname>Pgpool-II</productname> 将查询分派到适当的节点，但这不适用于多语句查询。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>认证/访问控制</term>
    <listitem>
     <para>
      支持 trust 和 pam 方法。自 <productname>Pgpool-II</productname> 3.0 起也支持 md5。md5 通过使用认证文件 <filename>pool_passwd</filename> 来支持。自 <productname>Pgpool-II</productname> 4.0 起还支持 scram-sha-256、cert 和明文密码。<filename>pool_passwd</filename> 是认证文件的默认名称。以下是启用 md5 认证的步骤：
     </para>

     <orderedlist>

      <listitem>
       <para>
        以数据库的操作系统用户身份登录并输入：
        <programlisting>
         pg_md5 --md5auth --username=<literal>your_username</literal> <literal>your_passwd</literal>
        </programlisting>
        用户名和 md5 加密密码将注册到 pool_passwd。如果 pool_passwd 尚不存在，pg_md5 命令将自动为您创建它。pool_passwd 的格式为 <literal>username:encrypted_passwd</literal>。
       </para>
      </listitem>

      <listitem>
       <para>
        您还需要在 pool_hba.conf 中添加相应的 md5 条目。详情请参阅 <xref linkend="auth-pool-hba-conf">。
       </para>
      </listitem>

      <listitem>
       <para>
        请注意，用户名和密码必须与在 <productname>PostgreSQL</productname> 中注册的相同。
       </para>
      </listitem>

      <listitem>
       <para>
        更改 md5 密码后（当然是在 pool_passwd 和 PostgreSQL 中），您需要执行 <application>pgpool reload</application>。
       </para>
      </listitem>
     </orderedlist>

     <para>
      有关设置 scram-sha-256 认证的详细信息，请参阅 <xref linkend="setting-scram-sha-256-authentication">。
     </para>

    </listitem>
   </varlistentry>

   <varlistentry>
    <term>大对象</term>
    <listitem>
     <para>
      在 <xref linkend="runtime-config-streaming-replication-mode"> 中，<productname>Pgpool-II</productname> 支持大对象。
     </para>
     <para>
      在 <xref linkend="guc-snapshot-isolation-mode">
      和 <xref linkend="guc-replication-mode"> 中，如果后端是 <productname>PostgreSQL</productname> 8.1 或更高版本，<productname>Pgpool-II</productname> 支持大对象。为此，您需要在 <filename>pgpool.conf</filename> 中启用 lobj_lock_table 指令。但不支持使用后端函数 <function>lo_import</function> 进行大对象复制。
     </para>
     <para>
      在其他模式下，不支持大对象。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>临时表</term>
    <listitem>
     <para>
      在本机复制模式下，创建/插入/更新/删除临时表始终在主节点上执行。对这些表的 SELECT 也在主节点上执行。但是，如果在 SELECT 中将临时表名称用作字面量，则无法检测到它，并且该 SELECT 将被负载均衡。这将触发"未找到表"错误，或者会找到另一个同名表。为避免此问题，请使用 SQL 注释。
     </para>
     <para>
      请注意，在访问系统目录的查询中使用此类字面量表名确实会导致上述问题。psql 的 \d 命令会产生这样的查询：
      <programlisting>
       SELECT 't1'::regclass::oid;
      </programlisting>
      在这种情况下，<productname>Pgpool-II</productname> 始终将查询发送到主节点，不会导致问题。
     </para>

     <para>
      如果使用 PostgreSQL 8.3 或更高版本，通过在 reset_query_list 中指定 DISCARD ALL，由 <command>CREATE TEMP TABLE</command> 创建的表将在会话结束时被删除。
     </para>
     <para>
      对于 8.2.x 或更早版本，由 <command>CREATE TEMP
       TABLE</command> 创建的表在退出会话后不会被删除。这是因为连接池从 PostgreSQL 后端的角度来看保持了会话的活动状态。为避免这种情况，您必须通过执行 <command>DROP TABLE</command> 显式删除临时表，或在事务块内使用 <command>CREATE TEMP TABLE ... ON COMMIT DROP</command>。
     </para>

    </listitem>
   </varlistentry>

   <varlistentry>
    <term><xref linkend="guc-snapshot-isolation-mode">
    和 <xref linkend="guc-replication-mode"> 中的函数等</term>
    <listitem>
     <para>
      无法保证使用上下文相关机制（例如随机数、事务 ID、OID、SERIAL、序列等）提供的任何数据能够在多个后端上正确复制。对于 SERIAL，启用 insert_lock 将有助于复制数据。insert_lock 也有助于 SELECT setval() 和 SELECT nextval()。
     </para>
     <para>
      使用 <function>CURRENT_TIMESTAMP</function>、<function>CURRENT_DATE</function>、
      <function>now()</function> 的 INSERT/UPDATE 将被正确复制。对于使用 <function>CURRENT_TIMESTAMP</function>、<function>CURRENT_DATE</function>、
      <function>now()</function> 作为其 DEFAULT 值的表的 INSERT/UPDATE 也将被正确复制。这是通过在查询执行时将这些函数替换为从 <productname>PostgreSQL</productname> 获取的常量来实现的。但存在一些限制：
     </para>
     <para>
      在 <productname>Pgpool-II</productname> 3.0 或更早版本中，某些情况下表默认值中的时间数据计算不准确。例如，以下表定义：
      <programlisting>
       CREATE TABLE rel1(
       d1 date DEFAULT CURRENT_DATE + 1
       )
      </programlisting>
      与以下内容处理方式相同：
      <programlisting>
       CREATE TABLE rel1(
       d1 date DEFAULT CURRENT_DATE
       )
      </programlisting>
      <productname>Pgpool-II</productname> 3.1 或更高版本正确处理这些情况。因此列 "d1" 将以明天作为默认值。但是，如果使用扩展协议（例如 JDBC、PHP PDO 中使用的）或 PREPARE，则此增强功能不适用。
     </para>
     <para>
      请注意，如果列类型不是时间类型，则不会进行重写。例如：
      <programlisting>
       foo bigint default (date_part('epoch'::text,('now'::text)::timestamp(3) with time zone) * (1000)::double precision)
      </programlisting>
      假设我们有以下表：
      <programlisting>
       CREATE TABLE rel1(
       c1 int,
       c2 timestamp default now()
       )
      </programlisting>
      我们可以复制
      <programlisting>
       INSERT INTO rel1(c1) VALUES(1)
      </programlisting>

      因为这变成

      <programlisting>
       INSERT INTO rel1(c1, c2) VALUES(1, '2009-01-01 23:59:59.123456+09')
      </programlisting>

      但是，
      <programlisting>
       INSERT INTO rel1(c1) SELECT 1
      </programlisting>
      无法被转换，因此在当前实现中无法正确复制。值仍会被插入，但不进行任何转换。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>SQL 类型命令</term>
    <listitem>
     <para>
      <link linkend="sql-commands">SQL 类型命令</link>不能在扩展查询模式下使用。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>多字节字符</term>
    <listitem>
     <para>
      <productname>Pgpool-II</productname> 不对客户端和 <productname>PostgreSQL</productname> 之间的多字节字符执行编码转换。客户端和后端的编码必须相同。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>libpq</term>
    <listitem>
     <para>
      构建 <productname>Pgpool-II</productname> 时会链接 libpq。libpq 版本必须为 3.0 或更高版本。使用 libpq 2.0 版本构建 <productname>Pgpool-II</productname> 将会失败。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>ParameterStatus</term>
    <listitem>
     <para>
      当客户端连接到 <productname>PostgreSQL</productname> 时，<productname>PostgreSQL</productname> 会向客户端返回一些参数/值对。此协议称为 <ulink url="https://www.postgresql.org/docs/14/protocol-flow.html#id-1.10.5.7.3"><firstterm>ParameterStatus</firstterm></ulink>。可以使用一些 API（例如 libpq 的 <function>PQParameterStatus</function>）来提取参数/值对。实际的参数名称可以在此 <ulink url="https://www.postgresql.org/docs/14/protocol-flow.html#PROTOCOL-ASYNC">处</ulink>找到。<productname>Pgpool-II</productname> 从多个 <productname>PostgreSQL</productname> 服务器收集 ParameterStatus 值，并且这些值在不同服务器之间可能不同。一个典型的例子是 <literal>in_hot_standby</literal>，它在 <productname>PostgreSQL</productname> 14 中引入。该变量在主服务器上的值为 <literal>off</literal>，在备用服务器上的值为 <literal>on</literal>。问题在于，<productname>Pgpool-II</productname> 只能向客户端返回其中一个值。在这种情况下，它选择主服务器报告的值。因此 <function>PQParameterStatus</function> 将返回 <literal>off</literal>。另一方面，当客户端发出 <literal>show in_hot_standby</literal> 时，返回值可以是 <literal>on</literal> 或 <literal>off</literal>，具体取决于会话的负载均衡节点。
     </para>
     <para>
      请注意，如果各服务器之间的值不同，<productname>Pgpool-II</productname> 将发出日志消息，但 <literal>in_hot_standby</literal> 除外。这是为了防止日志文件被淹没，因为 <literal>in_hot_standby</literal> 总是不同的。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry>
    <term>set_config</term>
    <listitem>
     <para>
      <productname>PostgreSQL</productname> 具有 <function>set_config</function> 函数，该函数允许像 <command>SET</command> 命令一样更改当前会话中的参数值（实际上 <function>set_config</function> 比 SET 有更多功能。有关更多详细信息，请参阅 <productname>PostgreSQL</productname> 手册）。当 <productname>Pgpool-II</productname> 在 <link linkend="guc-backend-clustering-mode">集群模式</link>设置为 <literal>streaming_replication</literal> 的情况下运行时，它仅将该函数发送到主服务器。由于该函数未发送到备用服务器，因此各服务器之间的参数值不同。为避免此问题，您可以使用 <command>SET</command> 命令代替 <function>set_config</function>。由于 <command>SET</command> 命令会发送到本次会话使用的所有服务器，因此不会出现此问题。但是，如果您使用超过 2 个 <productname>PostgreSQL</productname> 服务器，则需要禁用 <xref linkend="guc-statement-level-load-balance"> 并使用 <command>SET</command> 命令。这是因为，如果启用了 <xref linkend="guc-statement-level-load-balance">，查询可能会发送到第三个服务器，此外还有主服务器和分配给负载均衡节点的服务器。
     </para>
     <para>
      如果您需要使用 <function>set_config</function>，请关闭会话的负载均衡（不仅针对 <function>set_config</function>，整个会话都应禁用负载均衡）。您可以通过牺牲性能来避免此问题。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </para>
</sect1>