<sect1 id="release-4-6-7">
 <title>发行版 4.6.7</title>
 <note>
  <title>发行日期</title>
  <simpara>2026-06-04</simpara>
 </note>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-05-26 [3a54a7818]
    -->
    <para>
     允许 <literal>pgproto</literal> 接受换行数据。(Tatsuo Ishii)
    </para>
    <para>
     现在 <literal>"\n"</literal> 会被识别为换行。这对于 <command>COPY</command> 命令是必需的。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [89de41c0d]
    -->
    <para>
     当主 watchdog 报告初始的 -2 哨兵值时，保留本地的 primary_node_id。(Tatsuo Ishii)
    </para>
    <para>
     修复当所有 pgpool 节点同时重启后出现的 watchdog 同步问题：备用节点可能用初始的 -2 哨兵值覆盖有效的 primary_node_id，
     并无限期地停留在未初始化状态。修复方法是，当主节点报告初始的 -2 哨兵值时，保留本地的 primary_node_id。
    </para>
    <para>
     问题由 Emond Papegaaij 和 Claude Code 报告。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/CAGXsc%2BZmBoLs3Mz%3DG-Bdm4JJG%2BfH1NpHfR3qVJVwW4eBKWwStQ%40mail.gmail.com">干净启动时的主节点检测竞争</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [87e033615]
    -->
    <para>
     修复不在 <function>elog(DEBUG)</function> 中调用 <function>pool_get_transaction_isolation()</function>。(Tatsuo Ishii)
    </para>
    <para>
     修复在 <function>where_to_send_main_replica()</function> 和 <function>where_to_send_native_replication()</function> 中
     由 DEBUG 日志记录引起的中止事务 FATAL 错误。
     DEBUG 日志记录调用了 <function>pool_get_transaction_isolation()</function>，而该函数在内部通过 <function>do_query()</function> 执行查询，
     可能会在中止的事务中引发 FATAL 错误。修复方法是，从 DEBUG 日志记录中移除这些调用。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-19 [62cd61602]
    -->
    <para>
     修复中止事务中的空查询情况。(Tatsuo Ishii)
    </para>
    <para>
     修复中止事务中的空查询被错误地引发错误的问题。空查询应当被允许出现在中止的事务中，
     但之前的某个提交导致 <function>check_transaction_state_and_abort()</function> 拒绝了它们。
     修复方法是，在 <function>SimpleQuery()</function> 中检测空查询并绕过该检查。
    </para>
    <para>
     问题由 Ronaldo Raivil 报告。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/161">pgpool &gt;= 4.3.2 在中止的保存点内对驱动健康检查探针合成 25P02 错误，破坏了需要调用 ROLLBACK TO SAVEPOINT 的 ORM</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-05-06 [969b454e4]
    -->
    <para>
     修复关于 <varname>ssl_dh_params_file</varname> 的描述。(Tatsuo Ishii)
    </para>
    <para>
     实际上，不仅当参数为空（默认值）时，当指定的参数不正确时，
     它也会回退到编译时内置的默认值。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-27 [855faebb0]
    -->
    <para>
     修复 PCP 主进程在重启时记住子进程 pid 的问题。(Tatsuo Ishii)
    </para>
    <para>
     在故障切换/故障恢复时，pgpool 主进程会向 PCP 主进程发送信号以请求重启，PCP 主进程随后重启。问题在于，当
     PCP 主进程重启时，它会忘记其子 PCP worker 进程的 pid 列表。

     结果，诸如 <command>pcp_promote_node</command> 之类的命令可能会无限期挂起。
     修复方法是，将 PCP worker 的 PID 存储在共享内存中，以便它们在 PCP 主进程重启后仍然存活。
    </para>
    <para>
     问题由 Taiki Koshino 报告。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260425.223051.1207744844622514060.ishii%40postgresql.org">关于 pcp 进程的问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-07 [5eb8fcbc3]
    -->
    <para>
     修复使用 <command>MERGE</command> 语句时查询缓存未能失效的问题。(Tatsuo Ishii)
    </para>
    <para>
     <command>MERGE</command> 是在 <productname>PostgreSQL</productname> 15 中引入的，
     而 <productname>Pgpool-II</productname> 4.4 引入了 <productname>PostgreSQL</productname> 15 的解析器。
     然而，查询缓存子模块存在一个疏忽：
     <command>MERGE</command> 语句没有触发查询缓存的失效。
    </para>
    <para>
     问题由 Nadav Shatz 报告。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260407.090802.1335576118037412011.ishii%40postgresql.org">回复：关于内存中的近期变更表跟踪的提案</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-04-06 [0316f828c]
    -->
    <para>
     修复 SSL 模块中的内存泄漏。(Tatsuo Ishii)
    </para>
    <para>
     修复当提供无效的 DH 参数时 <function>load_dh_file()</function> 中的内存泄漏问题。
     该函数未能释放由 <function>PEM_read_DHparams()</function> 返回的内存。
     同时更新同一文件中的版权声明，以保留原有的 <productname>PostgreSQL</productname> 版权。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260403.091842.102589579921239540.ishii%40postgresql.org">SSL 模块中的内存泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-31 [51ffc75b2]
    -->
    <para>
     修复 <command>SET transaction_read_only = 0</command> 的错误路由。(Bo Peng)
    </para>
    <para>
     修复将 <command>SET transaction_read_only = 0</command> 错误地路由到备用节点，从而导致查询错误的问题。
     同时将错误的 bool ret = POOL_BOTH 替换为正确的枚举类型。
    </para>
    <para>
     问题由 liujinyang-highgo 报告。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/155">set 命令 'set transaction_read_only = 0;' 被路由到备用节点并执行失败。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-25 [4a522dda0]
    -->
    <para>
     移除 where_to_send_main_replica() 中不必要的 STREAM 检查。(Tatsuo Ishii)
    </para>
    <para>
     该检查已在 check_replication_delay() 中完成。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-20 [39a433adf]
    -->
    <para>
     修复会话本地关系缓存用例的错误。(Tatsuo Ishii)
    </para>
    <para>
     修复 <function>pool_search_relcache()</function> 中会话本地关系缓存处理不正确的问题。
     临时表信息可能被错误地注册到共享关系缓存中，导致后续查询
     使用过期的缓存条目，并可能错误地路由查询。
     修复方法是，避免将会话本地缓存条目的临时表信息注册到共享关系缓存中。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/154">查询临时表被路由到备用节点。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-19 [a804cc786]
    -->
    <para>
     修复 <function>pool_search_relcache()</function> 中的锁释放泄漏。(Tatsuo Ishii)
    </para>
    <para>
     修复 <function>pool_search_relcache()</function> 中的锁释放泄漏问题。
     在共享关系缓存更新期间，重新获取排他锁后锁状态未被正确重置，
     这可能导致共享内存锁一直被持有。同时将令人困惑的变量 <varname>locked</varname> 重命名为 <varname>locked_by_others</varname>，
     并修复不必要的 <varname>per_node_statement_log</varname> 输出。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260317.182615.1046517700474336117.ishii%40postgresql.org">pool_search_relcache 中的锁释放泄漏</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-03-09 [552c40db7]
    -->
    <para>
     在 fork 之前关闭监听套接字。(Tatsuo Ishii)
    </para>
    <para>
     修复 fork 出的子进程继承了不必要的监听套接字的问题。
     此前，诸如 pcp_main、健康检查以及 lifecheck 等子进程继承了它们不会使用的 pgpool 和/或 PCP 监听套接字，
     这可能会阻止后续的 pgpool 启动时正确绑定端口。
     修复方法是，在 fork 之后使用 <function>close_listening_sockets()</function> 关闭不必要的监听套接字。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20260302.100028.1346768433787074248.ishii%40postgresql.org">在 fork 之前关闭监听套接字</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-03-04 [47835089c]
    -->
    <para>
     文档：修复 <varname>ignore_leading_white_space</varname> 和 <varname>allow_sql_comments</varname> 的文档。(Tatsuo Ishii)
    </para>
    <para>
     它们缺少关于默认值的描述。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-03-25 [84e850ad0]
    -->
    <para>
     测试：为上一个提交添加 001.load_balance 测试。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-6">
 <title>发行版 4.6.6</title>
 <note>
  <title>发行日期</title>
  <simpara>2026-02-26</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-01-29 [118f61932]
    -->
    <para>
     允许并行 <command>make</command>。(Tatsuo Ishii)
    </para>
    <para>
     此前，并行 <command>make</command> 会失败，错误如下：
     <programlisting>
In file included from parser.c:27:
../../src/include/parser/gramparse.h:33:10: fatal error: gram.h: No such file or directory
33 | #include "gram.h"
     </programlisting>
    </para>
    <para>
     补丁由 anthonyroussel 创建，并由 Tatsuo Ishii 审查。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/pull/146">修复并行构建</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-26 [d638018ce]
    -->
    <para>
     添加 <xref linkend="guc-backend-hostname"> 与 <varname>primary_conninfo</varname> 主机不匹配时的详细日志。(Taiki Koshino)
    </para>
    <para>
     如果 <filename>pgpool.conf</filename> 中设置的 <xref linkend="guc-backend-hostname"> 与
     <productname>PostgreSQL</productname> 的 <varname>primary_conninfo</varname> 中的主机设置不匹配，
     将输出以下日志消息。
     <programlisting>
sr_check_worker pid 25099: DETAIL: pgpool.conf backend_hostname: "pgpool0" does not match PostgreSQL primary_conninfo host: "pgpool_logtest"
     </programlisting>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-12-24 [ef33aca38]
    -->
    <para>
     显式声明支持的最低 OpenSSL 版本。(Tatsuo Ishii)
    </para>
    <para>
     这可以在 configure.ac 中通过以下方式完成
     <programlisting>
AC_DEFINE(OPENSSL_API_COMPAT, [0x10002000L],
          [Define to the OpenSSL API version in use. This avoids deprecation warnings from newer OpenSSL versions.])
     </programlisting>

     支持的最低版本是 1.0.2，这与当前的
     <productname>PostgreSQL</productname> 相同。（实际上该 AC_DEFINE 是从
     <productname>PostgreSQL</productname> 引入的）。此外，如果
     你安装的 OpenSSL 较新（如 3.0），这也可以避免 OpenSSL 的弃用警告。
     另外，在安装文档中补充说明所需的最低
     OpenSSL 版本为 1.0.2。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2026-01-31 [55ee45801]
    -->
    <para>
     修复 <function>check_transaction_state_and_abort()</function> 中的疏忽。(Tatsuo Ishii)
    </para>
    <para>
     该函数没有考虑 frontend 为 NULL 的情况。
    </para>
    <para>
     问题由 Coverity 指出。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-22 [c0f2cb883]
    -->
    <para>
     修复 <function>walker()</function> 函数以提前返回。(Tatsuo Ishii)
    </para>
    <para>
     此前，<function>walker()</function> 函数一旦找到目标对象就会返回 false。
     但其实可以在遍历中短路，而且本应如此。

     另外，从 <productname>PostgreSQL</productname> 引入注释以澄清 walker 函数的
     设计。
    </para>
    <para>
     问题由 liujinyang-highgo 报告，并由 Tatsuo Ishii 审查。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/143">函数 view_walker() 中的处理不当。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-09 [7b00d24de]
    -->
    <para>
     修复 <literal>scram-sha-256</literal> 认证中的内存泄漏。(Tatsuo Ishii)
    </para>
    <para>
     问题由 Coverity 指出。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-07 [81e8d3ec6]
    -->
    <para>
     修复检查 <function>malloc()</function> 的返回值。(Tatsuo Ishii)
    </para>
    <para>
     <function>create_inet_domain_sockets()</function> 在分配套接字 fd 的内存时忘记检查
     <function>malloc()</function> 的返回值。
    </para>
   </listitem>

   <listitem>
    <!--
    2026-01-06 [2218b5e56]
    -->
    <para>
     修复健康检查进程无法启动的问题。(Tatsuo Ishii)
    </para>
    <para>
     此前，如果后端节点处于宕机状态，<function>pgpool_main()</function> 不会启动健康检查进程。
     这导致 auto_failback 无法工作。
     当未提供 <option>-D</option> 选项时，Pgpool 从 <varname>pool_status</varname> 文件读取状态。
     如果文件中记录的状态是 "down"，pgpool 将不会为该节点启动健康检查进程。
     由于 auto_failback 是从健康检查进程调用的，这意味着 auto_failback 对后端节点不起作用。

     修复方法是，无条件启动健康检查进程。
    </para>
    <para>
     问题由 Meng Feilong 报告，并由 Tatsuo Ishii 审查。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/140">处于宕机状态的节点无法故障恢复</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-24 [9acb78c01]
    -->
    <para>
     修复 nodes.h 中重复的 NodeTag 枚举。(Tatsuo Ishii)
    </para>
    <para>
     修复了从 <productname>PostgreSQL</productname> 引入的 nodes.h 中 NodeTag 定义与 Pgpool-II 自身定义之间的重复问题。
    </para>
    <para>
     问题由 liujinyang-highgo 报告并诊断，由 Tastuo Ishii 审查。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/138">关于 NodeTag 定义的问题。</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-23 [77096f651]
    -->
    <para>
     修复 <function>pcp_connect()</function> 中的资源泄漏。(Tatsuo Ishii)
    </para>
    <para>
     该函数在一些地方没有调用 <function>freeaddrinfo()</function>。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-23 [515d5fe85]
    -->
    <para>
     修复可能的整数溢出。(Tatsuo Ishii)
    </para>
    <para>
     在 <function>pool_write_flush()</function> 和 <function>pool_flus_it()</function> 中使用了 <function>write(2)</function>。<function>write(2)</function>
     返回 ssize_t，而代码中有一些地方将返回值
     赋给了 int 变量。因此返回值有可能被
     求值为负值，从而导致错误。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-5">
 <title>发行版 4.6.5</title>
 <note>
  <title>发行日期</title>
  <simpara>2025-12-15</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-12-08 [5bd10177c]
    -->
    <para>
     在 pgpool_setup 中使用 "grep -E" 替代已弃用的 "egrep"。(Bo Peng)
    </para>
    <para>
     将 "egrep" 替换为 "grep -E"，以避免在较新的 GNU grep 版本上出现过时警告。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-12-08 [8deccc15e]
    -->
    <para>
     修复 show pool_status 中的缓冲区溢出错误。(Tatsuo Ishii)
    </para>
    <para>
     "show pool_status"、"pgpool show" 和 pcp_pool_status 可能导致
     缓冲区溢出错误。如果 backend_flag 被设置为 "ALWAYS_PRIMARY"，
     负责生成 backend_flag 可打印格式的 pool_flag_to_str()，
     将数据写入到了静态缓冲区的末尾之外。
    </para>
    <para>
     问题由 zam bak 报告，并由 Bo Peng 审查。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20251202.140205.427777414210613577.ishii%40postgresql.org">回复：运行 SHOW POOL_STATUS 时检测到 "buffer overflow detected"</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-12-02 [c8865c703]
    -->
    <para>
     修复 "show pool_status" 中显示的 memqcache_stats_start_time。(Tatsuo Ishii)
    </para>
    <para>
     get_config() 使用 ctime() 生成 memqcache_stats_start_time 的可打印格式。但它没有考虑到
     ctime() 会在结果末尾添加换行符。结果，不仅
     memqcache_stats_start_time 的输出带有不必要的换行符，
     下一行还打印了空项。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20251130.102712.131456481338876013.ishii%40postgresql.org">回复：运行 SHOW POOL_STATUS 时检测到 "buffer overflow detected"</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-11-25 [0c45a1e01]
    -->
    <para>
     测试：稳定 037.failover_session。(Tatsuo Ishii)
    </para>
    <para>
     在某些平台（就我而言是运行在 VitualBox 上的 Rocky Linux 10）上，
     test1 之后无法完成 shutdownall。这可能是因为测试中的故障切换未能恢复
     信号处理器，而该处理器应接受来自 shutdownall 的关闭信号。在 shutdownall 之前添加 "sleep 5"
     似乎可以缓解该问题。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-4">
 <title>发行版 4.6.4</title>
 <note>
  <title>发行日期</title>
  <simpara>2025-11-25</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-11-07 [4fabca331]
    -->
    <para>
     移除不必要的 <varname>application_name</varname> 处理。(Tatsuo Ishii)
    </para>
    <para>
     之前的某个提交添加了在重用现有连接时设置 <varname>application_name</varname> 的步骤。
     然而，这是不必要的，因为 DISCARD ALL 会自动重置 <varname>application_name</varname>，
     而且 <function>send_params()</function> 已经向前端发送了所有必要的参数状态消息，包括 <varname>application_name</varname>。
     移除这个多余的步骤还能提高性能，尤其是对于地理上相距较远的后端节点。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/130">在异地分布式配置中 pgPool 的性能严重下降</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-10-09 [f91abf862]
    -->
    <para>
     使时间计算始终使用 long long。(Tatsuo Ishii)
    </para>
    <para>
     此前，<productname>Pgpool-II</productname> 将 time_t 视为普通的 long，这会在某些系统上引起编译时警告，
     例如 OpenBSD，其 time_t 是 __int64。此更改将计算提升为 long long，
     防止截断，并确保 pgpool 在 time_t 干净的 64 位系统上于 Y2038 之后仍能正常工作。
     此外，json_get_long_value_for_key 已改为 json_get_llong_value_for_key，
     使用 long long 参数，使其与 _json_value 的 int64 类型保持一致。这改善了
     32 位平台上的整数处理，并有助于避免潜在的整数溢出问题。
    </para>
    <para>
     问题由 Tatsuo Ishii 和 Gyorgy Sarvari 分析。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004584.html">修复 OpenBSD 上的 time_t 警告</ulink>
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20251003.211957.2067537305399895611.ishii%40postgresql.org">修复 OpenBSD 上的 time_t 警告</ulink>
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/128">修复 32 位目标的编译问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-31 [dced45a52]
    -->
    <para>
     允许针对 gcc 15 (C23) 编译。(Tatsuo Ishii)
    </para>
    <para>
     此更改包含若干修复，以使 <productname>Pgpool-II</productname> 能在 Fedora 42 上使用 gcc 15 (C23) 编译。
     它更新了 pool_type.h 以使用 stdbool.h，并将 TRUE/FALSE 定义为 (bool) 1/0，与 C99 标准保持一致。
     函数指针参数已修正以匹配其原型，
     并更新了 <function>pool_create_relcache()</function> 的原型。
     <function>raw_expression_tree_walker()</function> 中对 <function>walker()</function> 的调用已调整为通过 WALK 宏将第一个参数转换为 (Node *)。
     请注意，Fedora 42 上的 OpenSSL 警告仍然存在，将在未来处理。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/124">针对 GCC 15 的 4.6.X 构建问题</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-11-16 [b113027dd]
    -->
    <para>
     修复 <command>CopyOut</command> 时的段错误。(Tatsuo Ishii)
    </para>
    <para>
     当在扩展查询协议模式下执行 <command>COPY relname TO STDOUT</command> 时，pgpool 发生段错误。
    </para>
    <para>
     此问题由 <ulink url="https://github.com/tetesh">https://github.com/tetesh</ulink> 报告。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/133">datanymizer 和 pg-pool 段错误</ulink>
    </para>
   </listitem>
   <listitem>
    <!--
    2025-10-03 [b84553bf0]
    -->
    <para>
     在某些极端情况下防止 watchdog 出现脑裂场景。(Tatsuo Ishii)
    </para>
    <para>
     Watchdog 使用信标消息和心跳两种方式进行通信。
     如果心跳尚未激活，超过 30 秒缺失信标消息
     可能导致脑裂场景，
     即旧的主节点仍然活跃，同时又选举出了新的主节点。
     此更改通过撤销旧的主节点来防止同时存在两个主节点。
     当一个节点检测到它已超过两次未收到来自主节点的信标消息时，
     该主节点被设置为 LOST，并通过 set_state(WD_JOINING) 触发新的主节点选举。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-10-03 [ef8ccb9e7]
    -->
    <para>
     防止在使用不存在的已预处理语句时出现 FATAL 错误。(Tatsuo Ishii)
    </para>
    <para>
     此前，当使用不存在的已预处理语句时，<function>Bind()</function> 会引发 FATAL 错误，这与 PostgreSQL 不同。
     此更改使 <function>Bind()</function> 检查语句是否存在，如果不存在则向前端发送 ERROR 消息。
     请注意，不会写入日志，因为目前尚无法记录此信息。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-24 [59a359eef]
    -->
    <para>
     修复同时提供 -p 和 -P 时 <xref linkend="pg-enc"> 无法工作的问题。(Tatsuo Ishii)
    </para>
    <para>
     原始报告显示，运行 pg_enc -p -P 会提示输入密码和加密密钥，
     但如果没有提供密钥则会失败。
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/7f18c30b.237.1997555ca11.Coremail.liujy%40highgo.com">pg_enc</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-18 [f20f7041c]
    -->
    <para>
     修复 32 位环境下的编译问题。(Tatsuo Ishii)
    </para>
    <para>
     在 32 位系统上，编译 src/parser/snprintf.c 会因来自 math.h 的未定义函数 <function>isnan()</function> 和 <function>isinf()</function> 而失败。
     此更改包含了 math.h，并重新排列了包含文件以使其更清晰。
     该文件最初是从 PostgreSQL 引入的，其中已包含 math.h。
     此修复通过拉取请求提供：https://github.com/pgpool/pgpool2/pull/128
    </para>
    <para>
     讨论：<ulink url="https://www.postgresql.org/message-id/20250917.194736.353755422175293639.ishii%40postgresql.org">32 位目标的编译问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-10 [0a6868d40]
    -->
    <para>
     修复 recovery_1st_stage.sample 中 Bash 目录删除更安全的问题。(Taiki Koshino)
    </para>
    <para>
     引用 rm 命令中的变量以避免意外删除：
     <command>rm -rf "${DEST_NODE_PGDATA}"</command>

     如果变量为空，rm 可能删除意外的文件或目录。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-05 [1793db95f]
    -->
    <para>
     修复查询缓存失效中无意义的警告。(Tatsuo Ishii)
    </para>
    <para>
     当禁用 memcached 支持时，由查询引起的查询缓存失效
     会输出无意义的警告。这导致 006.memcached 回归测试
     失败。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-09-05 [441f338ab]
    -->
    <para>
     修复在没有 <option>--with-memcached</option> 选项的情况下构建时的编译错误。(Tatsuo Ishii)
    </para>
    <para>
     在未配置 <option>--with-memcached</option> 时发生了编译错误。
     问题在于编译器无法确定 ptr 只会在启用 memcached 时被释放。
     为解决此问题，相关代码块被包在 #ifdef USE_MEMCACHED 中。
    </para>
    <para>
     问题由 Bo Peng 报告。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-09-04 [9e0324575]
    -->
    <para>
     修复查询缓存锁文件处理。(Tatsuo Ishii)
    </para>
    <para>
     查询缓存模块在 <varname>logdir</varname> 中创建一个锁文件用于并发控制，
     但存在两个错误：pgpool 主进程可能创建一个多余的 "QUERY_CACHE_LOCK_FILE"，
     并且锁文件在关闭时没有被移除。此更改修复了这两个问题。
    </para>
    <para>
     问题由 Bo Peng 报告并分析。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-09-17 [1a975b539]
    -->
    <para>
     更新 <ulink url="https://www.pgpool.net/docs/latest/ja/html/example-cluster.html">8.2. Pgpool-II + Watchdog 安装示例</ulink> 中关于 "设置 PostgreSQL 备用服务器" 的文档。(Taiki Koshino)
    </para>
    <para>
     添加一条说明：在没有在线恢复的情况下设置备用服务器时，不要将 <varname>primary_conninfo</varname> 写入 postgresql.auto.conf。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/67">Follow primary 命令不修复 postgresql.auto.conf</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-22 [5bd535792]
    -->
    <para>
     修复 watchdog_setup 手册。(Tatsuo Ishii)
    </para>
    <para>
     它错误地声称 watchdog_setup 中没有设置心跳。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-09-30 [3ddbc64f1]
    -->
    <para>
     使 023.ssl_connection 适配 PostgreSQL 18。(Tatsuo Ishii)
    </para>
    <para>
     PostgreSQL 18 大幅更改了 psql 的 \conninfo 输出格式，这
     导致测试失败，因为测试依赖于 \conninfo。此更改
     使测试脚本感知 PostgreSQL 版本以解决问题。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-31 [3897b7afd]
    -->
    <para>
     修复 039.log_backend_messages。(Tatsuo Ishii)
    </para>
    <para>
     提交 8ff2b9f6e 错误地将同步提交参数放入了
     pgpool.conf。通过将这些参数放入
     postgresql.conf 来修复测试。同时检查集群模式是否为流式
     复制。因为如果集群模式不是流式复制，这些参数会导致 PostgreSQL 挂起。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-31 [0f19fc60a]
    -->
    <para>
     稳定 039.log_backend_messages 测试。(Tatsuo Ishii)
    </para>
    <para>
     在该测试中，向主服务器插入行后会立即向备用服务器发送查询。
     由于复制延迟，在较慢的机器上可能无法在备用服务器上找到插入的行。此
     更改尝试通过使用带 <varname>remote_apply</varname> 选项的同步复制来解决问题。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-29 [b4043e6c3]
    -->
    <para>
     修复 010.rewrite_timestamp 中的 ruby 脚本。(Tatsuo Ishii)
    </para>
    <para>
     ruby 脚本使用了 "File.exists"，据说在较新版本的 Ruby 中已被弃用。
     将其替换为 "File.exist"。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-29 [9c901e60a]
    -->
    <para>
     调整 034 和 075 测试中的超时。(Tatsuo Ishii)
    </para>
    <para>
     034.promote_node 和 075.detach_primary_left_down_node 为完成 follow primary 脚本设置了
     60 秒的超时。结果表明这些超时值不够长，有时会引发
     错误的错误。因此将它们从 60 秒改为 120 秒。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-27 [42b177614]
    -->
    <para>
     为 023.ssl_connection 添加 ssl_ecdh_curve 测试。(Tatsuo Ishii)
    </para>
    <para>
     023.ssl_connection 没有覆盖 ssl_ecdh_curve 的测试。此
     提交使用错误的 ssl_ecdh_curve 参数对其进行测试，以查看前端与 pgpool 之间的
     连接是否会失败。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-3">
 <title>发行版 4.6.3</title>
 <note>
  <title>发行日期</title>
  <simpara>2025-08-21</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-06-13 [ebfd2423c]
    -->
    <para>
     增强连接后端的进程。(Tatsuo Ishii)
    </para>
    <para>
     在某些环境下（尤其是 k8s），DNS 查找不稳定，导致连接后端进程失败。
     这发生在 <function>connect_inet_domain_socket_by_port()</function> 中调用
     <function>getaddrinfo()</function> 时。为改善这种情况，如果
     <function>getaddrinfo()</function> 以 EAI_AGAIN 失败，则最多重试 5 次（每次重试时睡眠 1 秒）。请注意，如果
     以 "retry" 参数为 false 调用
     <function>connect_inet_domain_socket_by_port()</function>，则不会发生重试。健康检查以 retry 标志为 false 调用
     <function>connect_inet_domain_socket_by_port()</function>，
     以便重试由健康检查自身的参数控制。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/104">单次 DNS 查找失败会触发后端故障切换</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-07-25 [75d905ff9]
    -->
    <para>
     修复 watchdog 打印不恰当的 NOTICE 消息。(Tatsuo Ishii)
    </para>
    <para>
     <function>read_ipc_socket_and_process()</function> 每次向 IPC 套接字写入命令时都会打印一条 notice 消息，即使写入成功也是如此。将其修复为仅在写入失败时
     打印 notice 消息。

     该错误未被发现的原因是，该消息仅在
     <varname>log_min_messages</varname> 设置为 notice 或更高级别时才会出现。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/121">pgpool IPC 套接字连接问题</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-07-19 [d05de5c73]
    -->
    <!--
    2025-07-19 [ed57af34e]
    -->
    <para>
     修复读取启动包时的资源泄漏。(Tatsuo Ishii)
    </para>
    <para>
     修复 pool_push_pending_data 中的资源泄漏。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-30 [bac455b2f]
    -->
    <para>
     修复大端机器上损坏的 scram-sha-256 认证。(Tatsuo Ishii)
    </para>
    <para>
     执行 <literal>scram-sha-256</literal> 认证时，会使用哈希函数
     <function>pg_sha_256_final</function>。它从 <productname>PostgreSQL</productname> 引入，使用
     预处理宏 WORDS_BIGENDIAN 来判断主机的大端小端。
     虽然 configure 时应该定义 WORDS_BIGENDIAN，但从 <productname>PostgreSQL</productname> 引入
     <function>pg_sha_256_final</function>（及其它函数）时漏掉了这一部分。结果，
     <literal>scram-sha-256</literal> 只能在小端机器上工作。通过在 configure.ac 中添加
     AC_C_BIGENDIAN 宏修复了此问题。
    </para>
    <para>
     问题由 Christoph Berg 报告，由 pranavkaruvally 分析。
    </para>
    <para>
     讨论：<ulink url="https://github.com/pgpool/pgpool2/issues/106">4.6.1 在大端 s390x 上失败：后端响应 kind 'E' 而期望 'R'</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-23 [2721c7b76]
    -->
    <para>
     修复源代码中的拼写错误。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-14 [f88b1c18f]
    -->
    <para>
     修复心跳设备处理。(Tatsuo Ishii)
    </para>
    <para>
     修复 <function>wd_create_hb_recv_socket()</function> 和 <function>wd_create_hb_send_socket()</function>，
     使其向 setsockopt(SO_BINDTODEVICE) 传递以空字符结尾的设备名字符串，
     而不是错误地传递 struct ifreq。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004603.html">[pgpool-hackers: 4602] 心跳和 SO_BINDTODEVICE</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-14 [73bcbd96e]
    -->
    <para>
     修复心跳接收进程中的资源泄漏。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-09 [4305c42d6]
    -->
    <para>
     修复心跳设备处理。(Tatsuo Ishii)
    </para>
    <para>
     处理 <filename>pgpool.conf</filename> 时，<varname>heartbeat_device</varname> 被错误地处理，
     导致第一个设备被忽略。
    </para>
    <para>
     问题由 Bo Peng 分析。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-05 [b249a118c]
    -->
    <para>
     修复心跳接收器无法工作的问题。(Tatsuo Ishii)
    </para>
    <para>
     IPv6 对心跳的支持是在 4.6 版本中新增的，
     但接收进程错误地只绑定到回环地址，
     导致无法接收来自其他节点的数据。
     在 <function>getaddrinfo()</function> 中添加 AI_PASSIVE 标志可通过绑定到所有网络接口来修复此问题。
    </para>
    <para>
     问题由 Bo Peng 分析。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-02 [06ba7dde4]
    -->
    <para>
     修复 <filename>pgpool.conf</filename> 注释中的拼写错误。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-08-15 [a54f5d1ad]
    -->
    <para>
     增强 "8.3. 复制模式与快照隔离模式配置示例" 文档。(Taiki Koshino)
    </para>
    <para>
     更新了示例脚本的链接以及 "8.3.3. 开始之前" 部分的说明。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-04 [e2f24fec8]
    -->
    <para>
     修复无法通过重载反映的参数的文档。(Taiki Koshino)
    </para>
    <para>
     "authentication_timeout" 和 "memqcache_oiddir" 无法通过重载反映。
     文档已改为 "此参数只能在服务器启动时设置。"。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-07-24 [bc5fa5e7e]
    -->
    <!--
    2025-07-23 [1e8963554]
    -->
    <para>
     统一 watchdog 主节点术语（后续）。 (Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-08-06 [31eee18d7]
    -->
    <!--
    2025-07-16 [34b07c2d5]
    -->
    <para>
     修复示例脚本链接。(Taiki Koshino)
    </para>
    <para>
     修改了 "<ulink url="https://www.pgpool.net/docs/46/en/html/example-cluster.html">8.2. Pgpool-II + Watchdog 安装示例</ulink>" 一节中的示例脚本
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-18 [f4ae9f39e]
    2025-06-18 [7a20dc419]
    -->
    <para>
     修复负载均衡说明遗漏了逻辑复制模式和 Slony 模式的问题。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-18 [9d2e4e692]
    -->
    <para>
     增强 <xref linkend="pcp-node-info"> 文档。(Bo Peng)
    </para>
    <para>
     澄清每个 <varname>backend_application_nameX</varname> 必须与 <varname>primary_conninfo</varname> 的
     <varname>application_name</varname> 中指定的值匹配，才能正确显示
     "<varname>replication_state</varname>" 和 "<varname>replication_sync_state</varname>"。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-08 [59f9df005]
    -->
    <para>
     添加内核资源一节。(Tatsuo Ishii)
    </para>
    <para>
     <productname>Pgpool-II</productname> 使用 System V 共享内存和信号量。最好在文档中描述这些要求。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-07 [40617fd90]
    -->
    <para>
     为 configure 的 <option>--with-ldap</option> 选项添加说明。(Tatsuo Ishii)
    </para>
    <para>
     在 v4.2 中引入 LDAP 支持时遗漏了该说明。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-05 [9ee9eb164]
    -->
    <para>
     修复 "8.2. <productname>Pgpool-II</productname> + Watchdog 安装示例" 中的命令以转义 $PGDATA。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-04 [ff60bf930]
    -->
    <para>
     澄清 <productname>Pgpool-II</productname> 支持的平台。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2025-06-03 [cc4da5979]
    -->
    <para>
     增强 <xref linkend="guc-child-life-time"> 文档。(Tatsuo Ishii)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-06-08 [78e45decf]
    -->
    <para>
     稳定 029.cert_passphrase 回归测试。(Tatsuo Ishii)
    </para>
    <para>
     当 <varname>ssl_passphrase_command</varname> 无效时，错误消息通常是 "bad decrypt"，
     但有时似乎是 "wrong tag"。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-2">
 <title>发行版 4.6.2</title>
 <note>  <title>发行日期</title>
  <simpara>2025-05-30</simpara>
 </note>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-17 [2c84c4358]
    -->
    <para>
     在认证失败时抑制不必要的信息。(Tatsuo Ishii)
    </para>
    <para>
     此前，当客户端认证失败时会显示消息 "password size does not match"。
     这可能帮助攻击者猜测密码。将其替换为仅 "password does not match"。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-15 [f49d239a4]
    -->
    <para>
     允许 pcp 客户端连接到 IPv6 地址。(Tatsuo Ishii)
    </para>
    <para>
     我们已经允许 pcp 服务器连接到 IPv6 地址，但
     直到今天，pcp 客户端还不被允许连接到它们。此
     提交允许 pcp 客户端连接到 IPv6 地址。

     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-May/009484.html">[pgpool-general: 9481] pgpool 4.6.0 是否支持纯 ipv6 配置？</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-27 [61223a1e4]
    -->
    <para>
     修复在没有 IPv6 的情况下创建 watchdog 接收套接字的问题。(<ulink url="https://github.com/pgpool/pgpool2/issues/99">#99</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     当 IPv6 网络不可用时，watchdog 进程可能无法启动。此前，<function>wd_create_recv_socket()</function>
     在创建或处理 IPv6 套接字失败时会发出 elog(ERROR)。
     不幸的是，在调用 <function>wd_create_recv_socket()</function> 时，异常栈尚未建立，elog 会
     愉快地将 <literal>ERROR</literal> 转换为 <literal>FATAL</literal>，
     这会导致 watchdog 进程退出，从而退出 pgpool 进程。
     为修复此问题，将 elog(ERROR) 调用改为 elog(LOG)。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-1">
 <title>发行版 4.6.1</title>
 <note>
  <title>发行日期</title>
  <simpara>2025-05-15</simpara>
 </note>

 <sect2>
  <title>概述</title>
  <para>
   本版本包含一项安全修复。
  </para>
  <para>
   <productname>Pgpool-II</productname> 的客户端认证机制中存在一个认证绕过漏洞。
   在 <productname>Pgpool-II</productname> 中，即使本应强制执行认证，认证也可能被绕过。结果，攻击者可以以
   任意用户的身份登录，可能导致信息泄露、数据篡改、
   甚至数据库的完全关闭。(CVE-2025-46801)
  </para>
  <para>
   此漏洞影响认证配置与以下任一模式匹配的系统：
  </para>
  <itemizedlist>
   <listitem>
    <para>
     模式 1：当满足以下所有条件时，会发生此漏洞：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       <filename>pool_hba.conf</filename> 中使用了 <literal>password</literal> 认证方法
      </para>
     </listitem>
     <listitem>
      <para>
       allow_clear_text_frontend_auth = off
      </para>
     </listitem>
     <listitem>
      <para>
       用户的密码未设置在 <filename>pool_passwd</filename> 中
      </para>
     </listitem>
     <listitem>
      <para>
       <filename>pg_hba.conf</filename> 中使用了 <literal>scram-sha-256</literal> 或 <literal>md5</literal> 认证方法
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <para>
     模式 2：当满足以下所有条件时，会发生此漏洞：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       enable_pool_hba = off
      </para>
     </listitem>
     <listitem>
      <para>
       pg_hba.conf 中使用了以下认证方法之一：
       <literal>password</literal>、<literal>pam</literal> 或 <literal>ldap</literal>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>

   <listitem>
    <para>
     模式 3：当满足以下所有条件时，会发生此漏洞：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       使用原始模式（backend_clustering_mode = 'raw'）
      </para>
     </listitem>
     <listitem>
      <para>
       <filename>pool_hba.conf</filename> 中使用了 <literal>md5</literal> 认证方法
      </para>
     </listitem>
     <listitem>
      <para>
       allow_clear_text_frontend_auth = off
      </para>
     </listitem>
     <listitem>
      <para>
       用户的密码以明文或 AES 格式注册在 <filename>pool_passwd</filename> 中
      </para>
     </listitem>
     <listitem>
      <para>
       <filename>pg_hba.conf</filename> 中使用了以下认证方法之一：
       <literal>password</literal>、<literal>pam</literal> 或 <literal>ldap</literal>
      </para>
     </listitem>
    </itemizedlist>
   </listitem>
  </itemizedlist>
  <para>
   Pgpool-II 4.0 和 4.1 系列的所有版本、4.2.0 至 4.2.21、4.3.0 至 4.3.14、
   4.4.0 至 4.4.11、4.5.0 至 4.5.6 以及 4.6.0 均受此漏洞影响。
   强烈建议升级到 <productname>Pgpool-II</productname>
   4.6.1、4.5.7、4.4.12、4.3.15 和 4.2.22 或更高版本。
   或者，你可以修改设置，使其不与任何
   易受攻击的配置模式匹配。
  </para>
 </sect2>

 <sect2>
  <title>变更</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-02 [32dee13dd]
    -->
    <para>
     在配置文件中添加主版本信息。(Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-13 [d92a7e2c1]
    -->
    <para>
     修复某些情况下的错误客户端认证。(Tatsuo Ishii)
    </para>
    <para>
     如果 enable_pool_hba = on，其认证方法为 "password"，pool_passwd 中未注册密码，
     且 pg_hba.conf 中的认证方法为 "scram-sha-256" 或 "md5"，那么当客户端第一次连接到 pgpool 时，
     认证会按预期执行。但如果客户端连接到已缓存的连接，则客户端的任何密码都会被接受。
    </para>
    <para>
     此外，如果 enable_pool_hba = off，在某些情况下客户端第一次不会被要求输入密码，
     或者当客户端连接到缓存的连接时也不会被要求输入密码，即使本应如此。
    </para>
    <para>
     除了修复上述问题外，还进行了以下更改：
    </para>
    <itemizedlist>
     <listitem>
      <para>
       移除单一的 PostgreSQL 代码路径以简化认证代码。
      </para>
     </listitem>
     <listitem>
      <para>
       移除前端和后端的 crypt 认证支持。该功能从未被记录，也从未经过
       测试。而且 crypt 认证很早之前就已从 PostgreSQL（8.4，2009）中移除。
      </para>
     </listitem>
     <listitem>
      <para>
       添加新的回归测试 "040.client_auth"。该测试使用
       以 CSV 格式编写的测试规范文件执行详尽的客户端认证测试。
      </para>
     </listitem>
    </itemizedlist>
   </listitem>
   <listitem>
    <!--
    2025-05-08 [e3451b560]
    -->
    <para>
     修复查询缓存的长期存在的 bind 错误。(Tatsuo Ishii)
    </para>
    <para>
     当预处理命名语句时，可以在没有 parse 消息的情况下执行 bind 然后 execute。
     问题在于，在 execute 或 COMMIT 时使查询缓存失效所需的表 oid 只在 parse 消息处理（Parse()）中收集。
     因此，如果在之前的 execute 之后在没有 parse 的情况下执行 bind，则不会收集表 oid，pgpool 将无法使查询缓存失效。
     修复方法是在 bind 时也收集表 oid。
     为 006.memqcache 添加回归测试。
    </para>
    <para>
     问题由 Achilleas Mantzios 报告，测试程序也由其提供。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-April/009430.html">[pgpool-general: 9427] 关于查询结果缓存可见性的澄清</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-02 [1dfacffed]
    -->
    <para>
     修复查询缓存失效错误。(Tatsuo Ishii)
    </para>
    <para>
     当收到 execute 消息时，pgpool 检查其最大行数参数。如果它不为零，
     pgpool 设置 "partial_fetch" 标志以指示 pool_handle_query_cache() 不要创建查询缓存。
     问题在于，提交 2a99aa5d1 忽略了即使 INSERT/UPDATE/DELETE 也会将 execute 消息参数设置为非 0（大多为 1），
     因此 pgpool 对非 SELECT 也设置了该标志。这导致无法使查询缓存失效，因为如果该标志为 true，
     pool_handle_query_cache() 中的后续代码会跳过缓存失效。
     为修复此问题，更改 Execute() 在设置该标志之前检查查询是否为只读 SELECT。
     同时为 006.memqcache 添加测试。
    </para>
    <para>
     问题由 Achilleas Mantzios 报告，测试程序也由其提供。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-general/2025-April/009430.html">[pgpool-general: 9427] 关于查询结果缓存可见性的澄清</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-05-05 [5947ba418]
    -->
    <para>
     修复对 OpenBSD 的可移植性。(Tatsuo Ishii)
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-May/004583.html">[pgpool-hackers: 4582] 使 pgpool 能在 OpenBSD 上构建</ulink>
    </para>
    <para>
     补丁由 Martijn van Duren 创建。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-05-01 [14c94473b]
    -->
    <para>
     修复 json_writer 未能正确编码特殊字符的问题。(Bo Peng)
    </para>
    <para>
     如果 <xref linkend="guc-wd-authkey"> 包含特殊字符（例如反斜杠），当启用 watchdog 时 <productname>Pgpool-II</productname> 会崩溃。
    </para>
    <para>
     补丁由 Martijn van Duren 创建，并由 Bo Peng 稍作修改。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-03-04 [e1e32536f]
    -->
    <para>
      修复 sr 检查和健康检查在重载时重新打开 <xref linkend="guc-pool-passwd">。(Tatsuo Ishii)
    </para>
    <para>
      流式复制检查和健康检查进程在重载时忘记重新打开 <xref linkend="guc-pool-passwd">。
      如果 <xref linkend="guc-sr-check-password"> 或
      <xref linkend="guc-health-check-password"> 为空字符串，则从
      <xref linkend="guc-pool-passwd"> 获取密码。因此，这些进程在重载时会读取过期的 <xref linkend="guc-pool-passwd"> 内容。
    </para>
   </listitem>

   <listitem>
    <!--
    2025-04-27 [5f4d2d683]
    -->
    <para>
     修复禁用 IPv6 的系统中的心跳进程问题。(<ulink url="https://github.com/pgpool/pgpool2/issues/99">#99</ulink>) (Tatsuo Ishii)
    </para>
    <para>
     从 <productname>Pgpool-II</productname> 4.6.0 开始，心跳进程可以处理 IPv6 接收套接字。然而，如果系统中禁用了 IPv6，
     该进程无法正常工作。与 <productname>Pgpool-II</productname> 主进程和 <productname>PostgreSQL</productname> 一样，我认为如果 IPv4 可用，它应该能正常工作。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-April/004579.html">[pgpool-hackers: 4578] 修复心跳中的 IPv6</ulink>
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>文档</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-05-09 [057e1131c]
    -->
    <para>
     增强查询缓存文档。(Tatsuo Ishii)
    </para>
    <para>
     Pgpool 拒绝缓存调用返回 TIMESTAMP WITH TIMEZONE、TIME WITH TIMEZONE 的函数的查询。如果有多个同名函数，
     且其中一个返回 TIMESTAMP WITH TIMEZONE、TIME WITH TIMEZONE，那么即使其中有一个不返回这些数据类型，
     pgpool 也会拒绝缓存。因此，请添加一条关于此问题的说明以及解决方法。
    </para>
   </listitem>
   <listitem>
    <!--
    2025-04-24 [e50114280]
    -->
    <para>
     增强 <xref linkend="guc-connection-life-time"> 上的说明 (Tatsuo Ishii)
    </para>
    <para>
     <xref linkend="guc-connection-life-time"> 是决定到 <productname>PostgreSQL</productname> 后端的缓存连接生命周期的配置值。当前文档缺少一条
     说明，即过期计算实际上是在客户端断开与持有缓存连接的进程的连接时完成的。
    </para>
    <para>
     讨论：<ulink url="https://www.pgpool.net/pipermail/pgpool-hackers/2025-April/004578.html">[pgpool-hackers: 4577] 文档：增强 connection_life_time 上的说明</ulink>
    </para>
   </listitem>

   <listitem>
    <!--
    2025-03-05 [a3086943f]
    -->
    <para>
     增强 <xref linkend="guc-sr-check-user"> 上的说明。(Tatsuo Ishii)
    </para>
    <para>
     它必须是超级用户或 <literal>pg_monitor</literal> 组的成员。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2>
  <title>回归测试</title>
  <itemizedlist>
   <listitem>
    <!--
    2025-04-01 [7969146b7]
    2025-03-27 [05a727e8d]
    -->
    <para>
     允许回归测试使用多个套接字目录。(Taiki Koshino, Tatsuo Ishii, Bo Peng)
    </para>
   </listitem>
  </itemizedlist>
 </sect2>
</sect1>

<sect1 id="release-4-6-0">
 <title>发行版 4.6.0</title>
 <note>
  <title>发行日期</title>
  <simpara>2025-02-28</simpara>
 </note>

 <sect2>
  <title>概述</title>
  <para>
   本版本为 <productname>Pgpool-II</productname> 的日志记录和管理
   添加了便捷的功能。现在，你可以通过使用新的 PCP
   命令在不重启或不向 <productname>Pgpool-II</productname> 发送信号的情况下
   轮转日志。<xref linkend="guc-logging-collector"> 相关参数
   可以通过重新加载配置文件来更改。此外，还添加了新的 PCP
   命令，可以在不重启 <productname>Pgpool-II</productname> 的情况下使查询缓存失效。
   查询缓存的其它功能包括强制创建查询缓存条目以及
   移除特定的查询缓存条目。新的配置参数
   <xref linkend="guc-log-backend-messages"> 允许记录来自后端的消息，
   以便更轻松地分析 <productname>Pgpool-II</productname>
   与 <productname>PostgreSQL</productname> 之间的通信。
   SQL 解析器已更新，与最新的 <productname>PostgreSQL
   17</productname> 版本保持同步，这与以往一样。最后，现在你可以为
   watchdog 的 hostname 和 heartbeat_hostname 参数使用 IPv6。
  </para>

  <para>
   主要增强如下：
  </para>

  <itemizedlist>

   <listitem>
    <para>
     用于触发日志轮转的<link linkend="pcp-log-rotate">新 PCP 命令</link>。
    </para>
   </listitem>

   <listitem>
    <para>
     允许通过重新加载来更改 <xref linkend="guc-logging-collector"> 相关参数。
    </para>
   </listitem>

   <listitem>
    <para>
     用于使查询缓存失效的<link linkend="pcp-invalidate-query-cache">新 PCP 命令</link>。
    </para>
   </listitem>

   <listitem>
    <para>
     允许强制创建<link linkend="runtime-in-memory-query-cache">查询
     缓存</link>
    </para>
   </listitem>

   <listitem>
    <para>
     新的 <link linkend="sql-pgpool-set-cache">PGPOOL SET CACHE DELETE</link> 命令。
    </para>
   </listitem>

   <listitem>
    <para>
     新的配置参数 <xref linkend="guc-log-backend-messages">。
    </para>
   </listitem>

   <listitem>
    <para>
     引入 PostgreSQL 17 的 SQL 解析器。
    </para>
   </listitem>

   <listitem>
    <para>
     为 watchdog 的 <xref linkend="guc-hostname">
     和 <xref linkend="guc-heartbeat-hostname"> 参数添加 IPv6 支持。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2 id="migration-4-6-0">
  <title>迁移到版本 4.6</title>
  <itemizedlist>

   <listitem>
    <para>
     <xref linkend="guc-health-check-user">、<xref linkend="guc-recovery-user">、
     <xref linkend="guc-sr-check-user">
     和 <xref linkend="guc-wd-lifecheck-user"> 的默认值已从
     'noboy' 改为 ''（空字符串）。你需要在
     4.6 中设置它们，否则会收到错误。
    </para>
   </listitem>

   <listitem>
    <para>
     忽略字符串列表类型配置参数中的前导/尾随空格，例如，<xref linkend="guc-unix-socket-directories">
     和 <xref linkend="guc-pcp-socket-dir">。
    </para>
   </listitem>

   <listitem>
    <para>
     当子进程因 <xref linkend="guc-child-life-time">
     或 <xref linkend="guc-child-max-connections"> 而退出时，会出现以下日志消息。将它们降低为
     DEBUG1 级别，因为它们是正常消息。
     <programlisting>
  reaper handler
  reaper handler: exiting normally
     </programlisting>
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>变更</title>
  <itemizedlist>

   <listitem>
    <!--
    2024-08-05 [55036fd6c]
    -->
    <para>
     添加 <xref linkend="pcp-log-rotate"> 以触发日志轮转。(Bo Peng)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-12-16 [5752de991]
    -->
    <para>
     允许通过重载 <productname>Pgpool-II</productname> 配置来更改
     <xref linkend="guc-logging-collector"> 相关参数。(Bo
     Peng)
    </para>
    <para>
     以下 <xref linkend="guc-logging-collector"> 相关参数现在可以通过重载来更改：
     <itemizedlist>
      <listitem>
       <para>
        log_truncate_on_rotation
       </para>
      </listitem>
      <listitem>
       <para>
        log_directory
       </para>
      </listitem>
      <listitem>
       <para>
        log_filename
       </para>
      </listitem>
      <listitem>
       <para>
        log_rotation_age
       </para>
      </listitem>
      <listitem>
       <para>
        log_rotation_size
       </para>
      </listitem>
      <listitem>
       <para>
        log_file_mode
       </para>
      </listitem>
     </itemizedlist>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-10-14 [09d4e59c5]
    -->
    <para>
     添加 <xref linkend="pcp-invalidate-query-cache"> 以使
     查询缓存失效。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-10-21 [c5c1ca6b3]
    -->
    <para>
     添加 <link linkend="sql-pgpool-set-cache">PGPOOL SET CACHE
     DELETE</link> 命令以使指定的查询缓存条目失效。
     (Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-11-25 [6d4106f9c]
    -->
    <para>
     添加新的配置参数 <xref linkend="guc-log-backend-messages"> 以记录每个后端的协议消息。(Tatsuo Ishii)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-10-21 [b58a3c630]
    -->
    <para>
     引入 <productname>PostgreSQL</productname> 17 RC1 的新解析器。(Bo Peng)
    </para>
    <para>
     <productname>PostgreSQL</productname> 17 解析器的主要更改包括：

     <programlisting>
- Allow MERGE to use NOT MATCHED BY SOURCE and RETURNING clause:

    MERGE INTO ... WHEN NOT MATCHED BY SOURCE ...
    MERGE INTO ... RETURNING ...

- Add new COPY option ON_ERROR ignore and LOG_VERBOSITY:

  COPY ... WITH (ON_ERROR ignore);
  COPY ... WITH (LOG_VERBOSITY verbose);

- Allow to use '*' to specify the COPY FROM options FORCE_NOT_NULL and FORCE_NULL for all columns.

  COPY ... WITH (FORCE_NOT_NULL *);
  COPY ... WITH (FORCE_NULL *);

- Add EXPLAIN option SERIALIZE and MEMORY

  EXPLAIN (MEMORY) ...
  EXPLAIN (ANALYZE, SERIALIZE ...) ...

- Allow ALTER TABLE to use SET STATISTICS DEFAULT to set a column to the default statistics target

  ALTER TABLE ... ALTER COLUMN ... SET STATISTICS DEFAULT;

- Allow ALTER TABLE to change a column's generation expression

  ALTER TABLE ... ALTER COLUMN ... SET EXPRESSION;

- Add DEFAULT setting for ALTER TABLE .. SET ACCESS METHOD

  ALTER TABLE ... SET ACCESS METHOD new_access_method DEFAULT;

- Allow event triggers to use login event:

  CREATE EVENT TRIGGER ... ON login ...

- Add event trigger support for REINDEX.
     </programlisting>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-08-27 [65dbbe7a0]
    -->
    <para>
     为 <xref linkend="guc-hostname"> 和 <xref linkend="guc-heartbeat-hostname"> 参数添加 IPv6 支持。(Kwangwon Seo)
    </para>
   </listitem>

   <listitem>
    <!--
    2024-05-09 [7c5ef8d09]
    -->
    <para>
     忽略字符串列表类型配置参数中的前导/尾随空格。(Bo Peng)
    </para>
    <para>
     如果字符串列表类型的配置参数（例如 <xref linkend="guc-unix-socket-directories">、<xref linkend="guc-pcp-socket-dir">、
     等）包含空白字符，可能会导致启动失败。
    </para>
   </listitem>

   <listitem>
    <!--
    2024-08-05 [2cde5031a]
    -->
    <para>
     降低 reaper handler 日志级别。(Bo Peng)
    </para>
    <para>
     当子进程因设置（例如 child_life_time 或 child_max_connections）而退出时，会出现以下日志消息。
     将它们降低为 DEBUG1 级别，因为它们是正常消息。
     <programlisting>
  reaper handler
  reaper handler: exiting normally
     </programlisting>
    </para>
   </listitem>

   <listitem>
    <!--
    2024-06-12 [a9d1df446]
    -->
    <para>
     消除扩展查询协议中的不必要内存分配。(Tatsuo Ishii)
     请注意，这仅与流式复制模式相关。
    </para>
   </listitem>

  </itemizedlist>
 </sect2>

 <sect2>
  <title>缺陷修复</title>
  <itemizedlist>
   <listitem>
    <para>
     本版本修复了与其他次要版本相同的缺陷。
     有关这些修复的更多详细信息，请参见 <xref linkend="release">。
    </para>
   </listitem>
  </itemizedlist>
 </sect2>

</sect1>
