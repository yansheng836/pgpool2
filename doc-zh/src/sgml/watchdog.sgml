<!-- doc/src/sgml/config.sgml -->

<sect1 id="runtime-watchdog-config">
 <title>Watchdog</title>

 <indexterm zone="runtime-watchdog-config">
  <primary>configuring watchdog</primary>
 </indexterm>
 <para>
  Watchdog 配置参数在 pgpool.conf 中描述。
  在 <filename>pgpool.conf.sample</filename> 文件的 WATCHDOG 部分有示例配置。
  以下所有选项都必须在 watchdog 进程中指定。
 </para>

 <sect2 id="config-enable-watchdog">
  <title>启用 watchdog</title>

  <variablelist>
   <varlistentry id="guc-use-watchdog" xreflabel="use_watchdog">
    <term><varname>use_watchdog</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>use_watchdog</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      如果设为 on，则激活 watchdog。默认是 off。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
     <para>
      <productname>Pgpool-II</productname> 4.1 或更早版本，由于需要指定
      自身的 pgpool 节点信息和目标 pgpool 节点信息，
      每个 pgpool 节点的设置各不相同。
      自 <productname>Pgpool-II</productname> 4.2 起，所有配置参数在所有主机上
      都相同。如果启用了 watchdog 功能，为了区分哪个主机是哪个，
      需要一个 <filename>pgpool_node_id</filename> 文件。
      你需要创建一个 <filename>pgpool_node_id</filename> 文件并指定
      pgpool（watchdog）节点编号（例如 0、1、2 ...）以标识 pgpool（watchdog）主机。
     </para>
     <example id="example-pgpool-node-id-1">
      <title>pgpool_node_id 配置</title>
      <para>
       如果你有 3 个主机名为 server1、server2 和 server3 的 pgpool 节点，请按如下方式在
       每个主机上创建 <filename>pgpool_node_id</filename> 文件。
       使用 RPM 安装 <productname>Pgpool-II</productname> 时，
       <filename>pgpool.conf</filename> 安装在 <filename>/etc/pgpool-II/</filename> 下。
      </para>
      <itemizedlist>
       <listitem>
        <para>
         <literal>server1</literal>
        </para>
        <programlisting>
[server1]# cat /etc/pgpool-II/pgpool_node_id
0
        </programlisting>
       </listitem>
       <listitem>
        <para>
         <literal>server2</literal>
        </para>
        <programlisting>
[server2]# cat /etc/pgpool-II/pgpool_node_id
1
        </programlisting>
       </listitem>
       <listitem>
        <para>
         <literal>server3</literal>
        </para>
        <programlisting>
[server3]# cat /etc/pgpool-II/pgpool_node_id
2
        </programlisting>
       </listitem>
      </itemizedlist>
     </example>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-communication-watchdog">
  <title>Watchdog 通信</title>

  <variablelist>

   <varlistentry id="guc-hostname" xreflabel="hostname">
    <term><varname>hostnameX</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>hostnameX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname> 服务器的主机名或 IP（IPv4 或 IPv6）
      地址。
      这用于发送/接收查询和数据包，
      也用作 watchdog 节点的标识符。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 hostname0）。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-listen-address" xreflabel="wd_listen_address">
    <term><varname>wd_listen_address</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_listen_address</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 watchdog 将要监听的地址上的主机名或 IP（IPv4 或 IPv6）地址。
      默认是 <literal>''</literal>（空）。这里只指定一个地址。
      如果设置了非默认值，
      则本地节点的 hostnameX 的值将被忽略，并会创建一个
      监听 wd_listen_address 中指定地址的 socket。
      对于 <varname>wd_listen_address</varname>，指定 <literal>*</literal> 将监听所有地址。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-port" xreflabel="wd_port">
    <term><varname>wd_portX</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_portX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 watchdog
      进程用于监听连接的端口号。默认是 9000。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 wd_port0）。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-listen-port" xreflabel="wd_listen_port">
    <term><varname>wd_listen_port</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_listen_port</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 watchdog 将要监听的端口号。
      默认是 0。这里只指定一个地址。
      如果设置了非默认值，
      则本地节点的 wd_portX 的值将被忽略，并会创建一个
      监听 wd_listen_port 中指定端口的 socket。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-pgpool-port" xreflabel="pgpool_port">
    <term><varname>pgpool_portX</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>pgpool_portX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname> 端口号。
      默认是 9999。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 pgpool_port0）。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>

     <example id="example-watchdog-1">
      <title>Watchdog 配置</title>
      <para>
       如果你有 3 个主机名为 server1、server2 和 server3 的 pgpool 节点，
       你可以像下面这样配置 <xref linkend="guc-hostname">、
       <xref linkend="guc-wd-port"> 和 <xref linkend="guc-pgpool-port">：
    <programlisting>
hostname0 = 'server1'
wd_port0 = 9000
pgpool_port0 = 9999

hostname1 = 'server2'
wd_port1 = 9000
pgpool_port1 = 9999

hostname2 = 'server3'
wd_port2 = 9000
pgpool_port2 = 9999
    </programlisting>
      </para>
     </example>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-authkey" xreflabel="wd_authkey">
    <term><varname>wd_authkey</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_authkey</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于所有 watchdog 通信的身份验证密钥。
      所有 <productname>Pgpool-II</productname> 必须使用相同的密钥。
      来自密钥不同的 watchdog 的数据包将被拒绝。
      当以 <literal>heartbeat</> 模式作为 lifecheck 方法时，
      此身份验证也应用于心跳信号。
     </para>
     <para>
      由于在 <productname>Pgpool-II</productname><emphasis>V3.5</emphasis> 或更高版本中
      <varname>wd_authkey</varname> 也用于验证 watchdog IPC 客户端，
      所有与 <productname>Pgpool-II</productname> watchdog 进程通信的客户端
      都需要在命令的 JSON 数据中
      为 <literal>"IPCAuthKey"</literal> 键提供此 wd_authkey 值。
     </para>
     <para>
      默认是 <literal>''</literal>（空），表示禁用 watchdog 身份验证。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-upstream-connections">
  <title>上游服务器连接</title>

  <para>
   上游服务器是由 <productname>Pgpool-II</productname> 服务的客户端（数据库客户端）。
   即使 <productname>Pgpool-II</productname> 处于存活状态并已连接到 <productname>PostgreSQL</productname>，
   如果与上游服务器的链路中断，
   <productname>Pgpool-II</productname> 也无法提供服务。
   因此，watchdog 也会监控来自 <productname>Pgpool-II</productname> 到上游服务器的连接。
  </para>

  <variablelist>

   <varlistentry id="guc-trusted-servers" xreflabel="trusted_servers">
    <term><varname>trusted_servers</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>trusted_servers</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于检查上游连接的受信任服务器列表。
      例如，<productname>Pgpool-II</productname> 与上游服务器之间的上游服务器或路由器的列表。
      列表中的每台服务器都必须能响应 ping。
      指定一个以逗号分隔的服务器列表，
      例如 <literal>"hostA,hostB,hostC"</literal>。
      如果没有任何服务器可达，watchdog 将视为
      <productname>Pgpool-II</productname> 发生故障。
      因此，建议指定多台服务器。
      请注意，你不应该将 PostgreSQL 服务器指定到此参数。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-trusted-server-command" xreflabel="trusted_server_command">
    <term><varname>trusted_server_command</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>trusted_server_command</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定当 <productname>Pgpool-II</productname> 检查受信任服务器是否响应 ping 时要运行的用户命令。字符串中的任何 %h 都会被替换为
      <varname>trusted_servers</varname> 中指定的各个主机名。默认是
      <literal>ping -q -c3 %h</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-vip-control">
  <title>虚拟 IP 控制</title>

  <variablelist>

   <varlistentry id="guc-delegate-ip" xreflabel="delegate_ip">
    <term><varname>delegate_ip</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>delegate_ip</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定客户端服务器（应用服务器等）所连接的
      <productname>Pgpool-II</productname> 的虚拟 IP 地址（VIP）。
      当一个 <productname>Pgpool-II</productname> 从 standby 切换到 leader 时，
      该 <productname>Pgpool-II</productname> 会接管这个 VIP。
      <emphasis>当不存在 quorum 时，VIP 不会被启用</emphasis>。默认是 <literal>''</literal>（空）：
      表示虚拟 IP 永远不会被启用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-if-cmd-path" xreflabel="if_cmd_path">
    <term><varname>if_cmd_path</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>if_cmd_path</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname> 将用于在系统上切换虚拟 IP
      的命令路径。
      只设置包含二进制的目录的路径，
      例如 <literal>"/sbin"</literal> 或类似的目录。
      如果 <xref linkend="guc-if-up-cmd"> 或 <xref linkend="guc-if-down-cmd"> 以 "/" 开头，
      将忽略此参数。
      默认是 <literal>/sbin</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-if-up-cmd" xreflabel="if_up_cmd">
    <term><varname>if_up_cmd</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>if_up_cmd</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定启用虚拟 IP 的命令。
      设置命令和参数，
      例如 <literal>"ip addr add $_IP_$/24 dev eth0 label eth0:0"</literal>。
      由于执行此命令需要 root 权限，
      请对 <command>ip</command> 命令使用 <command>setuid</command>，或
      允许 <productname>Pgpool-II</productname> 启动用户（默认是 <literal>postgres</literal> 用户）
      无需密码运行 <command>sudo</command> 命令，并指定为
      例如 <literal>"/usr/bin/sudo /sbin/ip addr add $_IP_$/24 dev eth0 label eth0:0"</literal>。
      <literal>$_IP_$</literal> 将会被替换为
      <xref linkend="guc-delegate-ip"> 中指定的 IP 地址。
     </para>
     <para>
      默认是 <literal>/usr/bin/sudo /sbin/ip addr add $_IP_$/24 dev eth0 label eth0:0</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-if-down-cmd" xreflabel="if_down_cmd">
    <term><varname>if_down_cmd</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>if_down_cmd</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定停用虚拟 IP 的命令。
      设置命令和参数，
      例如 <literal>"ip addr del $_IP_$/24 dev eth0"</literal>。
      由于执行此命令需要 root 权限，
      请对 <command>ip</command> 命令使用 <command>setuid</command>，或
      允许 <productname>Pgpool-II</productname> 启动用户（默认是 <literal>postgres</literal> 用户）
      无需密码运行 <command>sudo</command> 命令，并指定为
      例如 <literal>"/usr/bin/sudo /sbin/ip addr del $_IP_$/24 dev eth0"</literal>。
      <literal>$_IP_$</literal> 将会被替换为
      <xref linkend="guc-delegate-ip"> 中指定的 IP 地址。
     </para>
     <para>
      默认是 <literal>/usr/bin/sudo /sbin/ip addr del $_IP_$/24 dev eth0</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-arping-path" xreflabel="arping_path">
    <term><varname>arping_path</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>arping_path</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 <productname>Pgpool-II</productname> 在虚拟 IP 切换后
      将用于发送 ARP 请求的命令路径。
      只设置包含二进制的目录的路径，
      例如 <literal>"/usr/sbin"</literal> 或类似的目录。
      如果 <xref linkend="guc-arping-cmd"> 以 "/" 开头，
      将忽略此参数。
      默认是 <literal>/usr/sbin</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-arping-cmd" xreflabel="arping_cmd">
    <term><varname>arping_cmd</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>arping_cmd</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于在虚拟 IP 切换后发送 ARP 请求的命令。
      设置命令和参数，
      例如 <literal>"arping -U $_IP_$ -w 1 -I eth0"</literal>。
      由于执行此命令需要 root 权限，
      请对 <command>ip</command> 命令使用 <command>setuid</command>，或
      允许 <productname>Pgpool-II</productname> 启动用户（默认是 <literal>postgres</literal> 用户）
      无需密码运行 <command>sudo</command> 命令，并指定为
      例如 <literal>"/usr/bin/sudo /usr/sbin/arping -U $_IP_$ -w 1 -I eth0"</literal>。
      <literal>$_IP_$</literal> 将会被替换为
      <varname>delegate_ip</varname> 中指定的 IP 地址。
     </para>
     <para>
      默认是 <literal>/usr/bin/sudo /usr/sbin/arping -U $_IP_$ -w 1 -I eth0</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-ping-path" xreflabel="ping_path">
    <term><varname>ping_path</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>ping_path</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于检查虚拟 IP 是否启动的 ping 命令的路径。
      只设置包含 ping 工具的目录的路径，
      例如 <literal>"/bin"</literal> 或类似的目录。
      默认是 <literal>/bin</literal>。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-escalation-de-escalation">
  <title>升级和降级时的行为</title>

  <para>
   关于 <productname>Pgpool-II</productname> 升级为 leader（虚拟 IP 持有者）时的行为配置
  </para>

  <variablelist>

   <varlistentry id="guc-clear-memqcache-on-escalation" xreflabel="clear_memqcache_on_escalation">
    <term><varname>clear_memqcache_on_escalation</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>clear_memqcache_on_escalation</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当设为 on 时，当 pgpool-II 升级为 leader 时，watchdog 会清除共享内存中的所有查询缓存。这可以防止新的 leader <productname>Pgpool-II</productname>
      使用与旧 leader 不一致的旧查询缓存。
     </para>
     <para>
      默认是 on。
     </para>
     <para>
      这仅在 <xref linkend="guc-memqcache-method">
       为 <literal>'shmem'</literal> 时才有效。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-escalation-command" xreflabel="wd_escalation_command">
    <term><varname>wd_escalation_command</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_escalation_command</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      Watchdog 在升级为 leader watchdog 的节点上执行此命令。
     </para>
     <para>
      如果该节点上配置了虚拟 IP，
      此命令会在启用虚拟 IP 之前执行。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-de-escalation-command" xreflabel="wd_de_escalation_command">
    <term><varname>wd_de_escalation_command</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_de_escalation_command</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      当 leader watchdog 节点辞去 leader 节点职责时，watchdog 会在该 leader <productname>Pgpool-II</productname>
      watchdog 节点上执行此命令。
      当 leader 节点 <productname>Pgpool-II</productname> 关闭、检测到网络
      中断或检测到 <indexterm><primary>quorum</primary></indexterm><firstterm>quorum</firstterm> 丢失时，
      leader watchdog 节点可能会辞去 leader 节点的职责。
     </para>
     <para>
      如果 watchdog 节点上配置了虚拟/浮动 IP 地址，
      此命令会在停用该 IP 地址之前执行。
     </para>
     <para>
      <varname>wd_de_escalation_command</varname> 在 <productname>Pgpool-II </productname><emphasis>V3.5</emphasis> 之前不可用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-failover-behavior">
  <title>控制故障转移行为</title>

  <para>
   当 watchdog 启用时，这些设置用于控制后端节点故障转移的行为。
   这些配置的效果仅限于由 <productname>Pgpool-II</productname> 内部发起的
   故障转移/降级请求，而用户发起的 detach 后端请求
   （使用 PCP 命令）则会绕过这些配置设置。
  </para>

  <variablelist>

   <varlistentry id="guc-failover-when-quorum-exists" xreflabel="failover_when_quorum_exists">
    <term><varname>failover_when_quorum_exists</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>failover_when_quorum_exists</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      启用时，<productname>Pgpool-II</productname> 在对后端节点执行降级/故障转移时会考虑 <indexterm><primary>quorum</primary></indexterm>quorum。
     </para>
     <para>
      如果存活 watchdog 节点（也就是 <productname>Pgpool-II</productname> 节点的数量）的数量
      能够占 watchdog 节点总数的大多数，则可以说 "存在 quorum"。
      例如，假设 watchdog 节点数为 5。如果存活节点数
      大于或等于 3，则存在 quorum。
      另一方面，如果存活节点数为 2 或更少，则不存在 quorum，
      因为它永远不会占大多数。
     </para>
     <para>
      如果存在 quorum，<productname>Pgpool-II</productname> 在故障检测方面
      可能工作得更好，因为即使某个 watchdog 节点错误地检测到后端节点故障，
      它也会被其他主要的 watchdog 节点否决。
      <productname>Pgpool-II</productname> 在 <xref linkend="guc-failover-require-consensus"> 为 on
       （默认）时就是这样工作的，但你可以更改它，
      以便在检测到故障时立即进行故障转移。
      错误地检测到后端节点故障的 <productname>Pgpool-II</productname> 节点
      会隔离该后端节点。
     </para>
     <para>
      可以通过使用 <literal>--verbose</literal> 选项调用 <xref linkend="pcp-watchdog-info"> 命令来显示 quorum 是否存在。
       如果 <literal>Quorum state</literal> 为 <literal>QUORUM EXIST</literal> 或 <literal>QUORUM IS ON THE EDGE</literal>, 则存在 quorum。
       如果 <literal>Quorum state</literal> 为 <literal>QUORUM ABSENT</literal>, 则不存在 quorum。
     </para>
     <para>
      在没有
      quorum 的情况下，检测到后端故障的 <productname>Pgpool-II</productname> 节点
      会隔离发生故障的后端节点，直到再次存在 quorum。
     </para>
     <para>
      虽然可以使用 <command>pcp_detach_node</command> 命令强制解除对隔离节点的 detach，
      但无法使用 <command>pcp_attach_node</command> 命令再次 attach 该节点。
     </para>
     <para>
      <indexterm><primary>quarantine</primary></indexterm>
      隔离节点与被 detach 的后端节点行为类似，
      但与故障/降级的后端不同，隔离状态不会传播到
      watchdog 集群中的其他 <productname>Pgpool-II</productname> 节点，
      因此即使后端节点在一个 <productname>Pgpool-II</productname> 节点上处于隔离状态，
      其他 <productname>Pgpool-II</productname> 节点可能仍会继续使用该后端。
     </para>
     <para>
      虽然隔离和故障转移操作有很多相似之处，但它们在根本上有区别。
      隔离操作不会执行 <xref linkend="guc-failover-command">，
      而是静默地 detach 有问题的节点，因此当主后端节点被隔离时，
      <productname>Pgpool-II</productname> 不会提升 standby 来接管主节点职责，
      并且在主节点被隔离期间，<productname>Pgpool-II</productname> 不会有任何可用的主后端节点。
     </para>
     <para>
      此外，与故障节点不同，
      <productname>Pgpool-II</productname> 会继续对隔离节点运行健康检查，
      一旦隔离节点再次变得可达，它就会被自动重新 attach。
      请注意，这仅适用于 <productname>Pgpool-II </productname><emphasis>V4.1</emphasis>
      或更高版本。如果你使用的是以前的版本，
      当连接问题解决后，你需要使用 <xref linkend="pcp-attach-node"> 重新 attach 隔离节点。
     </para>
     <para>
      从 <productname>Pgpool-II </productname><emphasis>V4.1</emphasis> 开始，如果 watchdog leader 节点
      无法就主后端节点故障转移达成共识，并且主后端节点进入隔离状态，
      那么它会辞去 leader 职责，并降低其 wd_priority
      以用于下一次 leader 选举，让集群选举出某个不同的新 leader。
      <note>
       <para>
	当 leader 节点无法就 standby 后端节点故障达成共识时，它不会采取任何行动，
	并且 watchdog leader 上被隔离的 standby 后端节点也不会触发新的 leader 选举。
       </para>
      </note>
     </para>
     <para>
      如果此参数为 off，即使不存在 quorum，也会触发故障转移。
     </para>
     <para>
      默认是 on。
     </para>
     <para>
      <varname>failover_when_quorum_exists</varname> 在 <productname>Pgpool-II </productname><emphasis>V3.7</emphasis> 之前不可用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
     <note>
      <para>
       在原生复制模式中不允许启用 <varname>failover_when_quorum_exists</varname>。
      </para>
     </note>
   </listitem>
   </varlistentry>

   <varlistentry id="guc-failover-require-consensus" xreflabel="failover_require_consensus">
    <term><varname>failover_require_consensus</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>failover_require_consensus</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      启用时，如果 watchdog quorum 存在，并且至少有达成 quorum 所需的最少节点数
      对故障转移投了赞成票，<productname>Pgpool-II</productname> 就会对
      后端节点执行降级/故障转移。
     </para>
     <para>
      例如，在三节点 watchdog 集群中，只有在
      至少两个节点要求对特定后端节点执行故障转移时，才会执行故障转移。
     </para>
     <para>
      如果此参数为 off，即使没有共识，也会触发故障转移。
     </para>
     <para>
      默认是 on。
     </para>

     <caution>
      <para>
       当 <varname>failover_require_consensus</varname>
       启用时，<productname>Pgpool-II</productname> 在从其他
       <productname>Pgpool-II</productname> 节点获得足够的赞成票之前不会执行故障转移。因此强烈建议在所有
       <productname>Pgpool-II</productname> 节点上启用后端健康检查，以确保正确检测后端节点故障。
       有关健康检查的更多详细信息，
       请参阅 <xref linkend="runtime-config-health-check">。
      </para>
     </caution>

     <note>
      <para>
       在原生复制模式中不允许启用 <varname>failover_require_consensus</varname>。
      </para>
     </note>

     <para>
      <varname>failover_require_consensus</varname> 在 <productname>Pgpool-II </productname><emphasis>V3.7</emphasis> 之前不可用，并且只有在
      <xref linkend="guc-failover-when-quorum-exists"> 启用时才有效。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-allow-multiple-failover-requests-from-node" xreflabel="allow_multiple_failover_requests_from_node">
    <term><varname>allow_multiple_failover_requests_from_node</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>allow_multiple_failover_requests_from_node</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      此参数与
      <xref linkend="guc-failover-require-consensus"> 配合工作。启用后，单个 <productname>Pgpool-II</productname>
       节点可以针对故障转移投出多票。
     </para>
     <para>
      例如，在三节点 watchdog 集群中，如果一个 <productname>Pgpool-II</productname> 节点针对特定后端节点故障转移发送了两个
      故障转移请求，这两个请求都会被计为对该故障转移的独立赞成票，
      并且 <productname>Pgpool-II</productname> 会执行故障转移，
      即使没有从任何其他 <productname>Pgpool-II</productname> 节点获得赞成票。
     </para>
     <para>
      例如，如果一轮健康检查中发现一个错误但未获得足够的赞成票，并且该错误仍然存在，
      下一轮健康检查会再多投一票。如果你想要检测其他 watchdog 节点可能无法发现的
      持续性错误，此参数很有用。
     </para>
     <para>
      默认是 off。
     </para>
     <para>
      <varname>allow_multiple_failover_requests_from_node</varname> 在 <productname>Pgpool-II </productname><emphasis>V3.7</emphasis> 之前不可用，并且只有在
      <xref linkend="guc-failover-when-quorum-exists"> 和 <xref linkend="guc-failover-require-consensus"> 都启用时才有效。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-enable-consensus-with-half-votes" xreflabel="enable_consensus_with_half_votes">
    <term><varname>enable_consensus_with_half_votes</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>enable_consensus_with_half_votes</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      此参数配置 <productname>Pgpool-II</productname> 如何进行大多数规则的计算，
      用于计算 quorum 和解决故障转移的共识。
     </para>
     <note>
      <para>
       此参数不仅影响后端的故障转移行为，
       还影响 <productname>Pgpool-II</productname> 自身的 quorum 和故障转移行为。
      </para>
     </note>
     <para>
      启用时，quorum 的存在和故障转移共识只需要集群中配置的总票数的一半。
      否则，这两个决策都需要比总票数一半至少多一票。
      对于故障转移，此参数与
      <xref linkend="guc-failover-require-consensus"> 配合使用。在这两种情况下，
      无论是做出 quorum 存在的决策还是在故障转移上建立共识，
      此参数只在 watchdog 集群配置为偶数个 <productname>Pgpool-II</productname> 节点时才起作用。
      在参与者数量为奇数的 watchdog 集群中，大多数规则决策不受此配置参数值的影响。
     </para>
     <para>
      例如，在两节点 watchdog 集群中启用此参数时，
      需要一个 <productname>Pgpool-II</productname> 节点存活才能存在 quorum。如果参数为 off，
      则需要两个节点存活才能存在 quorum。
     </para>
     <para>
      在四节点 watchdog 集群中启用此参数时，
      需要两个 <productname>Pgpool-II</productname> 节点存活才能存在 quorum。如果参数为 off，
      则需要三个节点存活才能存在 quorum。
     </para>
     <para>
      通过启用此参数，你应该意识到存在发生脑裂的风险。
      例如，在由节点 A、B、C 和 D 组成的四节点集群中，
      集群有可能分裂成两个相互隔离的网络 (A、B) 和 (C、D)。
      对于 (A、B) 和 (C、D)，quorum 仍然存在，因为两个组在 4 个节点中都有两个存活节点。
      这两个组会选出各自的 leader watchdog，这即为脑裂。
     </para>
     <para>
      默认是 off。
     </para>
     <para>
      <varname>enable_consensus_with_half_votes</varname> 在 <productname>Pgpool-II
      </productname><emphasis>V4.1</emphasis> 之前不可用。之前的版本表现得
      如同该参数为 on。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-cluster-membership">
  <title>控制 watchdog 集群成员关系</title>

  <para>
	默认情况下，watchdog 集群由 <filename>pgpool.conf</filename> 文件中定义的
	所有 watchdog 节点组成，无论节点的当前状态如何。无论节点是
	<literal>LOST</literal>、<literal>SHUTDOWN</literal> 还是从未启动过，
	只要它在配置文件中配置了，就被视为 watchdog 集群定义中的成员。
	用于识别 quorum 存在和解决共识的所有大多数规则计算
	都是基于组成 watchdog 集群的 watchdog 节点数量进行的。
  </para>

  <para>
	<productname>Pgpool-II</productname> <emphasis>V4.3</emphasis> 通过引入
	<emphasis>Member</emphasis> 和 <emphasis>Nonmember</emphasis> 的概念，
	启用了动态集群定义。
	如果节点的成员资格被从 watchdog 集群中撤销，
	那么集群会动态地重新校准自身，以调整所有后续的大多数规则计算。
	</para>
	<para>
	所有大多数规则计算都是基于成员 watchdog 节点的数量进行的，
	而不是基于已配置节点的总数。
  </para>

  <para>
	例如：在一个五节点集群中（<filename>pgpool.conf</filename> 定义了五个
	watchdog 节点），至少需要三个节点存活才能存在 quorum。
	借助动态集群成员机制，集群可以重新调整自身，
	只计算 <emphasis>MEMBER</emphasis> 节点（Member 节点不一定是存活节点）。
	这意味着，如果集群在某时刻只有一两个成员节点，
	那么实际上单个存活节点也可以满足 quorum 要求（取决于成员资格设置）。
  </para>
  <para>
	<caution>
		<para>
		使用动态集群成员机制存在引发脑裂的相关风险。
		因此强烈建议仔细审查该设置是否确实需要动态集群成员机制，
		并考虑在使用相关设置时采用保守的值。
		</para>
	</caution>
  </para>
  <para>
  这些设置配置节点何时被标记为 Nonmember。
  将所有这些设置保持为默认值即可保留 <emphasis>V4.3</emphasis> 之前的行为。
  </para>

  <variablelist>

   <varlistentry id="guc-wd-remove-shutdown-nodes" xreflabel="wd_remove_shutdown_nodes">
    <term><varname>wd_remove_shutdown_nodes</varname> (<type>boolean</type>)
     <indexterm>
      <primary><varname>wd_remove_shutdown_nodes</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
	 启用时，<literal>SHUTDOWN</literal> 节点会立即被标记为 Nonmember
	 并从集群中移除。如果之前关闭的节点再次启动，
	 它会自动被添加到集群中。
     </para>
	 <para> 默认是 off。 </para>
	</listitem>
   </varlistentry>


   <varlistentry id="guc-wd-lost-node-removal-timeout" xreflabel="wd_lost_node_removal_timeout">
    <term><varname>wd_lost_node_removal_timeout</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_lost_node_removal_timeout</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      将 <literal>LOST</literal> watchdog 节点标记为 Nonmember 并从集群中移除的超时时间（秒）。
	  当 LOST 节点重新连接到集群时，其集群成员资格会被恢复。
     </para>
	 <para>
	 将超时设置为 <literal>0</literal> 可禁用从集群中移除 LOST 节点。
	 </para>
	 <para> 默认是 0。 </para>

	</listitem>
   </varlistentry>

   <varlistentry id="guc-wd-no-show-node-removal-timeout" xreflabel="wd_no_show_node_removal_timeout">
    <term><varname>wd_no_show_node_removal_timeout</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_no_show_node_removal_timeout</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      如果节点在集群初始化时没有出现，则在将其标记为 Nonmember 前的超时时间（秒）。一旦 Nonmember 节点启动并连接到集群，它就会成为集群 Member。
	 </para>
	 <para>
	 将超时设置为 <literal>0</literal> 可禁用从集群中移除 <literal>NO-SHOW</literal> 节点。
	 </para>
	 <para> 默认是 0。 </para>

	</listitem>
  </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-lifecheck">
  <title>存活检查 <productname>Pgpool-II</productname></title>

  <para>
   Watchdog 会周期性地检查 pgpool-II 的状态。这被称为 "life check"。
  </para>

  <variablelist>

   <varlistentry id="guc-wd-lifecheck-method" xreflabel="wd_lifecheck_method">
    <term><varname>wd_lifecheck_method</varname> (<type>enum</type>)
     <indexterm>
      <primary><varname>wd_lifecheck_method</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定存活检查的方法。可以是 <literal>heartbeat</literal>（默认）中的任何一个、
      <literal>query</literal> 或 <literal>external</literal>。
     </para>
     <para>
      <literal>heartbeat</literal>：在此模式下，watchdog 周期性地向其他
      <productname>Pgpool-II</productname> 发送心跳信号（<acronym>UDP</acronym> 数据包）。
      同样，watchdog 也接收来自其他 <productname>Pgpool-II</productname> 的信号。
      如果在某段时间内没有信号，watchdog 就认为
      <productname>Pgpool-II</productname> 发生故障。
     </para>
     <para>
      <literal>query</literal>：在此模式下，watchdog 向其他
      <productname>Pgpool-II</productname> 发送监控查询并检查响应。
      当 <productname>Pgpool-II</productname> 各服务器之间的安装位置相距较远时，
      <literal>query</literal> 可能很有用。
      <caution>
       <para>
        在 query 模式下，如果你打算使用 watchdog，你需要将 <xref linkend="guc-num-init-children">
	设置得足够大。
	这是因为 watchdog 进程会作为客户端连接到
	<productname>Pgpool-II</productname>。
       </para>
      </caution>
     </para>
     <para>
      <literal>external</literal>：此模式禁用
      <productname>Pgpool-II</productname> watchdog 的内置存活检查，
      并依赖外部系统提供本地和远程 watchdog 节点的健康检查。
     </para>

     <para>
      <literal>external</literal> 模式在 <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 之前的版本中不可用。
     </para>

     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-monitoring-interfaces-list" xreflabel="wd_monitoring_interfaces_list">
    <term><varname>wd_monitoring_interfaces_list</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_monitoring_interfaces_list</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定一个以逗号分隔的网络设备名称列表，由 watchdog 进程监控
      以检查网络链路状态。如果列表中的所有网络接口都变为不活动
      （已禁用或网线被拔出），watchdog 将认为这是完全的网络故障，
      并且 <productname>Pgpool-II</productname> 节点会自杀。
      指定一个 <literal>''</literal>（空）列表会禁用网络接口监控。
      将其设置为 <literal>'any'</literal> 会启用对所有可用网络接口的监控
      （环回接口除外）。默认是 <literal>''</literal> 空列表（监控已禁用）。
     </para>

     <para>
      <varname>wd_monitoring_interfaces_list</varname> 在 <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 之前的版本中不可用。
     </para>

     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-interval" xreflabel="wd_interval">
    <term><varname>wd_interval</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_interval</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定对 <productname>Pgpool-II</productname> 进行存活检查的时间间隔
      （秒）。默认是 10。
     </para>

     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-priority" xreflabel="wd_priority">
    <term><varname>wd_priority</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_priority</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      此参数可用于在选举 leader watchdog 节点的选举中提升本地 watchdog 节点的优先级。
      当集群在旧 leader watchdog 节点故障时选举新 leader 节点时，
      <varname>wd_priority</varname> 值较高的节点会被选为 leader watchdog 节点。
      <varname>wd_priority</varname> 在集群启动时也有效。
      当某些 watchdog 节点同时启动时，<varname>wd_priority</varname> 值较高的节点会被选为 leader 节点。
      因此，我们应该按照 <varname>wd_priority</varname> 优先级的顺序启动 watchdog 节点，
      以防止意外节点被选为 leader。
      如果每个节点的 <varname>wd_priority</varname> 值相同，
      leader 节点将根据 watchdog 的启动时间来决定。
     </para>
     <para>
      <varname>wd_priority</varname> 在 <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 之前的版本中不可用。
     </para>

     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-ipc-socket-dir" xreflabel="wd_ipc_socket_dir">
    <term><varname>wd_ipc_socket_dir</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_ipc_socket_dir</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      将创建接受 <productname>Pgpool-II</productname> watchdog <acronym>IPC</acronym> 连接的
      <acronym>UNIX</acronym> 域 socket 的目录。
      默认是 <literal>'/tmp'</literal>。
      请注意，此 socket 可能会被 cron 作业删除。
      我们建议将此值设置为 <literal>'/var/run'</literal> 或类似的目录。
     </para>
     <para>
      <varname>wd_ipc_socket_dir</varname> 在 <productname>Pgpool-II</productname> <emphasis>V3.5</emphasis> 之前的版本中不可用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-lifecheck-heartbeat">
  <title>Lifecheck Heartbeat 模式配置</title>
  <variablelist>
   <varlistentry id="guc-heartbeat-hostname" xreflabel="heartbeat_hostname">
    <term><varname>heartbeat_hostnameX</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>heartbeat_hostnameX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于发送和接收心跳信号的 <acronym>IP (IPv4 或 IPv6)</acronym> 地址或 <acronym>hostname</acronym>。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 heartbeat_hostname0）。
	  你可以在多个 <acronym>IP</acronym> 地址或 <acronym>hostname</acronym> 之间使用分号 (;) 分隔来指定它们。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'heartbeat'</literal> 时，
      <varname>heartbeat_hostnameX</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-heartbeat-listen-addresses" xreflabel="wd_heartbeat_listen_addresses">
    <term><varname>wd_heartbeat_listen_addresses</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_heartbeat_listen_addresses</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 heartbeat 进程将要监听的 <acronym>IP (IPv4 或 IPv6)</acronym> 地址或 <acronym>hostname</acronym>。
      默认是 <literal>''</literal>（空）。
	   你可以在多个 <acronym>IP</acronym> 地址或 <acronym>hostname</acronym> 之间使用分号 (;) 分隔来指定它们。
      如果设置了非默认值，
      则本地节点的 heartbeat_hostnameX 的值将被忽略，并会创建一个
      监听 wd_heartbeat_listen_addresses 中指定地址的 socket。
      对于 <varname>wd_heartbeat_listen_addresses</varname>，指定 <literal>*</literal> 将监听所有地址。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-heartbeat-port" xreflabel="heartbeat_port">
    <term><varname>heartbeat_portX</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>heartbeat_portX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于发送和接收心跳信号的端口号。
      这里只指定一个端口号。默认是 9694。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 heartbeat_port0）。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'heartbeat'</literal> 时，
      <varname>heartbeat_portX</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-heartbeat-listen-port" xreflabel="wd_heartbeat_listen_port">
    <term><varname>wd_heartbeat_listen_port</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_heartbeat_listen_port</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定 heartbeat 进程将要监听的端口号。
      这里只指定一个端口号。默认是 0。
      如果设置了非默认值，
      则本地节点的 heartbeat_portX 的值将被忽略，并会创建一个
      监听 wd_heartbeat_listen_port 中指定端口的 socket。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-heartbeat-device" xreflabel="heartbeat_device">
    <term><varname>heartbeat_deviceX</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>heartbeat_deviceX</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于发送和接收心跳信号的网络设备名称。
      参数名称末尾的数字被称为 "pgpool node id"，
      并且从 0 开始（例如 heartbeat_device0）。
	  你可以在多个网络设备之间使用分号 (;) 分隔来指定它们。
     </para>
     <para>
      仅当 <productname>Pgpool-II</productname> 以 root 权限启动时，
      <varname>heartbeat_deviceX</varname> 才适用。如果不是，请将其留为空字符串 (')。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'heartbeat'</literal> 时，
      <varname>heartbeat_deviceX</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
     <example id="example-heartbeat-1">
      <title>Heartbeat 配置</title>
      <para>
       如果你有 3 个主机名为 server1、server2 和 server3 的 pgpool 节点，
       你可以像下面这样配置 <xref linkend="guc-heartbeat-hostname">、
       <xref linkend="guc-heartbeat-port"> 和 <xref linkend="guc-heartbeat-device">：
	<programlisting>
     heartbeat_hostname0 = 'server1'
     heartbeat_port0 = 9694
     heartbeat_device0 = ''

     heartbeat_hostname1 = 'server2'
     heartbeat_port1 = 9694
     heartbeat_device1 = ''

     heartbeat_hostname2 = 'server3'
     heartbeat_port2 = 9694
     heartbeat_device2 = ''
	</programlisting>
      </para>
     </example>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-heartbeat-keepalive" xreflabel="wd_heartbeat_keepalive">
    <term><varname>wd_heartbeat_keepalive</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_heartbeat_keepalive</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定发送心跳信号之间的时间间隔（秒）。
      默认是 2。
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'heartbeat'</literal> 时，
      <varname>wd_heartbeat_keepalive</varname> 才适用。
     </para>

     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-heartbeat-deadtime" xreflabel="wd_heartbeat_deadtime">
    <term><varname>wd_heartbeat_deadtime</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_heartbeat_deadtime</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定在将远程 watchdog 节点标记为故障/死节点之前的时间（秒），
      如果在该时间内未收到心跳信号。
      默认是 <literal>30</literal>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'heartbeat'</literal> 时，
      <varname>wd_heartbeat_deadtime</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </sect2>

 <sect2 id="config-watchdog-lifecheck-query">
  <title>Lifecheck Query 模式配置</title>
  <variablelist>

   <varlistentry id="guc-wd-life-point" xreflabel="wd_life_point">
    <term><varname>wd_life_point</varname> (<type>integer</type>)
     <indexterm>
      <primary><varname>wd_life_point</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定对 pgpool-II 重试失败的存活检查的次数。
      有效值可以是大于或等于 1 的数字。
      默认是 3。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'query'</literal> 时，
      <varname>wd_life_point</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-lifecheck-query" xreflabel="wd_lifecheck_query">
    <term><varname>wd_lifecheck_query</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_lifecheck_query</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于对远程 <productname>Pgpool-II</productname> 进行存活检查的查询。
      默认是 <literal>"SELECT 1"</literal>。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'query'</literal> 时，
      <varname>wd_lifecheck_query</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-lifecheck-dbname" xreflabel="wd_lifecheck_dbname">
    <term><varname>wd_lifecheck_dbname</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_lifecheck_dbname</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于对远程 <productname>Pgpool-II</productname> 进行
      存活检查的连接所使用的数据库名称。
      默认是 <literal>"template1"</literal>。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'query'</literal> 时，
      <varname>wd_lifecheck_dbname</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-lifecheck-user" xreflabel="wd_lifecheck_user">
    <term><varname>wd_lifecheck_user</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_lifecheck_user</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于对远程 <productname>Pgpool-II</productname> 进行存活检查的连接所使用的用户名。
      默认是 <literal>''</literal>（空）。
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'query'</literal> 时，
      <varname>wd_lifecheck_user</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
    </listitem>
   </varlistentry>

   <varlistentry id="guc-wd-lifecheck-password" xreflabel="wd_lifecheck_password">
    <term><varname>wd_lifecheck_password</varname> (<type>string</type>)
     <indexterm>
      <primary><varname>wd_lifecheck_password</varname> configuration parameter</primary>
     </indexterm>
    </term>
    <listitem>
     <para>
      指定用于对远程 <productname>Pgpool-II</productname> 进行存活检查的用户的密码。
     </para>
     <para>
      如果 <varname>wd_lifecheck_password</varname> 留空，<productname>Pgpool-II</productname>
      在使用空密码之前，会首先尝试从 <xref linkend="guc-pool-passwd"> 文件中获取
       <xref linkend="guc-wd-lifecheck-user"> 的密码。
     </para>
     <para>
      你也可以在 <varname>wd_lifecheck_password</varname> 字段中指定 AES256-CBC 加密的密码。
      要指定 <literal>AES</literal> 加密的密码，密码字符串在加密（使用 <literal>aes-256-cbc</literal> 算法）并
      编码为 <literal>base64</literal> 后，必须以 <literal>AES</literal> 作为前缀。
     </para>
     <para>
      要指定未加密的明文密码，请以 <literal>TEXT</literal> 作为密码字符串的前缀。
      例如，如果你想将 <literal>mypass</literal> 设置为密码，
      你应该在密码字段中指定 <literal>TEXTmypass</literal>。
      在没有有效前缀的情况下，<productname>Pgpool-II</productname> 会将
      该字符串视为明文密码。
     </para>
     <para>
      你也可以使用 <xref linkend="PG-ENC"> 工具来创建格式正确的
       <literal>AES</literal> 加密密码字符串。
       <note>
	<para>
	 <productname>Pgpool-II</productname> 在启动时需要有效的解密密钥才能使用加密密码。
	 有关如何为 <productname>Pgpool-II</productname> 提供解密密钥的更多详细信息，
	 请参阅 <xref linkend="auth-aes-decryption-key">。
	</para>
       </note>
     </para>
     <para>
      仅当 <xref linkend="guc-wd-lifecheck-method"> 设置为 <literal>'query'</literal> 时，
      <varname>wd_lifecheck_password</varname> 才适用。
     </para>
     <para>
      此参数只能在服务器启动时设置。
     </para>
     <para>
      默认是 <literal>''</literal>（空）。
     </para>
    </listitem>
   </varlistentry>

  </variablelist>
 </sect2>
</sect1>