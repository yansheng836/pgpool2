<!-- doc/src/sgml/example-basic.sgml -->
<sect1 id="example-basic">
 <title>基本配置示例</title>

 <sect2 id="example-configs-begin">
  <title>开始吧！</title>
  <para>
   首先，我们必须了解在使用复制功能之前，如何安装和配置 <productname>Pgpool-II</productname> 以及数据库节点。
  </para>

  <sect3 id="example-configs-begin-installing">
   <title>安装 <productname>Pgpool-II</productname></title>
   <para>
    安装 <productname>Pgpool-II</productname> 非常简单。
    在您解压源码 tar 包的目录中，执行以下命令。
    <programlisting>
     $ ./configure
     $ make
     $ make install
    </programlisting>
    <command>configure</command> 脚本会收集您的系统信息
    并在编译过程中使用这些信息。您可以向 <command>configure</command> 脚本传递命令行参数来改变默认行为，
    例如安装目录。默认情况下，<productname>Pgpool-II</productname>
    将被安装到 <literal>/usr/local</literal> 目录。
   </para>
   <para>
    <command>make</command> 命令编译源代码，而
    <command>make install</command> 将安装可执行文件。
    您必须对安装目录具有写权限。
    在本教程中，我们将 <productname>Pgpool-II
    </productname> 安装到默认的 <literal>/usr/local</literal> 目录。
   </para>
   <note>
    <para>
     <productname>Pgpool-II</productname> 需要 <productname>PostgreSQL</productname> 7.4 或更高版本（version 3 协议）中的
     <literal>libpq</literal> 库。
    </para>
   </note>
   <para>
    如果 <command>configure</command> 脚本显示以下错误信息，则
    <literal>libpq</literal> 库可能未安装，或者版本不是 3。
    <programlisting>
     configure: error: libpq is not installed or libpq is old
    </programlisting>
    如果库是 version 3，但仍然显示上述消息，则
    <literal>libpq</literal> 库可能未被 <command>
     configure</command> 脚本识别。
    <command>configure</command> 脚本在 <literal>/usr/local/pgsql</literal> 下搜索
    <literal>libpq</literal> 库。如果您将
    <productname>PostgreSQL</productname> 安装到了 <literal>/usr/local/pgsql</literal> 之外的目录，请在执行
    <command>configure</command> 时使用
    <literal>--with-pgsql</literal>，或 <literal>--with-pgsql-includedir</literal>
    和 <literal>--with-pgsql-libdir</literal> 命令行选项。
   </para>
  </sect3>

  <sect3 id="example-configs-begin-config-files">
   <title>配置文件</title>
   <para>
    <productname>Pgpool-II</productname> 配置参数保存在
    <literal>pgpool.conf</literal> 文件中。该文件采用每行
    <literal>"parameter = value"</literal> 的格式。安装 <productname>Pgpool-II</productname> 时，
    会自动创建 <literal>pgpool.conf.sample</literal>。
    我们建议将其复制并重命名为 <literal>pgpool.conf</literal>，然后按需编辑。
    <programlisting>
     $ cp /usr/local/etc/pgpool.conf.sample /usr/local/etc/pgpool.conf
    </programlisting>
    默认情况下，<productname>Pgpool-II</productname> 仅接受来自 localhost 端口 9999 的连接。如果您希望接收来自其他主机的连接，
    请将 <xref linkend="guc-listen-addresses"> 设置为 <literal>'*'</literal>。
     <programlisting>
      listen_addresses = 'localhost'
      port = 9999
     </programlisting>
     本教程将使用默认参数。
   </para>
  </sect3>

  <sect3 id="example-configs-begin-config-pcp">
   <title>配置 <acronym>PCP</acronym> 命令</title>
   <para>
    <productname>Pgpool-II</productname> 提供了一个用于管理目的的接口，
    可通过网络获取数据库节点信息、关闭
    <productname>Pgpool-II</productname> 等。要使用
    <acronym>PCP</acronym> 命令，需要进行用户认证。
    此认证与 <productname>PostgreSQL</productname> 的用户认证不同。
    需要在 <literal>pcp.conf</literal> 文件中定义用户名和密码。在该文件中，
    每行列出一对用户名和密码，两者以冒号 (:) 分隔。密码以
    <literal>md5</literal> 哈希格式加密。

    <programlisting>
     postgres:e8a48653851e28c69d0506508fb27fc5
    </programlisting>

    安装 <productname>Pgpool-II</productname> 时，会自动创建 <literal>pcp.conf.sample
    </literal>。我们建议将其复制并重命名为
    <literal>pcp.conf</literal>，然后进行编辑。
    <programlisting>
     $ cp /usr/local/etc/pcp.conf.sample /usr/local/etc/pcp.conf
    </programlisting>
    要将密码加密为 md5 哈希格式，请使用 <command>pg_md5</command>
    命令，该命令作为 <productname>Pgpool-II</productname> 的可执行文件之一进行安装。<command>pg_md5</command>
    接受文本作为命令行参数，并显示其 md5 哈希后的文本。
    例如，将 <literal>"postgres"</literal> 作为命令行参数传递，
    <command>pg_md5</command> 会在标准输出上显示 md5 哈希后的文本。
    <programlisting>
     $ /usr/local/bin/pg_md5 postgres
     e8a48653851e28c69d0506508fb27fc5
    </programlisting>
    PCP 命令通过网络执行，因此必须在 <literal>pgpool.conf</literal> 文件中使用
    <xref linkend="guc-pcp-port"> 参数配置端口号。
     本教程将使用 <xref linkend="guc-pcp-port"> 的默认值 9898。
      <programlisting>
       pcp_port = 9898
      </programlisting>
   </para>
  </sect3>

  <sect3 id="example-configs-prep-db-nodes">
   <title>准备数据库节点</title>
   <para>
    现在，我们需要为 <productname>Pgpool-II
    </productname> 设置后端 <productname>PostgreSQL</productname> 服务器。这些服务器可以放置在
    与 <productname>Pgpool-II</productname> 相同的主机上，也可以放置在单独的主机上。如果您决定将
    服务器放置在相同的主机上，则必须为每个服务器分配不同的端口号。如果服务器放置在不同的主机上，
    则必须进行适当配置，使其能够接受来自
    <productname>Pgpool-II</productname> 的网络连接。
    在本示例中，我们创建 3 个 <productname>PostgreSQL</productname> 服务器，
    并在以下参数中指定 PostgreSQL 信息。

    <programlisting>
     backend_hostname0 = 'localhost'
     backend_port0 = 5432
     backend_weight0 = 1
     backend_hostname1 = 'localhost'
     backend_port1 = 5433
     backend_weight1 = 1
     backend_hostname2 = 'localhost'
     backend_port2 = 5434
     backend_weight2 = 1
    </programlisting>

    对于 <xref linkend="guc-backend-hostname">、<xref linkend="guc-backend-port">、
      <xref linkend="guc-backend-weight">，分别设置节点的 hostname、端口号和负载均衡比例。在每个参数字符串末尾，
      必须通过添加从 0 开始的正整数（即 0、1、2...）来指定节点 ID。
   </para>
   <note>
    <para>
     所有节点的 <xref linkend="guc-backend-weight"> 参数
      均设置为 1，这意味着 SELECT 查询将平均分配到
      三个服务器中。
    </para>
   </note>
  </sect3>

  <sect3 id="example-configs-start-stop-pgpool">
   <title>启动/停止 <productname>Pgpool-II</productname></title>
   <para>
    要启动 <productname>Pgpool-II</productname>，请在终端中执行以下
    命令。

    <programlisting>
     $ pgpool
    </programlisting>

    然而，上述命令不会打印任何日志消息，因为 <productname>
     Pgpool-II</productname> 会脱离终端运行。如果您希望显示
    <productname>Pgpool-II</productname> 的日志消息，请向 <command>pgpool</command> 命令传递
    <literal>-n</literal> 选项，使 <productname>Pgpool-II</productname>
    作为非守护进程执行，这样终端就不会被脱离。
    <programlisting>
     $ pgpool -n &
    </programlisting>

    日志消息将打印在终端上，因此建议使用以下选项。
    <programlisting>
     $ pgpool -n -d > /tmp/pgpool.log 2>&1 &
    </programlisting>

    <literal>-d</literal> 选项启用调试消息的生成。
    上述命令会将日志消息追加到 <literal>/tmp/pgpool.log
    </literal> 文件。如果需要轮换日志文件，请将日志传递给具有日志轮换功能的
    外部命令。
    例如，您可以使用 Apache2 的 <ulink url="https://httpd.apache.org/docs/2.4/programs/rotatelogs.html">
     <command>rotatelogs</command></ulink>：
    <programlisting>
     $ pgpool -n 2>&1 | /usr/local/apache2/bin/rotatelogs \
     -l -f /var/log/pgpool/pgpool.log.%A 86400 &
    </programlisting>

    这将生成一个名为 <literal>"pgpool.log.Thursday"</literal> 的日志文件，
    然后在午夜 00:00 进行轮换。rotatelogs 会在文件已存在时向其追加日志。若要删除轮换前的旧日志文件，可以使用 cron：
    <programlisting>
     55 23 * * * /usr/bin/find /var/log/pgpool -type f -mtime +5 -exec /bin/rm -f '{}' \;
    </programlisting>

    请注意，在某些发行版中，rotatelogs 可能位于 <literal>/usr/sbin/rotatelogs2</literal>。
    <literal>-f</literal> 选项在 <command>rotatelogs</command> 启动后立即生成日志文件，
    且仅在 apache2 2.2.9 或更高版本中可用。
    也可以使用 <ulink url="http://www.cronolog.org/">cronolog</ulink>。
    <programlisting>
     $ pgpool -n 2>&1 | /usr/sbin/cronolog \
     --hardlink=/var/log/pgsql/pgpool.log \
     '/var/log/pgsql/%Y-%m-%d-pgpool.log' &
    </programlisting>

    要停止 <productname>Pgpool-II</productname>，请执行以下命令。
    <programlisting>
     $ pgpool stop
    </programlisting>

    如果仍有客户端连接，<productname>Pgpool-II</productname>
    会等待其断开连接后再终止自身。如果您想强制关闭
    <productname>Pgpool-II</productname>，请改用以下命令。
    <programlisting>
     $ pgpool -m fast stop
    </programlisting>

   </para>
  </sect3>
 </sect2>

 <sect2 id="example-configs-replication">
  <title>您的第一次复制</title>
  <para>
   复制功能（参见 <xref linkend="guc-replication-mode">）可将
    相同的数据复制到多个数据库节点。
    在本节中，我们将使用已在 <xref linkend="example-configs-begin"> 中设置的三个数据库节点，
    一步步为您创建数据库复制系统。
     将要复制的示例数据由
     <ulink url="https://www.postgresql.org/docs/current/static/pgbench.html">
      <command>pgbench</command></ulink> 基准测试程序生成。
  </para>

  <sect3 id="example-configs-config-replication">
   <title>配置复制</title>
   <para>
    要启用数据库复制功能，请在 <literal>pgpool.conf</literal> 文件中将
    <xref linkend="guc-replication-mode"> 设置为 on。
     <programlisting>
      replication_mode = true
     </programlisting>
     当 <xref linkend="guc-replication-mode"> 开启时，<productname>Pgpool-II</productname>
      会将收到的查询副本发送到所有数据库节点。
      另外，当 <xref linkend="guc-load-balance-mode"> 设置为 true 时，
       <productname>Pgpool-II</productname> 会在数据库节点间分发 <acronym>SELECT</acronym> 查询。
       <programlisting>
	load_balance_mode = true
       </programlisting>
       在本节中，我们将同时启用 <xref linkend="guc-replication-mode">
	and <xref linkend="guc-load-balance-mode">。
   </para>
  </sect3>

  <sect3 id="example-configs-checking-replication">
   <title>检查复制</title>
   <para>
    为了使 <literal>pgpool.conf</literal> 中的上述更改生效，
    必须重启 <productname>Pgpool-II</productname>。
    请参阅"启动/停止 <productname>Pgpool-II</productname>"
    <xref linkend="example-configs-start-stop-pgpool">。
     配置好 <literal>pgpool.conf</literal> 并重启
     <productname>Pgpool-II</productname> 后，让我们尝试实际复制，
     看看是否一切正常。
     首先，我们需要创建一个待复制的数据库。我们将其命名为
     <literal>"bench_replication"</literal>。该数据库需要在所有节点上创建。通过
     <ulink url="https://www.postgresql.org/docs/current/static/app-createdb.html">
      <command>createdb</command></ulink> 命令经由
     <productname>Pgpool-II</productname> 创建该数据库，这样数据库将在所有节点上创建。
     <programlisting>
      $ createdb -p 9999 bench_replication
     </programlisting>
     然后，我们使用 <literal>-i</literal> 选项执行 <ulink url="https://www.postgresql.org/docs/current/static/pgbench.html">
      <command>pgbench</command></ulink>。
     <literal>-i</literal> 选项使用预定义的表和数据初始化数据库。
     <programlisting>
      $ pgbench -i -p 9999 bench_replication
     </programlisting>
     下表是 <ulink url="https://www.postgresql.org/docs/current/static/pgbench.html">
      <command>pgbench -i</command></ulink> 将创建的表和数据的摘要。如果所有节点上都创建了列出的表和
     数据，则说明复制功能正常工作。
   </para>

   <table id="example-configs-checking-replication-table">
    <title>数据摘要</title>
    <tgroup cols="2">
     <thead>
      <row>
       <entry>表名</entry>
       <entry>行数</entry>
      </row>
     </thead>

     <tbody>
      <row>
       <entry>pgbench_branches</entry>
       <entry>1</entry>
      </row>

      <row>
       <entry>pgbench_tellers</entry>
       <entry>10</entry>
      </row>

      <row>
       <entry>pgbench_accounts</entry>
       <entry>100000</entry>
      </row>

      <row>
       <entry>pgbench_history</entry>
       <entry>0</entry>
      </row>

     </tbody>
    </tgroup>
   </table>

   <para>
    让我们使用一个简单的 shell 脚本来检查所有节点上的上述情况。
    以下脚本将显示所有节点（5432、5433、5434）上 pgbench_branches、
    pgbench_tellers、pgbench_accounts 和 pgbench_history 表的行数。
    <programlisting>
     $ for port in 5432 5433 5434; do
     >     echo $port
     >     for table_name in pgbench_branches pgbench_tellers pgbench_accounts pgbench_history; do
     >         echo $table_name
     >         psql -c "SELECT count(*) FROM $table_name" -p $port bench_replication
     >     done
     > done
    </programlisting>

   </para>
  </sect3>
 </sect2>

</sect1>
