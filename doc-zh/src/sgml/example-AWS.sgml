<!-- doc/src/sgml/example-AWS.sgml -->

<sect1 id="example-AWS">
 <title>AWS 配置示例</title>

 <para>
  本教程演示在使用 <ulink url="https://aws.amazon.com/">AWS</ulink>上的
 </para>
 <para>
  在 AWS 上设置虚拟 IP 有多种方法。本示例介绍以下方法：
 </para>
 <itemizedlist>
  <listitem>
   <para>
    <xref linkend="example-AWS-eip"> 分配弹性 IP
   </para>
  </listitem>

  <listitem>
   <para>
    <xref linkend="example-AWS-rtb"> 更新路由表 更新路由表
   </para>
  </listitem>
 </itemizedlist>

 <sect2 id="example-AWS-eip">
  <title>分配弹性 IP</title>
  <para>
   本节说明如何将弹性 IP 地址用作虚拟 IP，并将其关联到主 Pgpool-II（watchdog）实例。
  </para>

  <sect3 id="example-AWS-eip-setup">
   <title>AWS 设置</title>
   <para>
    使用此方法时，假设 <productname>Pgpool-II</productname>
    EC2 实例位于公网子网中，客户端通过互联网连接到
    <productname>Pgpool-II</productname>。
    <EMPHASIS>此方法支持多可用区。</EMPHASIS>
   </para>
   <para>
    在本示例中，我们将使用三个节点的 <productname>Pgpool-II</productname>
    （watchdog）集群。因此我们将创建三个 EC2 实例和一个弹性 IP 地址。
   </para>
   <para>
    完成以下步骤以设置 AWS：
   </para>

   <itemizedlist>

    <listitem>
     <para>
      启动三个 EC2 实例。
     </para>
    </listitem>

    <listitem>
     <para>
      为 <productname>Pgpool-II</productname> 实例配置安全组。
     </para>
     <itemizedlist>
      <listitem>
       <para>
        允许 <productname>Pgpool-II</productname> 和 watchdog 所用端口的入站流量。
       </para>
      </listitem>
      <listitem>
       <para>
        由于 <productname>Pgpool-II</productname>（watchdog）会向虚拟 IP 发送 ICMP 探测以检查连通性，
        因此需允许来自弹性 IP（源）的入站 ICMP 流量。
       </para>
      </listitem>
     </itemizedlist>
    </listitem>

    <listitem>
     <para>
      在每个实例上安装 <productname>Pgpool-II</productname>。
     </para>
    </listitem>

    <listitem>
     <para>
      <ulink url="https://docs.aws.amazon.com/cli/index.html">AWS CLI</ulink> 是
      用于管理 EC2 实例上 AWS 服务的工具。
      在每个 <productname>Pgpool-II</productname> 实例上安装 <ulink url="https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-quickstart.html">AWS CLI</ulink>。
     </para>

     <para>
      此外，<EMPHASIS>请以 Pgpool-II 启动用户的身份运行 aws configure</EMPHASIS>
      以创建配置文件和凭证文件，从而使 <productname>Pgpool-II</productname>
      启动用户能够运行 AWS CLI。
      如果通过 RPM 包安装 <productname>Pgpool-II</productname>，
      <productname>Pgpool-II</productname> 默认以 <literal>postgres</literal>
      用户身份运行。
     </para>
    </listitem>

    <listitem>
     <para>
      分配一个弹性 IP 地址。
      本示例中使用 "35.163.178.3"。
     </para>
    </listitem>
   </itemizedlist>
  </sect3>

  <sect3 id="example-AWS-eip-pgpool-config">
   <title><productname>Pgpool-II</productname> 配置</title>
   <para>
    本节说明如何配置 <productname>Pgpool-II</productname>。
    本示例中的 <productname>Pgpool-II</productname> 配置与 <xref linkend="example-cluster"> 基本相同，
    不同之处在于使用 <xref linkend="guc-if-up-cmd">/<xref linkend="guc-if-down-cmd">
    来为/从主 <productname>Pgpool-II</productname> 实例分配/移除弹性 IP 地址。
   </para>
   <para>
    将弹性 IP 地址指定给 <xref linkend="guc-delegate-ip">。
   </para>
   <programlisting>
use_watchdog = on
delegate_ip = '35.163.178.3'
   </programlisting>

   <para>
    将要用于分配/移除弹性 IP 地址的脚本指定给
    <xref linkend="guc-if-up-cmd"> 和 <xref linkend="guc-if-down-cmd">。
    <xref linkend="guc-if-up-cmd"> 是由 watchdog 在主节点成为主节点时
    执行以将弹性 IP 分配给该实例的脚本。
    <xref linkend="guc-if-down-cmd"> 是由 watchdog 在 watchdog 不再作为主节点时
    执行以从该实例移除弹性 IP 的脚本。
   </para>

   <programlisting>
if_up_cmd = '&lt;path to script&gt; up $_IP_$ &lt;path to awscli&gt;'
if_down_cmd = '&lt;path to script&gt; down $_IP_$ &lt;path to awscli&gt;'
   </programlisting>

   <itemizedlist>
    <listitem>
     <para>
      &lt;path to script&gt;：指定用于分配或移除弹性 IP 地址的脚本路径。
     </para>
    </listitem>

    <listitem>
     <para>
      &lt;path to awscli&gt;：指定 AWS CLI 命令的路径。
     </para>
    </listitem>
   </itemizedlist>

   <para>
    在本示例中，我们使用以下设置。请替换为您自己的设置。
   </para>

   <programlisting>
if_up_cmd = '/etc/pgpool-II/aws_eip_if_cmd.sh up $_IP_$ /usr/local/bin/aws'
if_down_cmd = '/etc/pgpool-II/aws_eip_if_cmd.sh down $_IP_$ /usr/local/bin/aws'
   </programlisting>

   <para>
    因为在 AWS 上不需要 <command>arping</command>，将其设置为 'true' 以使始终返回成功退出状态。
   </para>
   <programlisting>
arping_cmd = 'true'
   </programlisting>

  </sect3>

  <sect3 id="example-AWS-eip-script">
   <title>弹性 IP 分配/移除脚本</title>
   <para>
    在每个 Pgpool-II 实例上创建由 if_up_cmd/if_down_cmd 执行的脚本。
   </para>
   <para>
    在本示例中，我们使用示例脚本 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=blob_plain;f=src/sample/scripts/aws_eip_if_cmd.sh.sample;hb=refs/heads/V4_4_STABLE">aws_eip_if_cmd.sh</ulink>. 如有需要请修改。
   </para>
   <programlisting>
(run the following command on each Pgpool-II instance)
# cp -p /etc/pgpool-II/sample_scripts/aws_eip_if_cmd.sh.sample /etc/pgpool-II/aws_eip_if_cmd.sh
   </programlisting>
  </sect3>

  <sect3 id="example-AWS-eip-try">
   <title>测试</title>
   <para>
    在每个 <productname>Pgpool-II</productname> 实例上启动 <productname>Pgpool-II</productname>。
    尝试使用弹性 IP 地址连接到 <productname>Pgpool-II</productname>。
   </para>
   <programlisting>
[user@someserver]$ psql -h 35.163.178.3 -p 9999 -U postgres -c "show pool_nodes"
   </programlisting>
   <para>
    停止主 <productname>Pgpool-II</productname>。
    然后弹性 IP 地址将重新分配给新的主 <productname>Pgpool-II</productname>。
    验证即使主 <productname>Pgpool-II</productname> 已切换，
    您仍可以使用弹性 IP 地址连接到 <productname>Pgpool-II</productname>。
   </para>
   <programlisting>
[user@someserver]$ psql -h 35.163.178.3 -p 9999 -U postgres -c "show pool_nodes"
   </programlisting>
  </sect3>
 </sect2>

 <sect2 id="example-AWS-rtb">
  <title>更新路由表</title>
  <para>
   本节说明如何通过更新路由表条目来控制主 Pgpool-II（watchdog）的路由。
  </para>
  <para>
   在此方法中，使用私有 IP 地址作为虚拟 IP 并分配给
   主 Pgpool-II 实例。如果发生故障切换，重新分配虚拟 IP，
   并通过更新路由表，将使目标地址为虚拟 IP 的流量路由到新主
   Pgpool-II 实例。
  </para>

  <sect3 id="example-AWS-rtb-setup">
   <title>AWS 设置</title>
   <para>
    使用此方法时，假设 <productname>Pgpool-II</productname>
    EC2 实例位于私网子网中，且客户端应用在
    同一 VPC 内连接到 <productname>Pgpool-II</productname>。
    <EMPHASIS>此方法支持多可用区。</EMPHASIS>
   </para>
   <para>
    在本示例中，我们将使用三个节点的 <productname>
    Pgpool-II</productname>（watchdog）集群。因此我们将在多个可用区中创建三个
    EC2 实例。
   </para>
   <para>
    完成以下步骤以设置 AWS：
   </para>

   <itemizedlist>

    <listitem>
     <para>
      创建一个包含一个公网子网和三个私网子网的 VPC，
      每个私网子网位于不同的可用区中。
      详细信息请参考
      <ulink url="https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Scenario2.html">文档</ulink>。
     </para>
    </listitem>

    <listitem>
     <para>
      创建两个路由表，分别与公网子网和私网子网关联。
      详细信息请参考此 <ulink url="https://docs.aws.amazon.com/vpc/latest/userguide/VPC_Scenario2.html#VPC_Scenario2_Routing">文档</ulink>。
     </para>
    </listitem>

    <listitem>
     <para>
      在公网子网中启动一个应用程序 EC2 实例，
      并在不同的私网子网中启动三个 Pgpool-II EC2 实例。
     </para>
    </listitem>

    <listitem>
     <para>
      配置安全组，允许来自 VPC CIDR 地址段的流量通过
      <productname>Pgpool-II</productname> 和 watchdog 所用端口。
     </para>
    </listitem>

    <listitem>
     <para>
      在每个 Pgpool-II EC2 实例上安装 <productname>Pgpool-II</productname>。
     </para>
    </listitem>

    <listitem>
     <para>
      <ulink url="https://docs.aws.amazon.com/cli/index.html">AWS CLI</ulink> 是
      用于管理 EC2 实例上 AWS 服务的工具。
      在每个 <productname>Pgpool-II</productname> 实例上安装 <ulink url="https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-quickstart.html">AWS CLI</ulink>。
     </para>

     <para>
      此外，<EMPHASIS>请以 Pgpool-II 启动用户的身份运行 aws configure</EMPHASIS>
      以创建配置文件和凭证文件，从而使 <productname>Pgpool-II</productname>
      启动用户能够运行 AWS CLI。
      如果通过 RPM 包安装 <productname>Pgpool-II</productname>，
      <productname>Pgpool-II</productname> 默认以 <literal>postgres</literal>
      用户身份运行。
     </para>
    </listitem>

    <listitem>
     <para>
      分配或移除虚拟 IP 地址需要 root 权限。
      如果以非 root 用户身份启动 <productname>Pgpool-II</productname>，
      请配置 sudoers 文件并允许该用户无需输入密码即可运行 sudo。
      如果通过 RPM 包安装 <productname>Pgpool-II</productname>，
      <literal>postgres</literal> 用户的设置将自动添加到 sudoers 中。
     </para>
    </listitem>

    <listitem>
     <para>
      选择要用作虚拟 IP 的 IPv4 地址。
      由于辅助 IP 地址无法在多个子网之间重新分配，
      因此需要选择一个不属于 VPC IP 地址范围的私有 IP 地址。
      在本示例中，我们使用 "20.0.0.50"。
     </para>
    </listitem>
   </itemizedlist>
  </sect3>

  <sect3 id="example-AWS-rtb-pgpool-config">
   <title><productname>Pgpool-II</productname> 配置</title>
   <para>
    本节说明如何配置 <productname>Pgpool-II</productname>。
    本示例中的 <productname>Pgpool-II</productname> 配置与 <xref linkend="example-cluster"> 基本相同，
    不同之处在于使用 <xref linkend="guc-if-up-cmd"> 和 <xref linkend="guc-if-down-cmd">
    来分配虚拟 IP 并更新路由表。
   </para>
   <para>
    将虚拟 IP 指定给 <xref linkend="guc-delegate-ip">。
   </para>

   <programlisting>
use_watchdog = on
delegate_ip = '20.0.0.50'
   </programlisting>

   <para>
    将要用于更新路由表的脚本指定给
    <xref linkend="guc-if-up-cmd"> 和 <xref linkend="guc-if-down-cmd">。
    <xref linkend="guc-if-up-cmd"> 是由 watchdog 在主节点成为主节点时
    执行以分配虚拟 IP 并更新路由表的脚本。
    <xref linkend="guc-if-down-cmd"> 是由 watchdog 在 watchdog 不再作为主节点时
    执行以移除虚拟 IP 并更新路由表的脚本。
   </para>
   <programlisting>
if_up_cmd = '&lt;path to script&gt; up $_IP_$ &lt;interface&gt; &lt;route table ID&gt; &lt;path to awscli&gt;'
if_down_cmd = '&lt;path to script&gt; down $_IP_$ &lt;interface&gt; &lt;route table ID&gt; &lt;path to awscli&gt;'
   </programlisting>

   <itemizedlist>

    <listitem>
     <para>
      &lt;path to script&gt;：指定用于分配虚拟 IP 并更新路由表的脚本路径。
     </para>
    </listitem>

    <listitem>
     <para>
      &lt;interface&gt;：指定用于分配虚拟 IP 的网络接口。
     </para>
    </listitem>

    <listitem>
     <para>
      &lt;route table ID&gt;：指定要更新的路由表 ID（例如客户端应用程序或
      <productname>Pgpool-II</productname> 实例）。
      可以指定多个以逗号分隔的路由表 ID。
     </para>
    </listitem>

    <listitem>
     <para>
      &lt;path to awscli&gt;：指定 AWS CLI 命令的路径。
     </para>
    </listitem>

   </itemizedlist>

   <para>
    在本示例中，我们使用以下设置。请替换为您自己的设置。
    指定与应用程序所在公网子网以及 <productname>Pgpool-II</productname>
    EC2 实例所在的私网子网相关联的路由表 ID。
   </para>

   <programlisting>
if_up_cmd = '/etc/pgpool-II/aws_rtb_if_cmd.sh up $_IP_$ eth0 rtb-012345abcd,rtb-67890abcd /usr/local/bin/aws'
if_down_cmd = '/etc/pgpool-II/aws_rtb_if_cmd.sh down $_IP_$ eth0 rtb-012345abcd,rtb-67890abcd /usr/local/bin/aws'
   </programlisting>

   <para>
    因为在 AWS 上不需要 <command>arping</command>，将其设置为 'true' 以使始终返回成功退出状态。
   </para>
   <programlisting>
arping_cmd = 'true'
   </programlisting>
  </sect3>

  <sect3 id="example-AWS-rtb-script">
   <title>路由表更新脚本</title>
   <para>
    在每个 <productname>Pgpool-II</productname> 实例上创建由 <varname>if_up_cmd/if_down_cmd</varname> 执行的脚本。
   </para>
   <para>
    在本示例中，我们使用示例脚本 <ulink url="https://git.postgresql.org/gitweb/?p=pgpool2.git;a=blob_plain;f=src/sample/scripts/aws_rtb_if_cmd.sh.sample;hb=refs/heads/V4_4_STABLE">aws_rtb_if_cmd.sh</ulink>。如有需要请修改。
   </para>
   <programlisting>
(run the following command on each Pgpool-II instance)
# cp -p /etc/pgpool-II/sample_scripts/aws_rtb_if_cmd.sh.sample /etc/pgpool-II/aws_rtb_if_cmd.sh
   </programlisting>
  </sect3>

  <sect3 id="example-AWS-rtb-eni">
   <title>禁用源/目的检查</title>
   <para>
    AWS EC2 实例默认会检查所有接收到的流量的源地址或目的地址。
    您<EMPHASIS>必须在每个 <productname>Pgpool-II</productname> 实例上禁用源/目的检查</EMPHASIS>，
    才能将虚拟 IP 的流量路由到目标实例。
   </para>
   <para>
    在所有 <productname>Pgpool-II</productname> 实例的网络接口设置中禁用"源/目的检查"。
    可以通过 AWS Management Console 配置，或使用 AWS CLI 配置。
    使用 AWS CLI 时，执行以下命令。
    将 <varname>LOCAL_INTERFACE</varname> 设置为将要添加虚拟 IP 的网络接口。
    示例中使用 eth0。
   </para>

   <programlisting>
$ sudo su - postgres
$ TOKEN=$(curl -sX PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")
$ LOCAL_INTERFACE=<EMPHASIS>eth0</EMPHASIS>
$ MAC_ADDR=$(ip -br link show dev ${LOCAL_INTERFACE} | tr -s ' ' | cut -d ' ' -f3)
$ EC2_NETWORK_INTERFACE_ID=$(curl -H "X-aws-ec2-metadata-token: ${TOKEN}" -s http://169.254.169.254/latest/meta-data/network/interfaces/macs/${MAC_ADDR}/interface-id)
$ aws ec2 modify-network-interface-attribute --network-interface-id ${EC2_NETWORK_INTERFACE_ID} --no-source-dest-check
   </programlisting>
  </sect3>

  <sect3 id="example-AWS-rbt-try">
   <title>测试</title>
   <para>
    在每个 <productname>Pgpool-II</productname> 实例上启动 <productname>Pgpool-II</productname>。
    从应用程序实例尝试使用虚拟 IP 连接到 <productname>Pgpool-II</productname>。
   </para>
   <programlisting>
[user@application instance]$ psql -h 20.0.0.50 -p 9999 -U postgres -c "show pool_nodes"
   </programlisting>
   <para>
    停止主 <productname>Pgpool-II</productname>。
    然后虚拟 IP 地址将重新分配给新的主 <productname>Pgpool-II</productname>。
    验证即使主 <productname>Pgpool-II</productname> 已切换，
    您仍可以从应用程序实例使用弹性 IP 地址连接到 <productname>Pgpool-II</productname>。
   </para>
   <programlisting>
[user@application instance]$ psql -h 20.0.0.50 -p 9999 -U postgres -c "show pool_nodes"
   </programlisting>
  </sect3>
 </sect2>

 <bibliography>
  <title>AWS 命令参考</title>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="http://docs.aws.amazon.com/cli/latest/userguide/cli-chap-getting-started.html">Configure AWS CLI</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: Configuring the AWS Command Line Interface</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="http://docs.aws.amazon.com/cli/latest/reference/ec2/associate-address.html">associate-address</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: associate-address reference</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="http://docs.aws.amazon.com/cli/latest/reference/ec2/disassociate-address.html">disassociate-address</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: disassociate-address reference</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="https://docs.aws.amazon.com/cli/latest/reference/ec2/create-route.html">create-route</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: create-route reference</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="https://docs.aws.amazon.com/cli/latest/reference/ec2/replace-route.html">replace-route</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: replace-route reference</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="https://docs.aws.amazon.com/cli/latest/reference/ec2/delete-route.html">delete-route</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: delete-route reference</title>
   </biblioset>
  </biblioentry>

  <biblioentry>
   <biblioset relation="article">
    <title><ulink url="https://docs.aws.amazon.com/cli/latest/reference/ec2/modify-network-interface-attribute.html">modify-network-interface-attribute</ulink></title>
   </biblioset>
   <biblioset relation="book">
    <title>AWS Documentation: modify-network-interface-attribute</title>
   </biblioset>
  </biblioentry>

  </bibliography>
</sect1>
